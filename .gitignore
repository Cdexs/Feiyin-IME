# ===== Build artifacts =====
/target
/src-tauri/target
/poc/target
patches/**/target/
vendor/**/target/

# ===== Frontend build =====
/dist
/ui/dist
/ui/node_modules
node_modules

# ===== AI models (too large for git) =====
/models

# ===== Runtime DLLs =====
/sherpa-onnx-lib

# ===== Large vendor binaries =====
/vendor/sherpa-onnx/*-Release.tar.bz2
/vendor/sherpa-onnx/*-Release/
/vendor/sherpa-onnx/*-shared-lib.tar.bz2
/vendor/sherpa-onnx/*-shared-lib/
/vendor/whisper-rs-sys/

# ===== Python =====
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/

# ===== AI agent configs (internal) =====
# collab/ 已于 2026-07-30 移出忽略并入库（Gavin 决定：便于 Windows / macOS 两侧团队信息对称交流）。
# 入库范围 = 协作文档：todo / handoffs(+archive) / decisions / troubleshooting / progress /
#            build-test-guide / research/*.md
# 以下三类仍不入库，理由见 docs/MACOS-NPMLOCK-WINDOWS-REPLY.md §5.1：
#   ① PoC 语音语料（62MB / 829 wav，含 Gavin 本人真实录音；本仓库为公开仓库，且会让全新 clone 暴涨）
#   ② 任务派发/结果/ACK 等瞬时 IPC（两侧各自本地，入库必冲突；outbox 另含 2.8MB 截图二进制）
#   ③ Windows 资源管理器产生的 desktop.ini
/collab/research/audio-*/
#   ④ 词库派生的中间数据（RESEARCH-ITN-LEXICON-001，972KB）：派生自 jieba/CC-CEDICT/THUOCL，
#      **许可证尚未取得原文证据**（CC-CEDICT 为 CC BY-SA，share-alike 有传染性）。
#      在许可证结论落实前一律不入库；最终产物只以「净化后的词条」形式进 itn-rules.toml，
#      并在文档致谢来源。此前该目录处于「未跟踪但未被忽略」状态，`git add -A` 会误提交（2026-07-30 主控发现）。
/collab/research/data/
# ===== SECRET-083（Gavin 2026-09-04 拍板）：实验产物与调试证据不入库 =====
# 起因：Gavin 问「collab 目录全部都会 push 吗」。答案是会 —— 除瞬时 IPC 外全量入库。
# 主控随即用 pre-commit/pre-push 的同一套判据全量扫了 93 个已跟踪 collab 文件：
# 密钥/邮箱/手机号/身份证层面干净（仅 1 处文档示例地址，误报）。
#
# 🔴 但闸门认的是「形态」，认不了「内容」。以下两类正在公开出去，正则永远抓不到：
#   ① collab/evidence/*.log —— 程序 debug 日志，含 `Online ASR result` / `[ASR-WORDS]`，
#      载荷是 Gavin 本人的真实口述转写文本（+ 当时开着哪个程序 app_exe=...）。
#   ② collab/research/*.{json,log,txt} —— LLM 实验的输入输出 dump
#      （verbatim-inputs-*.json 名字就叫「逐字」；prompt-parts-*-full.json 21KB），
#      很可能含真实口述语料。**[SECRET-IN-REPO-001] 那把 DeepSeek key 就出自这个目录**，
#      失效机制一模一样：你想留的是 prompt 或参数，真值是顺带被抄走的。
#
# 🔴 2026-09-04 Gavin 当场扩大范围（原方案只排除数据类文件、保留 research/*.md）：
#    「这两个目录不是项目必须的代码，整个目录都加进 gitignore」。
#    照办 —— evidence/ 与 research/ 整目录出库，43 个 .md 一并移出索引（本地一个不删）。
#    理由成立：分析过程文档不是构建产物也不是团队协作契约，
#    留在本地随时可查；入公开库只是把「我们研究过什么、拿什么语料研究的」一起送出去。
#    协作契约（todo/handoffs/decisions/troubleshooting/progress/build-test-guide）继续入库不变。
/collab/evidence/
/collab/research/
/collab/inbox/
/collab/outbox/
/collab/acks/
/collab/drafts/
/collab/**/desktop.ini
/collab/desktop.ini
# logs/ 已于 2026-07-30 移出忽略并入库（Gavin 决定：两侧团队都要跟踪代码改动记录）。
# 内容 = 每日代码级改动日志 logs/YYYYMMDD.md（53 份，约 780KB，纯文本）。
/tasks
# 协作文档快照备份（scripts/backup-docs.ps1 产出，每日计划任务轮转 30 份）
/Backup
/.claude
/memory
CLAUDE.md
AGENTS.md
GEMINI.md
QWEN.md
/.codex
/.qwen

# ===== Tauri 生成物：macOS 平台 schema =====
# macOS 上 cargo check / build 会生成 gen/schemas/macOS-schema.json。
# 其内容与已入库的 windows-schema.json / desktop-schema.json 逐字节相同
# （三者 md5 均为 2d534822642d73cf19e511b7b77c91f1），入库只是多一份 113 KB 重复副本。
# 注：同目录的 acl-manifests / capabilities / desktop-schema / windows-schema 仍为跟踪状态，
# 本条只排除 macOS 侧新产生的这一份（Gavin 2026-07-30 决定）。
src-tauri/gen/schemas/macOS-schema.json

# ===== Design draft icons =====
src-tauri/icons/preview-*.png
src-tauri/icons/preview2-*.png
src-tauri/icons/icon-new.png
src-tauri/icons/icon-source.png
src-tauri/icons/icon-v2.png
src-tauri/icons/icon-v3.png
src-tauri/icons/icon-final.png
src-tauri/icons/tray-source.png
ui/public/icons/icon-source.png
ui/public/icons/tray-source.png

# ===== Publish directory (large binaries, not for git) =====
/Publish/*
!/Publish/.gitkeep

# ===== Miscellaneous =====
nul
src/main*副本*.rs
tests/录音.m4a
*.sqlite
*.sqlite-shm
*.sqlite-wal
/poc
COMMIT-LIST.md

# ===== User app data =====
*.db
*.db-shm
*.db-wal
config.json

# ===== GitHub Actions (macOS CI not yet ready) =====
.github/

# ===== OS =====
.DS_Store
Thumbs.db
desktop.ini
