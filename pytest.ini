[pytest]
testpaths = tests
norecursedirs = .git .venv target node_modules __pycache__ *.egg-info dist build

addopts = -v --tb=short -m "not slow" --timeout=30

markers =
    audio: 需要 microphone 的测试
    slow: 执行时间较长的测试（默认跳过，使用 pytest -m slow 运行）
    gui: 需要 GUI 窗口的测试

timeout = 30
