# 2026-08-05 — coder-1 工作日志

## MACOS-P4-OVERLAY-001 验收返工（orchestrator 反馈 → 修复 → 重交付）

### 主控验收反馈（2 必改 + 1 待答）
1. **❌ BORDER_GRAY 配色 R/B 互换**：COLORREF `0x060607` = `0x00BBGGRR` → RR=0x07/GG=0x06/BB=0x06 → RGB `#070606`。我原实现 `(0.02353,0.02353,0.02745)` 是 `#060607`（R/B 反了）。
2. **❌ 性能四项未报数**：需补 ① 平均帧间隔 ② 单帧绘制耗时(<5ms) ③ CPU 占用 ④ 音频线程无丢帧证据。
3. **❓ 停止按钮出处**：1:1 规格表没有，指 Windows 对应段。

### 修复过程
- **返工1**：BORDER_GRAY 改 `(0.02745, 0.02353, 0.02353)`（R=0x07/255）。其余 4 色经核对均正确（/红/灰/背景/橙）。
- **返工2**：
  - overlay.rs 新增 `OverlayStats`（7 个 AtomicU64）+ `StatsSnapshot` + `record_tick`（timer 回调测帧间隔）/`record_draw`（drawRect 测耗时）/`monotonic_ns()`；`RecordingOverlay::stats()` 暴露。
  - **关键发现**：初版 probe 用 `sleep` 驱动，overlay 的 CFRunLoopTimer **从未 fire**（fps 59 只是 push 计数）。试 `CFRunLoopRunInMode` 泵法 → AppKit 有持久源长阻塞卡死。终版改音频模拟线程精确 16ms push + 主线程 `NSRunLoop runUntilDate:`（与真实 main.rs 主线程一致）。
  - **SIGBUS 真实缺陷**（run loop 跑起来后暴露）：`ACTIVE_OVERLAY` 存 `new()` 栈帧 `&overlay`，move 后悬垂 → drawRect 读死指针在 `Arc::clone` atomic_add 崩溃（crash 报告 faultingThread 0 全链确认）。重构：删 `ACTIVE_OVERLAY`/`active_levels()`，`OverlayIvars` 直接持有 `levels: AudioLevelBuf`，drawRect 用 `self.ivars().levels`。
- **30s 实测**：1876 帧（62.5fps），avg 帧间隔 16.00ms/max 18.03ms，avg 绘制 0.022ms/max 0.083ms，CPU 1.3–1.5%，push 间隔 avg 16.003ms/max 23.989ms。
- **待答停止按钮**：Windows `main.rs:1304-1344`（外框 bs=16/bl=right-25/PS_SOLID 1px+NULL_BRUSH 只描边；内芯 isz=8 实心只填充，(16-8)/2=4 居中）。非增补，为 draw_recording_overlay 完整实现。

### 主动上报（第 4 项）
- **指示灯形状差异**：Windows `main.rs:1099-1167` 实际是**麦克风图标**（72x72 画布 pill RoundRect(22,4,50,53) + stem LineTo(36,53→36,63) + base LineTo(24,63→48,63)，StretchBlt HALFTONE 缩 18px），非纯圆。我按规格表"直径 18px"实现为实心圆。基准以规格表还是 Windows 代码为准，待主控裁定。

### 验证
- `cargo check --all-targets` 0 errors；`cargo test --bin probe_overlay` 13 passed；`cargo fmt --check` clean。
- 已更新 result.md / CHANGELOG.md（追加 OVERLAY-001-R 行）；待 tmux 通知 orchestrator 收 ACK。

---

## MACOS-P4-AXINJECT-001 · coder-2 · 辅助功能 API 直写替代剪贴板注入

- **改动**：仅 `src/platform/macos/injection.rs`（+122/−1）。新增 `inject_via_ax` 第一级：CreateSystemWide → SetMessagingTimeout(0.5s) → CopyAttributeValue(kAXFocusedUIElementAttribute) → SetMessagingTimeout(focused, 0.5s) → SetAttributeValue(kAXSelectedTextAttribute, text)。`inject_text` 改为 AX → clipboard → enigo 三级降级。修复剪贴板恢复失败日志。
- **三条裁定遵守**：① 只用 `kAXSelectedTextAttribute`，未出现 `kAXValueAttribute`；② 0.5s 超时设于 system-wide 与 focused element 两处；③ 所有 `AXUIElementSetAttributeValue` 返回码均与 `kAXErrorSuccess(0)` 比对。
- **CF 生命周期**：Create/Copy 语义对象用 `CFType::wrap_under_create_rule` 自动 `CFRelease`；静态 attribute 常量用 `CFString::wrap_under_get_rule` 不获取所有权。
- **验证**：`cargo check --all-targets` 0 errors；`cargo test --bin feiyin-ime --no-fail-fast` 821/0/6；`cargo fmt --check` clean。debug 二进制短时启动可正常退出。
- **实机验证**：Agent 无法物理按键/说话/目视剪贴板，已留待 Gavin 端测表（备忘录/TextEdit/Safari 应走 AX；终端未知；Electron 应降级剪贴板）。
- **边界**：未碰 main.rs/mod.rs/accessibility.rs/Cargo.toml/windows/**；未新增模块；公开签名未改；未出包；未改版本号。

## MACOS-P4-AXINJECT-002 · coder-2 · 堵住 AX 直写静默丢词风险

- **改动**：仅 `src/platform/macos/injection.rs`。在 `SetAttributeValue` 前加 `AXUIElementIsAttributeSettable(kAXSelectedTextAttribute)` 预检：返回码必须为 0 且 `settable != 0`，否则降级剪贴板。`inject_via_ax` 改为 `Result<String>` 返回焦点元素 `AXRole`；新增 `ax_role_description` 最佳努力查 role，失败不影响主流程。成功日志改为 `text injected via AX (role=..., clipboard untouched)`。
- **Boolean 来源**：`core_foundation_sys::base::Boolean`（即 `u8`）。
- **验证**：`cargo check --all-targets` 0 errors；`cargo test --bin feiyin-ime --no-fail-fast` 821/0/6；`cargo fmt --check` clean。
- **边界**：同 AXINJECT-001；只改 injection.rs；未新增模块；公开签名未改；未出包；未改版本号。

---

# 🔖 主控断点存档（Gavin 2026-08-05 指令：额度用尽，明日续派）

## 一、本批次已闭环并验收的任务（8 项）

| # | 任务 | Worker | 主控验收要点 |
| --- | --- | --- | --- |
| 1 | `MACOS-P4-PROBE-001` | tester-1 | A1/A2/A5 PASS，**A4 FAIL 查出 CGEventTap 掩码越界真 bug**，A3 强度不足待复验 |
| 2 | `MACOS-P4-FIXHOTKEY-001` | coder-2 | `KEYBOARD_EVENTS` 5→3，删两个带外事件；带外回调与重启逻辑完好未动 |
| 3 | `MACOS-P4-NEUTRAL-001` | coder-1 | `run_pipeline` 薄封装 + 平台中立 `run_pipeline_core`；**402 行业务逻辑双侧共享**；主控用注入不存在类型反证 macOS 确实在编译它 |
| 4 | `TEST-SYNC-P4-NEUTRAL-001` | tester-1 | 解封 5 条 cfg 门控测试 + 新增 8 条；**盲区 22→17** |
| 5 | `TEST-EXEC-MERGE-001` | tester-1 | **880 passed / 0 failed / 8 ignored**（`--list` 888 自洽）；**A4 热键实机首次验证成功**；`time_half` 现 PASS |
| 6 | `MACOS-P4-HOST-001` | coder-2 | NSApplication + CFRunLoop 事件宿主；**tray-icon crate 实测不可用已上报**（5 种方案全试） |
| 7 | `MACOS-P4-EXIT-001` | coder-1 | `libc::signal` handler 只置 AtomicBool（async-signal-safe）；实测完整退出链 + flock 二次启动已释放 |
| 8 | `MACOS-P4-TRAY-001` | coder-2 | NSStatusItem 菜单栏图标 + 设置/分隔/退出（i18n 文案）；**5 处硬编码 `.exe` 抽为 cfg 常量**，Windows 仍拿 `.exe` |

**另有一次大合并**：Windows 侧 **68 个提交**并入（合并提交 `cc12381`），`src/main.rs` 自动合并成功，他们的双通道 ITN 代码落进 `run_pipeline_core`，macOS 编译同一份。

## 二、🔄 唯一进行中：`MACOS-P4-OVERLAY-001`（coder-1 返工中）

**主体已交付且质量高**（`src/platform/macos/overlay.rs`，NSPanel + CGContext 手绘 + 16ms CFRunLoopTimer）。

**主控逐项核对通过的**：边界干净（未碰 coder-2 占用文件）｜常数全对（240×36 / 圆角10 / 底部+64 / 指示灯18px左6 / 32根×宽3间隙2 / 48-12-8 / 增益2.5 / 衰减0.02 / 16ms）｜**余弦权重公式逐字符合**｜**时序映射 `idx=len.saturating_sub(1+i)` 中心=最新样本，方向没写反**｜**`[OVERLAY-LOCK-SCOPE-001]` 锁作用域正确**（锁内只快照、锁外绘制）。

**返工 2 项 + 待答 1 项（明日续）**：

1. ❌ **`BORDER_GRAY` 配色 R/B 互换**：COLORREF 是 `0x00BBGGRR`，字面量 `0x060607` → 正确 RGB `#070606`，实现为 `#060607`。**另外四色换算全对**，唯独边框三通道近似相等所以错了肉眼不可辨（差 1/255）。coder-1 已在 `logs/20260805.md` 记录并开始修
2. ❌ **性能四项未报数**：只报了 fps 59.1/59.0。仍需 ① 平均帧间隔（应≈16.7ms）② **单帧绘制耗时 <5ms** ③ 录音期 `top -pid` CPU ④ 音频线程无丢帧证据
3. ❓ **待答**：实现了 `STOP_BTN_SIZE`/`STOP_BTN_INNER` 停止按钮，但主控 1:1 规格表无此项，需指出对应 Windows 侧行号或说明自行增补理由

## 三、明日第一件事：派 **浮层接线**（「完整体验」最后一块）

把 coder-1 的 `overlay.rs` 挂进 `run_controller_macos` 的 `PipelineEvent` 消费处。**coder-2 空闲，边界上归它**（它独占 `main.rs`/`macos/mod.rs`）。

## 四、Gavin 已拍板但尚未落地的决策

| 决策 | 内容 |
| --- | --- |
| **形态定案** | **NSStatusItem 托盘路线**（非 IMKit 输入法）。理由：飞音是纯语音不处理键盘，做成输入法会导致切过去就打不了字、且需反复切换输入源。**主控 WebSearch 核实竞品 Typeless 同样是独立 App + 辅助功能注入 + 全局热键，不注册输入法** |
| IMKit | 「后续看需要再考虑」，短期不做 |
| `MACOS-P4-AXINJECT-001` | **独立 P1**，不与 READBACK 捆绑（已写入 todo 专节） |

## 五、⚠️ 遗留问题与风险（明日需处理）

| # | 项 | 说明 |
| --- | --- | --- |
| 1 | **工作区大量未提交** | 15 改动 + 3 新文件（`overlay.rs` / `tray.rs` / `logs/20260805.md`）。`origin/main` 之后本地已有 3 提交（`57be792` / `cc12381` / `5e496de`）**ahead 未 push**。建议明日尽快 commit，降低爆炸半径 |
| 2 | `src/bin/probe_overlay.rs` **未删** | 任务书要求跑完删除；coder-1 返工中可能仍在用，**验收时必须确认已删** |
| 3 | **rustfmt 跨平台漂移未解决** | `src/itn.rs`(+135/−111) 与 `src/llm/mod.rs`(+83/−38) 是 `cargo fmt` 全量连带。主控已证「当前内容 == `rustfmt(HEAD)`，零人为改动」。**两条修法均实测不可行**：`style_edition=2021` 反引入 3 文件新 churn；`rust-toolchain.toml` 钉 1.97.1 触发 rustup 解析卡死并拖慢所有 Worker（已撤销）。**根因是版本漂移而非配置漂移，需先问 Windows 侧的 `rustfmt --version` 再钉**。本侧 rustfmt 1.9.0-stable |
| 4 | **Tauri 设置 UI 的 macOS 产物未构建** | 托盘「设置」点了会报 `feiyin-ime-ui not found`（**路径已修对，只是产物不存在**）。构建需 npm，在禁令内，**等 Gavin 授权** |
| 5 | `foreground_window_id()` 返 0 | macOS 第一版降级，`focus_lost` 恒 false → 总是直接注入、不走失焦预览 |
| 6 | 闭环端测未由物理按键确认 | coder-1 用 `osascript` 模拟不经 CGEventTap；tester-1 A4 用 enigo 合成注入（Session tap 可见，证据成立但非硬件按键）。**最终确认需 Gavin 亲自按热键说一句话** |

## 六、Worker 状态

三个 Worker 均已通知暂停待命：coder-1（OVERLAY 返工中，可收尾后停）/ coder-2（空闲）/ tester-1（空闲）。

---

# 2026-08-05 11:4x-12:0x — orchestrator — 新 session 启动 + OVERLAY-001 验收 + WIRE-001 派发

## 一、Worker 启动（三个全就绪）

- coder-1 / coder-2 / tester-1 均为 OpenCode（`DeepSeek V4 Flash Free`，OpenCode Zen）。**注意：coder-1 不是 Codex**，故框架规则 ⑩「Codex ACK 后发 /permissions Full Access」本次不适用
- tester-1 启动时卡在 OpenCode 权限弹窗（`collab/worker-panes/*` 与 `collab/acks/tester-1/*` 外部目录访问），主控代为 Allow always 放行后恢复

## 二、MACOS-P4-OVERLAY-001 验收通过（返工版）

coder-1 三项声明**独立取证全部属实**（未采信汇报）：

| 声明 | 取证方式 | 结果 |
| --- | --- | --- |
| 返工已交付 | `git show d7b9f39 --stat` | ✅ `overlay.rs` +774 行在提交内 |
| 探针已清理 | `ls src/bin/probe_overlay.rs` | ✅ No such file |
| 工作区干净 | `git status --short` | ✅ 空输出 |
| BORDER_GRAY 修正 | `git show d7b9f39:...overlay.rs` grep | ✅ `(0.02745, 0.02353, 0.02353)` = `#070606`，COLORREF `0x00BBGGRR` 换算正确 |
| SIGBUS 重构 | grep `ACTIVE_OVERLAY` | ✅ 已消失，改由 `OverlayIvars` 直持 `levels` |

## 三、🔴 主控独立查出、Worker 未报告的一件事

**`src/platform/macos/overlay.rs` 不在模块树内** —— `src/platform/macos/mod.rs` 只声明了 accessibility/event_loop/hotkey/injection/tray 五个模块，**无 `mod overlay;`**。

上一轮它是靠临时 probe bin 的 `#[path]` 挂载才被编译的。**probe 删除后 overlay.rs 成为孤儿文件**：

- 不参与编译，`cargo check --all-targets` 对它是空跑
- 5 条 `#[test]` **一条都不会进 `cargo test`**
- 即：「probe 删除后 0 errors / 测试全绿」这条证据**对 overlay.rs 完全无效**

**教训**：`#[path]` 挂载 + 临时 bin 的验证方式，在临时 bin 删除的瞬间会让被验证对象**静默退出编译单元**，而所有常规验证命令（check/test/fmt）**全部照常绿**。这与 DEC-033 警告的 `#[cfg]` 静默漂移是同一类失败模式——**不响亮的失败**。后续凡用 `#[path]` 探针验证新模块，任务书必须把「探针删除后如何保证被测模块仍被编译」写成显式验收项。

## 四、MACOS-P4-OVERLAY-WIRE-001 派发（coder-1）

**边界评估**：接线必然同时改 `main.rs` + `macos/{mod,event_loop,overlay}.rs` 四文件，**无法拆给两个 Worker**，故推翻昨日断点存档「归 coder-2」的安排，整体归 coder-1（它独占 overlay.rs 且刚踩过 AppKit 线程坑）。coder-2 本轮不分配任务，避免文件级重叠。

**方案要点**：
- 核心约束：`RecordingOverlay::new()` 有 `MainThreadMarker` 断言，而 `handle_pipeline_event` 跑在逻辑线程 → **禁止直接调用**
- 解法：**照抄托盘已跑通的模式** —— `request_overlay()`（任意线程）+ `poll_pending_overlay()`（主线程，挂进 `event_loop.rs:195` 已有的 15ms timer，紧邻 `poll_pending_tray_states`）
- 单例用 `thread_local!`（`RecordingOverlay` 非 Send/Sync）
- 生命周期推荐 B（Show 时 new / Hide 时 destroy），理由：16ms timer 无条件常驻 = 62 次/秒唤醒常驻，托盘程序空闲烧电池不可接受；允许 coder-1 带实测数据反驳
- PipelineEvent 七分支映射真值表（Processing 也必须 Hide，否则挂一条静止波形误导用户）

**方案协商 1 轮（coder-1 提出，主控采纳）**：任务书 §3.A.3 要求在 `platform/mod.rs` 导出，§5 却把该文件排除在允许清单外 —— **确为自相矛盾**。主控核实 `src/platform/mod.rs:88-93` 的 TRAY-001 先例后裁定：允许新增独立 `#[cfg(target_os = "macos")]` `pub use` 块，三条硬约束（不碰 `:68-77` Windows 块 / 不进共享清单 / `poll_pending_overlay` 不导出走内部路径）。补丁 R1 已追加至 `inbox/coder-1/task.md`。

## 五、TEST-SYNC 同步派发（tester-1）

规则 ⑧「代码任务必须同步派 TEST-SYNC」与 worker-guide §二「五阶段禁止并行」冲突（同文件撞车）。**处置**：派**只读预研**版——读代码、设计用例、产出写 `outbox/result.md`，**禁止改任何 `src/**`、禁止跑任何命令**。阶段三落地与阶段四执行待 coder-1 交付后另派。

核心要求：走查 overlay.rs 现有 5 条单测**哪几条是空壳**（它们从未真正跑过）+ 七分支真值表 + 线程契约可测性 + `poll_pending_overlay` 非阻塞性判据 + 盲区变化预估。

## 六、待 Gavin 拍板（4 项，均不阻塞当前任务）

1. 是否授权 `git commit` / `push`（本地 ahead 3 未 push）
2. 是否授权 npm 构建 macOS 侧 Tauri 设置 UI 产物（托盘「设置」现报 `feiyin-ime-ui not found`，路径已对、产物不存在）
3. `MACOS-P4-AXINJECT-001`（Gavin 已定 P1）是否本轮开派给 coder-2 —— 文件域 `injection.rs` 与本批零重叠，可安全并行
4. 指示灯形状：Windows 实为麦克风图标（`main.rs:1099-1167`），现实现为实心圆。**主控已裁定本轮不动**（视觉保真不阻塞闭环，混改会让接线回归判别力失效），列入 OVERLAY-002

---

## MACOS-P4-OVERLAY-WIRE-001 · 录音浮层接线（Phase 4「完整体验」最后一块）

### 任务
把 `RecordingOverlay`（OVERLAY-001 交付的孤儿文件）接进 `run_controller_macos` 的 PipelineEvent 消费路径：热键按下→浮层出现并 60fps 跟随波形→转写结束→浮层消失。本轮只做 Recording 态。

### 方案协商
提 1 点异议：§3.A.3 要求在 `platform/mod.rs` 导出 overlay 符号，但 §5 边界排除该文件。主控裁定（补丁 R1）：允许，仿 TRAY-001 第 88-93 行模式加独立 macOS cfg 块，三条硬约束（不碰 Windows 块/不进共享清单/poll 不导出）。允许文件 5 个。其余方案全同意（线程模型 tray 模式/生命周期 B/init_levels 方案②/15ms timer 复用/指示灯维持实心圆）。

### 改动（5 文件）
1. `src/platform/macos/mod.rs`：`mod overlay;` + `pub use overlay::{init_overlay_levels, request_overlay, shutdown_overlay, OverlayRequest};`
2. `src/platform/macos/overlay.rs`：`OverlayRequest` enum + `PENDING_REQUEST`/`OVERLAY_LEVELS` static + `thread_local! OVERLAY` + 4 公开函数（init/request/poll/shutdown）；摘 `hide()`/`is_visible()` 的 `#[allow(dead_code)]` 补注释
3. `src/platform/macos/event_loop.rs`：`controller_timer_callback` 紧接 tray poll 后加 `overlay::poll_pending_overlay()`
4. `src/platform/mod.rs`：新增独立 `#[cfg(target_os="macos")]` `pub use` 块（仿 tray，Windows 块零触碰）
5. `src/main.rs`：`run_controller_macos` 调 `init_overlay_levels`；`handle_pipeline_event` 七分支加 `request_overlay`（RecordingStarted→Show，其余→Hide）；shutdown 调 `shutdown_overlay`；订正 :2711 注释

### 关键设计
- 线程模型照搬 tray：任意线程 `request_overlay`→`PENDING_REQUEST` Mutex；主线程 15ms timer `poll_pending_overlay` 消费（try-lock 不阻塞）
- 生命周期 B：Show→无实例则 `new()`（含 16ms timer）；Hide→`slot.take()`→Drop→`destroy()`（invalidate timer）。空闲无 timer 唤醒，省电
- `thread_local! OVERLAY: RefCell<Option<RecordingOverlay>>` 天然保证主线程独占，规避不 Send/Sync
- levels 用 `Arc::clone`（非 take）保留供多次 Show 复用；与 worker 线程共用同一个 Arc

### 验证
- `cargo check --all-targets` **0 errors**。overlay.rs 首次进模块树新 warning（如实列出，生命周期 B 下 dead code 为 OVERLAY-002 保留）：`hide/is_visible/stats never used`/`field stats never read`/`snapshot never used`/`StatsSnapshot never constructed`/`unused doc comment`
- `cargo test --bin feiyin-ime` **821 passed / 0 failed / 6 ignored**。`--list | grep overlay::` = 14（platform::macos::overlay 5 条**首次真实运行** + ui::overlay 9 条）
- `cargo fmt --check` clean（初查 1 长行已 fmt 修复）
- `git diff --numstat src/platform/windows/` 空（Windows 零回归）
- `git diff src/platform/mod.rs` 自证改动全在 macOS cfg 块

### 实机闭环 — 如实降级
无法实机：`libctranslate2.4.dylib` 未部署（`docs/MACOS-HANDOFF.md` §3.1/§五已记录 macOS 环境 gap，非本任务引入），dyld 加载即失败。未用 osascript 模拟（任务书禁止）。**静态闭环验证**：代码走查确认路径完整可达——hotkey Start→handle_hotkey_event→WorkerCommand::Start→worker send_event(RecordingStarted)(:2310)→handle_pipeline_event→request_overlay(Show)→PENDING_REQUEST→15ms timer poll_pending_overlay→thread_local new()→show；Hide 六分支同理。

### 边界红线
仅改 5 文件；Windows/Cargo.toml/版本号/Publish/ui/src-tauri 零触碰；未 build --release/出包/npm；未用 git 破坏命令；未建临时 probe；UTF-8 无 BOM 用 edit 工具。

### 临时文件清理
未新建任何临时文件；`/tmp/voice-ime-test/`（实机尝试日志）已删。

---

# 2026-08-05 12:1x — orchestrator — WIRE-001 验收 + 🔴 查出 P0 跨平台缺陷

## 一、WIRE-001 接线本身：验收通过（主控独立取证，未采信汇报）

| 项 | 取证方式 | 结果 |
| --- | --- | --- |
| 模块树接入 | `grep "mod overlay" macos/mod.rs` | ✅ `:18 mod overlay;` |
| overlay 单测首次真跑 | 主控自跑 `cargo test --bin feiyin-ime -- --list \| grep overlay` | ✅ `platform::macos::overlay::tests::*` **5 条**在列（decay_rate / indicator_state / center_symmetric / height_mapping / weight_center） |
| platform/mod.rs 边界 | `git diff src/platform/mod.rs` | ✅ 只加 `:95-96` cfg(macos) 块，Windows `:71-77` 块零改动 |
| windows/ 零改动 | `git diff --numstat -- src/platform/windows/` | ✅ 空 |
| 七分支映射 | `grep request_overlay src/main.rs` | ✅ 1 Show + 6 Hide = 7 分支齐全 |
| timer 接入 | `sed -n '193,200p' event_loop.rs` | ✅ 紧邻 `poll_pending_tray_states`，注释标注 try-lock 非阻塞 |

## 二、🔴 P0：macOS 专用函数缺 cfg 门控 → Windows 编译必炸

**判据是 Rust 语言规则，不需要 Windows 机器即可定论。**

`src/main.rs` 两个函数**头上无任何 `#[cfg]`**（前一行均为文档注释，逐行核对）：`:2880 handle_hotkey_event`、`:2930 handle_pipeline_event`。`grep '#\[cfg'` 在 2600–3000 区间仅 6 条，**最后一条 `:2701` 挂在 `run_controller_macos` 上**，其后至 3000 行无 cfg。

而 `handle_pipeline_event` 体内引用了 macOS 专属符号：

| 符号 | Windows 侧 | 引入者 |
| --- | --- | --- |
| `platform::request_tray_state` | ❌ 不在 `platform/mod.rs:71-77` Windows 清单，`src/platform/windows/` 亦无定义 | **MACOS-P4-TRAY-001（既有，08-04 coder-2）** |
| `platform::request_overlay` / `OverlayRequest` | ❌ 仅在 `:95-96` cfg(macos) 块导出 | **本次 WIRE-001，7 处** |

Rust 对无 `#[cfg]` 的 item 一律编译并类型检查（未被调用也照查，dead_code 仅 warning）→ Windows `cargo check` 必报 E0425/E0433。

**归因**：`request_tray_state` 那条自 TRAY-001 起已存在，**Windows 侧已处于编不过状态一天多而无人察觉**（本机无法编译 Windows）。WIRE-001 把违规点从 1 个放大到 8 个，非首犯。

**这是 DEC-033 附则二警告的原话在本仓库的第一次真实兑现**——「破坏发生在提交那一刻、暴露在数周后切换机器那一刻」。

### 必须固化的判据修正（写给所有 Agent）

> **共享文件（`main.rs` / `config/mod.rs` 等）里的 macOS 代码，「Windows 零回归」的证据不是「没碰 `src/platform/windows/` 目录」，而是「每一处平台专属符号引用都落在对应 cfg item 内」。**
> `git diff --numstat -- src/platform/windows/` 为空是**必要不充分**条件。此前三个 macOS 任务（FIXHOTKEY / NEUTRAL / TRAY）的红线自证全部用的是前者口径，故全部漏掉了本缺陷。

## 三、既有汇报的一处纠正

coder-1 报「实机闭环降级：CT2 dylib 缺失致 dyld 加载失败」。**主控实测该归因不准确**——dylib 存在：`target/release/` 下有 `libctranslate2.dylib` / `libctranslate2.4.dylib` / `libctranslate2.4.6.0.dylib` / `libsherpa-onnx-c-api.dylib` / `libonnxruntime.dylib`。系跑的是 `target/debug/` 二进制、`@rpath` 解析不到，**是运行方式问题不是产物缺失**。已要求改用 `DYLD_LIBRARY_PATH="$PWD/target/release"` 重试。

## 四、MACOS-P4-CFGGATE-001 已派发（coder-1）

A 补两处 cfg（调用点安全性主控已预核：唯一调用点 `:2793`/`:2839` 均在 cfg(macos) 的 `run_controller_macos` 内）｜**B 系统性扫 11 个 macOS 专属符号在 `main.rs` 的全部引用点并列表**（比 A 更重要）+ 反向扫 Windows 专属符号被 macOS 引用｜C 实机闭环用 `DYLD_LIBRARY_PATH` 重试。

---

## MACOS-P4-CFGGATE-001 · 修 macOS 专用函数缺 cfg 门控致 Windows 编译必炸（P0）

### 缺陷
`src/main.rs` `handle_hotkey_event`(:2880) 与 `handle_pipeline_event`(:2930) 头上无 cfg 门控。后者引用 `platform::request_tray_state`(7处,TRAY-001 既有违规)/`request_overlay`(7处,WIRE-001 新增)/`OverlayRequest`(7处)——三者 Windows 侧均不存在 → Windows cargo check 必 E0425。WIRE-001 把违规点从 1 个变 8 个。

### 改动（1 文件 2 行）
两函数各加 `#[cfg(target_os = "macos")]`。安全性：唯一调用点都在 cfg(macos) 的 run_controller_macos 内逻辑线程闭包（:2793/:2839），加门控后 Windows 侧连同调用点消失，无悬空引用。

### B 项系统性扫描（11 macOS 专属符号 + 反向）
逐个 grep 所有引用点判定所在 item 是否 cfg(macos)。唯一漏网=handle_pipeline_event（已修）。handle_hotkey_event 虽不引用 macOS专属符号但加 cfg 保语义清晰。反向扫描 SendHwnd/spawn_overlay_thread/OverlayThreadHandle/OverlayCommand 无交叉污染。

### 验证
- cargo check --all-targets 0 errors
- cargo test --bin feiyin-ime --no-fail-fast **821/0/6**
- cargo fmt --check clean
- git diff --numstat src/platform/windows/ 空

### C 项实机（主控纠正有效）
CT2 dylib 确在 target/release/，用 `DYLD_LIBRARY_PATH="$PWD/target/release"` 指向 release 后 debug 二进制成功启动。启动链完整（audio 预热→ASR→hotkey CGEventTap(Session)→tray→message loop），RUNNING 零 panic。kill -INT 干净退出（SIGINT→request_stop→logic thread exiting→exited cleanly），无残留。**浮层 Show/Hide 端到端降级**：无法物理按热键（远程 Agent），osascript 不经 CGEventTap 禁用；启动链+退出链已实机验证，浮层视觉行为需 Gavin 亲按 F6 确认。本任务不改变 WIRE-001 接线逻辑（已验收）。

### 教训
判据更新：共享文件里的 macOS 代码，零回归证据不是「没碰 windows/ 目录」，而是「每一处 macOS 专属符号引用都在 cfg(macos) item 内」。git diff windows 空只证明未改 Windows 目录，不证明 Windows 能编译。

### 临时文件清理
未新建临时文件；/tmp/cfggate-run.log 已删。

---

## TEST-SYNC-P4-OVERLAY-WIRE-001 · 阶段三预研（tester-1，只读零改动）

- 性质：只读预研，零 src/ 文件改动，未执行任何 cargo/npm 命令（避免与 coder-1 抢 target 锁）。
- 走查结论：overlay.rs 现有 5 条单测全部为「独立重算的恒等式/常量自比」，未触及 RecordingOverlay/draw_overlay，接进模块树后必然全绿但保护价值低；仅 indicator_state_logic 半有效（调真实 AudioLevelBuf/push_level）。
- 契约前置（已 flag 主控）：request_overlay/poll_pending_overlay 当前不存在，由 coder-1 本轮新增；测试依赖其签名与 last-wins 语义。
- 核心需求：① 线程契约 new() 非主线程 Err（可单测）② 请求通道 last-wins ③ PipelineEvent 7 分支真值表（推荐抽纯函数 overlay_command_for_event）④ poll 非阻塞（行为判据并入 2.2）⑤ Windows 零回归（git diff + 共享测试 + 端测）。
- 盲区预估：overlay 进树 macOS +13~15 可见、Windows +0、盲区分母仍≈17。
- 产出：collab/outbox/tester-1/result.md；阶段三落地待 coder-1 交付后派发。

## 五、MACOS-P4-CFGGATE-001 验收通过（主控独立重扫，未采信 Worker 扫描表）

主控**自写脚本重扫**（解析 main.rs 全部顶层 item 的 cfg 归属，再把 11 个 macOS 专属符号的每个引用点映射到所属 item）：

| 项 | 结果 |
| --- | --- |
| 11 符号引用点门控 | **未门控引用点 0**（`request_tray_state` 7 处 / `request_overlay` 7 处 / `OverlayRequest` 7 处 / `request_stop` 4 处 / `TrayCommand` 3 处 / 其余各 1 处，**全部落在 cfg(macos) item 内**） |
| 两处 cfg 已加 | ✅ `:2880` `handle_hotkey_event` / `:2931` `handle_pipeline_event` |
| diff 增量自洽 | WIRE-001 后 `main.rs` 为 21/3，CFGGATE 后 23/3 → **恰好 +2 行**，与「只加 2 行 cfg」吻合 |
| `cargo check --all-targets` | **CARGO_EXIT=0，error 计数 0**（`Finished dev profile in 5.34s`）。注意：首次取值时误用了 grep 管道的退出码，已重取 cargo 自身退出码 |
| 新增 warning | 无。`unused import: platform::HotkeyEvent`(:27) 经 `git show HEAD` 比对为**既有**——非限定引用点 `:1897/:1950/:1967` 全在 `#[cfg(windows)]` 的 `process_controller_events` 内，macOS 上本就用不到 |
| windows/ 零改动 | ✅ `git diff --numstat -- src/platform/windows/` 空 |

**主控追加的全仓扫描（Worker 未做，任务书也没要求，是主控自加的）**：11 个符号在 `src/` 下、`platform/macos/` 与 `platform/mod.rs` 与 `main.rs` **之外零引用** → 全仓干净，不只是 main.rs 干净。

**🟢 Windows 编译阻塞解除**（限于静态判据；本机无 Windows 工具链，无法实证编译）。

## 六、仍未闭合：实机浮层视觉确认

coder-1 报「实机启动链验证通过（浮层降级）」—— 即进程能起、链路日志正常，但**没有目视确认浮层真的出现/波形真的动/结束真的消失**。

这是 WIRE-001 与 CFGGATE-001 两轮累计的**唯一遗留缺口**。可选路径：① tester-1 走阶段四/五出 release 包后实机 ② Gavin 亲自按热键目视。**建议 ②**——按 `[VERIFY-001]` 强制规则，原生窗口行为的最终判据本就必须人工目视，自动化覆盖不到。

## 七、MACOS-P4-AXINJECT-001 验收通过（coder-2）+ 主控查出静默丢词风险

**三条主控裁定逐条 Read 代码核对，全部落实**：`kAXSelectedTextAttribute`（注释显式声明绝不用 `kAXValueAttribute`）｜双重 0.5s 超时（system-wide + focused）｜四处 `AXError` 全部对 `AX_ERR_SUCCESS` 校验｜CF 内存 Create/Copy 用 `wrap_under_create_rule`、常量用 `wrap_under_get_rule`（语义正确）｜AX 置于 `use_clipboard` 判断之外（两分支都先试 AX）｜失败走 `log::info!` 不刷屏｜缺陷 2 恢复失败告警已补。`CARGO_EXIT=0`/error 0；`windows/`、`Cargo.toml` 零改动；源文件仅 `injection.rs`（+122/−1）。

### 🔴 主控 code review 查出：AX 报成功 ≠ 文字真的进去了

`inject_via_ax` 的成功判据是 `SetAttributeValue` 返回 `kAXErrorSuccess`，而 `inject_text` 拿到 Ok 即 return、**不再走剪贴板兜底**。

部分 App（Electron/Chromium 系、部分 Java Swing）AX 为**部分实现**——接受调用并返回成功，但不真的插入文字。命中即：**用户说完一整段，屏幕上什么都没有**。

**这推翻了本任务立项时写在任务书里的安全前提**——「AX 写不进去就回退剪贴板，**最坏情况等同现状，不会更差**」。该前提只在「写不进去 = 返回错误」时成立；「写不进去但返回成功」这条路径**比改动前更差**，且是 Gavin 最难反馈的一类（他只会觉得「这次没识别出来」）。

→ 派 `MACOS-P4-AXINJECT-002`：`AXUIElementIsAttributeSettable` 预检（向目标 App **询问**可写性，比事后看返回码更早拦截）+ AX 成功日志带 `AXRole`（排障用）。任务书明确否定两条歧路：**写后读回验证**（选区插入后塌缩为空，读回空串不能作判据）、**AXRole 白名单**（误伤自定义文本控件）。coder-2 独立复核后认同该两条否定理由。

## 八、TEST-SYNC 预研交付（tester-1，13683 B）+ 由此派生 OVERLAY-WIRE-002

**tester-1 最有价值的一条判定（主控复核认同）**：overlay.rs 现有 5 条单测 **4 条凑数 / 1 条半有效**——

| 测试 | 判定 |
| --- | --- |
| `decay_rate_matches_windows` | ⚪ **空壳**：`assert_eq!(常量, 它自己的字面量)`，教科书式自证断言 |
| `waveform_weight_center_is_largest` / `waveform_height_mapping` / `waveform_center_symmetric_mapping` | ⚪ **凑数**：独立重算数学公式，未调用生产绘制路径；生产改公式测试不会红。且 `lx<rx` 对任意 i≥0 恒真、`assert_eq!(8<=16)` 恒真 |
| `indicator_state_logic` | 🟡 **半有效**：用了真实 `AudioLevelBuf`/`push_level`，但阈值映射是局部重写 |

**结论：本次接线目前零真护栏。** 5 条测试是「接线后首次进树」的通行证（保证会说「跑过了」），不是质量护栏。

**tester-1 另一条建议（主控采纳）**：七分支真值表内联在 `handle_pipeline_event` 里，夹着 `log::*` + `request_tray_state` + `request_overlay` 三种副作用，单测无法调用断言，只能静态走查 → 派 `MACOS-P4-OVERLAY-WIRE-002` 抽纯函数 `overlay_request_for_event`。

**主控在任务书里追加的一条硬约束**：必须穷举 match，**禁止 `_ => Hide` 通配符**——通配符会让将来新增的 `PipelineEvent` 变体**静默落进 Hide**，穷举则编译器报错强制作者决策。这正是本批次一路踩过来的同一类病（局部规则不声明适用边界 → 在更大上下文里静默生效，见 `[ITN-LOCAL-RULE-OVERREACH-001]`）。

## 九、实机验证归属澄清

分 App 实机验证已从 coder-2 手里收回：① worker-guide §十「Worker 分工边界」明确运行时验证是 tester-1 职责，coder 不做 ② 听写链路需真人说话，Agent 无法完成。改由 tester-1 用**探针直调 `inject_text`**（绕开 ASR）做分 App 覆盖，配 `SENTINEL-12345` 剪贴板哨兵法验证「AX 成功时剪贴板确实未被动过」。

---

## MACOS-P4-OVERLAY-WIRE-002 · 抽出七分支纯函数使真值表可测

### 改动（2 文件，纯重构零行为变更）
- src/main.rs：新增纯函数 `overlay_request_for_event`（cfg(macos)，穷举 match 无通配符，RecordingStarted→Show，其余六态→Hide）；handle_pipeline_event 删七分支内 request_overlay 行，改 match 前统一调一次
- src/platform/macos/overlay.rs：OverlayRequest derive 加 PartialEq/Eq（供单测 assert_eq）

### 验证
- cargo check --all-targets 0 errors
- cargo test --bin feiyin-ime --no-fail-fast **821/0/6**（与 CFGGATE 基线逐数一致，零行为变化）
- cargo fmt --check clean
- git diff --numstat src/platform/windows/ 空
- 行为等价自证：七分支重构前后 overlay 指令逐条一致（RecordingStarted→Show，其余→Hide）

### 边界
仅 main.rs + overlay.rs derive 一行；未碰 injection.rs（coder-2 AXINJECT 域）；未写测试（tester-1 阶段三）；未出包/改版本号/npm；UTF-8 无 BOM。

---

## MACOS-P4-OVERLAY-WIRE-003 · poll_pending_overlay 注释声称 try-lock 但实现是阻塞 lock

### 缺陷
poll_pending_overlay 注释写「try-lock 形态绝不阻塞」，实现是 PENDING_REQUEST.lock().ok()（阻塞 lock，.ok() 只处理中毒不处理竞争）。注释在撒谎。

### 方案选择
A（改实现对齐注释）。timer 回调「结构上不可能阻塞」比「实测通常不阻塞」强；漏一拍 15ms 后补取无感知。

### 改动（1 文件 1 行 + 注释）
src/platform/macos/overlay.rs：PENDING_REQUEST.lock()→try_lock()；注释补漏拍说明；OVERLAY_LEVELS.lock() 保持阻塞但加注释如实说明（只在建浮层时走到、微秒级临界区、无竞争；timer 不阻塞由 PENDING_REQUEST try_lock 守住）。

### 验证
cargo check 0err / cargo test --no-fail-fast 821/0/6(与基线一致) / fmt clean / 本轮仅动 overlay.rs（injection.rs 是 coder-2 并行工作）。

### 边界
仅 overlay.rs；未碰 injection.rs/main.rs/Cargo.toml/windows；未写测试；未出包/改版本号/npm；UTF-8 无 BOM。overlay.rs 已腾出，tester-1 阶段三可开始。

---

# 2026-08-05 — tester-1 工作日志（续）

## TEST-SYNC-P4-OVERLAY-WIRE · 阶段三落地

预研已验收（主控纠正 `decay_rate_matches_windows` 为有效常量钉，非自证断言，**保留**）。写测试，零边界违规。

交付 4 组 / 5 条新 `#[test]`，全在 `mod tests` 块内：
- **2.1 七分支真值表**（`main.rs` → `mod overlay_wire_tests`）：模块+函数均 `#[cfg(target_os = "macos")]`，7 断言调真实 `overlay_request_for_event`+真实 `PipelineEvent`，RecordingStarted→Show / 其余六→Hide，无 `_=>` 兜底，注释写明临时契约。
- **2.2 last-wins**（`overlay.rs`）：自建 `static TEST_GUARD: Mutex<()>` 串行化（不依赖 --test-threads=1）；正向+反向+跨线程（spawn 连发），poll 后断言 `PENDING_REQUEST` 排空。
- **2.3 线程契约**：`RecordingOverlay::new` 非主线程 `is_err()`（MainThreadMarker 短路安全）；`poll_pending_overlay` 测试线程 Show 不 panic、slot 保持 None。
- **2.4 标注**：三无效测试各加 ⚠️ 诚实注释；`decay_rate_matches_windows` 保留。

验收：`cargo check --all-targets` 0 errors｜`cargo test --bin feiyin-ime --no-fail-fast` **826/0/6（基线 821 净增 +5，全绿）**｜`cargo fmt --check` clean。生产零改动：我的 diff 全在 `mod tests`；`overlay_request_for_event`/`handle_pipeline_event` 等生产段为 coder-1 基线既有（不开工前已 6 M）。未碰 injection.rs/未出包/未 build--release/未 npm/未改版本号；无 git 破坏性命令。UTF-8 无 BOM。结果：outbox/tester-1/result.md。

---

## MACOS-P4-UIBUILD-001 · 构建 macOS Tauri 设置界面产物

Gavin 授权 npm。托盘点「设置」报 `feiyin-ime-ui not found` → 产物不存在（`src-tauri/target/release/feiyin-ime-ui`、`target/release/feiyin-ime-ui` 均无，`ui/dist/` 空）。

执行（零生产代码改动）：
- Step A 基线：lock 干净，sha256 `00aac05b…d909f`。
- Step B `npm run build`（未 npm install）→ 成功，dist 13:28 重建（`index-CTgGziQm.js` 201.90kB / `index-BNQZfcUG.css` 25.01kB / index.html / overlay.html / icons/）。
- Step C 跳过（B 未因缺依赖失败），lock 零触碰。
- Step D `cargo build --release --features custom-protocol`（src-tauri）→ 成功 1m54s，13 条既有 unused-method 类 warning。
- Step E `cp` 至 `target/release/feiyin-ime-ui`，两处 sha256 `b68d82e2…e50` 一致，mtime 13:30。

运行时验证：直接启动 UI 进程存活；AX 读到窗口标题「飞音智能语音输入」、1179×720、group+3 button（Tab 导航）+static text；WebKit.WebContent 渲染进程在跑 → 非白屏。截图 /tmp/ui-verify/window.png。

平台路径核实：main.rs:188 macOS 常量 `"feiyin-ime-ui"` 裸名 ✅；`spawn_settings_process`（main.rs:415）先查 `exe_dir/`（=target/release/，已就位）再回退 src-tauri/target/{release,debug}/ ✅。另发现 target/debug/feiyin-ime（12:00 构建）dev 路径同样可命中 src-tauri/target/release。

验收 6 项全过；`git status src/` 非空均为 coder-1/coder-2 在途（本任务零改动）。未碰占用文件/未 build--release 主程序/未改版本号/未同步 Publish/未 npm install/无 git 破坏性命令。UTF-8 无 BOM。结果：outbox/tester-1/result.md。

---

# 2026-08-05 — coder-1 工作日志（续）

## MACOS-P4-SCENE-001 · 场景感知真实现（stub → real）

### 背景
`src/platform/macos/mod.rs` 的 `capture_scene_signals` 是 stub（`log::warn!` + `return None`），macOS 上所有场景降级 Unknown，LLM 拿不到 F4 场景风格指令。补丁 R1 追加 D 项：`foreground_window_id()` 真实现（前置 FocusLost 浮层开关）。

### 关键实现决策
1. **pid 来源不用 NSWorkspace 生成绑定**：`NSWorkspace` feature 未启用（禁改 Cargo.toml），且任务书 D-1 写的 `NSWorkspace::sharedApplication()` 方法名有误（`sharedApplication` 是 NSApplication 的）。改用 objc2 `msg_send` 动态调用 `NSWorkspace.sharedWorkspace.frontmostApplication.processIdentifier`（运行时方法查找，不受 feature 门控，AppKit 已被 overlay/tray 链接必然可加载），失败降级 AX `kAXFocusedApplicationAttribute`。
2. **exe 名用 `proc_pidpath`**（libSystem，自动链接）取可执行文件 basename，等价 `NSRunningApplication.executableURL().lastPathComponent()`，含空格名（`Microsoft Word`/`Google Chrome`）实测原样保留。
3. **AX 常量是 `#define CFSTR("...")` 宏非导出符号**：`kAXTitleAttribute`/`kAXFocusedWindowAttribute`/`kAXFocusedApplicationAttribute` 链接报 Undefined symbols（injection.rs 用的 3 个是历史导出符号）。改用 `CFString::new("AXTitle")` 等 Rust 字面量，语义与 CFSTR 宏完全等价。
4. **D-2 消除 A 项局限**：`capture_scene_signals(id)` 在 `id != 0` 时用 pid 定位 App（对齐 Windows「录音开始捕获、管线后期取场景」语义），`id == 0` 才降级实时查 frontmost。
5. AX 一律 `AXUIElementSetMessagingTimeout(0.5)`（AXINJECT-001 同理由，防无响应 App 阻塞管线线程）。

### 实测证据（临时探针 `src/bin/poc_scene.rs`，跑完已删）
前台 pid + (exe, title) 真实返回值（`launchctl asuser` 注入 Aqua 会话）：

| App | exe (proc_pidpath) | title (AX) |
| --- | --- | --- |
| Safari | `Safari` | "DeepSeek | 深度求索" |
| VS Code | `Code` | "Welcome" |
| Microsoft Word | `Microsoft Word`（含空格） | "催眠见生死之间.docx" |
| Claude | `Claude` | "Claude" |
| Terminal | `Terminal` | "CodeLab — tmux attach -t collab — 145×68" |
| Microsoft Edge | `Microsoft Edge`（含空格） | "DeepSeek Platform - Microsoft Edge" |
| Xcode | `Xcode` | "Xcode" |

classify 归属验证（include 真实 scene 模块调 classify_scene）：
- `Safari`→Browser(false)、`Google Chrome`→Browser、`Microsoft Edge`→Browser、`Firefox`→Browser
- `WeChat`→Chat(false)、`Claude`→Chat(false)
- `Code`→IdeTerminal(true)、`Xcode`→IdeTerminal(true)、`Sublime Text`→IdeTerminal(true)
- `iTerm2`→IdeTerminal(false)
- `Microsoft Word`→Doc(true)、`Obsidian`→Doc(true)、`Notes`→Doc(true)
- `Mail`→Email(true)
- Safari 标题含 "DeepSeek" 时被 chat title_keywords 正确覆盖为 Chat（浏览器细分预期行为）

### scene-rules.toml 追加（纯追加零删除，macOS 可执行名，与 Windows `.exe` 条目零冲突）
- chat/IM：`WeChat`/`Weixin`/`QQ`/`Telegram`/`Slack`/`Discord`/`Lark`/`DingTalk`/`WeChatHelper`
- chat/AI：`Claude`（实测）/`ChatGPT`
- email：`Microsoft Outlook`/`Outlook`/`Spark`/`Foxmail`（`Mail` 已存在）
- ide_terminal(false)：`Warp`/`Alacritty`/`kitty`（`Terminal`/`iTerm2` 已存在）
- ide_terminal(true)：`Code`（实测）/`Cursor`/`Zed`/`Sublime Text`/`IntelliJ IDEA`/`PyCharm`（`Xcode` 已存在）
- doc：`Microsoft Word`（实测）/`Obsidian`/`Notion`/`Bear`/`Typora`（`Notes`/`TextEdit`/`Pages` 已存在）
- browser：`Safari`（实测）/`Google Chrome`/`Microsoft Edge`（实测）/`Firefox`/`Arc`

### 验证
- `cargo check --all-targets` → 0 errors
- `cargo test --bin feiyin-ime --no-fail-fast` → **827 passed / 2 failed / 6 ignored**
- 🔴 **2 failed 是设计好的绊线（D-3 预告），如实报告不改测试（归 tester-1）**：
  - `foreground_window_id_returns_zero_in_first_version`：left=824 right=0（真实 pid 非 0）
  - `capture_scene_signals_by_id_returns_none_for_zero_id`：id=0 现在实时查 frontmost 取到信号
- `cargo fmt --check` clean
- toml 解析：9 scenes 全部合法，macOS 名已入 HashSet

### 行为变更告知（D-4，报给 Gavin）
`focus_lost` 从「恒 false」变为「真实生效」：用户录音途中切走窗口 → 文本不再直接注入，改走 `PipelineEvent::FocusLost` 分支（与 Windows 对齐的正确行为）。

### 边界
仅 `src/platform/macos/scene.rs`(新) + `src/platform/macos/mod.rs` + `scene-rules.toml`。未碰 main.rs/overlay.rs（tester-1 测试域）/injection.rs（coder-2）/Cargo.toml/windows/platform/mod.rs（基线遗留未动）。未出包/未改版本号/未 npm。git diff windows 空。UTF-8 无 BOM。

### 下游提醒（TOML-STALE-001）
`scene-rules.toml` 三副本（根目录 / target/release/ / Publish/）出包时必须三处同步，否则外置旧 toml 静默赢过新内置默认——同步归 tester-1 出包时处理。

---

# 2026-08-05 — coder-1 工作日志（续）

## MACOS-P4-SCENE-002 · 焦点身份从「应用级(pid)」提升到「窗口级(CGWindowID)」（Gavin 指令）

### 背景
Gavin 原话「力度必须要提到窗口级，否则不够精确」。旧 `foreground_window_id()` 返回 pid，同应用双窗口切换 `focus_lost` 判不出。基线 SCENE-001 交付后。

### 实现（仅 scene.rs）
1. `foreground_window_id()` → CGWindowID：frontmost pid → `CGWindowListCopyWindowInfo` → 第一个 `owner_pid==frontmost && layer==0` 的 `kCGWindowNumber`。
2. `capture_scene_signals(id)` id 语义 pid→CGWindowID：`window_id_to_owner_pid(id)` 反查 owner pid，找不到降级实时查 frontmost。
3. 用 `core_graphics::window` 现有 API（`copy_window_info` + kCG 常量），`CFDictionary<*const c_void,*const c_void>` 裸指针 key/值提取。
4. `dict?` 陷阱：downcast 失败改 `continue` 跳过而非提前返回整个函数。
5. 权限边界：不读 `kCGWindowName`（屏幕录制权限）；标题走 AX 已有辅助功能权限。

### 实测证据（临时探针 poc_scene2.rs 跑完已删）
- **决定性证据**：Terminal 同 pid 双窗口（CGWindowID 2230/690），AXRaise 切换前台 → `foreground_window_id()` 在 2230/690 间正确变化。窗口级身份生效。
- layer 过滤：56 窗口含 12 个非 0 layer（菜单栏/Dock 等）被正确排除，frontmost 只返回 layer==0 前台窗口。
- B 项时序：捕获 id=690 → 切前台 → 仍用 690 反查 → ("Terminal","CodeLab — tmux…")，不追实时焦点。
- 不存在的 id 正确降级实时查 frontmost。

### 验证
- `cargo check --all-targets` → scene.rs 0 errors
- `cargo test --bin feiyin-ime --no-fail-fast` → **836/1/6**
- `cargo fmt --check` clean
- git 确认本轮只改 scene.rs（mod.rs 是 tester-1 换锚 + SCENE-001 遗留，未动）

### 🔴 1 failed（非 SCENE-002 引入，未改测试）
- `overlay_wire_tests::overlay_request_for_event_truth_table`：Processing→ShowProcessing 非 Hide，coder-2 在途 overlay 工作
- 中途曾见 `foreground_window_id_contract` 红（tester-1 的存活 pid 断言，SCENE-002 pid→CGWindowID 后暂过期），终态已通过（tester-1 换锚/并发修复）

### 边界
仅 scene.rs。未碰 mod.rs/main.rs/overlay.rs/injection.rs/Cargo.toml/windows；未读 kCGWindowName；未用私有 API；未写测试；未出包/改版本号/npm；UTF-8 无 BOM。探针已删。

### 下游提醒
- tester-1：foreground_window_id_contract 已确认通过（pid → CGWindowID 换锚完成）
- 主控：SCENE-002 与 coder-2 overlay 并行可能短暂冲突（overlay_wire_tests 仍红，归 coder-2）

---

# 2026-08-05 — coder-2 工作日志（OVERLAY-002）

## MACOS-P4-OVERLAY-002 · coder-2 · 停止按钮可交互 + Processing/Error 两态浮层

- **改动**：仅 `src/platform/macos/overlay.rs` + `src/main.rs`。
  - **A（P0 停止按钮）**：`init_overlay_levels` 加参 `stop_recording/cancel`（与 worker 同一 Arc）存 `static OVERLAY_SIGNALS`；`OverlayView` 加 `mouseDown:`（locationInWindow→convertPoint→命中 `w-25/(h-16)/2` 矩形→双信号 true，CancelStop 同款）+ `acceptsFirstMouse:` true（非激活面板首击须接受）；非命中 return 不吞事件不抢焦点。
  - **B（Processing）**：`OverlayRequest::ShowProcessing(String)`；`OverlayMode::Processing(String)`；`draw_processing_overlay` 1:1 复刻 Windows（#181A18 背景/圆角16/#060607 边框/30-slice 高斯银色 shimmer 2.4s 周期/居中橙文案）；`overlay_request_for_event: Processing(_) => ShowProcessing(msg)`。
  - **C（Error）**：`OverlayRequest::ShowError{message, auto_close_ms}`；`RecordingOverlay::schedule_auto_close(ms)`（一次性 CFRunLoopTimer → request_overlay(Hide)）+ `cancel_auto_close`（销毁/切态 invalidate+release）；`draw_error_overlay` 1:1 复刻 Windows（#1A1D21 背景/圆角10/8px 红点 RGB(0.8,0.2,0.0)/橙左对齐文案）；映射 `Error(_) => 2000ms`、`FormatFailed => 2500ms`（文案由 handle_pipeline_event 按 ui_language 补 format_failed_hint）。
  - **文本绘制**：NSFont/NSStringDrawing/NSParagraphStyle feature 未启用且 Cargo.toml 禁改 → raw objc2（`AnyClass::get("NSFont")` + `msg_send_id!` + `NSMutableDictionary` setObject + `sizeWithAttributes:` 测量 + `drawInRect:withAttributes:`）。
- **验收**：`cargo check --all-targets` 0 errors；`cargo test --bin feiyin-ime --no-fail-fast` **836/1/6**；`cargo fmt --check` clean。
  - 🔴 1 failed = `overlay_wire_tests::overlay_request_for_event_truth_table`：**预期设计变更**，非回归。Processing/Error/FormatFailed 三条旧断言 Hide 改为 ShowProcessing/ShowError（测试注释已写明「OVERLAY-002 实现三态浮层时这张表要改」）。未改测试（归 tester-1）。
- **🔴 实机验证降级**：debug 链接被 AXINJECT 遗留 `_kAXFocusedUIElementAttribute/_kAXRoleAttribute/_kAXSelectedTextAttribute` Undefined symbols 卡死（本机 SDK/框架解析问题，非本任务代码），禁止 `cargo build --release` → 无法起主程序物理实测。四项真机验证（浮层出现/点停中止/Processing/Error 自动关闭）留待 Gavin 端测表。
- **边界**：未碰 mod.rs/scene.rs/scene-rules.toml/injection.rs/Cargo.toml/platform/mod.rs/windows/**；未新增需 mod.rs 导出的符号（OverlayMode 模块私有）；未改测试；未出包/改版本号/npm；UTF-8 无 BOM。

---

# 2026-08-05 — tester-1 工作日志（续）

## TEST-SYNC-P4-SCENE-002 · 二次换锚：foreground_window_id pid → CGWindowID（假绿修复）

主控排序失误致返工（SCENE-002 在换锚未完成时把返回值从 pid 提升为 CGWindowID），我上一轮思路正确。修掉 `foreground_window_id_contract` 的「假绿」——拿窗口号去 `kill(pid,0)` 探测进程语义不成立，本机绿只是巧合撞上存活 pid，换机/重启后可能 ESRCH 随机红。

改动（仅 `src/platform/macos/mod.rs` 的 `mod macos_platform_contract_tests`）：
- 保留 ① 紧邻两次调用一致（与语义无关，继续有效）。
- 删除 ②b `libc::kill` 存在性探测整段（libc 为全限定引用，无 import 清理）。
- ②a 上界从 `i32::MAX` 改为 `u32::MAX as usize`（CGWindowID 是 u32）。
- ②b 换成语义正确的**窗口列表反查强判据**：新增 `window_layer_by_id` 辅助，在 `CGWindowListCopyWindowInfo(OnScreenOnly|ExcludeDesktopElements)` 中反查 `kCGWindowNumber`，并断言其 `kCGWindowLayer == 0`（= scene.rs 筛选条件，闭环护栏）。理由：`core-graphics` 是直接依赖（Cargo.toml:113）、API 公共，测试直接调用即可，无需改生产可见性，优于退化 `u32::MAX` 上界。
- `pid_a`/`pid_b` → `wid_a`/`wid_b`，注释全部 pid → CGWindowID/窗口号。
- `capture_scene_signals_by_id` 那条锚「Some 时 exe 非空」，与 id 语义无关仍成立，未改。

验收：`cargo check --all-targets` 0 errors｜`cargo test --bin feiyin-ime --no-fail-fast` **836/1/6**（与基线一致：唯一红 = coder-2 在途真值表保持红，不归我；我的两条契约测试绿）｜`cargo fmt --check` clean。生产零改动：我的 diff 全在 mod tests 内，生产段为 coder-1 SCENE-002 基线。未碰 main.rs/overlay.rs/scene.rs/scene-rules.toml；未 build--release/出包/npm/改版本号；无 git 破坏性命令。UTF-8 无 BOM。结果：outbox/tester-1/result.md。

---

# 2026-08-05 — coder-2 工作日志（AXFIX-001）

## MACOS-P4-AXFIX-001 · coder-2 · 主程序链接失败 `_kAX*` undefined symbols（P0，先于一切）

- **根因**：`injection.rs` extern 块声明 `static kAXFocusedUIElementAttribute / kAXSelectedTextAttribute / kAXRoleAttribute: CFStringRef`，但 SDK 中它们是 `#define CFSTR(...)` 宏非导出符号，链接期必报 Undefined symbols。AXINJECT-001 落地即引入，主程序自那起构建不出（`cargo check` 不做链接、`cargo test` 测试二进制无引用把符号当死代码回收，两道门禁全被骗过）。
- **修复**（只改 `injection.rs`，照抄 scene.rs 现成解法）：
  1. 删 extern 块三个 `static kAX*` 声明；
  2. 三处 `wrap_under_get_rule(kAXxxx)` → `CFString::new("AXxxx")` 字面量：`"AXFocusedUIElement"` / `"AXSelectedText"` / `"AXRole"`；
  3. ⚠️ 生命周期：每个 `CFString::new` 先 `let attr = ...` 绑定局部再取 `as_concrete_TypeRef()`，避免临时值链式调用产生悬垂指针。函数行为/降级顺序/IsAttributeSettable/0.5s 超时/返回码校验未动。
- **验收**：`cargo build --bin feiyin-ime` **BUILD_EXIT=0**（关键硬门禁，真跑通过，产物 Mach-O arm64 18.6MB 链接成功）；`cargo check --all-targets` 0 errors；`cargo test` **836/1/6**（唯一红=overlay_wire_tests 真值表，OVERLAY-002 预期设计变更归 tester-1）；`cargo fmt --check` clean。实机短跑：`-debug` 启动消息循环正常、标点模型加载成功、SIGINT 干净退出。
- **边界**：只改 injection.rs；未碰 overlay.rs/main.rs/scene.rs/mod.rs/Cargo.toml/windows/**；未改测试/未出包/未改版本号/npm；UTF-8 无 BOM。

---

# 2026-08-05 — coder-2 工作日志（OVERLAY-003）

## MACOS-P4-OVERLAY-003 · coder-2 · Preview / FocusLost 失焦返显浮层（Gavin 指令，OVERLAY-002 补做）

- **改动**：仅 `src/platform/macos/overlay.rs` + `src/main.rs`。
  - `OverlayRequest::ShowPreview(String)` + `OverlayMode::Preview(String)`（PartialEq/Eq/Debug/Clone 保留）。
  - `draw_preview_overlay` 1:1 复刻 Windows：320×140/#1A1D21 背景/圆角10/28px 标题栏(i18n preview_title_bar 居中橙)/分隔线/正文 #F2F2F2（单行近似 DT_WORDBREAK|DT_END_ELLIPSIS）/底部 45×18 两按钮并排居中（复制橙、关闭灰 #808080）/标题栏 ✕ 关闭按钮 18×18 圆角6。三可点区域由 `preview_geometry(bounds)` 统一计算（绘制与命中同源）。
  - **窗口尺寸随态变化**：`resize_to(w,h)` 用 `panel.setFrame:display:` 重设 + 重新居中（水平居中、底部上方 64px），同尺寸跳过；set_mode 按 Preview→320×140、其余→240×36 分派，切回自动还原。
  - **mouseDown 按 OverlayMode 分派**：Recording→停止按钮（抽 `handle_recording_click`）；Preview→`handle_preview_click` 三热区（复制→`copy_text_to_clipboard`+Hide；关闭/标题关闭→Hide）；Processing/Error 不响应。
  - main.rs：`FocusLost(text) => ShowPreview(text)`；**剪贴板兜底 `copy_text_to_clipboard` 未删**（预览是叠加非替换），只更新过时日志文案。
  - 自动关闭：已核 Windows FocusLost 走 show_overlay（auto_close_ms=0），Preview 不自动关闭，macOS 一致。
- **验收**：`cargo build --bin feiyin-ime` **BUILD_EXIT=0**；`cargo check --all-targets` 0 errors；`cargo test` **836/1/6**（唯一红=真值表，Processing 首断言 panic 计 1，FocusLost 分支又变，未改测试归 tester-1）；`cargo fmt --check` clean。
- **🔴 实机验证降级**：debug `-debug` 启动正常（消息循环+标点模型+SIGINT 干净退出）；热键/切窗/点按钮需物理 GUI 交互，Agent 无法执行，留待 Gavin 端测。
- **边界**：未碰 injection.rs（只读 copy_text_to_clipboard）/scene.rs/macos/mod.rs/platform/mod.rs/Cargo.toml/windows/**；未新增需 mod.rs 导出的符号（PreviewRects pub(crate) 仅内部用）；未改测试/未出包/未改版本号/npm；UTF-8 无 BOM。

---

# 2026-08-05 — tester-1 工作日志（续）

## TEST-SYNC-P4-OVERLAY-TRUTHTABLE · 真值表换锚（收掉唯一红条）

基线 836/1/6，唯一红 = `overlay_wire_tests::overlay_request_for_event_truth_table`（我上轮按 Show/Hide 两态写的，coder-2 OVERLAY-002/003 三态+Preview 扩展后过期）。

改动（仅 `src/main.rs` 的 `mod overlay_wire_tests`）：对照生产 `overlay_request_for_event`（main.rs:2941-2958）核验七分支后逐条重写——RecordingStarted→Show；Processing(msg)→ShowProcessing(msg)（载荷透传）；FocusLost(text)→ShowPreview(text)（载荷透传，OVERLAY-003 Preview 态已实现，删旧「无对应浮层故 Hide」）；Error(msg)→ShowError{msg,2000}；FormatFailed→ShowError{String::new(),2500}（纯函数空文案，调用侧按 ui_language 补）；Done/Cancelled→Hide。七分支穷举、带载荷变体连载荷断言。PartialEq 已由 coder-2 derive（overlay.rs:146）。两处断言 message 内 `{` 转义为 `{{`（assert_eq 第 3 参按 format string 解析）。

验收：`cargo build --bin feiyin-ime` **BUILD_EXIT=0**；`cargo check --all-targets` 0 errors；`cargo test --bin feiyin-ime --no-fail-fast` **837/0/6**（净增 1，红条转绿）；`cargo fmt --check` clean。生产零改动：git diff HEAD 前 6 hunk 是 coder-2 未提交在途生产改动，末 hunk（尾部 -4822,3 +4884,72）是测试模块替换。未碰 overlay.rs/scene.rs/injection.rs/macos/mod.rs 生产段/Cargo.toml/windows/**；未出包/改版本号/npm；git 无破坏性命令；UTF-8 无 BOM。结果 outbox/tester-1/result.md。
