# 2026-08-05 — coder-1 工作日志

## MACOS-P4-OVERLAY-001 验收返工（orchestrator 反馈 → 修复 → 重交付）

### 主控验收反馈（2 必改 + 1 待答）
1. **❌ BORDER_GRAY 配色 R/B 互换**：COLORREF `0x060607` = `0x00BBGGRR` → RR=0x07/GG=0x06/BB=0x06 → RGB `#070606`。我原实现 `(0.02353,0.02353,0.02745)` 是 `#060607`（R/B 反了）。
2. **❌ 性能四项未报数**：需补 ① 平均帧间隔 ② 单帧绘制耗时(<5ms) ③ CPU 占用 ④ 音频线程无丢帧证据。
3. **❓ 停止按钮出处**：1:1 规格表没有，指 Windows 对应段。

### 修复过程
- **返工1**：BORDER_GRAY 改 `(0.02745, 0.02353, 0.02353)`（R=0x07/255）。其余 4 色经核对均正确（/红/灰/背景/橙）。
- **返工2**：
  - overlay.rs 新增 `OverlayStats`（7 个 AtomicU64）+ `StatsSnapshot` + `record_tick`（timer 回调测帧间隔）/`record_draw`（drawRect 测耗时）/`monotonic_ns()`；`RecordingOverlay::stats()` 暴露。
  - **关键发现**：初版 probe 用 `sleep` 驱动，overlay 的 CFRunLoopTimer **从未 fire**（fps 59 只是 push 计数）。试 `CFRunLoopRunInMode` 泵法 → AppKit 有持久源长阻塞卡死。终版改音频模拟线程精确 16ms push + 主线程 `NSRunLoop runUntilDate:`（与真实 main.rs 主线程一致）。
  - **SIGBUS 真实缺陷**（run loop 跑起来后暴露）：`ACTIVE_OVERLAY` 存 `new()` 栈帧 `&overlay`，move 后悬垂 → drawRect 读死指针在 `Arc::clone` atomic_add 崩溃（crash 报告 faultingThread 0 全链确认）。重构：删 `ACTIVE_OVERLAY`/`active_levels()`，`OverlayIvars` 直接持有 `levels: AudioLevelBuf`，drawRect 用 `self.ivars().levels`。
- **30s 实测**：1876 帧（62.5fps），avg 帧间隔 16.00ms/max 18.03ms，avg 绘制 0.022ms/max 0.083ms，CPU 1.3–1.5%，push 间隔 avg 16.003ms/max 23.989ms。
- **待答停止按钮**：Windows `main.rs:1304-1344`（外框 bs=16/bl=right-25/PS_SOLID 1px+NULL_BRUSH 只描边；内芯 isz=8 实心只填充，(16-8)/2=4 居中）。非增补，为 draw_recording_overlay 完整实现。

### 主动上报（第 4 项）
- **指示灯形状差异**：Windows `main.rs:1099-1167` 实际是**麦克风图标**（72x72 画布 pill RoundRect(22,4,50,53) + stem LineTo(36,53→36,63) + base LineTo(24,63→48,63)，StretchBlt HALFTONE 缩 18px），非纯圆。我按规格表"直径 18px"实现为实心圆。基准以规格表还是 Windows 代码为准，待主控裁定。

### 验证
- `cargo check --all-targets` 0 errors；`cargo test --bin probe_overlay` 13 passed；`cargo fmt --check` clean。
- 已更新 result.md / CHANGELOG.md（追加 OVERLAY-001-R 行）；待 tmux 通知 orchestrator 收 ACK。

---

# 🔖 主控断点存档（Gavin 2026-08-05 指令：额度用尽，明日续派）

## 一、本批次已闭环并验收的任务（8 项）

| # | 任务 | Worker | 主控验收要点 |
| --- | --- | --- | --- |
| 1 | `MACOS-P4-PROBE-001` | tester-1 | A1/A2/A5 PASS，**A4 FAIL 查出 CGEventTap 掩码越界真 bug**，A3 强度不足待复验 |
| 2 | `MACOS-P4-FIXHOTKEY-001` | coder-2 | `KEYBOARD_EVENTS` 5→3，删两个带外事件；带外回调与重启逻辑完好未动 |
| 3 | `MACOS-P4-NEUTRAL-001` | coder-1 | `run_pipeline` 薄封装 + 平台中立 `run_pipeline_core`；**402 行业务逻辑双侧共享**；主控用注入不存在类型反证 macOS 确实在编译它 |
| 4 | `TEST-SYNC-P4-NEUTRAL-001` | tester-1 | 解封 5 条 cfg 门控测试 + 新增 8 条；**盲区 22→17** |
| 5 | `TEST-EXEC-MERGE-001` | tester-1 | **880 passed / 0 failed / 8 ignored**（`--list` 888 自洽）；**A4 热键实机首次验证成功**；`time_half` 现 PASS |
| 6 | `MACOS-P4-HOST-001` | coder-2 | NSApplication + CFRunLoop 事件宿主；**tray-icon crate 实测不可用已上报**（5 种方案全试） |
| 7 | `MACOS-P4-EXIT-001` | coder-1 | `libc::signal` handler 只置 AtomicBool（async-signal-safe）；实测完整退出链 + flock 二次启动已释放 |
| 8 | `MACOS-P4-TRAY-001` | coder-2 | NSStatusItem 菜单栏图标 + 设置/分隔/退出（i18n 文案）；**5 处硬编码 `.exe` 抽为 cfg 常量**，Windows 仍拿 `.exe` |

**另有一次大合并**：Windows 侧 **68 个提交**并入（合并提交 `cc12381`），`src/main.rs` 自动合并成功，他们的双通道 ITN 代码落进 `run_pipeline_core`，macOS 编译同一份。

## 二、🔄 唯一进行中：`MACOS-P4-OVERLAY-001`（coder-1 返工中）

**主体已交付且质量高**（`src/platform/macos/overlay.rs`，NSPanel + CGContext 手绘 + 16ms CFRunLoopTimer）。

**主控逐项核对通过的**：边界干净（未碰 coder-2 占用文件）｜常数全对（240×36 / 圆角10 / 底部+64 / 指示灯18px左6 / 32根×宽3间隙2 / 48-12-8 / 增益2.5 / 衰减0.02 / 16ms）｜**余弦权重公式逐字符合**｜**时序映射 `idx=len.saturating_sub(1+i)` 中心=最新样本，方向没写反**｜**`[OVERLAY-LOCK-SCOPE-001]` 锁作用域正确**（锁内只快照、锁外绘制）。

**返工 2 项 + 待答 1 项（明日续）**：

1. ❌ **`BORDER_GRAY` 配色 R/B 互换**：COLORREF 是 `0x00BBGGRR`，字面量 `0x060607` → 正确 RGB `#070606`，实现为 `#060607`。**另外四色换算全对**，唯独边框三通道近似相等所以错了肉眼不可辨（差 1/255）。coder-1 已在 `logs/20260805.md` 记录并开始修
2. ❌ **性能四项未报数**：只报了 fps 59.1/59.0。仍需 ① 平均帧间隔（应≈16.7ms）② **单帧绘制耗时 <5ms** ③ 录音期 `top -pid` CPU ④ 音频线程无丢帧证据
3. ❓ **待答**：实现了 `STOP_BTN_SIZE`/`STOP_BTN_INNER` 停止按钮，但主控 1:1 规格表无此项，需指出对应 Windows 侧行号或说明自行增补理由

## 三、明日第一件事：派 **浮层接线**（「完整体验」最后一块）

把 coder-1 的 `overlay.rs` 挂进 `run_controller_macos` 的 `PipelineEvent` 消费处。**coder-2 空闲，边界上归它**（它独占 `main.rs`/`macos/mod.rs`）。

## 四、Gavin 已拍板但尚未落地的决策

| 决策 | 内容 |
| --- | --- |
| **形态定案** | **NSStatusItem 托盘路线**（非 IMKit 输入法）。理由：飞音是纯语音不处理键盘，做成输入法会导致切过去就打不了字、且需反复切换输入源。**主控 WebSearch 核实竞品 Typeless 同样是独立 App + 辅助功能注入 + 全局热键，不注册输入法** |
| IMKit | 「后续看需要再考虑」，短期不做 |
| `MACOS-P4-AXINJECT-001` | **独立 P1**，不与 READBACK 捆绑（已写入 todo 专节） |

## 五、⚠️ 遗留问题与风险（明日需处理）

| # | 项 | 说明 |
| --- | --- | --- |
| 1 | **工作区大量未提交** | 15 改动 + 3 新文件（`overlay.rs` / `tray.rs` / `logs/20260805.md`）。`origin/main` 之后本地已有 3 提交（`57be792` / `cc12381` / `5e496de`）**ahead 未 push**。建议明日尽快 commit，降低爆炸半径 |
| 2 | `src/bin/probe_overlay.rs` **未删** | 任务书要求跑完删除；coder-1 返工中可能仍在用，**验收时必须确认已删** |
| 3 | **rustfmt 跨平台漂移未解决** | `src/itn.rs`(+135/−111) 与 `src/llm/mod.rs`(+83/−38) 是 `cargo fmt` 全量连带。主控已证「当前内容 == `rustfmt(HEAD)`，零人为改动」。**两条修法均实测不可行**：`style_edition=2021` 反引入 3 文件新 churn；`rust-toolchain.toml` 钉 1.97.1 触发 rustup 解析卡死并拖慢所有 Worker（已撤销）。**根因是版本漂移而非配置漂移，需先问 Windows 侧的 `rustfmt --version` 再钉**。本侧 rustfmt 1.9.0-stable |
| 4 | **Tauri 设置 UI 的 macOS 产物未构建** | 托盘「设置」点了会报 `feiyin-ime-ui not found`（**路径已修对，只是产物不存在**）。构建需 npm，在禁令内，**等 Gavin 授权** |
| 5 | `foreground_window_id()` 返 0 | macOS 第一版降级，`focus_lost` 恒 false → 总是直接注入、不走失焦预览 |
| 6 | 闭环端测未由物理按键确认 | coder-1 用 `osascript` 模拟不经 CGEventTap；tester-1 A4 用 enigo 合成注入（Session tap 可见，证据成立但非硬件按键）。**最终确认需 Gavin 亲自按热键说一句话** |

## 六、Worker 状态

三个 Worker 均已通知暂停待命：coder-1（OVERLAY 返工中，可收尾后停）/ coder-2（空闲）/ tester-1（空闲）。
