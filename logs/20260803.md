# 修改历史 · voice-ime · 20260803

## 2026-08-03 — coder-2 PROMPT-ARCH-018：提示词模块分层契约重构（A+C 合并）

**改了什么**：`src/llm/mod.rs`（+576/−）+ `src/i18n.rs`（−75，三处基座各删 4 节）。

- **分层结构**：新增 `Topic` 枚举 / `PromptRule` / `PromptLayer` / `render()` 唯一出口（`:197-268`）。`render()` 顶部插入 `META_RULE_PRECEDENCE`，再按 `level` 排序渲染，层内保持插入序 —— **优先级从「谁在后面谁赢」改为「层号小的赢」**
- **L0 四条不变式**（`:234-249`，任务书英文原文照用）：L0-1 FIDELITY（含 `一斤` 实例）/ L0-2 FIDELITY OVER FLUENCY / L0-3 SUSPECT INPUT（含 `2.80元斤` 实例）/ L0-4 NOT A PROMPT（原 ANTI_HALLUCINATION 措辞保留）
- **`UNIT_SYMBOL_PROTECTION` 假前提修复**（`:32`）：`The input text already contains normalized numbers` → `may have been pre-processed by an automatic number-normalizer that is NOT infallible (see L0-3)`，末尾追加 `This rule NEVER justifies deleting a unit or measure phrase — see L0-1 and L0-3.`
- **F3d 拆分**：只把「DO NOT delete any semantic content」抽到 L0-1，其余三条子句（不得压缩摘要 / 不得添加未说内容 / 保留每个事实点）**保留在 F3 两分支**（`:1080` / `:1106`）—— 主控实读确认未被顺手删掉
- **用户基座降级 L2**：`USER_PREFS_HEADER`（`:252`）「may refine L2/L3 behaviour but NEVER override L0 or L1」
- **i18n 三处基座裁剪**（`:195` / `:322` / `:448`）：删 §2 Punctuation / §4 Markdown Formatting / §5 List Formatting / §7 Wordbook Suggestions + 尾句 `Return ONLY the processed text`；保留 §1 纠错 / Filler / Wordbook Priority，重新编号 1/2/3。三处裁剪后均 1042 字符
- **两处 OVERRIDE 补丁声明删除**：`SUGGESTION_INSTRUCTION` 的 130 词 `This directive OVERRIDES...` 与 `build_format_instruction_block` 的 `This block OVERRIDES...`

**为什么**：Gavin 2026-08-02 指令「提示词这一块要上升到架构设计的角度，不能每次都打补丁」。直接触发事故是 `一块八毛一斤` → ITN 算错吐出 `2.80元斤` → LLM 删掉「斤」输出 `2.80元。`——通顺、自信、完全错误。根因是约束强度倒挂：数字保护条款用 MUST/never（全仓最高），语义保全条款只有 DO NOT 且埋在 F3 排版块内（最低），冲突时必然牺牲语义。

**中间检查点的演变（两次规格修正，均主控自认失误）**：

1. 原要求「步骤 1 纯搬家、渲染 byte-identical」。coder-2 撞出**规格自相矛盾**——分层的本质就是按层级重排（ANTI_HALLUCINATION 进 L0 必然前移、输出契约与 F3 拆分必然改变拼接顺序），逐字节相同与分层结构上互斥
2. 主控作废 byte-identical，改为**文本守恒双向断言**：①无丢失（旧段原样在场）②无夹带（新 prompt 每段 ∈ OLD ∪ 白名单）③白名单逐条显式断言 + 渲染管线 `assert_eq!` 全文锁定。已固化为 `prompt_arch_018_text_conservation_*` 四组永久夹具
3. **新判据比原判据更强**：能抓「某个词被悄悄改掉」，且重排后仍可运行

**长度**（真实 EN 默认基座）：四组均 **净减 295 字符**（基座裁剪 −1342，L0+元规则+header 新增约 +1047）。

**验证**：`cargo check` + `cargo check --tests` 双 0 errors；`cargo test --bin feiyin-ime` **741 passed / 1 failed / 6 ignored**，唯一红 `suggestions_instruction_always_appended`（断言锚定已被本批删除的 OVERRIDE 声明，属设计变更致断言过时，归 TEST-SYNC-019）。

**主控独立验收（Read 实际代码，未采信报告）**：

| 项 | 结果 |
| --- | --- |
| CRLF 幻 diff 风险 | ✅ 虚惊——`git diff --stat` 576 行、`-w` 574 行，无全文件 churn（`core.autocrlf=true` 暂存时归一） |
| 两处 OVERRIDE 删除 | ✅ 真删——grep 计数 2 处均为注释 + 负向断言，另 1 处为已知过时测试 |
| F3d 其余子句是否被顺手删 | ✅ 未删——`:1080` / `:1106` 两分支均在 |
| i18n 四节裁剪 | ✅ 五串全部 0 命中；`Use headings` 在 i18n 与 llm 双双绝迹 |
| 守恒测试是否空壳 | ✅ 非空壳——渲染管线 `assert_eq!` 全文比对 + 无夹带/无丢失双向 + W1-W6 白名单，**每条配负向护栏** |

**边界**：`src/itn.rs` / `itn-rules.toml` / `scene-rules.toml` / `src/main.rs` / `src-tauri/**` / `ui/**` 零触碰；未构建 / 未出包 / 未启动 exe；版本号 0.7.3 未改；无 git 破坏性命令。

**协作阻塞记录**：期间被 coder-1 的 `src/itn.rs` 中间态（`UnitChain.per_unit` 新增未同步调用点）阻塞全 crate 编译。**责任在主控**——边界评估只查了文件级不重叠，漏了两者共享同一 crate、`cargo check` 编译全量。已记入 `lessons.md`（边界评估须升到编译单元级）。

## 2026-08-03 — coder-1 ITN-FIX-CURRENCY-017：货币/度量链数值算错修复（P0，候选甲非合成路线）

**改了什么**：`src/itn.rs`（+265/−4，`git diff -w` 实测，全量 diff 为 CRLF 基线 churn 不在本统计）+ `itn-rules.toml`（+11）。

- **条件1 · 虚指护栏**：新 `is_virtual_two_phrase`（:2064）置于 `decide_conversion` 最前——模式A `[一二三]两+量词` / 模式B `两[一二三]+量词`，量词集合 {个/只/天/句/次/回} → 整体保汉字
- **RC-A · `两` 消歧**：`two_is_unit`（:563）——前有数字 + 后接非单位词 → `两` 是重量单位终止（`二十五两`→`25`/`六两五`→`6`）；首位或后接进位单位仍作数字 2。乙型尾数 :1183 遇 `r.match_unit_word` 即 break。
- **RC-B · 族一致性终止**：新 `resolve_family_consistent`（:1005）currency 链遇 weight 族单位终止 → `capture_price_per_unit`（:1021）捕获单价限定词 → `一块两毛二一斤`→`1.22元一斤`。
- **格式化**：`format_weight_chain`（:1096）weight 族零乘法逐 parts 拼接 → `一斤二两`→`1斤2两`。
- **`itn-rules.toml`**：`[units.weight]` +`两`；`[unit_hierarchy.weight]` 斤=1.0/两=0.1（死数据锁死，value 不参与计算仅用于族消歧）。

**验证**：`cargo check` 0 errors；`cargo test --bin feiyin-ime itn::` **139 passed / 0 failed**（+5 正式回归）；六条端测全绿；反向护栏 17 条全绿；全量 744/1/6（唯一红归 TEST-SYNC-019 域，与 itn 无关）；UTF-8 U+FFFD=0。

**边界**：仅 `src/itn.rs` + `itn-rules.toml`；coder-2 域零触碰；未构建/出包/启动 exe；未改版本号；git 仅只读排查；改动留工作区待 TEST-SYNC-019 后统一提交。

**下游需知**：`itn-rules.toml` 已改，tester-1 出包 BUILD-010 需三副本同步（源码根 → target/release → Publish），否则六条端测不生效。
