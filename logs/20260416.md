# 修改历史 · voice-ime · 20260416

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-04-16 — BUG-018/019 模型路径依赖工作目录 ✅ 完成

**修改文件**: `src/transcription/mod.rs`

**根因**: `model_dir()` 使用相对路径 `models/`，从非 voice-ime 目录启动时找不到模型，触发 huggingface.co 下载（连接慢导致阻塞）

**修复**: 改为 `exe 同级目录/models/`，使用 `std::env::current_exe()` 获取 exe 路径

**验证**: 从任意工作目录启动均能正确找到模型

---

## 2026-04-16 — T1 UI 绘制优化 ✅ 完成

### 修改内容
1. 边框宽度 2px → 1px（4 处：draw_recording_overlay、draw_processing_overlay、draw_preview_overlay）
2. 取消按钮高度 4px padding → 6px padding（按钮高度从 20px 变为 16px）
3. 波形条宽度 2/3 → 1/2
4. 处理文案 "LLM 优化中..." → "识别处理中..."

### 验证
`cargo check --release` ✅ 通过（15 warnings，无 errors）

### 任务来源
T1 (qwen-1)

---

## 2026-04-16 — T2: LLM 单次调用逻辑（6秒超时）

**修改文件**: `src/llm/mod.rs`

**修改内容**:
- `ATTEMPT_TIMEOUTS` 从 3 次 30 秒改为 1 次 6 秒
- 行为变化：LLM 调用只尝试一次，失败后直接返回错误，由 pipeline 回填转录文字

**影响范围**: `optimize()`, `probe()` 函数

**任务来源**: T2 (qwen-2)

---

## 2026-04-16 — T3 错误提示窗口 ✅ 完成

**修改文件**: `src/ui/overlay.rs`, `src/main.rs`

**修改内容**:
1. 新增 `OverlayStatus::Error(String)` 状态
2. 新增 `draw_error_overlay()` 函数（红色背景+感叹号图标）
3. 新增 `convert_to_friendly_error()` 函数（内部错误转用户友好信息）
4. 新增 `OverlayRequest.auto_close_ms` 字段（自动关闭时间）
5. 新增 `WM_TIMER` 处理（定时自动关闭窗口）
6. `PipelineEvent::Error` 显示错误窗口 2 秒后自动关闭

**用户友好信息**:
- 转录错误
- 模型接口调用超时
- 模型接口调用错误
- 处理错误

**任务来源**: T3 (Orchestrator)

---

## 2026-04-16 — T4 LLM 禁用逻辑调整 + 麦克风状态指示 ✅ 完成

### A. LLM 禁用逻辑调整

**修改文件**: `src/config/mod.rs`, `src/main.rs`, `src/ui/settings.rs`

**修改内容**:
- 移除 `consecutive_failures` 和 `marked_unavailable` 字段
- 删除 `update_llm_failure_count` 函数
- 简化 LLM 调用逻辑：每次语音输入都尝试调用一次（6秒超时），失败直接用转录文本

**行为变化**: 用户勾选启用 LLM 后，每次语音输入都会尝试调用，不再因连续失败而自动禁用

### B. 麦克风状态指示

**修改文件**: `src/main.rs` (`draw_recording_overlay`)

**修改内容**: 左侧圆点颜色根据音频输入状态动态变化
- 绿色 `rgb(34, 197, 94)`：有音频输入（波形动）
- 红色 `rgb(239, 68, 68)`：无输入/静音（波形不动）

**验证**: `cargo check --release` ✅ 通过（16 warnings，无 errors）

**任务来源**: T4 (qwen-1)

### C. 波形条加载方向优化

**修改内容**: 波形条索引计算改为反向映射，让最新数据显示在左边
- 原行为：从右往左加载（新数据在右边）
- 新行为：从左往右加载（新数据在左边），符合视觉习惯

**修改代码**:
```rust
let idx = levels.len().saturating_sub(1) - (i * levels.len() / bars).min(levels.len().saturating_sub(1));
```

---

## 2026-04-16 — T5 窗口边缘锯齿优化（DWM API） ✅ 完成

**修改文件**: `Cargo.toml`, `src/main.rs`

**修改内容**:
1. 新增 `Win32_Graphics_Dwm` feature
2. 在 overlay 窗口创建后调用 `DwmSetWindowAttribute` 设置 `DWMWCP_ROUND`

**兼容性**:
- Windows 11: DWM API 成功，系统自动抗锯齿
- Windows 10/7: DWM API 失败，fallback 到 SetWindowRgn（有轻微锯齿）

**效果**: Windows 11 用户看到平滑圆角边缘

---

## 2026-04-16 — T6 配置窗口 LLM UI 改进 ✅ 完成

**修改文件**: `src/ui/settings.rs`

**修改内容**:
1. 测试按钮尺寸缩小约35%（130x42 → 85x28）
2. 状态显示改为小圆点（半径5px）
   - 橙色：测试中
   - 绿色：连接已验证
   - 灰色：未测试
3. 测试结果显示3秒后自动消失（新增 `llm_test_result_timestamp` 字段）

**新增函数**: `small_primary_button()`（第 1050-1061 行）

**验证**: `cargo build --release` 编译通过（16 warnings，0 errors）

**任务来源**: T6 (qwen-1)

---

## 2026-04-16 — T9 crash 模块基础实现 ✅ 完成

**新建目录**: `src/crash/`（4 个文件）

**新建文件**:
- `src/crash/mod.rs`：模块入口、create_report_from_panic()、get_windows_version()
- `src/crash/storage.rs`：CrashReport/CrashType/SystemInfo/RuntimeInfo 结构、本地存储函数
- `src/crash/reporter.rs`：ReporterApp eframe UI
- `src/crash/email.rs`：send_crash_email() SMTP 发送

**新增依赖**:
- backtrace = "0.3"
- chrono = "0.4"
- lettre = { version = "0.11", features = ["smtp-transport", "builder", "native-tls"] }

**验证**: `cargo check --release` 成功（35 warnings，0 errors）

**遗留**: SMTP 配置为占位符；T10 需集成 panic hook 和 `--crash-reporter` CLI

**任务来源**: T9 (qwen-1)

---

## 2026-04-16 — T8 失焦窗口优化 ✅ 完成

**修改文件**: `src/main.rs`

**修改内容**:
1. 窗口尺寸增大：280x56 → 320x100
2. 新增标题 "音频文本"
3. 新增提示 "(按Esc 取消)"
4. 复制按钮居中、尺寸缩小（56x24）
5. 右上角新增关闭按钮（X）
6. 新增 `close_btn_rect` 状态字段
7. ESC 键触发 CancelRequested（关闭窗口）
8. 复制后自动关闭窗口
9. 关闭按钮点击触发 CancelRequested

**行为变化**:
- ESC 或点击关闭按钮 → 关闭窗口
- 点击复制按钮 → 复制文本 + 关闭窗口

---

## 2026-04-16 — T10 主控集成 crash 模块 ✅ 编译成功

**修改文件**: `src/main.rs`

**修改内容**:
1. --crash-reporter 参数处理：
   - main() 开头检测参数
   - 直接调用 crash::run_reporter()（无参数）
   - 不传递 crash_file（内部自动 load）

2. Panic Hook 注册：
   - 在 runtime_config 创建后注册
   - 使用 crash::RuntimeInfo::default()（避免生命周期问题）
   - 调用 crash::create_report_from_panic 创建报告
   - 调用 crash::save_crash_report 保存
   - 启动子进程：exe --crash-reporter

**设计决策**:
- RuntimeInfo 使用默认值（panic 时 RwLock 可能已污染）
- recent_logs 暂不收集（日志系统集成复杂）

**验证**: cargo build --release ✅ 编译成功（16 warnings，无 errors）

**任务来源**: T10 (qwen-2)

---

## 2026-04-16 — UI-015 配置窗口顶部图标更换 ✅ 完成

**修改文件**: `src/ui/settings.rs`, `src/i18n.rs`

**修改内容**:
1. 配置窗口左侧导航栏顶部图标从 `⚙`（齿轮）改为 `🎤`（麦克风）
2. 新增 `settings_nav_label` 国际化字符串（"导航" / "Navigation"）

**验证**: cargo build --release ✅ 编译成功（24.66s，13 warnings）

---

## 2026-04-16 — TEST-001 自动化测试框架 ✅ 完成

**新建目录**: `tests/test_cases/`（4 个 Python 测试文件）

**新建文件**:
- `tests/README.md`：测试框架说明文档（技术栈、运行方法、18 个用例）
- `tests/test_requirements.txt`：pytest + pytest-html + pytest-timeout + pyautogui + pywinauto
- `tests/conftest.py`：pytest fixtures（进程启动/关闭、失败截图、跳过音频测试）
- `tests/test_voice_ime.py`：主测试入口（启动/关闭、CLI 参数）
- `tests/test_cases/test_hotkey.py`：Toggle/PTT 模式、ESC 取消测试
- `tests/test_cases/test_audio.py`：录音状态、静音检测测试
- `tests/test_cases/test_ui.py`：Overlay/托盘/设置窗口测试
- `tests/test_cases/test_config.py`：配置文件读写、默认值测试

**验证标准**:
- pip install -r test_requirements.txt 可执行
- pytest fixtures 完整配置
- 18 个测试用例定义（覆盖核心功能）

**任务来源**: TEST-001 (qwen-2)

---

## 2026-04-16 — Inno Setup 脚本更新 ✅ 完成

**修改文件**: `installer/voice-ime.iss`

**修改内容**:
1. 版本号 0.1.0 → 0.3.4
2. 名称 "Voice IME" → "飞音语音输入"
3. 新增 DLLs打包：
   - sherpa-onnx-c-api.dll
   - sherpa-onnx-cxx-api.dll
   - onnxruntime.dll
   - onnxruntime_providers_shared.dll
4. 新增 ASR 模型打包：`models/paraformer-zh-int8-2025-10-07/*`
5. MinVersion 10.0.18362 → 6.1（Win7 兼容，符合 DEC-000）

**影响范围**: 安装包将包含所有运行时依赖

---

## 2026-04-16 — UI-016 取消按钮尺寸调整 ✅ 完成

**修改文件**: `src/main.rs`

**修改内容**: 取消按钮（X）尺寸缩小
- 宽度 14px → 12px
- 高度 18px → 14px

**影响范围**: 录音窗口右侧取消按钮显示更精简

---

## 2026-04-16 — TEST-001 音频测试自动化升级 ✅ 完成

**修改文件**: `tests/test_requirements.txt`, `tests/conftest.py`, `tests/test_cases/test_audio.py`

**新增功能**:
1. 集成 `tests/录音.m4a` (287KB) 作为测试音频素材
2. 新增 pygame>=2.5.0 依赖（音频播放）
3. 新增 `audio_player` fixture（播放/停止/等待完成）
4. 新增 `TestAudioWithTestFile` 测试类：
   - `test_audio_playback_capture`：播放音频 + 麦克风捕获
   - `test_audio_with_text_injection`：完整流程（录音→转录→注入）

**自动化程度提升**:
- 音频录制 + 转录：全自动（pygame 播放测试音频）
- 热键响应：全自动（pyautogui 模拟按键）
- 启动/关闭进程：全自动

**任务来源**: TEST-001 续 (qwen-2)

---

## 2026-04-16 — TEST-002 自动化测试框架增强 ✅ 完成

**新建文件**: `tests/utils/state_detector.py`, `tests/test_cases/test_ui_operations.py`, `tests/test_cases/test_settings_operations.py`

**核心实现**:
1. Win32 API 状态检测（state_detector.py）：
   - FindWindowW 查找 overlay 窗口（类名：voice-ime-overlay-window）
   - GetWindowRect 获取窗口尺寸
   - 尺寸匹配判断状态：Recording=240x36, Processing=200x36, FocusLost=320x110
   - OverlayState 枚举、OverlayDetector 类

2. 界面操作测试（test_ui_operations.py）- 13 用例：
   - 状态检测（Hidden/Recording/Processing/FocusLost）
   - 录音界面停止按钮点击
   - 失焦窗口操作（ESC/关闭/复制按钮）
   - 完整流程 + 状态检测

3. 配置界面操作测试（test_settings_operations.py）- 14 用例：
   - 窗口启动（--settings-ui CLI）
   - Tab 导航 + 点击切换
   - 勾选框操作（LLM/自动启动）
   - 输入框操作（API URL/模型）
   - 热键设置变更
   - 界面语言切换

**新增依赖**: pytesseract>=0.3.10（OCR可选）、Pillow>=10.0.0

**优先级调整**: P4 托盘操作跳过（Win11 任务栏复杂，用 --settings-ui 替代）

**任务来源**: TEST-002 (qwen-2)

---

## 2026-04-16 — UI-017 录音窗口多项优化 ✅ 完成

**修改文件**: `src/main.rs`, `Cargo.toml`

**修改内容**:
1. 取消按钮图标：X 线条 → 绿色实心方块（6x6px，停止按钮风格）
2. 取消按钮尺寸：12x14px（固定，不随窗口变化）
3. 波形条宽度：缩小 20%（wave_width = wave_area_width * 80/100）
4. 波形条水平居中：在左侧圆点间隔线和右侧按钮间隔线之间居中
5. 左侧间隔线：圆点右侧增加 8px 空白间隔
6. 右侧间隔线：按钮左侧增加 8px 空白间隔（对称）
7. 设置窗口标题栏图标：使用 app.ico（麦克风图标）
   - Cargo.toml 新增 `image` crate 的 `ico` feature
   - settings.rs 新增 `load_settings_window_icon()` 函数

**影响范围**: 录音窗口视觉效果更精致，设置窗口图标统一

---

## 2026-04-16 — 空转录结果跳过 LLM ✅ 完成

**修改文件**: `src/main.rs` (约 2179-2183 行)

**修改内容**: 在 `process_audio_and_transcribe()` 函数中添加空转录检测

```rust
if raw_text.trim().is_empty() {
    log::warn!("Transcription result is empty, skipping LLM and injection");
    send_event(event_tx, PipelineEvent::Error("转录结果为空".to_string()));
    return;
}
```

**影响范围**: 防止空转录触发无意义的 LLM 调用，避免返回通用回复

---

## 2026-04-16 — UI-018 边框颜色优化 ✅ 完成

**修改文件**: `src/main.rs`

**修改内容**: 录音窗口和处理窗口边框颜色优化
- 第一轮：rgb(60, 60, 70) → rgb(85, 85, 100)（加深）
- 第二轮：rgb(85, 85, 100) → rgb(50, 50, 60)（偏黑，视觉上显得更细）

**修改位置**（2处）:
- 录音窗口边框 (draw_recording_overlay)
- 处理窗口边框 (draw_processing_overlay)

**影响范围**: 录音窗口、处理窗口边框视觉效果更清晰精致

---

