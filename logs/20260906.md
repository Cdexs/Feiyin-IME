# 修改历史 · voice-ime · 20260906

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## REPRO-114 · 热键第二下按不停根因定位（tester-1，2026-09-06，只查不修）

**Gavin 手测复现（P0）：按下热键有时出现录音窗口、有时不出现；按第 2 下经常不停止、录音也不退出。**

### 根因（决定性定位，probe3 逐句镜像生产代码复现）

**`src/platform/windows/hotkey.rs:221`** —— WM_KEYUP 分支 `PTT_ACTIVE.store(false)` **无条件执行**，
Toggle 模式下把第一下按下置 true 的标志在松键时重置 → 第二下按下判定 `!PTT_ACTIVE==true` → **又发 Start 而非 Stop**。

**RegisterHotKey 路径（F9 等）另有缺陷**：`hotkey.rs:536-547` Toggle 分支无 toggle 状态，每次触发都发 Start。

### 实验证据链

| 实验 | 结果 |
| --- | --- |
| probe1（镜像生产回调 spawn+send，SendInput 自驱动） | 钩子收到 20 次事件，回调 **60-83us**（默认超时 1000ms 的 1/10000） |
| probe2（+loader-lock 压力线程 LoadLibrary d2d1/dwrite 等） | 钩子仍 60-83us 正常，**H1 loader-lock 假设证伪** |
| probe3（逐句镜像生产 PTT_ACTIVE 逻辑） | **决定性复现**：两次 DOWN 都 START，第一次 UP 无条件重置 PTT_ACTIVE |
| 真实 app 双按（exp_fast_slow） | gap=1000ms 与 20ms 都复现第二下恒为 Start；最终 overlay 卡 recording ~20s |
| 真实 app 单按（×5） | 第一下 5/5 都进 recording（第一下可靠） |
| RegisterHotKey 路径（F9 配置） | 两次都 `Controller received hotkey start`，无 Stop |

### 判定

- **H1（loader lock → 超时摘钩）：不成立**（probe2 证伪）。
- 根因在 **PTT_ACTIVE 生命周期逻辑**（非阻塞，非环境）。
- 两条热键路径（hook / RegisterHotKey）都缺 toggle-stop 语义。

### 建议修法（交 coder）

1. `:221` 把 `PTT_ACTIVE.store(false)` 移入 `if should_stop_translate_poll_on_keyup(mode)` 块内（仅 PTT 松键重置）；
2. `:536` RegisterHotKey Toggle 分支加入 `PTT_ACTIVE` toggle 状态（第一下 Start、第二下 Stop）。

### 红线

生产代码零改动（`git diff src/main.rs` 空）／ 未出包 ／ 未 commit ／ v0.9.0 未动 ／
`Publish/config.toml` sha `3186ec8c` ／ 无残留进程 ／ 探针工程保留于 `outbox/tester-1/repro114/` ／ 零凭证。# 2026-09-06 · 开发日志

## HOTKEY-115 · Toggle 模式第二下按不停双缺陷修复（coder-1，2026-09-06）

- **缺陷 1（钩子路径）**：`keyboard_hook_proc` KEYUP 分支 `PTT_ACTIVE.store(false)` 在
  `if should_stop_translate_poll_on_keyup(mode)` 之外，Toggle 模式松键也被重置 → 第二次
  DOWN 恒走 Start，`else if mode == 0` Stop 分支实际不可达。修：store 移进 if 块内 +
  DOWN 按模式分支（PTT 分支逐字保留）。
- **缺陷 2（RegisterHotKey 路径，如无修饰键 F9）**：`handle_hotkey_trigger` Toggle 分支
  无任何状态、恒发 Start，靠控制器「Start 时已在录音则转 Stop」兜底生存。修：加
  `TOGGLE_ACTIVE` 翻转，与钩子路径共用。
- **选型**：另开 `TOGGLE_ACTIVE`（不复用 PTT_ACTIVE）——PTT_ACTIVE 被
  `poll_ptt_release_thread` while 自旋消费，复用两生命周期打架；按模式分支后 B1（PTT 零
  回归）由构造成立。互斥证据：`handle_hotkey_trigger:574` uses_hook 早退 + 钩子只在
  CURRENT_HOOK 非空时被调用 + sync_binding 清理→归零→安装的写入顺序 + 中间态两路径都不产事件。
- **B3 单一收口（「有时」的来源）**：控制器所有录音非热键结束路径（Done/Cancelled/
  FocusLost/Error/FormatFailed/EditRequested/CancelStop/ESC，main.rs 8 处含本单新增 1 处）
  汇聚调用 `platform::notify_translate_poll_stop()` → 在该函数（hotkey.rs:146）内复位
  TOGGLE_ACTIVE，零控制器侧新增分发。兜底时序竞态（第二下快过 worker 置 is_recording）
  由真 Stop 无条件置 stop_recording_signal 消除。
- **主控扩单③**：mic-muted 拒绝出口（main.rs:5101）补一行 notify —— Start 唯一无 pipeline
  事件的出口，否则 toggle 态滞留反转。
- **B4 复位点**：install (:292)/uninstall (:323)/sync_binding 清理块 (:537) 三处两态归零。
- **教训(六) 护栏建议**：调用点状态机不可抽纯函数（重蹈覆辙），建议 tester-1 阶段三用
  include_str 结构护栏（store 必须在 if 内 / 分支行窗 atom 归属 / 收口函数含复位），
  行窗+startswith 防注释自命中。
- **auto-repeat 如实报备**：钩子路径长按在 Toggle 下新旧实现都会随 repeat 翻转（行为持平
  无回归），Windows 钩子无 autorepeat 字段，去抖另立单。
- **macOS 同源性**（docs/MACOS-HANDOFF.md 已追加）：缺陷 ② 同源（Toggle 恒发 Start）、
  缺陷 ① 不同源（边沿检测正确）、B3 方案与对端验证点已写明。
- **验证**：fmt --check exit 0；check --all-targets 0 error；warning stash 对照实测与基线
  持平（111/102）；未跑 test/release；未 commit；v0.9.0 未动；零凭证。

## HOTKEY-115-B · 钩子路径 auto-repeat 结构性抑制（coder-1，2026-09-06）

- **定性**：长按 >0.5s 进入 ~30 次/秒 repeat，钩子路径 Toggle 会 Start/Stop 高速交替、
  终态随机 —— Gavin「按下有时出现有时不出现」的另一半。旧实现在此同样坏（行为持平
  不等于正确），本批任务就是「按键行为可预期」，一并修。
- **钩子路径**：新增 `KEY_PHYSICALLY_DOWN`（:65），DOWN 置 true / UP 清 false，DOWN 闸
  `swap(true)` 只让「真正的首次按压」进模式状态机（:207）；KEYUP 分支在模式判定之前
  无条件清（:268，两模式都要清，KEYUP 本就无 modifiers 门控）。不用计时窗 —— 物理按键
  状态是精确判据。PTT 路径原 `!PTT_ACTIVE` 门控已使 repeat 为 no-op，此闸零行为影响。
  复位点与另两态一致：install (:314) / uninstall (:346) / sync_binding (:561)。
- **RegisterHotKey 路径实测（不加代码）**：py-ctypes 双对照 —— F10 长按 2s，带
  `MOD_NOREPEAT` 与不带均只收 1 次 WM_HOTKEY，单击对照各 1 次 ⇒ 该路径天然无 repeat
  （`MOD_NOREPEAT` 在 register_binding :651 既有）。按主控指示「不重复就别加代码」。
- **实施事故自查（如实记录）**：批量 sed 式 Edit 时 uninstall 的
  `TRANSLATE_POLL_STOP.store(true)` 被连带删除、sync_binding 漏加 KEY 复位 —— grep 复查
  发现后补回（uninstall 三态+TRANSLATE、sync_binding 三态），fmt/check 复验过。教训：
  相同前缀的多处编辑必须用带上下文的唯一 oldString，禁止盲信「替换成功」。
- **macOS 对齐**：`!is_repeat` 与 `KEY_PHYSICALLY_DOWN` 语义等价（判据来源不同），
  MACOS-HANDOFF 已补「两端语义一致，macOS 无需改动」。
- **验证**：fmt --check 0；check --all-targets 0 error；warning 与基线持平（111/102）；
  main.rs 本单零触碰（仍只有 115 的 :5101 一行）；未 commit；v0.9.0 未动；零凭证。

## HOTKEY-115-C · 陈旧 KEY_PHYSICALLY_DOWN 自愈（coder-1，2026-09-06）

- **新失效模式（主控指出，采纳）**：KEY_PHYSICALLY_DOWN 成唯一 DOWN 闸门后，一次未见的
  KEYUP（Win+L 锁屏安全桌面 / 上游吞 KEYUP / 钩子重装窗口）→ 标志永久 true → 热键失灵
  到重绑定，比 repeat 掷硬币更严重。
- **判据选型：时间戳，不用 GetAsyncKeyState（主控建议反向，已给理由）**：DOWN 到达时真
  repeat 与陈旧后的新按下键都处于按下态，GetAsyncKeyState 高 bit 判别力为零；LSB 转变位
  文档明示多进程下不可靠。时间戳可用但阈值 🔴 必须 > Windows 重复延迟上限 1000ms
  （250/500/750/1000 四档；只按 33ms 取阈值会在长延迟设置下让首个 repeat 误判陈旧、
  抑制退回翻转），取 2 倍 = 2000ms。
- **实现**：`LAST_TARGET_DOWN_TICKS`（AtomicU64，GetTickCount64 ms，时间戳随每次 DOWN
  无条件刷新）+ DOWN 分支 `!was_down || now.saturating_sub(last) > 2000` 进首次按下路径。
  长 hold 期间间隔被连续刷新恒 < 阈值 ⇒ 真 repeat 永不误判；吞 UP 后秒级重按 > 阈值 ⇒ 自愈。
  残余窗口（2s 内重按仍被吞一次）如实声明。陈旧自愈进状态机后动作由 TOGGLE_ACTIVE/
  PTT_ACTIVE 语义态决定，行为正确。
- **时间戳无需复位点**：仅在 was_down=true 时被参考，三处复位点均清 KEY_PHYSICALLY_DOWN
  ⇒ 复位后必走 !was_down 分支。
- **验证**：fmt --check 0 / check --all-targets 0 error（新增 SystemInformation 导入）/
  warning 持平（111/102）；未 commit；v0.9.0 未动；零凭证。

## 主控 · tester-1 默认启动档位改为 OpenCode Go（Gavin 2026-09-06 指令，不重启）

- **事由**：新会话派发 `TEST-SYNC-116` 后 `dispatch.sh` 三次重发全无 ACK、打出 `[ACK_FAIL]`。
  capture-pane 实证根因是 `Insufficient balance`（`DeepSeek V4 Flash · OpenCode Zen`），
  非僵死非卡顿 —— `[WORKER-RESTART-MODEL-RESET-001]` 第三次复发，且**本次未重启**，
  说明该条目标题「重启后」收窄了适用面。
- **改动**：`collab/configs/team-voiceime.yaml` tester-1 的
  `model: "opencode/deepseek-v4-flash"` → `"opencode-go/deepseek-v4-flash"`
  （provider 由 Zen 改 Go，与 coder-1/2 同源、额度独立），附 4 行原因注释。
- **验证**：`load_config` + `get_worker_model tester-1` 实测返回 `opencode-go/deepseek-v4-flash`；
  coder-1 对照 `opencode-go/glm-5.3-flash` 未受影响。
- **按 Gavin 指令不重启**：运行中的 pane 已由 Gavin 手工切到同一档位并 `go on` 续跑，
  配置改动只影响下次启动，两者已对齐。
- **主控踩坑同步记入 troubleshooting**：弹层开着时 `send-keys` 文本会落进 prompt 输入框
  而非搜索框，本次误把档位切成非预期值；结论是**带弹层的交互操作交给 Gavin 手工完成**。
- **任务未丢**：`inbox/tester-1/task.md`（4055B）完好，tester-1 已回 ACK，TEST-SYNC-116 进行中。

## TEST-SYNC-116 · 热键 7 条结构护栏（tester-1，2026-09-06，阶段三只写用例）

**hotkey.rs 测试模块 +373/-0（单 hunk `@@ -1192,0 +1193,373`），生产代码零改动。**

- **为什么结构护栏**：HOTKEY-115 三缺陷本体全在钩子回调状态机调用点（coder-1 教训(六) +
  TEST-EXEC-106 A4 教训），抽纯函数=测不到真实调用点，故 include_str! 自读源码做结构护栏。
- **G1**：`PTT_ACTIVE.store(false)` 必须落在 `should_stop_translate_poll_on_keyup(mode)` if 块内。
- **G2**：钩子 DOWN 路径 Toggle 分支用 `TOGGLE_ACTIVE.swap(true)` 翻转（`else_branch_contains` 花括号深度追踪）。
- **G3**：RegisterHotKey Toggle 分支同翻转 + `:652` 复位。
- **G4**：`notify_translate_poll_stop` 函数体内含 `TOGGLE_ACTIVE.store(false)`（B3 单一收口）。
- **G5a/b/c**：install / uninstall / sync_binding 三处清理块各归零三态；uninstall 额外含 `TRANSLATE_POLL_STOP.store(true)`。
- **G6a/b/c**：`LAST_TARGET_DOWN_TICKS` 存在 + 闸判据形态 `!was_down || now.saturating_sub(last) > 阈值` +
  **阈值 > 1000 语义断言**（从闸行 RHS 解析 token，字面量直读 / const 回查，不断言 `==2000`）。
- **G7**：main.rs mic-muted 拒绝出口含 `notify_translate_poll_stop()` 调用（include_str 只读 main.rs）。
- **自扫描规避**：按首个 `#[cfg(test)]` 切分只扫生产区 + 一律 startswith（禁 contains）+ needle 全 `concat!` 拆串。
- **验证**：fmt --check exit 0 / check --all-targets 0 error（102 warnings 与基线持平）/ 未跑 cargo test（白名单设计如此，首跑归阶段四 TEST-EXEC-117）。
- **逻辑预演**：沙箱逐字复刻护栏逻辑对当前生产源码 —— 19 项主断言全 PASS + G1/G2/G3/G4/G6c/G7 六组消融模拟全 RED-OK + G7 注释无误命中。
- **红线**：未 commit / v0.9.0 未动 / 零凭证 / 无临时文件 / 生产零改动（numstat 373/0）。

## TEST-EXEC-117 · 阶段四全量回归 + 13 条真代码消融 + Step3 A+ 结构定界（tester-1，2026-09-06）

- **Step1 三层回归**：root **1088P/0F/9I**（1077+11 逐位对账）/ src-tauri 76P / vitest 89P，全绿。
- **Step2 消融（13/13 全红，真代码）**：G1 移 store 出 if / G2+G3flip 改恒发 Start 形态 / G3reset 删 store / G4 删收口 store / G5a·G5b·G5c 各删三态之一 / G5b-附加 删 POLL_STOP=true / G6a static 改名保编译 / G6b 去掉陈旧闸 / **G6c 阈值 2000→1000 边界值**（消息实录 "got 1000" 证判据 `>` 非 `>=`）/ G7 删 mic-muted 出口 notify。每次消融后还原。
- **Step3 A+（主控批准 + 三条修正落实）**：G3/G5a/b/c 由行窗改 **block_contains 花括号结构定界**（零行窗=余量无穷、构造上不可能越界）。修正1 block_contains 增强支持多行签名锚点（fn install_keyboard_hook(/fn sync_binding( 锚点行无 `{`，前扫首个开括号）——干净代码 PASS 实证；修正2 G3 锚 HotkeyMode::Toggle => { 精确排除 PushToTalk arm；修正3 每条转换护栏 ①干净 PASS + ②消融 RED 双证据。window_has 死代码删除。测试模块 +29/-34 全在 mod tests，生产零触碰。
- **Step4 E2E**：门禁 **66P/0F/0E** 通过；🔴 偏离任务书「预期红」——热键 6/6 PASS，**E2E-GATE-103 环境失效已恢复**（如实报）；33 skip 为既有 slow/audio 类别，无新增失败；E2E 跑旧包，HOTKEY-115 真 E2E 归阶段五 BUILD-118。
- **Step5 还原自证**：numstat 仅剩 Step3 测试模块 29/34 + main.rs 空；三层重跑与 Step1 逐位一致；消融变量全归零（counts 逐一核对）；无进程残留；config.toml sha 3186ec8c 基线；fmt --check exit 0。
- **红线**：未 commit / v0.9.0 未动 / 零凭证 / 无临时文件 / 未出包。
