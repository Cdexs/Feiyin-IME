# 修改历史 · voice-ime · 20260814

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-08-14 — coder-1/coder-2 切换 Ollama Cloud 模型并重启（Gavin 指令）

**指令**：coder-1 → `ollama-cloud/glm-5.2`，coder-2 → `ollama-cloud/kimi-k2.7-code`，**并重启**。
tester-1 未动，仍为 `opencode/deepseek-v4-flash-free`。

**改动文件**：`collab/configs/team-voiceime.yaml` 两处 `model:` 字段（coder-1 / coder-2）。
生产代码零改动，版本号未动。

### 切换前先纠正了一个长期假象

`collab/todo.md` 与 pane 编辑框脚注长期显示 coder-1 = `glm-5.2 Ollama Cloud`、
coder-2 = `kimi-k2.7-code Ollama Cloud`，但**实际一次请求都没走过这两个模型**。

取证（`~/.local/share/opencode/log/`，`directory=D:\Workspace\CodeLab\voice-ime`）：
今日两份日志 `modelID` 计数 21 + 8 **全部为 `deepseek-v4-flash-free`**；
`configs/team-voiceime.yaml` 三个 Worker 的 `model` 字段亦全为 `opencode/deepseek-v4-flash-free`。

**结论**：OpenCode 编辑框脚注 `┃ Build · <model>` 不可作为「当前实跑模型」的判据，
唯一权威判据是 `~/.local/share/opencode/log/*.log` 的 `modelID=`（配合 `directory=` 定位是哪个项目）。
消息体上方的 `▣ Build · <model> · <耗时>` 才是产出该条消息的真实模型。

### 切换方式：必须重启，禁止 TUI 内切

`configs/team-voiceime.yaml` 内有明令：🔴「不要在 OpenCode TUI 里手动切换模型——切换会导致
Worker 卡死」（Gavin 2026-08-03 实测，`troubleshooting.md [WORKER-HANG-001]` 补充三）。
配套 `todo.md` 2026-08-04 记录：`opencode -s <session> -m <model>` 恢复会话时 `-m` **静默不生效**。

执行：`source replace-worker.sh && replace-worker <id> OpenCode <provider/model>`
（该脚本第 3 参数可显式指定模型，会拼成 `opencode -m <provider/model>` 全新启动）。

**验证**：重启后新日志 `055558.log` → `modelID=glm-5.2`（coder-1）、
`055547.log` → `modelID=kimi-k2.7-code`（coder-2）。切换生效。

**代价**：两个 Worker 会话上下文清零（coder-1 99.7K/50%、coder-2 33.2K/17%）。
因当时三 Worker 全空闲、outbox 全 0 字节、工作区 `src/` 干净、无在途任务，代价仅为重读协作文档。

### 🔴 顺带发现一个基础设施缺陷（见 troubleshooting `[REPLACE-WORKER-INJECT-LOST-001]`）

`replace-worker.sh` 的上下文注入**两次全部丢失**，两个 pane 重启后均为零对话空白欢迎页。
根因：`wait_agent_ready()` 的 OpenCode 分支匹配 `Build|Ollama|kimi|glm|ctrl\+p|esc interrupt`，
这些字样在 OpenCode **启动横幅阶段就已出现**，早于 TUI 可接受输入，于是判定就绪、
注入文本敲进未就绪的终端被吞。主控已手动补注入（含角色、路径、通讯协议、当前批次状态、
禁 TUI 切模型警告），两 Worker 随后正常发起请求。

**✅ 2026-08-14 当日已按 Gavin 指令修复**，详见下方「注入丢失缺陷修复」节与
`troubleshooting.md [REPLACE-WORKER-INJECT-LOST-001]`。

---

## 2026-08-14 — 修复 Worker 重启上下文注入静默丢失 + 启动模型默认值落库（Gavin 指令）

**Gavin 指令**：「注入的缺失缺陷要修，而且你要把启动初始化脚本里面，把 coder1 和 coder2 的模型
修改为现在设置的模型，接下来每趟启动都默认使用这个模型来启动。」

### 一、启动模型默认值

**结论：改 yaml 即可，无需改 init-collab.sh。** 取证：`init-collab.sh:336` 调用
`build_worker_cmd`（`lib/config-parser.sh:329`），后者读 yaml 的 `model:` 字段拼 `-m <provider/model>`
—— **yaml 是启动模型的唯一权威源**，`init-collab.sh` 全文零处硬编码模型。

`collab/configs/team-voiceime.yaml` 现值：

| Worker | model |
| --- | --- |
| coder-1 | `ollama-cloud/glm-5.2` |
| coder-2 | `ollama-cloud/kimi-k2.7-code` |
| tester-1 | `opencode/deepseek-v4-flash-free`（未动） |

`state.json` 的 `config_file` 指向该 yaml；`configs/default.yaml` 是 `worker-1/worker-2` 通用模板，
与 voice-ime 无关，不冲突。

### 二、注入丢失缺陷修复

**根因两层**：① 就绪判据 `Build|Ollama|kimi|glm|ctrl\+p|esc interrupt` 在 OpenCode 启动横幅
阶段即命中，早于输入框挂载 → 注入被终端吞掉；② 注入后零校验且带 `|| true`，
「发出去」被当成「送到了」，故障完全静默。

**修复**：① 三处判据统一改为只认输入框占位符 `Ask anything`；
② 注入改为「打字校验（占位符应消失）+ 提交校验（`esc interrupt` 或占位符回归）」双段回读，
各最多 3 次；③ 三次全败则明确报错并写 `comms.log` 标 `[CONTEXT_INJECT_FAIL]`，不再谎称成功。

**改动文件**：`collab/replace-worker.sh`（判据 1 处 + 注入块）、
`collab/init-collab.sh`（判据 2 处 + 注入块 1 处）。两者 `bash -n` 语法验证通过，旧判据零残留。

**残余风险已写明**：判据依赖 OpenCode 占位符字面量，上游改文案会使就绪检测失效并走满 30s 超时，
但第二层回读校验仍生效，不会退化为静默丢失。详见 `troubleshooting.md [REPLACE-WORKER-INJECT-LOST-001]`。

---

## 2026-08-14 — BUILD-015 端测：无序枚举不出列表，取证排除 030 提示词改动

**Gavin 端测提问**：具有无序列表的内容没有输出为无序列表，是不是之前改动 LLM 提示词改坏了？

**结论（经两次更正后的最终版）**：从 08-03 成功实验（`7a1329e`）到今天失败（`HEAD`），
在自动标点开启路径上，**整个系统提示词有且仅有一处实质文字变化** ——
`ADD_PUNCT` 常量被 `94bfb0b`（030 标点治理批次）加了一句
「Also correct punctuation the ASR may have inserted incorrectly or might be missing:
fix it so the text reads naturally.」。**Gavin 的怀疑方向部分成立。**
因果未证实（该句字面与列表无关），已设计单变量 A/B 实验待派发。零代码改动。

> 🔴 **主控本次连续犯错两次并已更正，记录以存问责链**：
> **错误一**：把「模型型号」判为未测盲区 —— 实为 08-03 阶段六已做过模型对照，
> `deepseek-v4-pro` 使 IN-B/IN-C 由 0/3 转 3/3，Gavin 已采纳，`config.toml:8` 现为 v4-pro。
> 根因是该实验从未回流进 `troubleshooting.md` 正式条目（条目停留在阶段五「机制未明」）。
> **错误二**：宣称「030 全批已被结构性排除」—— 只核对了分支结构有没有变，
> **未核对常量本体内容有没有变**，漏掉 `ADD_PUNCT`。Gavin 追问「要确认提示词模块没问题」
> 才触发逐常量比对查出。
> **共同教训：抽查代替穷举 = 假阴性。** 「改动挂在 false 分支」只证明分支选择没变，
> 不证明 true 分支取到的值没变，两者是独立的两件事。

**日志定位**：`target/release/debug.log` 本次共 7 次转录，无序列表端测是**第 1 条**
（05:41:45Z，Notepad 场景），后 6 条均为切模型指令。Gavin 口述的「第 8 个日志」为顺序记忆偏差。

**030 的三处分支型改动确已排除**：030-A/B/C 的这三处挂在 `punctuation_enabled == false` 分支
（`src/llm/mod.rs:414` L2 槽位、`:1146` `f3_rules_text` 分隔符与买菜示例、
`build_translate_system_content` B4/B5 属翻译路径）；本次输出带完整中文标点且无空格分隔
→ 走 `true` 分支 → 这三处未生效。

> ⚠️ **但「030 全批排除」不成立**：`ADD_PUNCT` 常量本体被同一批次（`94bfb0b`）改过，
> 且它走的正是 `true` 分支。见本节开头的最终结论。

**同时排除**：F4 场景块（`scene-rules.toml:323`）虽在 BUILD-015 首次同步 macOS 端 `f96c817` 新版，
但 `git diff f96c817^ f96c817 -- scene-rules.toml` 核对**枚举段零改动**。

**同时排除**：① 规则缺失（日志 `Scene F4 block injected` 原文含无序标记与 `use "- "`）；
② 单行压制（`multiline_safe=true`）；③ 后处理剥离（`LLM response text` → `LLM optimized`
→ `Injecting text` 三行完全一致）。**是 LLM 自己没做列表化**（`completion_tokens=44`）。

**详细取证表、未证实边界、以及对后续 B8/B9/B10 实验设计的增量价值**：
见 `collab/troubleshooting.md` `[F3-UNORDERED-LIST-001]` 第八节。


---

## 2026-08-14 — TEST-PROMPT-AB-033 派发 + 7.3 修订（保真度门禁逼出阶段六不保真事实）

**派发**：`ADD_PUNCT` 单变量 A/B，三组 24 次调用，模型锁死 `deepseek-v4-pro`。
G0 复刻 08-03 阶段六配置独立判定模型漂移，G1/G2 在生产配置下只切换 `ADD_PUNCT` 那一句。

**tester-1 按 7.3 门禁停手上报**（正确动作），主控复核后确认其**定性发现属实**：
快照 `SCENE_F4`（452 B，`Scene-Specific Formatting Rules (Document):`）是手工占位块，
生产真实 F4 为 1119 B（`Scene Context (F4): ...`），首 token 即不同。

**但其量化数字有误**：主控独立组装 —— 生产 F4 = 21341 B（−3.2%），
再补 `EXTRA_INSTRUCTION` = 21556 B（−2.3%），**均通过 ±5% 门禁**；
tester-1 报的 18296 疑漏 `BASE_PROMPT`(2309 B) 与 `WORDBOOK`(619 B)，
`21556−2309−619=18628` 与其数字高度接近。已要求核对。

**主控任务书缺陷两处，已下发 7.3 修订版**：
① 假定「快照 = 今天的提示词」—— 该假定对源码常量成立（git 比对已证），对快照 JSON 不成立；
② **完全没写组装顺序** —— 而 `harness_025f.py` 把 `WORDBOOK` 排在 `SUGGESTION` 之后，
与生产层序（`src/llm/mod.rs:356-443`）不符，这比长度偏差更影响结论。

**附带查出的重要事实**：阶段六实验**本身就不是生产保真的**（无场景块、无 extra、层序不同、
总长 −8.3%）。故其「v4-pro 让 IN-B/IN-C 由 0/3 转 3/3」的结论**只对那个手工提示词成立**，
对生产未必成立 —— 这解释了「实验说解决了、生产仍出问题」的表面矛盾。详见
`troubleshooting.md [F3-UNORDERED-LIST-001]` 第十节。

**G0 明确不套保真度门禁**：其参照系是「那次实验」而非「今天生产」，
原样复刻才是它能独立判定模型漂移的前提。


---

## 2026-08-14 — TEST-PROMPT-AB-033 实测完成：模型侧漂移（G0 IN-B 0/3）

**24 次调用全跑完**（G0 6 + G1 9 + G2 9，模型 `deepseek-v4-pro`，temperature 0.3，max_tokens 512）。
保真度门禁按 **UTF-8 字节口径**（Rust `String::len()`，与 `debug.log` 对齐）：

| 组 | 字节 | 偏差 |
| --- | --- | --- |
| G0 | 20220 | −8.3%（不套门禁） |
| G1 | 21863 | **−0.9% ✅** |
| G2 | 21743 | **−1.4% ✅** |

> ⚠️ 修正早期 18% 假阳性：字符数≠字节数（中文 1 字符=3 字节 UTF-8），Rust `len()` 是字节数。

**结果矩阵**：

| 组 | IN-D | IN-B | IN-C |
| --- | --- | --- | --- |
| G0（无 F4/旧 ADD_PUNCT） | — | **0b/0o/3p** | 2b/0o/1p |
| G1（含 F4/新 ADD_PUNCT） | 0b/0o/3p | 3b/0o/0p | 2b/0o/1p |
| G2（含 F4/旧 ADD_PUNCT） | 0b/0o/3p | 3b/0o/0p | 1b/0o/2p |

**结论（判定表第 4 行）**：🔴 G0 IN-B 0/3（08-03 同提示词 3/3 bullet）→ **模型侧已漂移**。
G1 vs G2 在 IN-B 均为 3/3、IN-C 2/3 vs 1/3 → 新 ADD_PUNCT 无稳定可测影响；
IN-D 在 G1/G2 均 0/3 → 其失败与 ADD_PUNCT 版本无关。**责任不在那句改的提示词。**

**产物**：`collab/research/harness_033_addpunct_ab.py` + `ab033-components.json` +
`ab033-results.json`（24 条全文）+ `harness-033-addpunct-ab.md`；`outbox/tester-1/result.md`。

**零生产代码改动**：`git status -- src/` 的 14 个修改文件 mtime 均为 8月8日（任务开始前已存在于工作区，
非本会话产生），未做任何 git reset/checkout/restore。

---

## 2026-08-14 · TEST-PROMPT-IND-034 实测完成：IN-D 分叉点二分 → 主命题证否

**18 次调用全跑完**（D0/D1/D2/D3/D4/CTRL 各 3，模型 `deepseek-v4-pro`，temperature 0.3，max_tokens 512）。
提示词**完全复用 033 已验证 G1**（21863B / −0.9%，生产层序 + 生产真实 SCENE_F4_PROD + 新 ADD_PUNCT_NEW），
本任务零改动、零复验。

**结果汇总**：

| 变体 | form | edited | filler_removed | ct |
| --- | --- | --- | --- | --- |
| D0（IN-D 原句） | 3p | 2/3 | 1/3 | [45,45,44] |
| D1（删「啊」） | 3p | 0/3 | 0/3 | [44,44,44] |
| D2（删收尾句） | 3p | 3/3 | 0/3 | [34,34,34] |
| D3（删开头引入句） | 3p | 2/3 | 2/3 | [34,33,33] |
| D4（三项改名词短语） | 3p | 3/3 | 0/3 | [42,42,42] |
| CTRL（IN-B 正向对照） | 3b | 3/3 | 3/3 | [78,79,77] |

**结论（判定表第 2 行）**：主命题**被证否**。IN-D 失败**不是**「整句被判定为无需处理」——
模型大部分调用确实进入处理模式（D0/D2/D3/D4 的 edited 多为 true：删口水词、补句号、并列项逗号→顿号），
但**从不拆列表**（全 para）。**D4 最致命**：三次统一把 `青椒，西瓜，香蕉` 改为 `青椒、西瓜、香蕉`（顿号=列举
连接符），证明模型**识别出并列/枚举结构却停留单段落** → 分叉点在「枚举识别 → 列表路由」，回到老问题域。
CTRL 3/3 bullet 确认模型状态批内稳定，数据可用。

**附加发现**：
- D1（仅删「啊」）是唯一全复述变体（edited=0/3，ct=44 恒定）→「是否处理」对细微扰动敏感、存在批内随机性。
- D0 内部不稳定（seq01 复述 / seq12 改标点 / seq13 删「啊」），提示需要更大 n 量化。
- 与任务书前提的修正：D0 filler_removed=1/3、D3=2/3 证明模型**至少部分执行了 F1**，与"连删口水词都没做"
  的推演不符，属实测对前提的直接修正（非设计偏离，18 次按任务书写定执行完毕）。

**未验证部分**：① 判定表第 2 行依赖 edited 逐字敏感口径，若换"语义级处理"口径结论可能反转；② D1→D0
行为跳变未归因（随机性 vs 「啊」触发，n=3 太小）；③「是否处理」随机性未量化；④「列表路由」环节未隔离
（未构造显式列举词对照，下一步最直接）；⑤ 未复测 IN-C。

**产物**：`collab/research/harness_034_ind_bisect.py` + `ind034-results.json`（18 条全文）+
`harness-034-ind-bisect.md`；`outbox/tester-1/result.md`。

**零生产代码改动**：未触碰 `src/`；未 cargo build；无 git 破坏性命令。

---

## 2026-08-14 — TEST-PROMPT-IND-034 收口：机制定位到 F3 定义空隙，Gavin 拍板不修（DEC-049）

**18 次调用**（D0/D1/D2/D3/D4/CTRL 各 3，`deepseek-v4-pro`，temp 0.3，max_tokens 512）。
主命题「整句未被处理」**证否** —— D0 有 2/3 发生编辑、D4 3/3 做了逗号→顿号改写，
模型确实处理了，只是没走列表。

**机制（代码侧，`src/llm/mod.rs:1219-1233`）**：F3 两道闸之间存在定义空隙。

| 项目形态 | 归属 | 实测 | 判定 |
| --- | --- | --- | --- |
| `新鲜的青椒`（5 字名词短语） | 明确 SHORT | 顿号内联 3/3 | ✅ 规则正确执行 |
| `有些同学下课之后徘徊在校园里不走，三五成群` | 明确 LONG | bullet 列表 3/3 | ✅ 规则正确执行 |
| `有没有新鲜的青椒`（无主语疑问谓词短语） | **空隙** | 既不列表也不加顿号 9/9 | 命中闸一保守兜底 |

**模型全程执行正确**，「什么都不做」正是闸一「If unsure, DO NOT use a list」在命令它。

**顺带推翻旧结论**：`[F3-UNORDERED-LIST-001]` 第四节「差别不在长度」是错的 ——
旧实验 B6「缩短项 → 0/3」其实是 SHORT 规则生效（缩短把项目推进了顿号内联分支），
**长度一直是变量，当年读反了**。方法论：结果相同不代表路径相同，
「改了 X 仍不出列表」不能直接读作「X 不是原因」。

**决策 DEC-049（Gavin）**：保持现状，吃不准的不走列表化，避免规则过分精细化
造成易失误和交叉误判。四条理由：① 两类错误代价不对称（误切列表是每句都可能撞上的
高频伤害，漏切只是少一次锦上添花）；② 规则精细化边际收益递减、交叉误判风险递增，
与 DEC-039/DEC-040 同向；③ 闸一闸二兜底本就相反，只补闸二会加剧已知矛盾；
④ 已有零成本规避方式（改说「第一/第二/第三」，跨全部实验从未失败）。

**处置**：F3 规则不改动；`[F3-UNORDERED-LIST-001]` 由「机制未明」转「机制已明、刻意不修」并**关闭**；
原计划 D5 确认实验**取消**（机制已足够支撑决策）。**零生产代码改动。**

**两个独立遗留不随本条关闭**：① 模型漂移（033 G0：同提示词同模型同参数，08-03 3/3 → 08-14 0/3）；
② 场景块为何是正向变量（同日同模型：无场景块 IN-B 0/3、含场景块 3/3），机制未明。
