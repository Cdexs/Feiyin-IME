# 修改历史 · voice-ime · 20260907

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 主控 · session 启动现场核对 + 本轮派发（2026-09-07）

- **现场核对（以 git 为准，非记忆）**：`HEAD = 4cfa2a0`，**与 `origin/main` 一致** ——
  09-06 收工存盘时记的「本地领先 42 个提交未 push」已不成立，其后已完成 push。
  工作区仅 `logs/20260907.md` untracked（今日空模板）。
- **三 Worker 全部 ACK 就绪**（周额度已恢复）：coder-1 / coder-2 均 GLM-5.3-Flash，tester-1 deepseek-v4-flash。
  三者均为 OpenCode 类型，**非 Codex，不发 `/permissions Full Access`**。
- **运维改动**：`collab/handoffs.md` 276 行 > 200 阈值 → 2026-09-06 全部 **24 条**移入
  `handoffs-archive.md`（追加式 + 分隔注释），主文件回到 4 行表头。
- **`collab/todo.md`** 顶部插入 2026-09-07 本轮入口节（现场核对 / 09-06 夜~09-07 凌晨已闭环的 6 个提交 /
  本轮待办队列 / Worker 占用 / 待 Gavin 拍板三项）。

### 本轮派发（阶段一，文件级零重叠已核）

| 任务 | Worker | 写哪些文件 | 要点 |
| --- | --- | --- | --- |
| `OVERLAY-141-IMPL` | coder-2 | `src/main.rs` 生产区（唯一写方） | 帧末解析式圆角 SDF 直接写 alpha，不再依赖 D2D 是否保真；**前置**把窗口外框半径收敛为单一来源（主控已查清 9 态全集散落点并写入任务书，含「内部元素半径不许动」的甄别红线） |
| `INVESTIGATE-142` | coder-1 | `collab/drafts/overlay-142-editing-flicker.md`（只读 `src/main.rs`） | 编辑态移光标仍闪的**二次**取证。第一次（120→130）修完症状仍在，主控怀疑 OVERLAY-121 P2 图层模式切换是**独立的第二个源**。任务书强制引用 `[PLAUSIBLE-FIX-NOT-ACTUAL-CAUSE-001]`，要求穷举候选源、每条标注证据类型、区分「能解释症状」与「确实发生」 |
| 待命 | tester-1 | — | 阶段四/五之前不派发（省额度），已 tmux 告知待命 |

- **阶段三预定**：`TEST-SYNC-143` 派 **coder-1**（非本批代码作者，符合 worker-guide 第十三节）。
- **主控给 coder-2 的取证补充**（写进任务书，省去 Worker 重复普查）：9 个 `OverlayStatus` 的外框半径全集 ——
  Recording 系 4 态 + Processing = r16（D2D :4048/:4154/:4175/:3487；GDI :2761/:2748/:2816/:2326）；
  FocusLost/Error/Info = r10（D2D :4299/:4199/:4241）；StreamingEditing = r10 且**走 SLWA 不走 ULW**，须逐位不变。

### OVERLAY-141-IMPL 方案协商第 1 轮（coder-2 异议 → 主控裁定，2026-09-07）

coder-2 同意规格，提两点补充。主控逐条 grep 复核后**全部采纳**，另订正一处并加一条自证口径：

| # | coder-2 的补充 | 主控取证 | 裁定 |
| --- | --- | --- | --- |
| ① | 外框半径全集比任务表多 **5 处 GDI 站点**（`:4478` Processing r16 / `:4651` Preview r10 / `:4822` Error r10 / `:4898` Info r10 / 死代码 `:3083` r10 的本地 `const CORNER_RADIUS`） | `grep CORNER_RADIUS` 全库落点 6 处，其列的 5 处**全部属实**。主控原表在 FocusLost/Error/Info 的 GDI 侧写「—」系未查透 | **批准收敛，以 coder-2 清单为准** |
| —— | 第 6 处 `:2922` coder-2 **未列** | 主控核实：位于 `draw_submit_button` 函数体内，是**提交按钮内部元素**圆角，非窗口外框 | **甄别正确，不许动**；但要求 result.md 显式列出「已识别为内部元素、故意不收敛」，否则验收方看到 grep 还剩一处会误判为漏改 |
| ② | 死代码 `draw_editing_overlay_chrome` 一并收敛 | 主控 grep：全库仅 `:3077` 定义处一处出现，**零调用点**，编译器实证成立 ⇒ 改它运行时零影响，不违反「编辑态逐位不变」红线 | **批准**，但要求**单独成 hunk + result.md 单列**——该函数是否删是待 Gavin 拍板项，若拍板删，整函数删除会连带带走这处改动，零残留 |
| ③ | G3 应仍绿、G6 必然红=预期红 | 主控核对断言：G3 只认函数名与调用行首、不认签名 ⇒ 绿；G6 断言要求含「GDI 提亮分支 + 预乘缩放分支」，新 SDF 逻辑两分支消失 ⇒ 红 | **同意**。补一条：**G2（ULW 提交恰 1 处）不受签名变化影响，应保持绿；若它红了说明结构被动了，立即停手报主控** |

**验收标准 3 口径调整**：不再要求「全库 `CORNER_RADIUS` 剩余 0 处」（`:2922` 是合法留存），
改为「**外框半径字面量剩余 0 处** + **内部元素半径未动清单**（3.5 药丸 / 5 停止键 / 4 提交键 / 4 底部按钮 / 3 标题✕ / `:2922` 提交按钮 10）」，两份 grep 证据都要给。

🔴 **文档订正**：`draw_editing_overlay_chrome` 的行号长期记作 `main.rs:2809`，**实际为 `:3077`**，
系陈旧值。已订正 `collab/todo.md` 待拍板表第 1 项（09-06 历史存盘节保持原样不追改）。

**过程观察**：coder-2 在等主控回复期间已把 `OVERLAY_FRAME_RADIUS_LG/SM` 常量落到 `main.rs:1075-1083`，
注释中对内部元素（3.5/5/4/4/3）的排除说明书写准确，主控已确认放行。


## 2026-09-07 — coder-1 — INVESTIGATE-142 ✅ 编辑态移光标仍闪 · 二次取证（🔴 只查不修，src 零改动）

- **结论**：编辑稳态（光标移动时刻）应用层零重绘触发器——主控怀疑的「图层模式切换」与旧根因「迟到流式包」均被调用门控静态证伪（switch 早退 `:599`+调用点恰 2；Show 唯一编辑分支 `:5686` 被 FLICKER-130 门控挡死，`editing⇒stopped` 不变量有 F3 护栏钉死）。**根因未定，头号在案候选=EDIT 子控件自身重绘（caret/ES_AUTOHSCROLL 滚动）+DWM 对 SLWA 分层窗重合成**——发生性静态实证（箭头键不经过任何应用逻辑），可见性需运行时实测。
- **新发现（前两次取证未记录）**：SLWA 模式父窗 WM_PAINT 全窗 BitBlt 直打屏幕（`:2172`）且父窗无 WS_CLIPCHILDREN（`:1172`/`:1222`）——父窗一旦重绘，EDIT 文字被背景整块盖掉且 EDIT 不知情（结构性放大器；应用级触发器静态为零，是否被系统路径触发=实测 P2）。
- **FLICKER-130 定性**：没有判错，多源叠加——修掉的 Show 风暴源真实存在至今有效；光标移动闪来自一直存在、先前被更剧烈症状掩盖的另一源（新旧症状触发器不同佐证）。
- **产出**：`collab/drafts/overlay-142-editing-flicker.md`（六候选三态判定 + M0-M3 修法建议不实施 + 五探针最小实测方案含预先声明判读方式 + WebSearch 平台调研：SLWA+子控件闪烁无文档定论）。
- **红线**：src/** 零写入（diff 中 M 属 coder-2 在途 OVERLAY-141-IMPL，任务书预告）；未 commit/未动版本/未出包/零凭证/无临时文件。行号口径：取证期间 main.rs 被 coder-2 并行修改（11669→11727 行），以符号名锚点为准。


## 2026-09-07 — coder-2 — OVERLAY-141-IMPL ✅ 圆角灰边根治：帧末解析式 SDF 写 alpha + 外框半径单一来源（待主控验收）

- **改动 A（半径单一来源）**：新增文件级常量 `OVERLAY_FRAME_RADIUS_LG=16.0` / `OVERLAY_FRAME_RADIUS_SM=10.0`（`src/main.rs:1081,1083`，`OVERLAY_BTN_BORDER` 之后）+ 映射函数 `overlay_frame_radius(status)`（`:2216`，Recording 系/FallingToProcessing/Processing→LG；StreamingEditing/FocusLost/Error/Info→SM）。**16 处外框站点全部收敛**：D2D 7 处（`draw_processing_primitives:3552`、editing `:4001`、waveform `:4116`、idle `:4223`、streaming_text `:4245`、error `:4271`、info `:4320`、preview `:4387`——前四个 chrome、后三个 chrome_with）、GDI 调用点 5 处（`:2383` FallingToProcessing、`:2424` editing、`:2807/:2821/:2877` Recording 系 fallback，`OVERLAY_FRAME_RADIUS_* as i32`）、GDI 本地 `const CORNER_RADIUS` 4 处（Processing `:4564`→LG、Preview `:4738`/Error `:4910`/Info `:4987`→SM）+ **死代码独立 hunk** `draw_editing_overlay_chrome:3141`→SM（零调用实证，若 Gavin 拍板删函数则整函数带走零残留）。`draw_submit_button:2977` 的 `CORNER_RADIUS=10` 经主控裁定为**提交按钮内部元素半径，故意不动**。
- **改动 B（SDF 解析 alpha）**：`apply_alpha_fixup` 重写（`:2243`），签名 `(bits, width, height, opacity, radius)`。每像素：圆角矩形 SDF（q 夹逼 ≥0 后 `d = len(q) - r`，与精确 SDF 覆盖率逐位同值——内部 d≤-r→cov=1）→ `cov=clamp(0.5-d)`；cov==0 四通道全零；否则反预乘还原原色（a>0 时 `rgb·255/a`，GDI 像素直通色）后按 `k=cov·op` 写回预乘域。同时消除旧规则「形状内纯黑像素变透明洞」缺陷（按几何给 alpha、与颜色无关）。**落地前自查（任务书要求）**：`chrome_with:3670` 填充矩形 `(0,0,w,h)` r、`draw_processing_primitives` 填充 `(0,0,w,h)` r16 —— SDF 原点/尺寸与实际填充逐位同框 ✅（描边 0.5..w-0.5 为 pen 内缩，轮廓 = 填充边界，无需调整）。
- **改动 C（WM_PAINT 调用点）**：`state.lock()` 内与 opacity 同处取 `frame_radius = overlay_frame_radius(&req.status)`（`:2136`，request 为 None 兜底 LG）；调用 `:2153` 传新签名。**G3 结构保持**：fixup 调用与 ULW 提交仍同处 `if use_ulw` 块。
- **验证**：`cargo fmt` 通过；`cargo check --all-targets` **0 error**，warnings **111/102 与基线逐位持平**；`cargo test` **1104P/1F/9I**，唯一失败 = `g6_fixup_three_branches_present` = **预期红**（旧三分支标记消失，交 TEST-SYNC-143 换血，本单未动护栏）；G2/G3/G5/G7/H7 及全部既有护栏绿。
- **半径单一来源自证（验收 3）**：外框字面量 grep `chrome(res…16|10` / `chrome_with(res…,16|10` / `draw_overlay_chrome(hdc, rect, 1[0-9])` / `let corner_radius = 16.0` / `const CORNER_RADIUS: i32 = 1[06];` **全部 0 命中**（常量引用除外）。**内部元素未动清单**：d2d 药丸 3.5（mic_indicator `:3810`）、d2d 停止键 5.0（`:3961/:3975`）、指示圆 `bw`（`:4078/:4097`）、错误/信息圆点 `circ_d=4.0`（`:4290/:4339`）、标题✕键 3.0（`:4431`）、底部按钮 4.0（`:4499`）、GDI 药丸 RoundRect(…28,28)（`:2631/:3070`）、指示圆 bw*2（`:2704/:2727`）、GDI 停止/提交键 10（→r5，`:2987/:3154/:3231`）、标题✕ 6（→r3，`:4754`）、底部按钮 8（→r4，`:4794/:4860`）、**GDI 提交按钮 `const CORNER_RADIUS=10`（`:2977`，主控裁定内部元素故意不动）**。
- **编辑态逐位不变自证（验收 4）**：SLWA 运行路径（BitBlt `:2176`、switch_overlay_layered_mode、EDIT 子控件）**零代码改动**；diff 中 StreamingEditing 相关仅为 `overlay_frame_radius` 映射分支（新函数）与注释；`draw_editing_overlay` 的 `chrome(res,w,h,10.0)`→`OVERLAY_FRAME_RADIUS_SM` 为同值替换（10.0），GDI fallback 同理 —— 视觉逐位不变。
- **macOS 影响复核（验收 5）**：全部改动位于 `#[cfg(target_os = "windows")]`（常量/映射函数/apply_alpha_fixup/mod d2d/各绘制函数/WM_PAINT），`src/platform/macos/**` 零引用 ⇒ **不适用**；已同步 `docs/MACOS-HANDOFF.md`（§ OVERLAY-141）。
- **红线自证**：只改 `src/main.rs` 生产区（+187/-44，24 hunks）；hotkey.rs/ui/**/git-hooks 未碰；未 commit/v0.9.0 未动/未出包；仅跑白名单 `cargo fmt`/`cargo check`/`cargo test`（阶段一允许）；零凭证落盘；无临时文件。
### INVESTIGATE-142 主控验收：✅ 通过（2026-09-07）

**独立核验（不只看报告，逐条 grep 实证）**：

| coder-1 的论断 | 主控独立核验 | 结论 |
| --- | --- | --- |
| 候选 1（主控怀疑的图层模式切换）不成立 | `switch_overlay_layered_mode` 首行早退 `if state.layered_mode == target { return; }` 属实（`:599-601`）；全库调用点**恰 2 处**（`:1363` Show 臂 / `:1559` EnterEditMode 臂）属实 | **主控假设被证伪，采信** |
| 新发现：父窗无 `WS_CLIPCHILDREN`，SLWA 下全窗 BitBlt 会盖掉 EDIT 文字 | `grep WS_CLIPCHILDREN src/main.rs` **全库零命中**（exit 1）；类样式 `WNDCLASS_STYLES(0)`（`:1172`）属实；SLWA 分支全客户区 `BitBlt(... SRCCOPY)`（`:2177`）属实 | **结构性放大器成立，采信** |
| 五文档已更新 | 五份逐一 grep `INVESTIGATE-142`：logs 2 / CHANGELOG 1 / handoffs 1 / progress 1 / todo 1，**全部命中，无虚报** | 通过 |
| `src/**` 零写入 | 如实报告「无法以 `git diff src/` 为空自证」（coder-2 并行在途），改以「唯一可写文件=draft」口径 —— 符合 `[WORKER-GIT-AUTOCRLF-ENV-DIFF-001]` 与协作纪律，未擅自清理他人改动 | 通过 |

**🔴 主控顺带执行 P1 探针（零改码，可立刻做）+ 抢存易失证据**：

- 按 `[EVIDENCE-LOG-VOLATILE-001]`，发现 `target/release/debug.log`（23,970,807 B，mtime 09-07 01:36）
  即 Gavin 端测 BUILD-135 当次产物，**下一次运行即被冲掉**，已第一时间抢存为
  `collab/evidence/debug-gavin-endtest-BUILD135-20260907-0136.log`。
- P1 判读结果：日志时间跨度 `2026-09-06T17:01:27Z ~ 17:36:43Z`（= 本地 09-07 01:01~01:36）。
  `ASR-038-C` **0 命中**、`StreamingEditing` **0 命中**、`created EDIT control` **0 命中**
  ⇒ 🔴 **该 session 全程未进入过编辑态，这份日志无法回答闪烁问题**。
  `ignoring late StreamingText after stop` 1 命中，属非编辑态下的正常停止后丢包，非本案证据。
- **P1 结论：现有日志不可判，需一次「显式使用编辑态 + 开 debug 日志」的新采集**。
- 附：主控一度怀疑 coder-1 的 P1 探针引了不存在的日志串，复核后**是主控查错了**——
  `created EDIT control` 确实存在于 `:738` `log::info!`，探针规格无误。

**🔴 主控发现的一个交付载体缺陷（非 coder-1 责任）**：
`collab/drafts/` 被 `.gitignore:72` 整目录屏蔽 ⇒ 本次调查文档**不会入库，换机/清理即永久丢失**。
OVERLAY-141 的规格当初正是因此被转入 `docs/`。本次同样处理：
已复制为 `docs/OVERLAY-142-EDITING-FLICKER.md`（已验证不被忽略，可入库）。
**后续凡有长期价值的 draft，一律须转入 `docs/`**。

**根因状态**：未定，需实测。头号候选 = EDIT 子控件重绘 + DWM 对 SLWA 分层窗重合成（候选 4），
次选 = 父窗全窗 BitBlt 结构性放大器（候选 3）。FLICKER-130 **定性为多源叠加，非判错**。

