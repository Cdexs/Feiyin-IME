# 修改历史 · voice-ime · 20260907

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 主控 · session 启动现场核对 + 本轮派发（2026-09-07）

- **现场核对（以 git 为准，非记忆）**：`HEAD = 4cfa2a0`，**与 `origin/main` 一致** ——
  09-06 收工存盘时记的「本地领先 42 个提交未 push」已不成立，其后已完成 push。
  工作区仅 `logs/20260907.md` untracked（今日空模板）。
- **三 Worker 全部 ACK 就绪**（周额度已恢复）：coder-1 / coder-2 均 GLM-5.3-Flash，tester-1 deepseek-v4-flash。
  三者均为 OpenCode 类型，**非 Codex，不发 `/permissions Full Access`**。
- **运维改动**：`collab/handoffs.md` 276 行 > 200 阈值 → 2026-09-06 全部 **24 条**移入
  `handoffs-archive.md`（追加式 + 分隔注释），主文件回到 4 行表头。
- **`collab/todo.md`** 顶部插入 2026-09-07 本轮入口节（现场核对 / 09-06 夜~09-07 凌晨已闭环的 6 个提交 /
  本轮待办队列 / Worker 占用 / 待 Gavin 拍板三项）。

### 本轮派发（阶段一，文件级零重叠已核）

| 任务 | Worker | 写哪些文件 | 要点 |
| --- | --- | --- | --- |
| `OVERLAY-141-IMPL` | coder-2 | `src/main.rs` 生产区（唯一写方） | 帧末解析式圆角 SDF 直接写 alpha，不再依赖 D2D 是否保真；**前置**把窗口外框半径收敛为单一来源（主控已查清 9 态全集散落点并写入任务书，含「内部元素半径不许动」的甄别红线） |
| `INVESTIGATE-142` | coder-1 | `collab/drafts/overlay-142-editing-flicker.md`（只读 `src/main.rs`） | 编辑态移光标仍闪的**二次**取证。第一次（120→130）修完症状仍在，主控怀疑 OVERLAY-121 P2 图层模式切换是**独立的第二个源**。任务书强制引用 `[PLAUSIBLE-FIX-NOT-ACTUAL-CAUSE-001]`，要求穷举候选源、每条标注证据类型、区分「能解释症状」与「确实发生」 |
| 待命 | tester-1 | — | 阶段四/五之前不派发（省额度），已 tmux 告知待命 |

- **阶段三预定**：`TEST-SYNC-143` 派 **coder-1**（非本批代码作者，符合 worker-guide 第十三节）。
- **主控给 coder-2 的取证补充**（写进任务书，省去 Worker 重复普查）：9 个 `OverlayStatus` 的外框半径全集 ——
  Recording 系 4 态 + Processing = r16（D2D :4048/:4154/:4175/:3487；GDI :2761/:2748/:2816/:2326）；
  FocusLost/Error/Info = r10（D2D :4299/:4199/:4241）；StreamingEditing = r10 且**走 SLWA 不走 ULW**，须逐位不变。

### OVERLAY-141-IMPL 方案协商第 1 轮（coder-2 异议 → 主控裁定，2026-09-07）

coder-2 同意规格，提两点补充。主控逐条 grep 复核后**全部采纳**，另订正一处并加一条自证口径：

| # | coder-2 的补充 | 主控取证 | 裁定 |
| --- | --- | --- | --- |
| ① | 外框半径全集比任务表多 **5 处 GDI 站点**（`:4478` Processing r16 / `:4651` Preview r10 / `:4822` Error r10 / `:4898` Info r10 / 死代码 `:3083` r10 的本地 `const CORNER_RADIUS`） | `grep CORNER_RADIUS` 全库落点 6 处，其列的 5 处**全部属实**。主控原表在 FocusLost/Error/Info 的 GDI 侧写「—」系未查透 | **批准收敛，以 coder-2 清单为准** |
| —— | 第 6 处 `:2922` coder-2 **未列** | 主控核实：位于 `draw_submit_button` 函数体内，是**提交按钮内部元素**圆角，非窗口外框 | **甄别正确，不许动**；但要求 result.md 显式列出「已识别为内部元素、故意不收敛」，否则验收方看到 grep 还剩一处会误判为漏改 |
| ② | 死代码 `draw_editing_overlay_chrome` 一并收敛 | 主控 grep：全库仅 `:3077` 定义处一处出现，**零调用点**，编译器实证成立 ⇒ 改它运行时零影响，不违反「编辑态逐位不变」红线 | **批准**，但要求**单独成 hunk + result.md 单列**——该函数是否删是待 Gavin 拍板项，若拍板删，整函数删除会连带带走这处改动，零残留 |
| ③ | G3 应仍绿、G6 必然红=预期红 | 主控核对断言：G3 只认函数名与调用行首、不认签名 ⇒ 绿；G6 断言要求含「GDI 提亮分支 + 预乘缩放分支」，新 SDF 逻辑两分支消失 ⇒ 红 | **同意**。补一条：**G2（ULW 提交恰 1 处）不受签名变化影响，应保持绿；若它红了说明结构被动了，立即停手报主控** |

**验收标准 3 口径调整**：不再要求「全库 `CORNER_RADIUS` 剩余 0 处」（`:2922` 是合法留存），
改为「**外框半径字面量剩余 0 处** + **内部元素半径未动清单**（3.5 药丸 / 5 停止键 / 4 提交键 / 4 底部按钮 / 3 标题✕ / `:2922` 提交按钮 10）」，两份 grep 证据都要给。

🔴 **文档订正**：`draw_editing_overlay_chrome` 的行号长期记作 `main.rs:2809`，**实际为 `:3077`**，
系陈旧值。已订正 `collab/todo.md` 待拍板表第 1 项（09-06 历史存盘节保持原样不追改）。

**过程观察**：coder-2 在等主控回复期间已把 `OVERLAY_FRAME_RADIUS_LG/SM` 常量落到 `main.rs:1075-1083`，
注释中对内部元素（3.5/5/4/4/3）的排除说明书写准确，主控已确认放行。


## 2026-09-07 — coder-1 — INVESTIGATE-142 ✅ 编辑态移光标仍闪 · 二次取证（🔴 只查不修，src 零改动）

- **结论**：编辑稳态（光标移动时刻）应用层零重绘触发器——主控怀疑的「图层模式切换」与旧根因「迟到流式包」均被调用门控静态证伪（switch 早退 `:599`+调用点恰 2；Show 唯一编辑分支 `:5686` 被 FLICKER-130 门控挡死，`editing⇒stopped` 不变量有 F3 护栏钉死）。**根因未定，头号在案候选=EDIT 子控件自身重绘（caret/ES_AUTOHSCROLL 滚动）+DWM 对 SLWA 分层窗重合成**——发生性静态实证（箭头键不经过任何应用逻辑），可见性需运行时实测。
- **新发现（前两次取证未记录）**：SLWA 模式父窗 WM_PAINT 全窗 BitBlt 直打屏幕（`:2172`）且父窗无 WS_CLIPCHILDREN（`:1172`/`:1222`）——父窗一旦重绘，EDIT 文字被背景整块盖掉且 EDIT 不知情（结构性放大器；应用级触发器静态为零，是否被系统路径触发=实测 P2）。
- **FLICKER-130 定性**：没有判错，多源叠加——修掉的 Show 风暴源真实存在至今有效；光标移动闪来自一直存在、先前被更剧烈症状掩盖的另一源（新旧症状触发器不同佐证）。
- **产出**：`collab/drafts/overlay-142-editing-flicker.md`（六候选三态判定 + M0-M3 修法建议不实施 + 五探针最小实测方案含预先声明判读方式 + WebSearch 平台调研：SLWA+子控件闪烁无文档定论）。
- **红线**：src/** 零写入（diff 中 M 属 coder-2 在途 OVERLAY-141-IMPL，任务书预告）；未 commit/未动版本/未出包/零凭证/无临时文件。行号口径：取证期间 main.rs 被 coder-2 并行修改（11669→11727 行），以符号名锚点为准。


## 2026-09-07 — coder-2 — OVERLAY-141-IMPL ✅ 圆角灰边根治：帧末解析式 SDF 写 alpha + 外框半径单一来源（待主控验收）

- **改动 A（半径单一来源）**：新增文件级常量 `OVERLAY_FRAME_RADIUS_LG=16.0` / `OVERLAY_FRAME_RADIUS_SM=10.0`（`src/main.rs:1081,1083`，`OVERLAY_BTN_BORDER` 之后）+ 映射函数 `overlay_frame_radius(status)`（`:2216`，Recording 系/FallingToProcessing/Processing→LG；StreamingEditing/FocusLost/Error/Info→SM）。**16 处外框站点全部收敛**：D2D 7 处（`draw_processing_primitives:3552`、editing `:4001`、waveform `:4116`、idle `:4223`、streaming_text `:4245`、error `:4271`、info `:4320`、preview `:4387`——前四个 chrome、后三个 chrome_with）、GDI 调用点 5 处（`:2383` FallingToProcessing、`:2424` editing、`:2807/:2821/:2877` Recording 系 fallback，`OVERLAY_FRAME_RADIUS_* as i32`）、GDI 本地 `const CORNER_RADIUS` 4 处（Processing `:4564`→LG、Preview `:4738`/Error `:4910`/Info `:4987`→SM）+ **死代码独立 hunk** `draw_editing_overlay_chrome:3141`→SM（零调用实证，若 Gavin 拍板删函数则整函数带走零残留）。`draw_submit_button:2977` 的 `CORNER_RADIUS=10` 经主控裁定为**提交按钮内部元素半径，故意不动**。
- **改动 B（SDF 解析 alpha）**：`apply_alpha_fixup` 重写（`:2243`），签名 `(bits, width, height, opacity, radius)`。每像素：圆角矩形 SDF（q 夹逼 ≥0 后 `d = len(q) - r`，与精确 SDF 覆盖率逐位同值——内部 d≤-r→cov=1）→ `cov=clamp(0.5-d)`；cov==0 四通道全零；否则反预乘还原原色（a>0 时 `rgb·255/a`，GDI 像素直通色）后按 `k=cov·op` 写回预乘域。同时消除旧规则「形状内纯黑像素变透明洞」缺陷（按几何给 alpha、与颜色无关）。**落地前自查（任务书要求）**：`chrome_with:3670` 填充矩形 `(0,0,w,h)` r、`draw_processing_primitives` 填充 `(0,0,w,h)` r16 —— SDF 原点/尺寸与实际填充逐位同框 ✅（描边 0.5..w-0.5 为 pen 内缩，轮廓 = 填充边界，无需调整）。
- **改动 C（WM_PAINT 调用点）**：`state.lock()` 内与 opacity 同处取 `frame_radius = overlay_frame_radius(&req.status)`（`:2136`，request 为 None 兜底 LG）；调用 `:2153` 传新签名。**G3 结构保持**：fixup 调用与 ULW 提交仍同处 `if use_ulw` 块。
- **验证**：`cargo fmt` 通过；`cargo check --all-targets` **0 error**，warnings **111/102 与基线逐位持平**；`cargo test` **1104P/1F/9I**，唯一失败 = `g6_fixup_three_branches_present` = **预期红**（旧三分支标记消失，交 TEST-SYNC-143 换血，本单未动护栏）；G2/G3/G5/G7/H7 及全部既有护栏绿。
- **半径单一来源自证（验收 3）**：外框字面量 grep `chrome(res…16|10` / `chrome_with(res…,16|10` / `draw_overlay_chrome(hdc, rect, 1[0-9])` / `let corner_radius = 16.0` / `const CORNER_RADIUS: i32 = 1[06];` **全部 0 命中**（常量引用除外）。**内部元素未动清单**：d2d 药丸 3.5（mic_indicator `:3810`）、d2d 停止键 5.0（`:3961/:3975`）、指示圆 `bw`（`:4078/:4097`）、错误/信息圆点 `circ_d=4.0`（`:4290/:4339`）、标题✕键 3.0（`:4431`）、底部按钮 4.0（`:4499`）、GDI 药丸 RoundRect(…28,28)（`:2631/:3070`）、指示圆 bw*2（`:2704/:2727`）、GDI 停止/提交键 10（→r5，`:2987/:3154/:3231`）、标题✕ 6（→r3，`:4754`）、底部按钮 8（→r4，`:4794/:4860`）、**GDI 提交按钮 `const CORNER_RADIUS=10`（`:2977`，主控裁定内部元素故意不动）**。
- **编辑态逐位不变自证（验收 4）**：SLWA 运行路径（BitBlt `:2176`、switch_overlay_layered_mode、EDIT 子控件）**零代码改动**；diff 中 StreamingEditing 相关仅为 `overlay_frame_radius` 映射分支（新函数）与注释；`draw_editing_overlay` 的 `chrome(res,w,h,10.0)`→`OVERLAY_FRAME_RADIUS_SM` 为同值替换（10.0），GDI fallback 同理 —— 视觉逐位不变。
- **macOS 影响复核（验收 5）**：全部改动位于 `#[cfg(target_os = "windows")]`（常量/映射函数/apply_alpha_fixup/mod d2d/各绘制函数/WM_PAINT），`src/platform/macos/**` 零引用 ⇒ **不适用**；已同步 `docs/MACOS-HANDOFF.md`（§ OVERLAY-141）。
- **红线自证**：只改 `src/main.rs` 生产区（+187/-44，24 hunks）；hotkey.rs/ui/**/git-hooks 未碰；未 commit/v0.9.0 未动/未出包；仅跑白名单 `cargo fmt`/`cargo check`/`cargo test`（阶段一允许）；零凭证落盘；无临时文件。
### INVESTIGATE-142 主控验收：✅ 通过（2026-09-07）

**独立核验（不只看报告，逐条 grep 实证）**：

| coder-1 的论断 | 主控独立核验 | 结论 |
| --- | --- | --- |
| 候选 1（主控怀疑的图层模式切换）不成立 | `switch_overlay_layered_mode` 首行早退 `if state.layered_mode == target { return; }` 属实（`:599-601`）；全库调用点**恰 2 处**（`:1363` Show 臂 / `:1559` EnterEditMode 臂）属实 | **主控假设被证伪，采信** |
| 新发现：父窗无 `WS_CLIPCHILDREN`，SLWA 下全窗 BitBlt 会盖掉 EDIT 文字 | `grep WS_CLIPCHILDREN src/main.rs` **全库零命中**（exit 1）；类样式 `WNDCLASS_STYLES(0)`（`:1172`）属实；SLWA 分支全客户区 `BitBlt(... SRCCOPY)`（`:2177`）属实 | **结构性放大器成立，采信** |
| 五文档已更新 | 五份逐一 grep `INVESTIGATE-142`：logs 2 / CHANGELOG 1 / handoffs 1 / progress 1 / todo 1，**全部命中，无虚报** | 通过 |
| `src/**` 零写入 | 如实报告「无法以 `git diff src/` 为空自证」（coder-2 并行在途），改以「唯一可写文件=draft」口径 —— 符合 `[WORKER-GIT-AUTOCRLF-ENV-DIFF-001]` 与协作纪律，未擅自清理他人改动 | 通过 |

**🔴 主控顺带执行 P1 探针（零改码，可立刻做）+ 抢存易失证据**：

- 按 `[EVIDENCE-LOG-VOLATILE-001]`，发现 `target/release/debug.log`（23,970,807 B，mtime 09-07 01:36）
  即 Gavin 端测 BUILD-135 当次产物，**下一次运行即被冲掉**，已第一时间抢存为
  `collab/evidence/debug-gavin-endtest-BUILD135-20260907-0136.log`。
- P1 判读结果：日志时间跨度 `2026-09-06T17:01:27Z ~ 17:36:43Z`（= 本地 09-07 01:01~01:36）。
  `ASR-038-C` **0 命中**、`StreamingEditing` **0 命中**、`created EDIT control` **0 命中**
  ⇒ 🔴 **该 session 全程未进入过编辑态，这份日志无法回答闪烁问题**。
  `ignoring late StreamingText after stop` 1 命中，属非编辑态下的正常停止后丢包，非本案证据。
- **P1 结论：现有日志不可判，需一次「显式使用编辑态 + 开 debug 日志」的新采集**。
- 附：主控一度怀疑 coder-1 的 P1 探针引了不存在的日志串，复核后**是主控查错了**——
  `created EDIT control` 确实存在于 `:738` `log::info!`，探针规格无误。

**🔴 主控发现的一个交付载体缺陷（非 coder-1 责任）**：
`collab/drafts/` 被 `.gitignore:72` 整目录屏蔽 ⇒ 本次调查文档**不会入库，换机/清理即永久丢失**。
OVERLAY-141 的规格当初正是因此被转入 `docs/`。本次同样处理：
已复制为 `docs/OVERLAY-142-EDITING-FLICKER.md`（已验证不被忽略，可入库）。
**后续凡有长期价值的 draft，一律须转入 `docs/`**。

**根因状态**：未定，需实测。头号候选 = EDIT 子控件重绘 + DWM 对 SLWA 分层窗重合成（候选 4），
次选 = 父窗全窗 BitBlt 结构性放大器（候选 3）。FLICKER-130 **定性为多源叠加，非判错**。

### 🔴 主控发现：BUILD-135 与 BUILD-145 之间存在一次未记录的构建（2026-09-07）

**发现经过**：验收 tester-1 的 BUILD-145 方案评估时核对产物链，发现对不上。

| 口径 | 主程序 exe | sha 前缀 | 大小 |
| --- | --- | --- | --- |
| CHANGELOG 最后一条出包记录（`TEST-EXEC+BUILD-135`） | 09-06 17:55 | `ac70a990` | 12,263,424 B |
| **实际 `Publish/feiyin-ime.exe`** | **09-07 00:59** | **`7f1869ad`** | **12,267,520 B** |

⇒ **BUILD-135 之后又出过一个包（+4,096 B），五文档零记录、无 BUILD 任务号。**

**成因**：09-07 01:05 提交的 `9c91ecf`（L0 保真放宽 / DEC-061 提示词）由上一个 session 主控
在三 Worker 额度耗尽时自行构建自测，**出包动作没走 BUILD 任务流程、没进 CHANGELOG**。
Gavin 报「端测四项全过」验的就是这个包。

**为什么必须当场处理（不是记一笔就算）**：
tester-1 原计划拿 BUILD-135 的 `ac70a990` 做判别探针的反向对照。
但 BUILD-135 与本次之间隔了**两代**改动（提示词 + OVERLAY-141），
sha 不同只能证明「有变化」，**证明不了变化来自 OVERLAY-141 —— 判别力是空的**。
（同类教训：`[ABLATION-MODEL-TOO-LIGHT-001]` 判别力不足即等于没验。）

**主控已做的处置**：

1. 在 tester-1 Step 4 覆盖 `Publish/` **之前**抢存紧邻上一代产物为
   `collab/evidence/binaries/feiyin-ime-PRE141-20260907-0059.exe`（目录被 `.gitignore` 屏蔽，只本地留存）。
2. tmux 通知 tester-1 改绑基线：七项核验第 2 项（sha 异于）与第 5 项（判别探针反向对照）
   一律改用 `7f1869ad`，大小对照基准改用 12,267,520 B。
3. 要求 tester-1 在 result.md 如实记录该未记录构建的存在（非其责任，属流程缺口）。

**流程缺口定性**：主控自行构建时绕过了 BUILD 任务流程 ⇒ 产物链在文档里断了一节。
与 `[DOC-STATE-DRIFT-001]` 同源（规则存在但不在完成判据里）。
**主控侧新判据**：凡产生 `Publish/` 产物的构建，无论由谁执行（含主控自建），
**必须补一条 CHANGELOG 出包记录**，至少含：时间戳 / sha / 大小 / 含哪些改动 / 由谁构建。


## 2026-09-07 — tester-1 — BUILD-145 ✅ 阶段五出包：首个含 OVERLAY-141 圆角灰边根治的包（待主控验收/Gavin 端测）

**基线**：HEAD `bd5a160`，主控明确下达「现在可以出包」。工作区开工前确认 clean（仅 logs/20260907.md 在途）。

**Step 2 跳过（主控核实的合法跳过）**：ui/ 最后改动 `f85c550`(09-06 18:47)，`target/release/feiyin-ime-ui.exe` 构建于 09-07 01:15（在其之后），已含全部 ui/ 改动（UITEST-137/139/138）；两处 UI 副本 sha 一致 `05cef408…`。沿用 BUILD-129/135 先例。

**反向对照基线改绑（主控补充指令）**：因存在一次未记录的构建（09-07 00:59，sha `7f1869ad…`，12,267,520 B，主控已抢存 `collab/evidence/binaries/feiyin-ime-PRE141-20260907-0059.exe`），七项核验第 2/5 项与大小基准一律改绑该紧邻上一代包，**不用 BUILD-135 的 `ac70a990…`**（隔两代、判别力空）。

**Step 1 清进程** → **Step 3 主程序**（2m25s，feiyin-ime 111 warnings 与基线持平 / crash-reporter 5）→ **Step 4 同步 Publish/** + toml 三副本。

**七项核验全 PASS**：
① 时间戳：主程序 `target/release` 与 `Publish/` 均 11:50 本次构建；UI 01:15 沿用（ui/ 零改动未重建，非漏建）；crash-reporter 11:49 同批重建
② sha 两副本三对相等：主 `59d64c4d…`（异于 PRE141 `7f1869ad…` ✓）/ UI `05cef408…`（未重建）/ crash `8f27237e…`
③ toml 三副本：scene `0a3a0b9a…` / itn `311cbb96…` 各三处一致
④ ProductVersion **0.9.0.0**（版本号未动）
⑤ 🔴 **判别探针三证（如实说明，未编造二进制命中）**：OVERLAY-141 是纯几何/数学改动，`git show 134fdab` 确认**零新增字符串字面量、零新增 Windows API import**，二进制字符串探针不可构造。改用三证：**源码 grep**（HEAD `src/main.rs` `OVERLAY_FRAME_RADIUS_LG`×12 / `fn apply_alpha_fixup`×3 / `fn overlay_frame_radius`×1 命中）+ **构建日志时间戳**（11:49-11:50 当前构建）+ **sha 差异**（`59d64c4d`≠`7f1869ad`，PE 头外字节级对比确认 10.9MB 代码字节真实不同，非仅元数据）。大小 +512 B（12,267,520→12,268,032）与 OVERLAY-141 源码增量（+143/-44）一致
⑥ 大小同量级：12,268,032 B（基准 PRE141 12,267,520 B，+512 B）
⑦ 冒烟启动无 panic：PID 18772 Responding=True，debug.log 无 `panicked` 行（ASR-DROP WARN 洪流为环境无 ASR 消费所致，非本批问题），已清理进程
⑧ 运行时数据零覆盖：config.toml sha 构建前后不变（target/release `c3676644…` / Publish `3186ec8c…`），version_check.json 由冒烟启动自写（非 Step4 覆盖）

**红线**：未 commit / v0.9.0 未动 / 未用 cargo tauri build / 未 cargo clean / 零凭证。

**产物**：`Publish/feiyin-ime.exe` 59d64c4d… / `feiyin-ime-ui.exe` 05cef408… / `crash-reporter.exe` 8f27237e…，Gavin 端测清单 7 项见 result.md。

### BUILD-145 主控验收：✅ 通过（2026-09-07 12:05）

**独立核验**：产物 11:50（现时 12:05，新鲜）｜ `feiyin-ime.exe` 两副本 sha 均 `59d64c4d` 一致
｜ 异于反向对照 PRE141 `7f1869ad` ✓ ｜ 12,268,032 B（较 PRE141 **+512 B**，OVERLAY-141 增量，合理）
｜ `crash-reporter` 24,887,808 B 与基线同 ｜ HEAD `bd5a160` 且工作区仅五文档改动
｜ 五文档 grep `BUILD-145` 命中 4 份，`progress.md` 填 **N/A** 并引用规则 7 ——
主控核对 `collab/progress.md:14` 确有「测试用例同步、构建和出包任务不记录到 progress」，**N/A 用得对**
（对照 2026-08-17 tester-1 连续两单在此行勾错 ✅ 的教训，本次三态判断正确）。

**判别探针**：OVERLAY-141 零新增字符串/import，二进制探针不可构造，tester-1 **如实声明未编造**，
改走「源码 grep + 时间戳链 + sha 差异」三证。主控认可 —— 判别力来自
「HEAD 含 `134fdab` + 工作区 clean + 产物新鲜 + sha 异于紧邻上一代」这条闭合链。

**两处文档订正（不影响验收，已要求 tester-1 补记）**：

| # | 实况 | 性质 |
| --- | --- | --- |
| ① | 报告称「UI exe 01:15 沿用」，但 `Publish/feiyin-ime-ui.exe` 实际 mtime = **11:50**（cp 未带 `-p`）。内容无恙：Publish 与 `target/release` 两副本 sha 均 `05cef408` **逐位相同** | **报告口径问题，非产物问题**。BUILD-118 起惯例是「沿用 = `cp -p` 保时间戳」，将来拿「沿用 01:15」对一个 11:50 的文件会误判为漏建/串包。后续 cp 须带 `-p` |
| ② | 那次未记录构建**连 UI 也重建了**：CHANGELOG 记 BUILD-135 UI = 10,053,632 B / `280b0650`；现 `target/release` UI = 10,053,120 B / `05cef408` @01:15（**-512 B**） | 未记录构建的证据比先前更足，主控将一并补记 CHANGELOG |

**阶段编排**：BUILD 先于阶段四完成，依据 worker-guide 十三节提速三条第 1 条
（代码验收完立刻开构建，回归并行，挂了再废包）。理由：本批修的是圆角抗锯齿视觉，
`cargo test`/Vitest/Browser Mode **一条都覆盖不到**，唯一判据是 Gavin 目视，exe 是关键路径；
且阶段三只改测试区，release 不编译 `#[cfg(test)]`，**本产物不会因阶段三失效，无需重出**。


## 2026-09-07 — coder-1 — TEST-SYNC-143 ✅ 阶段三：OVERLAY-141 护栏换血（G6）+ 半径单一来源新护栏（G8/G9）

- **交付**：src/main.rs 测试区 +146/-15（四 hunk 全在 overlay_121_guard_tests 及模块头注释，生产区 0 字节）。G6 换血：旧「三分支完整」命题退役 → `g6_fixup_sdf_alpha_invariants` 四条（SDF 覆盖率 needle / `if cov <= 0.0` 块四通道全零逐通道断言 / 反预乘 `if a > 0` / 🔴反向断言 contains 级钉死 `a == 0 && (r|g|b)` 禁复活=圆角灰边事故机器判据）。G8 新增：映射赋值恰 1（needle 绑完整赋值形态防误计）+ 调用行实参绑 frame_radius。G9 新增：三类外框绘制调用（draw_overlay_chrome 5 / chrome 4 / 折行 chrome_with 4）实参必须引用 OVERLAY_FRAME_RADIUS_，rustfmt 折行逐调用收集实参，总数钉 13；豁免=fn chrome 内部透传形参 radius；🔴 不扫全库 CORNER_RADIUS 防假红。新增 helper block_contains_raw（contains 级反向匹配）；模块名沿用。
- **验证**：cargo fmt 0（幂等）/ check --all-targets 0 error / warnings 111/102 基线逐位持平；沙箱预演（独立 rustc harness，未触仓库 cargo test）对 fmt 后真实文件三条全 PASS；消融 10 变异全验红 + 豁免假红核对（result.md 附录 A）——消融在临时副本执行（变异不落仓库，git diff src/ 始终空，规避还原误伤教训）；🔴 G6④ 已实测「塞回旧提亮规则 ⇒ 红」。
- **macOS**：模块 cfg(all(test, windows)) 整体不编译，不适用。
- **红线**：未 commit / v0.9.0 未动 / 未出包 / 零凭证 / 临时目录 C:\msys64\tmp\opencode\ts143 已删净。


## 2026-09-07 — coder-2 — OVERLAY-147-DIAG ✅ BUILD-145 端测两问诊断（🔴 只查不修，src 零改动，待主控验收）

- **Part A**：①纯数学探针（独立 rustc 单文件，交付前已删）实证 **SDF 正确**——与 64×64 超采样精确面积覆盖率全幅比对，240×36 r16 平均偏差 0.0002/最差 0.041，r10 平均 0.0001/最差 0.032，四角 cov 剖面干净单调坡 ⇒ 「掩码算错」候选整片砍掉。②**H1a 如实推翻**（主控欢迎推翻条款）：fill/stroke 圆心错位 (0.5,0.5) 属实，但描边带深度偏差实测 [-0.008,+1.207]（r16）vs [-0.012,+1.207]（r10）——**r 无关**、描边从不探出轮廓（角部外沿仍深 0.21px），量级 ~0.2px 解释不了 r16/r10 零反例分界；主控「stroke radius 改 r-0.5 同心化」的修法方向本身正确，列为顺手项 F2。③**H2（持续重绘）被静态证据推翻**：RecordingStreamingIdle = r16+静态（`:1678-1686` 仅 needs_repaint 重绘，音频更新不置脏）+ 端测报坏；待 Gavin 一句话确认看的是占位文本变体（与 Recording 同尺寸易混看）。④混淆变量收敛：窗口高度混淆死（STATUS_OVERLAY_SIZE==RECORDING_OVERLAY_SIZE==240×36 `:1086/:1088`）、轮廓区内容两组同构（mic/波形/文本/按钮全内缩、光晕已裁圆角 `:3656-3668`）、fixup 新旧在 cov=1 内部区逐位等价（r16 8356px 同 / 284px 变）⇒ **剩余候选={半径值经未测运行时路径, 底色}，机制未定，需 E1/E2 半径翻转 + E3 角区像素 dump（同时验证「BindDC alpha 未存活」这一从未直接测过的立论前提）**。
- **Part B**：①**EDIT 本体不可能存活到录音帧**（Show 臂 `:1366` 无条件销毁，全退出路径表证死）⇒ 截图光标不是活的 EDIT。②**主假设=线程级 caret 对象泄漏**：全库零 DestroyCaret/HideCaret（grep 实证）、destroy_edit_control `:814-848` 无 caret/focus 处置、带焦 DestroyWindow 不触发 WM_KILLFOCUS、caret 归线程队列且直打屏幕不走 ULW 合成 ⇒ 与症状精确吻合；判别探针=退出编辑后 `GetGUIThreadInfo().hCaret`。③**新钉死可达缺陷**：HotkeyEvent::Stop `:5598-5614` 缺 OVERLAY_EDITING 守卫——编辑中按热键停止→直接 Show(FallingToProcessing)（销毁 EDIT）但 editing 标志不清→Done/Cancelled 全被 `:5718-5725` 压制→**Processing 浮层永久卡屏**。④Hide 双重压制卡窗路径（`:1610-1616`+`:5718-5725`）静态存在但当前可达触发器疑似为零（编辑态 ESC 到不了父窗、取消钮已移除），守卫语义矛盾建议随③统一收口。⑤**FLICKER-130 耦合证伪**：被丢弃的 Show(StreamingEditing) 本来就是销毁 EDIT 的肇事路径（`:5679-5681` 注释自述），清理从不依赖它；与 INVESTIGATE-142 候选 3/4 不冲突，候选 6 建议顺带测 hCaret。
- **产出**：`docs/OVERLAY-147-DIAG.md`（直接写 docs/，非 drafts/）；修法建议 F1-F5 全部标注不实施；证据类型逐条标注（数学/静态/需运行时）。
- **红线自证**：`git diff --numstat -- src/` 空 + `git status --short -- src/` 空；探针文件已删；未 commit/未动版本/未出包/未跑 cargo clean；零 git 破坏性命令；零凭证；无残留临时文件。

## 2026-09-07 — coder-1 — OVERLAY-149 ✅ 两个实缺陷修复（F1 caret 泄漏 / F2 热键停止臂守卫）+ 圆角判别探针（E3/E4）

- **F1（待端测判定）**：destroy_edit_control 在 DestroyWindow 前新增焦点移出（GetFocus==edit ⇒ SetFocus 父窗）+ HideCaret + DestroyCaret —— 修 Win32「带焦销毁收 WM_DESTROY 而非 WM_KILLFOCUS ⇒ 线程级 caret 对象跨态存活，系统 caret 屏幕级绘制不走 ULW 合成 ⇒ 录音帧可见文字光标」（DIAG §B1/B2）。附带 GetGUIThreadInfo 运行时证据（OVERLAY-149-PROBE，destroy 后 hwndCaret=0⇒清理生效）。🔴 不预先宣布已修，端测复现「录音→编辑→退出→再录音」判定。
- **F2**：HotkeyEvent::Stop 臂加 OVERLAY_EDITING.store(false)（无条件，非编辑态逐位不变，单写者无竞态）——修复「编辑态按停止热键 → Done/Cancelled 被 :5720 压制 → Processing 浮层永久卡屏」。方案协商：否决改发 RestoreAndHide 备选（Case-B：worker 已 finalize 时会多余销毁 EDIT 藏窗；Case-A 下无条件清 flag 即可让 Cancelled 自然收口）。完整事件序列（Step1-4 + Case-A/B/C 边界）写 result.md。
- **E3**：WM_PAINT ULW 分支 apply_alpha_fixup 前/后各采样四角 8×8+左缘整列 BGRA hex（OVERLAY-141 立论前提「BindDC 后 alpha 未存活」首次直接测量）；节流=状态切换后前 3 帧（static 双计数器），仅 RecordingStreamingIdle/Info 对照态。
- **E4（口径替代已报备）**：GetDpiForWindow 需 Cargo.toml 加 Win32_UI_HiDpi feature（红线只许改 src/main.rs）→ 改用 GetDeviceCaps(LOGPIXELSX/SY) 对同一 hdc（DIB 源 DC）取 DPI + d2d::with_d2d 内 RT GetDpi 进程级 once 日志。
- **验证**：fmt 0（幂等）/check --all-targets 0 error/warnings 111/102 基线持平；cargo test（本单允许）1107P/0F + 51P/0F 零预期红（G1-G9 全绿）。
- **明确不做**（主控裁定未顺手）：stroke 同心化/Hide 双压制/编辑态 ESC/E1·E2/ui/**。
- **红线**：只改 src/main.rs（+230/-14，七 hunk）；探针四处删除清单列 result.md §六；未 commit/版本未动/未出包/零凭证/无临时文件；MACOS-HANDOFF 已同步。

## 2026-09-07 — tester-1 — BUILD-151 ✅ 阶段五出包：首个含 OVERLAY-149 caret 修复 + 热键停止卡屏修复 + 圆角判别探针 E3/E4 的包（待主控验收/Gavin 端测）

**基线**：开工 HEAD = `115eb5b`（与任务书一致），工作区仅 `docs/MACOS-HANDOFF.md`（coder-2 的 macOS 复核同步）。构建期间主控补提交 `69aded0`（该文档），`git diff 115eb5b 69aded0` 实证仅 MACOS-HANDOFF.md +6 行、零代码差异 ⇒ **报告基线记 69aded0，二进制零影响无需重来**。

**Step 2 跳过（判据升级）**：改用主控指定的 git-log 法 —— `git log -1 --format='%h %ad' -- ui/ src-tauri/` ⇒ `f85c550 @09-06 18:47` **早于** UI exe 构建 01:15 ⇒ 已含全部 ui/ 改动。不依赖选对 diff 区间起点，直接证命题本身。补充 `git diff bd5a160..HEAD -- ui/ src-tauri/` 为空。

**Step 1 清进程** → **Step 3 主程序**（1m58s，feiyin-ime 111 warnings 与基线持平）→ **Step 4 同步 Publish/**（🔴 **cp -p**，BUILD-145 订正项，UI mtime 保留 01:15）+ toml 三副本。

**七项核验全 PASS**：
① 时间戳：主程序 13:07:47、crash 13:06:48 本次构建，`Publish/` 经 cp -p 与 target 逐位同 mtime
② sha 两副本三对相等：主 `b20fbe14…`（异于 PRE149 `59d64c4d…` ✓）/ UI `05cef408…`（未重建）/ crash `4f78331c…`
③ toml 三副本 scene `0a3a0b9a…` / itn `311cbb96…` 一致
④ ProductVersion 0.9.0.0（版本号未动）
⑤ 🔴 **硬判别探针正反双向 PASS**：OVERLAY-149 新增 log 字面量进 `.rdata` —— 新包 `OVERLAY-149-PROBE E3`×2 / `E4`×2 / `F1`×1 / `pre-fixup`×1 / `post-fixup`×1 / `OVERLAY-149-PROBE`×5 全命中；PRE149 基线（`59d64c4d…`）**6 串全 0 命中**。判别力成立（本轮不再走三证法）
⑥ 大小 12,277,248 B（PRE149 12,268,032 +9,216 B，OVERLAY-149 +244/-14 含探针代码，合理）
⑦ 冒烟 PID 27704 Responding=True 无 panic 已清理
⑧ 运行时数据零覆盖：config.toml sha 前后不变（target `7b19ace7…` / Publish `3186ec8c…`）

**红线**：未 commit / v0.9.0 未动 / 未用 cargo tauri build / 未 cargo clean / 零凭证。

**产物**：`Publish/feiyin-ime.exe` b20fbe14… / `feiyin-ime-ui.exe` 05cef408…（沿用）/ `crash-reporter.exe` 4f78331c…。Gavin 端测清单 5 项（caret 是否消失 / 编辑态停止热键收口 / 圆角三态预期无变化 / 🔴 保留 debug.log 给 E3/E4 / 编辑态闪烁仍在）见 result.md。


## 2026-09-07 — coder-2 — TEST-SYNC-150 ✅ 阶段三：OVERLAY-149 双修复护栏 G10/G11（生产区零字节，待主控验收）

- **G10（caret 清理顺序护栏，:12106 起）**：断言 `destroy_edit_control` 块内 `let _ = SetFocus(` / `let _ = HideCaret(` / `let _ = DestroyCaret(` 三行行号全部早于 `let _ = DestroyWindow(`——带焦销毁走 WM_DESTROY 非 WM_KILLFOCUS，销毁后清 caret 是空操作，顺序错=修复失效。锚点自证：`fn destroy_edit_control(` prod 区 raw 计数=1 唯一；needle 全取代码行首形态，注释行 `//` 开头天然不命中 starts_with；块定界起点 `bounds.0==anchor` 断言防锚点漂移；OVERLAY-149-PROBE 探针块删除不影响（相对顺序不变）。
- **G11（Stop 臂清标志护栏，:12148 起）**：断言 `HotkeyEvent::Stop => {` 臂块内含 `OVERLAY_EDITING.store(false`。结构锚不绑文案（主控裁定采纳）：raw `HotkeyEvent::Stop => {` 计数=1（macOS 臂行首带 `platform::` 前缀结构性不可命中）⇒ find_line 唯一锚定 Windows 臂；sanity 断言用结构特征 `if is_recording.load(`（macOS 臂无此结构），若将来出现第二个 raw 同形臂且序在前，sanity 红=误锚显式暴露不假绿。
- **沙箱预演（独立 rust 程序复刻全部 helper 语义，验后已删）**：baseline G10/G11 双 PASS + 锚点计数核验。消融四条全验红：**A1 删 `let _ = DestroyCaret();` 代码行、OVERLAY-149 注释块保留 → G10 红**（主控追加要求的「注释喂绿」验证：注释在但代码行消失时护栏必红，证明没有被注释喂绿）；A2 三步整块挪到 DestroyWindow 之后 → G10 顺序红（SetFocus=869 > DestroyWindow=860，最重要一条）；A3 删 Stop 臂 `OVERLAY_EDITING.store(false` 行 → G11 红；A4 注入裸形态重复 Stop 臂于 Windows 臂之前 → G11 sanity 红（误锚暴露路径实测）。验后还原，`git diff --numstat src/main.rs` = **+82/-0 单 hunk @ mod overlay_121_guard_tests（:12102，首个 `#[cfg(test)]`=:8293 之后）**。
- **macOS 结论（主控 cfg 归属扫描定稿，写入 G11 注释与 result.md）**：OVERLAY_EDITING 唯一 `store(true)` 落在 `cfg(windows)` 块内 ⇒ macOS 上恒 false ⇒ Done/Cancelled 压制臂不可能生效 ⇒ **F2 缺陷 macOS 不可达，macOS Stop 臂无需同款守卫**（跨平台强制结论，非「后续再看」）。
- **明确不做（任务书 §三）**：不给 E3/E4 探针写护栏（临时 instrumentation）、不加「探针必须被删除」护栏（现在就会红）、不碰 G1-G9。
- **红线自证**：fmt 0 / check --all-targets 0 error / warnings **111/102 基线逐位持平**；只改测试区；未跑 cargo test/build；未 commit/未动版本/未出包；零 git 破坏性命令；零凭证；沙箱目录 `C:\msys64\tmp\opencode\sync150\`（rehearsal.rs/exe + a1-a4.rs）已整目录删除。

## 2026-09-07 — tester-1 — TEST-EXEC-152 ✅ 阶段四：OVERLAY-149 + TEST-SYNC-150 全量回归 + G10/G11 消融真跑（生产零改动，待主控验收）

**基线**：HEAD `7ddf943` clean。

**Step 1a 三层回归逐位对账**：root **1109P/0F/9I**（= 预期 1109P/0F，coder-1 实测 1107 + G10/G11 两条逐位命中）+ hotkey bin 51P/0F + src-tauri **76P/0F**。G10/G11 单跑各 1P 确认在列。

**Step 1b Skip（git-log 法）**：`git log -1 --format='%h %ad' -- ui/` ⇒ `f85c550 @09-06 18:47`、`-- src-tauri/` ⇒ `9c9ff73 @08-30`，均早于上次跑测 ⇒ ui/ 零改动。Browser Mode 不跑（无 diff）。

**Step 2 消融 3/3 RED（真跑，非沙箱复刻）**：
- **A1** 删 `let _ = DestroyCaret();` 行留注释 → G10 红「块内未找到代码行首形态 let _ = DestroyCaret(」—— 注释不喂绿，护栏注释免疫设计实证成立
- **A2** caret 三步整块挪到 DestroyWindow 后 → G10 红「SetFocus L869 / HideCaret L875 / DestroyCaret L876 必须全部早于 DestroyWindow（L862）」—— 顺序守卫精确命中（最重要一条）
- **A3** 删 Stop 臂 `OVERLAY_EDITING.store(false)` → G11 红「HotkeyEvent::Stop 臂必须清 OVERLAY_EDITING」
- 逐条 Edit 还原（禁 git 破坏性命令）

**还原自证**：`git diff src/main.rs` = **0 行**；复跑全量 **1109P/0F/9I** + 51P/0F 0 红；无残留进程；`Publish/config.toml` sha `3186ec8c` 未变；v0.9.0 未动。

**红线**：零生产改动（最终 diff 空）/ 未 commit / 未出包 / 未 cargo clean / 零凭证。


## 2026-09-07 — coder-2 — OVERLAY-153 ✅ 圆角二次修法：覆盖率当乘数不当替代品（待主控验收，Gavin 在等，免阶段三/四直出包）

- **推论验证（验收 1）：成立**。代码级论证：现行 fixup `*p.add(3) = premul(255, k)` 无条件覆写 alpha，内部区（cov=1）即强制 255·op——与像素原 alpha 无关；OVERLAY-141 相对 121 在内部区是退步（121 保等比、141 强制不透明）属实。伪影具体身份：D2D `DrawRoundedRectangle` 1px 描边在**直线段**与像素栅格对齐无 AA，在**圆角弧段**AA 带铺 2-3px 且全部落在 cov=1 内部区 ⇒ 被 141 规则压成实心灰团 ⇒ 「圆角**包边**灰线粗乱」的角区集中伪影。
- **§三「两种世界都安全」复核（验收 4）：成立，附一个 nuance 如实报告**。alpha 存活世界：a>0 走等比缩放，描边 AA 保住=修好；未存活世界：a==0 走提亮分支，与 141 逐位相同=零风险。唯一能构造的第三情况：**alpha 存活世界的轮廓 1px 坡 cov²**（D2D 自身 AA 覆盖 ≈ cov，再乘 cov → 平方，边缘略陡/略透）——量级远小于内部 2-3px 实心灰带的收益，不构成停手理由；若 E3 数据日后证明 alpha 存活且轮廓坡肉眼可见变差，可在下一批把 a>0 分支的乘子改为 `max(cov, a/255·op)` 类形式精化。
- **修法落地（仅 apply_alpha_fixup，+27/-19 中生产区 1 hunk）**：三分支 = cov≤0 四通道归零（不变）/ a>0 四通道等比 ×(cov·op)（**保留原 alpha**，反预乘整步删除，恢复 121 预乘不变式）/ a==0 GDI 提亮（rgb×k，a=255·k，未存活世界与 141 逐位相同）。
- **G6 处理（流程变更授权项）**：四条断言 needle 改后**全部仍绿**（`if a > 0` 分支保留、旧提亮形态不出现）——零 needle 改动，仅更新 ③ 的语义注释与断言消息（「反预乘分支」→「等比缩放分支，防强制不透明复活」，:11946/:11988 两 hunk，纯测试区文字）。
- **验证**：fmt 0 / check --all-targets 0 error / warnings **111/102 基线逐位持平** / cargo test **三跑稳定 1109P/0F**（首跑 config::tests 批量红为瞬态——与 fixup 无关、未复现、二/三跑全绿；当时无 feiyin 进程持锁，疑 tester-1 并行动作，如实记录）。OVERLAY-121 护栏模块 10P/0F（G1-G11 全绿）。
- **红线**：只动 apply_alpha_fixup + G6 注释（流程变更授权）；**未顺手改 stroke r-0.5（F2 仍被否决）**；E3/E4 探针照留未删；未 commit/v0.9.0 未动/未出包；零 git 破坏性命令；零凭证。
- **三方差异对照表**（验收 5，详见 result.md）：121（a>0 等比×op、无 cov、无反预乘）→ 141（a>0 反预乘后强制 a=255·cov·op）→ 153（a>0 等比×cov·op 保原 alpha；a==0 与 141 同）。

## 2026-09-07 — tester-1 — BUILD-154 ✅ 快速出包：OVERLAY-153 圆角二次修法（精简流程，Gavin 在等，待主控验收/端测）

**基线**：HEAD `14bce8f` clean。**精简流程**（Gavin 指示，免阶段四/消融）。

**Step 2 Skip（git-log 法）**：`git log -1 -- ui/ src-tauri/` ⇒ `f85c550 @09-06 18:47` 早于 UI exe mtime 01:15 ⇒ UI 已含全部改动。

**Step 1 清进程** → **Step 3 主程序**（2m09s，feiyin-ime 111 warnings 与基线持平）→ **Step 4 cp -p 同步 Publish/**（UI mtime 保留 01:15）。

**四项核验全 PASS**：① 主程序 13:55:38 本次构建；② sha 两副本一致 `fb9fcf50…` **异于** PRE153 `b20fbe14…`（`collab/evidence/binaries/feiyin-ime-PRE153-20260907-1307.exe`）；③ ProductVersion 0.9.0.0 未动；④ 冒烟 PID 27380 Responding=True 无 panic 已清理。

**跳过项**（按任务书）：toml 三副本 / 判别探针 / 大小对照 / cargo test / vitest / E2E / 消融 —— 本批只改 `apply_alpha_fixup` 单函数。

**补充**：`Publish/config.toml` sha `3186ec8c` 未变（运行时数据零覆盖）；变更范围确认 `git show 14bce8f --stat` 仅 src/main.rs。

**红线**：未 commit / v0.9.0 未动 / 未用 cargo tauri build / 未 cargo clean / 零凭证。

**产物**：`Publish/feiyin-ime.exe` fb9fcf50… 12,277,248 B @13:55:38 / `feiyin-ime-ui.exe` 05cef408… 沿用 / `crash-reporter.exe` 同批重建。Gavin 端测清单 5 项（🔴 圆角灰边是否恢复 1px / 信息·错误窗不回退 / 录音窗光标复验 / 编辑态停止热键复验 / 🔴 保留 debug.log 给 E3）见 result.md。

## 2026-09-07 — coder-1 — OVERLAY-155 ✅ 圆角三次修法：GDI chrome 收进 D2D 失败分支 + LG 半径 16→10

- **差异 A 结论（成立+收窄）**：全库逐位排查「GDI chrome 先于 D2D 无条件执行」——唯一肇事点 = `draw_recording_overlay` 波形分支旧行 :3010（波形变体每帧重绘，每帧先落无 AA 的 GDI RoundRect，D2D 再盖上：1px 灰描边 + D2D AA 描边错位 ~0.5px 叠画 → 角区粗乱；这些像素在 SDF cov>0 区，fixup 清不掉 ⇒ 前三轮改 fixup 改错了地方）。placeholder/with_text/FallingToProcessing/Processing/Editing/FocusLost/Error/Info 七处本来就是 Info 同构（D2D 优先失败才 GDI），未动。
- **改动 1**：:3010 无条件 chrome 移进 `show_placeholder` 分支（D2D idle 失败兜底）；波形分支 chrome 只在 `d2d::draw_recording_waveform_overlay` 返回 false 后画。D2D 失败兜底契约逐位保留（改前后失败路径相同；`if !d2d::draw_` 形态护栏绿）。
- **如实声明**：Idle（D2D idle 成功即 return，无叠画）与 Processing（:2603 D2D 优先，无叠画）端测也报坏——差异 A 解释不了这两态（DIAG §A2 的「Idle 是否单独看过」确认问题未答）；改动 2（半径 16→10）是覆盖全部 r=16 组的共同修复，两改动互补，端测判分。
- **改动 2**：OVERLAY_FRAME_RADIUS_LG 16.0→10.0（对齐 Info 几何，Gavin 指向 r=10 组；LG/SM 常量保留，改回一个字面量即可回 16，可逆性）。
- **验证**：fmt 0（幂等）/check --all-targets 0 error/warnings 111/102 基线持平/cargo test 1109P/0F（=基线）护栏零变红未改 needle。
- **红线**：apply_alpha_fixup 零字节未动；E3/E4 探针照留；只改 src/main.rs（+16/-5）；未 commit/版本未动/未出包/零凭证/无临时文件；MACOS-HANDOFF 已同步。

## 2026-09-07 — tester-1 — BUILD-156 ✅ 快速出包：OVERLAY-155 圆角三次修法（精简流程，Gavin 在等，待主控验收/端测）

**基线**：HEAD `aeaebe1` clean。**精简流程**（免阶段四/消融）。

**Step 2 Skip（git-log 法）**：`git log -1 -- ui/ src-tauri/` ⇒ `f85c550 @09-06 18:47` 早于 UI exe mtime 01:15 ⇒ UI 已含全部改动。

**Step 1 清进程** → **Step 3 主程序**（1m45s，feiyin-ime 111 warnings 与基线持平）→ **Step 4 cp -p 同步 Publish/**（UI mtime 保留 01:15）。

**四项核验全 PASS**：① 主程序 14:55:13 本次构建；② sha 两副本一致 `a0521728…` **异于** PRE155 `fb9fcf50…`（`collab/evidence/binaries/feiyin-ime-PRE155-1355.exe`）；③ ProductVersion 0.9.0.0 未动；④ 冒烟 PID 8956 Responding=True 无 panic 已清理。

**跳过项**（按任务书）：toml 三副本 / 判别探针 / 大小对照 / cargo test / vitest / E2E / 消融。

**补充**：`Publish/config.toml` sha `3186ec8c` 未变；UI sha 两副本 `05cef408…` 一致（沿用）。

**红线**：未 commit / v0.9.0 未动 / 未用 cargo tauri build / 未 cargo clean / 零凭证。

**产物**：`Publish/feiyin-ime.exe` a0521728… 12,277,248 B @14:55:13 / `feiyin-ime-ui.exe` 05cef408… 沿用 / `crash-reporter.exe` 同批重建。Gavin 端测清单 5 项（🔴 录音+处理中窗圆角灰边恢复 1px / 信息·错误窗不回退 / 录音窗光标复验 / 编辑态停止热键复验 / 🔴 保留 debug.log 给 E3）见 result.md。


## 2026-09-07 — coder-2 — EDIT-FLICKER-157 ✅ 编辑态右侧文字闪烁：EDIT 开双缓冲（待主控验收 → 直出包）

- **机制复核：成立**（主控定位三条逐条核对：EDIT 样式 `WS_EX_NOACTIVATE`+`WS_CHILD|WS_VISIBLE|ES_AUTOHSCROLL` 无 COMPOSITED；子类只拦 WM_NCPAINT/WM_KEYDOWN 未拦 WM_ERASEBKGND；EDIT 右界/分隔线/提交键互不重叠）。光标右移 → ES_AUTOHSCROLL 横滚 → 右侧待重绘条带 → 擦背景+画字两步直接上屏 → 右侧闪烁；左侧不滚不闪。
- **修法（一行 + import）**：`CreateWindowExW` 扩展样式 `WS_EX_NOACTIVATE` → `WS_EX_NOACTIVATE | WS_EX_COMPOSITED`（离屏合成，擦-画不上屏）；import 表补 `WS_EX_COMPOSITED`。共 +8/-3 两个 hunk（:102 import / :678 创建处）。
- **风险留痕（任务书要求实测项）**：`WS_EX_COMPOSITED` 用在 SLWA 分层窗子控件上**未运行时实测**——若 Gavin 端测见 EDIT 不显示/显示异常，回退方案 = 子类拦 `WM_ERASEBKGND` 自绘背景（父窗同色刷+内存 DC BitBlt）或创建后 `SetWindowLongPtrW` 设置；已在代码注释标注回退路径。
- **验证**：fmt 0 / check --all-targets 0 error / warnings **111/102 持平** / cargo test **1109P/0F** 全绿。
- **红线**：只动 create_edit_control + import；未碰 apply_alpha_fixup/圆角/ui；未 commit/未动版本/未出包；零凭证。
