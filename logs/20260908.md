# 修改历史 · voice-ime · 20260908

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---


## 2026-09-08 — 主控 session 启动：handoffs.md 归档（防膨胀规则）

- **动作**：`collab/handoffs.md` 启动时 354 行（>200 阈值），按 worker-guide 第九节「只保留当天条目」，
  将 39 条 2026-09-07 条目（原 18-354 行）整段追加到 `collab/handoffs-archive.md`（带归档批次分隔注释），
  主文件只保留 2026-09-08 的 BUILD-186 一条（17 行）。
- **验证**：archive 6402 行、`grep -c '^## 2026-09-07'` = 39 条全部落地；中文 UTF-8 完好（bash 字节级 sed/cat，未经 PowerShell）。
- **影响范围**：纯文档搬运，零内容删改，零代码改动。

## 2026-09-08 — 主控 BUILD-186 代码验收（Read 实际改动，非只看报告）

- **ESC-178**（`main.rs:2088-2107` 轮询旁路 / `:950-` 子类日志链 / `:6755-` 消费端日志）：
  轮询置于 dirty 判定之前，不触碰 `needs_repaint`/`InvalidateRect`，**零重绘约束核验属实**，
  编辑态防闪（FLICKER-170-A/B'、FIX-172-B）无回归风险。
- **EDITFONT-183**（`main.rs:1402` 常量 -16 / `:6146-6152` 测量字号随状态）：测渲一致已收口，
  编辑态按 16px 量宽，避免低估 ~14% 导致 `ES_AUTOHSCROLL` 提前滚动。
- **EDITICON-185**（`menu_icons.rs:156-157/194`）：纯几何 `rasterize(size, edit_icon_covered)`，
  **零字体依赖核验属实**（无 `CreateFont`/`DrawText`，`Segoe MDL2` 仅存在于 `:85` 设计参考注释）。
- 🔴 **新发现（已立项，见 todo.md 首节）**：`:2101` 读的 `0x0001` 转变位在进入编辑态前会持续累积
  （全仓仅 `:2102`/`:2172` 两个读者，两态之外无人清位）⇒ 进编辑态首个 tick 可能凭空误取消，
  表现为转写文本秒失。已具备判别手段（误触发时无 subclass 日志伴随），拟修法为进入态时空读一次清位。
- **顺带**：`menu_icons.rs:27` 注释过时（仍称字体优先），非缺陷，下次碰该文件捎带订正。

## 2026-09-08 — 派发两单（并行，零文件重叠）：EDITICON-187 + ESC-188

- **EDITICON-187**（coder-2，`src/ui/menu_icons.rs`）：Gavin 端测定裁，编辑态图标 B → A
  （双文本线 → 单下划线，铅笔几何逐位不动），顺带订正 `:27` 过时注释。
  🔴 A 的几何常量因 185「旧候选已清理」+ 184/185 squash 进同一提交而永久丢失，本单为**按描述重建**。
- **ESC-188**（coder-1，`src/main.rs`）：进入编辑态前空读一次 `GetAsyncKeyState(VK_ESCAPE)`
  排掉陈旧转变位，防「转写文字凭空消失」。约 +5 行零删除，不碰轮询本体与重绘路径。
- **出包**：🔴 Gavin 明确指示本轮不出包，攒后续改动一起出。tester-1 待命无任务。

## ESC-188 · 编辑态入口排掉 ESC 陈旧转变位（coder-1，2026-09-08 12:4x）

- **背景**：主控验收 BUILD-186 时 Read 代码发现 —— ESC-178 轮询旁路（:2101）读 `GetAsyncKeyState(VK_ESCAPE)` 的 `0x0001` 转变位（「自上次读取以来被按过」），全仓位判断读者仅 2 处（编辑态 :2109 / FocusLost :2179），两态之外无人清位 ⇒ 用户在别处（关弹窗/退全屏/IDE 取消补全）按的 ESC 会把位留着 ⇒ 进入编辑态首个 tick 凭空命中 ⇒ `CancelRequested` ⇒ 转写文本丢失（数据丢失形态）。FocusLost 同写法多年未暴露是因误取消语义不可见。
- **改动（唯一文件 src/main.rs，+7/-0）**：`OverlayCommand::EnterEditMode` 臂内、状态赋值 `StreamingEditing` 之前插入一次**不判位的空读** `unsafe { let _ = GetAsyncKeyState(VK_ESCAPE.0 as i32); }`，排掉陈旧位。MSDN 转变位按调用消费、两读者与插入点同在窗口过程线程 ⇒ 同线程空读可靠清位。
- **零触碰**：:2109 轮询本体 / :2179 FocusLost / ESC-178 三段日志链 / needs_repaint / InvalidateRect / 一切绘制路径（编辑态防闪三轮修复零回归风险）。
- **方案评估**：备选（入口时间戳门控、FocusLost 同步加固、GetKeyboardState）均更复杂或无收益，主控建议的入口空读为最优，无异议直接实施。
- **验证**：cargo fmt 零改动；cargo check --all-targets 零 error，warnings **111/102 持平**；cargo test 全量主二进制 **1112P/0F/9I**（基线一致）+ crash-reporter 51P/2I + 集成 36P 全过；`grep '& 0x0001'` 读者仍恰 2 处；diff 仅 src/main.rs +7 净增零删除。
- **环境插曲（如实报）**：首次 cargo test 曾报瞬时 E0425（TEXT_LINE_1/TEXT_LINE_2 找不到），重跑消失 —— 与本单改动无关（我的 hunk 不含这两个符号，且 grep 全仓零命中），判定为 coder-2 并发编辑 EDITICON-187 的中间态，未复现。
- **红线**：不出包（Gavin 指示攒批）/ 未 commit（待主控验收）/ 未动版本号 / 无 git 破坏性命令 / 零凭证。


## 2026-09-08 — coder-2 — EDITICON-187 ✅ 编辑图标换回候选 A「铅笔+单条下划线」（只写 src/ui/menu_icons.rs）

- **任务**：Gavin 端测定裁「换成上次的A图标吧，实际使用下来觉得B不太合适」——形态偏好最终裁定，非 bug。
- **🔴 重建而非 revert**：A 的几何常量未留底（185 清理旧候选 + 184/185 squash 进同一提交 fd527e0），git 里只有 B。取证命中 `logs/20260907.md:571` 的 184 档案描述，拿到 A 的下划线参数（y=0.361、x -0.42..0.40）与邻居关系（距分割线 7px、距盒右缘 1.8px、笔尖-线气隙 1.3px）。
- **关键取舍（与任务书红线的冲突处置）**：档案表明 184 的 A 铅笔是「上移缩距」版（笔尖 y≈0.26，与线 1.3px 气隙），与 185 定稿笔位（tip y=0.14）不同——原始铅笔常量已丢失。任务书红线「铅笔逐位不动」优先遵守，**用下划线 y=0.25 补偿**：笔尖-线气隙 1.5px ≈1.3px 构图（直接沿用档案 y=0.361 会变 3.5px 悬空）。SSAA 子采样网格手算+实测确认 18px 渲染干净单行（行 13）。
- **改动**（`menu_icons.rs` 44+/32-，三处）：① `:27` 过时注释订正（184 已撤字体路线，实为纯几何）；② `TEXT_LINE_1/2` 双线 → `UNDERLINE=(0.25,-0.42,0.40)` 单线，`text_line_covered` → `underline_covered`；③ 测试改 A：出 `edit-A-{18,16}+x8`，断言换下划线（行13 col5=252 / 右端 col16=255 即 7px 净距端像素 / col17=0 分割线侧无墨 / 行12 col8=0 笔尖-线气隙）。铅笔几何五常量逐位未动。
- **验证**：fmt 幂等；`cargo check --all-targets` 零 error，warnings **111/102** 逐位持平；全量 cargo test **1112P/0F**（断言首跑即中）；零字体依赖 grep=0；我的 diff 仅 menu_icons.rs 一个文件（main.rs +7/-0 为 coder-1 ESC-188 在途，未触碰）。
- **预览**：`collab/outbox/coder-2/icons/edit-A-{18,16}+x8` 4 张，**保留不清理**（任务书明示：上次清理致 A 参数永久丢失）。ASCII 网格自检：笔尖指向基线构图正确。
- **红线**：未 commit/版本未动/未 cargo clean/未 cargo build --release/零凭证。

## 2026-09-08 — ESC-188 验收通过（主控 Read 核验，非只看报告）

- **改动**：`src/main.rs:1895-1901` 进入编辑态前 `let _ = GetAsyncKeyState(VK_ESCAPE.0 as i32);` 空读，+7/-0（4 注释 3 代码）。
- **主控独立核验**：diff 与方案逐字一致；`& 0x0001` 判位读者仍恰 2 处（`:2109`/`:2179`），
  新增的是不判位空读（`GetAsyncKeyState(VK_ESCAPE` 总计 4 处 = 3 判位 + 1 空读）；未碰重绘路径。
- **未出包未 commit**（Gavin 攒批指令）。

## 2026-09-08 — 🔴 主控判断失误两处（如实记录）

1. **误判「候选 A 参数彻底丢失」**：只查了 git / 周备份 / 预览 PNG，**漏查每日日志**。
   coder-2 在 `logs/20260907.md:571` 找到 EDITICON-184 完整档案
   （A 下划线 `y=0.361, x -0.42..0.40`，笔尖-基线 1.3px 气隙）。
   ⇒ 判据：查历史参数必须把 `logs/YYYYMMDD.md` 列入检索面，它是 Rule 7 强制写入的最全档案。
2. **任务书红线基于错误假设**：`EDITICON-187` 写死「铅笔 `PENCIL_*` 逐位不动」，
   前提是「A 与 B 共用同一支笔」——**该前提错误**。档案 1.3px 气隙反推 A 版笔尖 y≈0.26，
   而 185 定稿笔位 tip y=0.14。主控独立复算（fy 向下为正，18px 下 1 单位=18px：
   `0.361-0.14=0.221 → 3.98px`，减半线厚 0.5px ≈ **3.5px**）与 coder-2 数字吻合，判定其诊断正确。
   ⇒ coder-2 守红线 + 如实报取舍的处理方式正确，不算返工；已派 `EDITICON-187-ALT` 补 A2 变体预览。

## 2026-09-08 — 派发两单（并行，零文件重叠）

- **EDITICON-187-ALT**（coder-2，`menu_icons.rs`）：只出 A2 变体预览（cfg(test)），**生产零改动**，
  A1 既有预览保留，Gavin 并排二选一。
- **STREAMFONT-189**（coder-1，`main.rs`）：实时上屏文字 14→16。已附 6 产出源盘点表，
  改 3 留 3；`RecordingWithText` 测量字号同步归 16（正确性项，防「最右侧闪烁假复发」）；
  GDI 新增第二 HFONT 须照抄 `edit_font` 生命周期范式（`:1340-1342` 警告双删）。


## 2026-09-08 — coder-2 — EDITICON-187-ALT ✅ A2 变体预览（cfg(test)，生产零改动，待 Gavin 二选一）

- **背景**：主控自认 187 任务书「铅笔逐位不动」红线基于错误假设（A/B 非同一支笔，档案 1.3px 气隙反推 A 版笔尖 y≈0.26 ≠ 185 定稿 0.14）。本单补齐选项：cfg(test) 出 A2 预览，Gavin 对 A1/A2 二选一。
- **A2 几何（档案实数 vs 推算值，已分开声明）**：下划线 y=0.361、x -0.42..0.40 = **档案原值逐位照抄**；tip y=0.26 = **推导自档案**（线带顶 0.3335 − 1.3px）；tip x=-0.24、END=(0.26,-0.24) = **纯推算**（沿 175 tip(-0.40,0.40)→187 tip(-0.10,0.14) 迁移线取 y=0.26 对应点 t=0.5385→x=-0.2385，END 随同位移，笔形三常量不变）。
- **改动**（仅 `mod tests` 内，+~110 行）：`A2_TIP/A2_END/A2_LINE` 常量 + `a2_pencil_covered`（生产 `pencil_covered` 的参数化同数学副本，生产函数逐字节未动）+ `a2_covered` + `dump_edit_icon_a2_preview`（出 `edit-A2-{18,16}+x8`，断言：行 15 单行 252 / 右端 col16≥128 即 7px 净距 / col17=0 / 行 14 气隙 0（1.3px 档案值）/ 笔尖楔行 12 有墨 / 三角透明+右上近角有墨）。
- **A1 vs A2 构图差异**（18px）：A1 笔尖行 11.5、线行 13、气隙 1.5px、线下留白 4 行；A2 笔尖行 13.7、线行 15、气隙 1.3px（档案值）、线下留白 2 行——A2 整组重心更低、笔-线更贴近，且铅笔不再被盒缘裁切（s=1.0 于角内收尾，右上留 ~0.8px 空隙），A1 沿用 185 裁切原样。
- **验证**：fmt 幂等；check 零 error、warnings **111/102** 持平；全量 cargo test **1113P/0F**（+1=A2 预览测试，断言首跑即中）；零字体 grep=0；`git diff --numstat` 仅 menu_icons.rs（153+/31- 累计含 187）与 coder-1 在途 main.rs(+7/-0)；生产区 `edit_icon_covered/UNDERLINE/pencil_covered/PENCIL_*` 逐字节未动（改动全在 `mod tests`）。
- **预览**：`outbox/coder-2/icons/` 现 12 张——`edit-A-*`（A1）+ `edit-A2-*`（A2）+ `edit-FINAL-*`（B 对照）各 {18,16}+x8，全部保留供 Gavin 并排比。
- **红线**：未 commit/版本未动/未出包（Gavin 攒批指令）/零凭证。

## 2026-09-08 — 图标定裁：Gavin 选 A2，派 EDITICON-190 迁入生产

- **裁定**：Gavin 目视 `edit-A-18-x8`（A1）vs `edit-A2-18-x8`（A2）后原话「图标选a2」。
  A2 = 铅笔整体下移（tip y=0.26，由档案 1.3px 气隙反推）+ 下划线档案原值 `y=0.361, x -0.42..0.40`。
  coder-2 提示的「A2 笔杆右上不再被盒缘裁切、留 0.8px 空隙」已转达，Gavin 看过放大图后仍选 A2 ⇒ 按预览定稿。
- **派发**：`EDITICON-190`（coder-2，`menu_icons.rs`），照 185 迁移先例反向做一次；
  与 coder-1 的 `STREAMFONT-189`（main.rs）零重叠，并行。
- 🔴 **本单额外要求**：A1 几何常量必须留在 `cfg(test)` 对照组，且 result.md / logs 要**逐个数字**
  写出 A1/A2 完整常量表，预览 PNG 全部保留 —— 直接针对 187 的成因（185 清理候选致参数只剩一句描述）。
- **附带发现**：`edit-FINAL-18-x8.png` 与 `edit-A-18-x8.png` md5 逐位相同
  （`dadbf31c…`）—— FINAL 命名已失真，它 dump 的是「当前生产」，而 187 后生产已是 A1，
  故 B 的预览再次消失。命名应改为语义名而非「FINAL」。

## 2026-09-08 — Gavin 端测报障：编辑态窗口右侧一大片空白（待开诊断单）

- **现象**（Gavin 2026-09-06 端测发现，09-08 报）：进入编辑态后窗口最右侧出现一大段空白，
  光标移不过去，该区域不显示文字。附截图。
- **主控初步分析（🔴 假设未证，不得据此改代码）**：
  - 「光标移不过去」= EDIT 控件内文字末尾之后的空白（点击只会把 caret 送到文本末），
    ⇒ 空白**在 EDIT 控件内部**，即控件比文字宽出一大截，不是浮层底色。
  - 几何核对：`w = 42(LEFT_MARGIN) + text_w + 49(RIGHT_MARGIN) + 24(edit extra)`，
    `edit_w = w - 91 = text_w + 24` ⇒ **设计上只该多 24px**，与截图规模不符 ⇒ `text_w` 被算大。
  - 头号嫌疑：`measure_text_width`（`:6203`）用 `create_clear_type_font` = **"Segoe UI"** 测宽，
    而 Gavin 文本为**纯中文**。Segoe UI 无 CJK 字形，`GetTextExtentPoint32W` 的缺字形处理
    与实际绘制时的字体回退**不同源** ⇒ 每字偏大、随字数累积。与截图「整句中文、空白特别大」吻合。
  - 次要嫌疑：`:1651` 收缩节流 100ms —— 只会慢半拍，撑不出大片，判为非主因。
  - 已排除：宽度「只增不减」策略（`:1651-1661` 明确允许收缩，仅节流）。
- **处置**：开只查不修诊断单，用真实中文样本实测「GDI 测得宽 vs 实际渲染宽」的差值再定修法。
  🔴 不猜着改 —— 参照 `TRAY-ICON-158` 教训（拿预览当运行时证据致 Gavin 白测一轮）。
  排队等 coder-1 的 `STREAMFONT-189`（同 main.rs）完成。

## STREAMFONT-189 · 实时上屏文字 14→16 与编辑态同号（coder-1，2026-09-08 13:3x）

- **来源**：Gavin 2026-09-08 拍板「实时上屏窗口文字与编辑态同号」—— 即 EDITFONT-183 注释里预告取舍的裁定（「若 Gavin 不要跳变，两处常量合一」→ Gavin 要合一）。
- **改动（唯一文件 src/main.rs，+91/-36 含未 commit 的 ESC-188 +7）**：
  1. `OVERLAY_EDIT_FONT_SIZE` 改名 `OVERLAY_TEXT_FONT_SIZE`(-16)，语义升为「转写文字统一字号」，注释订正（不再写「独立于自绘文字」）；EDIT 控件三处引用随名更新（edit_font 本身未动）。
  2. D2D：`streaming_text_format` size 绑 `-OVERLAY_TEXT_FONT_SIZE`，**仅此一个 format**；text_format（处理中）/centered_text_format（箭头/Error/Info）/wrap_text_format（预览正文）仍绑 `-OVERLAY_FONT_SIZE`(-14) 逐位未动（grep 自证 3+1）。
  3. GDI：新增 `streaming_font: Option<HFONT>` 字段（16px），生命周期照 edit_font/cached_font 范式 —— 选择点惰性创建+存回、隐藏/退出清理臂与 cached_font 同臂 take+DeleteObject，两字体互不复用无双删。选择收在 `draw_overlay_to_dc` 唯一 SelectObject 处按状态分流：`RecordingStreamingIdle | RecordingWithText` → streaming_font，其余态维持 cached_font(-14)。测渲同号在此结构性成立：兜底路径的 `measure_text_width`（:3579，D2D 成败都消费）继承所选字体。
  4. 测量：`adjust_overlay_pos_size_for_text` match 增加 `RecordingWithText` 归 16（与 StreamingEditing 同臂）—— 正确性项（防宽度低估 ~14% ⇒ 假性横向滚动）。
  5. 守卫：`overlay_text_area_capacity_guard` 扩展为 14/16 双字号断言。
- **容量算式**：available = 36 − 2×OVERLAY_TEXT_DRAW_VERTICAL_INSET(4) = 28；16px Segoe UI tmHeight ≈ 21，desired(≈23) ≤ 28，D2D 行高 ≈21 ≤ 28 ⇒ 36px 窗高装得下不裁字。
- **验证**：fmt --check 零改动；check --all-targets 零 error，warnings **111/102 持平**；cargo test 全量主二进制 **1113P/0F/9I**（1112 基线 +1 = coder-2 的 menu_icons 新测试，同 test bin；本单 main.rs 零新增 test）+ 集成全 0F。crash_reporter 假红再现一次（26P/25F/2I，与立案特征逐位吻合）后立即复跑消失 + 单独跑 51P —— 维持「并行争用偶发」归档。
- **红线**：不出包 / 未 commit / 版本号未动 / 无 git 破坏性命令 / 零凭证。


## 2026-09-08 — coder-2 — EDITICON-190 ✅ A2 几何迁入生产（Gavin 定裁「图标选a2」，只写 src/ui/menu_icons.rs）

- **改动**：照 185 先例反向迁移，**只换常量取值、函数结构零改动**：`PENCIL_TIP (-0.10,0.14)→(-0.24,0.26)`、`PENCIL_END (0.40,-0.36)→(0.26,-0.24)`、`UNDERLINE (0.25,-0.42,0.40)→(0.361,-0.42,0.40)`；HALF_W/S_TIP/S_GAP_END 不变。`edit_icon_covered/underline_covered/pencil_covered` 函数体逐字节未动；三 pub 签名不变 → main.rs D2D/GDI 兜底自动吃新像素，免集成单。
- **🔴 参数留底（本单额外要求）**：A1 几何以 `A1_TIP/A1_END/A1_LINE` + `a1_pencil_covered/a1_covered` + `dump_edit_icon_a1_preview` 全套留在 cfg(test) 对照组（非只留 PNG）；测试注释+生产块注释均写明「勿删」。完整常量表见 result.md。
- **逐字节自证**：生产 `edit_icon_rgba(18)` 重新 dump 出的 `edit-A2-18.png` 与 Gavin 定裁的既有预览 **md5 一致 `187ab98e3311d791ee94acab04f49cd5`**（迁移前备份比对）。
- **验证**：fmt 幂等；check 零 error、warnings **111/102** 持平；全量 cargo test **1113P/0F**（用例数不变：`dump_edit_icon_a2_preview` 因 A2 即生产而移除，新增 `dump_edit_icon_a1_preview` 对照，-1+1=0）；零字体 grep=0；我的 diff 仅 menu_icons.rs（main.rs 98+/36- 为 coder-1 STREAMFONT-189 在途，零触碰）。
- **测试断言迁移**：生产 dump 测试改名 edit-A2-* 并换 A2 断言（行 15 单行 252 / col16≥128 / col17=0 / 行 14 气隙 0 / 笔尖楔行 12 有墨 / 右上近角有墨但不再满贴盒缘）；A1 对照断言=187 交付原断言（行 13 / 气隙行 12 / 触角）。
- **预览**：icons/ 现 16 张（edit-A-*（187 名旧档）+ edit-A1-* + edit-A2-* + edit-FINAL-* 各 {18,16}+x8），全部保留不清理。
- **红线**：未 commit/版本未动/未出包（Gavin 攒批）/零凭证/未 cargo build --release。

## DIAG-191 · 编辑态右侧大片空白诊断（coder-1，2026-09-08，只查不修）

- **主因（新立 H5，四假设之外）**：EDIT 控件在 SetWindowPos 扩窗**之前**用旧 client rect 创建（:1923→:1926→:1936 顺序），edit_w 唯一计算点 :761；全仓 5 处 SetWindowPos 全打父窗口、无 WM_SIZE/WM_COMMAND 触发的 EDIT resize ⇒ EDIT 恒为旧宽。BUILD-186 上：流式窗 14px 度量=903 ⇒ edit_w=812，EDIT 16px 渲染需 986（离线工装实测，GetTextExtent 与 DT_CALCRECT 交叉验证偏差 0.8%）⇒ 缺 174px ⇒ 扩窗到 1101 后 EDIT 右缘到分割线 ~211px 父背景空白（1920 屏封顶场景 ~70px）。光标/文字不出 EDIT ⇒ 与报障「光标移不过去/不显示文字」逐条吻合。
- **四假设判定**：H1 机制成立但非本案主因（短句实测 desired 145/160/220 全 <240，长句 1101 不受限）；H2 测宽偏大证伪（0.8%），1920 封顶造成「裁字」非空白；H3 不成立（同一 text.clone() 三处同源）；H4 不成立（0.8% 无 font-linking 级偏差）。
- **🔴 关键交叉**：已交付未 commit 的 STREAMFONT-189 把 RecordingWithText 测量归 16 ⇒ 流式窗 1077 ⇒ edit_w=986=恰好文字宽 ⇒ 174px 主项消失，残余 24px+边距。Gavin 下批端测应见空白基本消失；H5 结构缺口（创建时序 + 无跟随 resize）仍需修法 1（先扩窗再建 EDIT + EDIT 宽度跟随窗口单一收口，SetWindowPos on EDIT 须纳入 WM_SETREDRAW 包裹防闪）。
- 工装插曲如实报：临时 encode_utf16 漏 null / EM_POSFROMCHAR 单行不支持，均无生产影响；工装删净，diff sha 与快照逐位一致。
- **红线**：src/ 零改动交付 / 未 commit / 未出包 / 零凭证 / 全量回归 1113P/0F 持平。

## FIX-192 · 编辑态右侧空白结构修复：先扩窗再建 EDIT（coder-1，2026-09-08）

- **定案**：Gavin 拍板立即修 DIAG-191 H5。EnterEditMode 臂重排为「adjust 算尺寸 → SetWindowPos 扩窗 → 取新 client rect → create_edit_control」，纯移动 +10/-3（7 行注释）。create_edit_control 算式零触碰；SLWA 切换/remove_noactivate 原位（G5 判据不受影响，测试实证）。
- **闪烁复核**：重排块全程在 :1883 SW_HIDE 与 :1951 SW_SHOW 隐藏区间内，无可见重绘，与三轮闪烁修复零交集。
- **修后数字**：2560 屏 edit_w=1010 vs 需 986 ⇒ +24px 设计余量（修前 −174px 缺口+211px 空白）；1920 屏 edit_w=869 ⇒ −117px（max_w 封顶既定行为，裁字非空白）。
- **验证**：fmt 零改动 / check 零 error / warnings 111-102 持平 / 全量 1113P/0F。
- **红线**：未 commit / 未出包 / 版本号未动 / 零凭证。

## 2026-09-08 — BUILD-193 出包 + Gavin 端测全过（本批收官）

- **产物**：13:44:56，sha `d081c742…`，两副本一致，v0.9.0.0 未动，config.toml 未变。
- **主控独立核验**（tester-1 中途 API `Bad Request` 故障未交报告，核验由主控代做）：
  ① 两副本 sha 一致且异于上包；② 源码最后改动 13:30:06 < 产物 13:44:56 ⇒ 四改动全在包内；
  ③ 版本号/用户配置未动；④ 两 toml 已修复。
  🔴 **字面量判别探针本批不适用**：`FIX-192`（纯换序）/`STREAMFONT-189`（改常量）/
  `ESC-188`（加空读）三者**均不产生字符串字面量**，命中 0 属正常，改用「源码 mtime < 产物 mtime」
  作新鲜度判据。—— 判据：探针只对带日志文案的改动有效，行为型改动须另择判据，不得因命中 0 误判为未进包。
- **端测**：Gavin「端侧全部通过！」，含一票否决项「进出编辑态/移光标/最右侧无新增闪烁」。
- **闪烁未复发的机制归因**：FIX-192 重排全程在 `:1883 SW_HIDE` 与 `:1955 SW_SHOW` 之间，
  窗口不可见、无可见重绘 ⇒ 与三轮闪烁修复零交集。该前提由主控独立复核行号确认后才允许动这块。
