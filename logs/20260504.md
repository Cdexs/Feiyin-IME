# voice-ime 改动日志 · 2026-05-04

## Session 启动

- Orchestrator 就绪，上下文完整读取
- 上次构建产物：voice-ime.exe 10.20MB + voice-ime-ui.exe 17.68MB + crash-reporter.exe 23.58MB (2026-05-03 22:11)
- 测试状态：133 PASS / 0 FAIL / 4 IGNORED
- Workers：coder-1/coder-2/tester-1 启动中（权限弹窗处理中）

---

## 2026-05-04 — PERF-INIT-001 · LlmClient + TranslationEngine 预初始化优化

### 改动文件
- `src/main.rs`：`spawn_worker_thread` 函数（第 1870-1943 行区域）

### 改动内容

1. **LlmClient 预初始化**（第 1870-1871 行）
   - 在 `loop` 之前创建 `let mut llm_client = llm::LlmClient::new(config.llm.clone())`
   - 循环内改为 `llm_client.update_config(config.llm.clone())`，复用已有 `reqwest::Client`（TCP 连接池 + TLS 会话）

2. **TranslationEngine 热重载缓存**（第 1873-1880 行）
   - 新增 `cached_translation: Option<(TranslationLanguage, TranslationEngine)>`
   - 启动时按配置预初始化翻译引擎
   - 循环内新增 `needs_reload` 判断：仅在 `enabled` 状态翻转或 `target_language` 变更时重新加载模型
   - 无变更时直接复用缓存引擎（避免每次录音结束重新加载 CT2 模型 ~1-2s）

3. **LlmClient::update_config 移除 `#[allow(dead_code)]`**（`src/llm/mod.rs` 第 89 行）
   - `update_config()` 现在被 `spawn_worker_thread` 使用，不再是 dead code

### 影响范围
- 仅 `spawn_worker_thread` 内部逻辑变更
- `run_pipeline` 签名不变
- `load_translation_engine_for_target` 函数不变

### 性能预期
- LLM 请求：首次 `LlmClient::new` 仍创建 `reqwest::Client`（含 TLS），后续只替换 config
- 翻译引擎：配置不变时跳过模型加载（~1-2s 延迟消除），方向切换时仍完整重载

## 任务派发

### BORDER-DARKEN-001（coder-2）
- 录音窗口边框加深一倍
- draw_recording_overlay 中 BORDER_GRAY + CIRC_BORDER: 0x181910 → 0x0C0C08
- 状态：✅ 已完成（cargo check 0 errors）

### PTT 延迟分析（咖啡完成）
- 当前最大延迟：~17ms（PTT_POLL_MS=15ms 为主要来源）
- 链路：poll检测(15ms) → PostMessageW(<1ms) → WM_APP_OVERLAY_WAKE(<1ms)
- 结论：LATENCY-001/OVERLAY-WAKE-001 已消除主要卡顿，当前延迟 < 1帧
- 待 Gavin 确认是否需要降低 PTT_POLL_MS 到 5ms

### PERF-INIT-001（coder-1）
- LlmClient + TranslationEngine 预初始化优化
- LlmClient：startup 初始化一次 + 每次录音前 update_config()
- TranslationEngine：startup 初始化一次 + enabled/target_language 变更时热重载
- 状态：已派发

### TEST-SYNC-PERF-INIT-001（tester-1）
- 对应 PERF-INIT-001 的测试用例同步
- 状态：✅ 已完成
- 新增测试：
  - `src/llm/mod.rs`: 3 个 `update_config` 测试
  - `src/main.rs`: 提取 `translation_needs_reload()` 辅助函数 + 6 个测试
- 覆盖缺口：LlmClient 复用、TranslationEngine 热重载条件逻辑

### BUILD-RELEASE-20260504A（tester-1）
- 合并 BORDER-DARKEN-001 + PERF-INIT-001 出包
- 仅 Rust 改动，跳过前端重建
- 状态：已派发

---

## 2026-05-04 — UI-OVERLAY-OPT-001 · Overlay 窗口视觉优化

### 改动文件
- `src/main.rs`: `draw_recording_overlay()`, `draw_processing_overlay()`, `draw_error_overlay()`, `draw_overlay_to_dc()`
- `src/i18n.rs`: `Strings` struct + `ZH`/`EN` static

### 改动内容

1. **录音窗口波形从中间往两边展开**（第 1121 行区域）
   - `half = bc / 2 = 16`，取最近 16 帧音频数据
   - 左半：`x = wl + (half-1-i) * (bw+bgap)`，数据 `levels[start_idx + i]`
   - 右半：`x = wl + (half+i) * (bw+bgap)`，数据 `levels[start_idx + i]`（镜像）
   - 效果：中间两根条是最新音频，向两侧对称递减

2. **录音窗口边框加深**（第 1013-1014 行）
   - `BORDER_GRAY`/`CIRC_BORDER`: 0x0C0C08 → 0x2E2A26

3. **处理中窗口全宽渐变光扫动效**（第 1269 行区域）
   - 替换 Slim Shimmer（3px 条）为 GradientFill + AlphaBlend 光带
   - `band_width = window_width / 2`，`TRIVERTEX` 三段：黑→橘→黑
   - `GRADIENT_RECT` 两段水平渐变，`AlphaBlend` SourceConstantAlpha: 180
   - 修复 `GradientFill` Windows API 调用签名（`*const c_void` + `nmesh` 参数）

4. **处理中窗口边框统一**（第 1240 行）
   - `BORDER_GRAY`: 0x413D3A → 0x2E2A26

5. **错误窗口小红圆修复**（第 1470 行区域）
   - 修复 Bug：`CreateSolidBrush(red)` 后未 `SelectObject`，RoundRect 无填充
   - 新设计：8px 实心红圆（`Ellipse` + `SelectObject` 红画刷 + `PS_NULL` 空笔）
   - 文字 left 边距自动调整为 `circ_x + circ_d/2 + 8`

6. **错误窗口文字橘色**（第 1496 行）
   - `SetTextColor`: 0xEDEDED → `BRAND_ORANGE` (0x006BFF)

7. **错误窗口边框统一 + ui_language 参数**（第 1440、1443 行）
   - `BORDER_GRAY`: 0x413D3A → 0x2E2A26
   - 函数签名添加 `_ui_language: config::UiLanguage`（为后续 i18n 预留）
   - `draw_overlay_to_dc()` 调用点传入 `request.ui_language`

8. **i18n.rs 添加 overlay_error 字段**
   - `Strings` struct 新增 `pub overlay_error: &'static str`
   - ZH: "识别出错"，EN: "Recognition error"

### 编译验证
- `cargo check`: ✅ 零 error（Finished in 13.65s）
- warnings 均为既有代码未使用项，与本次改动无关

---

## 2026-05-04 — I18N-ERROR-001 · 错误提示文本本地化

### 改动文件
- `src/i18n.rs`: `Strings` struct + `ZH`/`EN` static
- `src/main.rs`: `convert_to_friendly_error()` + `PipelineEvent::Error` 调用处

### 改动内容

1. **i18n.rs 新增 4 个错误字段**（`Strings` struct 末尾）
   - `error_network_timeout`: 网络超时
   - `error_api_unavailable`: 服务不可用
   - `error_model_init`: 语音模型初始化失败
   - `error_microphone`: 麦克风不可用

2. **ZH/EN static 添加对应本地化文本**
   - ZH: "网络超时，请重试。" / "服务不可用，请检查网络或 API 设置。" / "语音模型初始化失败。" / "麦克风不可用。"
   - EN: "Network timeout. Please try again." / "Service unavailable. Check network/API settings." / "Speech model initialization failed." / "Microphone is unavailable."

3. **main.rs `convert_to_friendly_error` 接入 i18n**
   - 签名：`fn convert_to_friendly_error(message: &str, ui_language: config::UiLanguage) -> String`
   - 内部使用 `i18n::get(ui_language)` 获取对应语言文本
   - `PipelineEvent::Error` 调用处传入 `ui_language`

### 编译验证
- `cargo check`: ✅ 零 error（Finished in 11.72s）
- warnings 均为既有代码未使用项，与本次改动无关

---

## 2026-05-04 — UI-OPT-004 · 录音窗口指示灯改为麦克风图标（GDI 手绘）

### 改动文件
- `src/main.rs`：`draw_recording_overlay()` 内的超采样绘制区（第1089-1119行区域）+ Gdi imports

### 改动内容

1. **GDI imports 新增 `Arc`**
   - `windows::Win32::Graphics::Gdi` import 新增 `Arc`

2. **圆形指示灯 → 麦克风图标**（56×56 超采样画布）
   - **Part 1**：胶囊体 `RoundRect(21,1,35,30,14,14)`，`PS_NULL` 笔 + `circ_color` 实心刷
   - **Part 2**：弧形支架 `Arc(12,10,44,42, 44,26, 12,26)`，`PS_SOLID` 笔 width=4 + `NULL_BRUSH`
   - **Part 3**：垂直连接杆 `MoveToEx(28,42) → LineTo(28,50)`
   - **Part 4**：底座横线 `MoveToEx(18,50) → LineTo(38,50)`
   - Part 3+4 共用同一个 `line_pen`，合并清理
   - 所有 GDI 对象（pen/brush）严格 `SelectObject` 还原 + `DeleteObject` 释放

3. **保留不动**
   - 4x 超采样框架（`CreateCompatibleDC` / `CreateCompatibleBitmap` / `StretchBlt HALFTONE`）
   - `circ_color` 三态颜色逻辑（灰/橘/红）
   - `BG_DARK` 背景填充
   - `circ_size`/`circ_l`/`circ_t` 位置计算

### 编译验证
- `cargo check`: ✅ 麦克风图标代码零 error
- 2 个既有 error（`DT_WORDBREAK` + tuple mismatch）与本次改动无关
# 2026-05-04 Log

## tester-1 启动
- 时间：17:21:37
- 已阅读：worker-guide.md, build-test-guide.md, todo.md, handoffs.md, decisions.md, troubleshooting.md
- 状态：已就绪，等待任务派发


---

## Session 恢复（重启后）

- 中断点确认：UI-OVERLAY-OPT-001 + I18N-ERROR-001 + UI-OPT-004 代码完成但未出包
- 上次构建（14:50）包含：BORDER-DARKEN-001 + PERF-INIT-001 + TRAY-FREEZE-FIX-001
- cargo check 验证：0 error ✅
- 托盘冻结修复确认：MENU_VISIBLE + show_tray_popup_menu 均已在代码中 ✅
- 已派发：BUILD-RELEASE-20260504B → tester-1（含3个UI改动）
- todo.md 已更新：今日完成任务记录 + TRAY-FREEZE-BUG-001 标记已关闭
- coder-2 notify.md 旧通知（WINDOW-TITLEBAR-002）已清除

---

## 2026-05-04 — BUILD-RELEASE-20260504B · 补完 UI 改动出包

### 执行者：tester-1

### 构建步骤
- Step 1 (kill): ✅ taskkill voice-ime/voice-ime-ui/crash-reporter
- Step 2 (cargo test): ✅ 142 PASS / 0 FAIL / 4 IGNORED
- Step 3 (release build): ✅ ~1m 41s (80 warnings)
- Step 4 (timestamps): ✅ voice-ime.exe 10.20MB (17:33) / crash-reporter.exe 23.58MB (17:33)

### 包含改动
- UI-OVERLAY-OPT-001：波形中心展开/渐变光扫/错误窗口修复
- I18N-ERROR-001：错误提示 i18n
- UI-OPT-004：麦克风图标（GDI 手绘）
- BORDER-DARKEN-001：边框加深

### 验收结果
- ✅ cargo test ≥133 PASS：实际 142 PASS
- ✅ 产物时间戳为今日 2026-05-04：17:33
- ✅ voice-ime.exe 大小约 10MB：10.20 MB

---

## 2026-05-04 — TEST-SYNC-OVERLAY-FIX-002 · 测试用例对齐分析

### 执行者：tester-1

### 分析结论
- 麦克风图标 GDI 参数：❌ 不可自动化（Win32 API 硬编码像素值，现有框架无法深入 HDC）
- 三角波 phase 计算：✅ 可自动化（如提取为独立纯函数，可补充 Rust 单测）
- 处理中动效颜色：❌ 不可自动化（视觉设计变更，需目视确认）

### 产出
`collab/outbox/tester-1/test_sync_overlay_fix_002.md`

---

## 2026-05-04 — OVERLAY-FIX-002 · 麦克风图标断裂 + 处理中动效修复

### 执行者：coder-2

### 修复 1：麦克风图标黑色间隔（断裂）
- **根因**：胶囊体底部 y=30，弧形端点 y=26，错位 4px
- **Part 1**：`RoundRect(mem_dc, 21, 1, 35, 30, 14, 14)` → `(..., 28, ...)`（胶囊底对齐）
- **Part 2**：`Arc(mem_dc, 12, 10, 44, 42, 44, 26, 12, 26)` → `(12, 12, 44, 44, 44, 28, 12, 28)`（弧底对齐）
- **Part 3+4**：`MoveToEx(mem_dc, 28, 42, None)` → `(28, 44, None)`（茎杆起点对齐弧底）

### 修复 2：处理中动效 → 淡银白色光晕平滑滑动
- **根因**：橘色渐变与前景文字橘色混淆；phase `% 1.0` 硬重置导致跳跃
- **颜色**：TRIVERTEX 中心颜色 `0xFF8C00` → `0xE0E0E0`（淡银白）
- **透明度**：AlphaBlend `SourceConstantAlpha: 220` → `140`
- **周期**：`shimmer_phase = (p + 0.04) % 1.0` → `% 2.0`
- **三角波**：`p = if shimmer_phase <= 1.0 { shimmer_phase } else { 2.0 - shimmer_phase }`
- **效果**：光带左→右→左往返，无跳跃

### 验证
- `cargo check`：0 errors（80 warnings 均为既有）

---

## 2026-05-04 — BUILD-RELEASE-20260504C · OVERLAY-FIX-002 出包

### 执行者：tester-1

### 构建步骤
- Step 1 (kill): ✅ taskkill voice-ime/voice-ime-ui/crash-reporter
- Step 2 (cargo test): ✅ 146 PASS / 0 FAIL / 4 IGNORED
- Step 3 (release build): ✅ ~1m 40s (80 warnings)
- Step 4 (timestamps): ✅ voice-ime.exe 10.20MB (18:09) / crash-reporter.exe 23.58MB (18:08)

### 包含改动
- OVERLAY-FIX-002：麦克风图标断裂修复 + 处理中动效淡银白色平滑
- TEST-EXEC-OVERLAY-FIX-002：三角波 shimmer 相位计算 4 个 Rust 单测

### 验收结果
- ✅ cargo test ≥146 PASS：实际 146 PASS
- ✅ 产物时间戳为今日 2026-05-04：18:08/18:09
- ✅ voice-ime.exe 大小约 10MB：10.20 MB

---

## 2026-05-04 — TEST-SYNC-OVERLAY-FIX-003 · 测试用例编写

### 执行者：tester-1

### 新增测试
- `src/main.rs` overlay_shimmer_tests 模块 +3 个 band_width 单测
- shimmer_band_width_quarter_window: 240px → 48px
- shimmer_band_width_480px_window: 480px → 96px
- shimmer_band_width_travel_range: travel_range = 192px

### 不可自动化项
- 麦克风 Chord 填实、圆角一致性、光带宽度 → 需目视验收

---

## 2026-05-04 — OVERLAY-FIX-003 · 四项 Overlay 修复

### 执行者：coder-2

### 修复 1：处理中窗口动效 → 窄光带扫过
- `band_width` 从 `* 2 / 3` (~160px) 改为 `/ 5` (~48px)
- 呈现"一抹银白"扫过效果，避免满窗口闪

### 修复 2：麦克风图标 → Chord 填实下半圆
- 删除旧 Part 2（`arc_pen` + `Arc` 勾勒弧线）
- 在 Part 1 `body_brush` 作用域内新增 `Chord(mem_dc, 12, 12, 44, 44, 44, 28, 12, 28)`
- 胶囊体与下半圆一体填实，无断裂痕迹
- 新增 `Chord` GDI 导入，移除弃用 `Arc` 导入

### 修复 3：错误窗口圆角对齐录音窗口
- `draw_error_overlay()` + `apply_overlay_window_region()`：`16` → `10`

### 修复 4：预览窗口圆角对齐录音窗口
- `draw_preview_overlay()` + `apply_overlay_window_region()`：`16` → `10`

### 验证
- `cargo check`：0 errors（79 warnings，较上次减少 1 个弃用 `Arc` 警告）

---

## 2026-05-04 — TEST-EXEC-OVERLAY-FIX-003 · 测试执行

### 执行者：tester-1

### 测试结果
- cargo test：149 PASS / 0 FAIL / 4 IGNORED ✅
- 新增 3 个 band_width 单测全部通过

---

## 2026-05-04 — OVERLAY-FIX-004 · 麦克风图标简化 + 处理中光晕修复

### 执行者：coder-2

### 修复 1：麦克风图标改为简化版竖椭圆+杆+底座
- 删除旧胶囊 RoundRect + Chord 填实结构
- 替换为：`RoundRect(mem_dc, 17, 2, 39, 40, 22, 22)`（22px宽、38px高、全圆角）
- 茎杆：`MoveToEx(28,40)` → `LineTo(28,50)`
- 底座：`MoveToEx(18,50)` → `LineTo(38,50)`

### 修复 2：处理中光晕渐变端点改为背景色
- TRIVERTEX 左端+右端：`0x0000` → `0x1800, 0x1A00, 0x1800`（BG_DARK #181A18）
- 中间保持银白色 `0xE0E0E0`
- 效果：背景色→银白→背景色，无黑色暗晕压背景

### 验证
- `cargo check`：0 errors

---

## 2026-05-04 — OVERLAY-FIX-005 · 处理中动效修复 + 预览窗口 5 项优化

### 执行者：coder-2

### 修复 1：处理中动效 — 相位移入 WM_PAINT，光带加宽
- **WM_TIMER**：删除 `state.shimmer_phase = (p + 0.04) % 2.0`，只保留 `InvalidateRect`
- **WM_PAINT**：在 `data.state.lock()` 后、`draw_overlay_to_dc` 前插入 phase 更新
  - `state.shimmer_phase = (state.shimmer_phase + 0.015) % 2.0`
  - 每帧绘制只递增一次，解决 WM_TIMER 与 WM_PAINT 不同步导致的跳跃
- **光带宽度**：`/ 5` (~48px) → `* 2 / 3` (~160px)，单光晕感更柔和

### 修复 2：预览窗口 5 项优化
- 新增 `BRAND_ORANGE` 常量
- 标题文字：灰色 → 橘色，左对齐 → 水平居中，左右边距对称(26px)
- 标题栏 X 叉：灰色 → 橘色
- 复制/关闭按钮文字：灰色 → 橘色

### 验证
- `cargo check`：0 errors

---

## 2026-05-04 — WAVEFORM-FIX-001 · 波形自由落体过渡 + 中心频谱加权

### 执行者：coder-1

### 功能 1：录音结束后波形自由落体过渡到处理中状态

| 改动 | 位置 | 内容 |
|------|------|------|
| A | `src/ui/overlay.rs:59` | `OverlayStatus` 新增 `FallingToProcessing { message: String }` 变体 |
| B | `src/main.rs:744-770` | overlay 线程主循环 `FallingToProcessing` 分支：GRAVITY_RATE=0.25 衰减，全归零后切换 Processing；持续 InvalidateRect |
| C | `src/main.rs:979-985` | `draw_overlay_to_dc` 添加 `FallingToProcessing` 分支：复用 `draw_recording_overlay` |
| D | `src/main.rs:1878-1887` + `1903-1910` | `HotkeyEvent::Stop` + `PipelineEvent::Processing` 均改为先进入 `FallingToProcessing` |
| E | `src/main.rs:1701` | `overlay_geometry` 添加 `FallingToProcessing { .. } => RECORDING_OVERLAY_SIZE` |
| F | `src/main.rs:779-784` | `has_active_overlay` 判定包含 `FallingToProcessing`（16ms 短计时器） |

### 功能 2：波形中心加权（cos² 曲线）

- 左半/右半 for 循环各添加 `weight = 0.4 + 0.6 * cos²(π/2 × i / (half-1))`
- 中心 bar weight=1.0，边缘 bar weight=0.4
- `v_gain` 改为 `(v * gain * weight).min(1.0)`

### 编译验证
- `cargo check`: ✅ 0 error（80 warnings 均为既有未使用代码）
