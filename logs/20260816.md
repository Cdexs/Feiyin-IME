# 修改历史 · voice-ime · 20260816

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-08-16 — tester-1 · BUILD-016 v0.8.0 首包出包（阶段五，全构建 + 七项核验 + 双探针，零生产代码改动）

### 一、背景

Gavin 已明确下达出包指令。这是 **v0.8.0 的第一个包**，也是新 ASR 引擎（真流式 + qwen-audio-3.0 Inference）**第一次进 exe**。基线 HEAD `be76fc1`（TEST-SYNC-038-B + TEST-EXEC-038 全量回归零红条提交）。

### 二、构建（照 build-test-guide 第一节 Step 1-4）

| Step | 内容 | 耗时 | 结果 |
| --- | --- | --- | --- |
| 1 | 清进程（feiyin-ime PID 23888） | ~2s | ALL_CLEAR |
| 2 | npm build | 718ms | 新 `index-DkzLqu_f.js` 201.95kB + `index-BNQZfcUG.css` 25.01kB |
| 2 | Tauri UI release（custom-protocol） | 2m14s | `src-tauri/target/release/feiyin-ime-ui.exe` → cp 到 `target/release/` |
| 3 | 主程序 `cargo build --release` | 2m40s | `feiyin-ime.exe` + `crash-reporter.exe` |
| 4 | 同步 Publish/ | — | 三 exe + scene/itn 两 toml；config.toml 等运行时数据未覆盖 |

### 三、七项核验（全部 PASS，机器实测）

① 六 exe 时间戳全部 00:45-00:48 本次构建；② 三 exe 两副本 sha256 逐一相等（feiyin-ime `86176283…` / ui `db095564…` / crash `a9c30e4e…`）；③ 两 toml 三副本一致（scene-rules `0a3a0b9…` / itn-rules `b208271…`）；④ ProductVersion 主程序 0.8.0.0 / UI 0.8.0 / crash 0.8.0.0；⑤ 新前端 `index-DkzLqu_f.js` 在 ui.exe 命中 1 / 旧名 `CTgGziQm` 0 命中（i18n 裸串 grep 为 0 系 Tauri 压缩存储已知行为，改用文件名字符探针）；⑥ 冒烟 PID 30276 Responding=True 无 panic，测后清理；⑦ 主程序 +149504B / UI 0B / crash +2048B，差异方向已解释（删 686 行 vs 累加真流式与 overlay 代码）。

### 四、🔴 额外决定性后端探针

| 探针 | 方向 | exe 命中 |
| --- | --- | --- |
| `qwen-audio-3.0-asr-flash-streaming` | 正向（新模型） | **1** ✅ |
| `api-ws/v1/inference` | 正向（新端点） | **1** ✅ |
| `api-ws/v1/realtime` | 反向（旧端点） | **0** ✅ |
| `qwen3-asr-flash-realtime` | 反向（旧模型） | **0** ✅ |

反向 0 命中证明「新代码进去了 + 旧代码真的被清干净了」。旧串仅残留在测试字符串（config/mod.rs:1224 构造 Gavin 旧配置模拟），release 产物不含测试。

### 五、验收 / 待主控

- 七项核验主控将独立复算，然后交 Gavin 端测（`-debug` 跑，重点采集：新端点连通性 / usage.duration 计费口径 / 040-A 四段连接耗时 / VAD 门控行为，供 ASR-PERF-040-B/C 优化）。
- 版本号未动（0.8.0）。生产代码零改动。

---

## 2026-08-16 — orchestrator · 新 session 启动 + handoffs.md 归档（文档维护，零代码改动）

### 一、启动状态核查（实测取证）

| 项 | 实测结果 |
| --- | --- |
| HEAD | `9087d3f` build: BUILD-016 v0.8.0 首包 |
| 工作区 | `git status --short` 空 —— 无悬空改动 |
| Worker panes | coder-1 `%2` / coder-2 `%1` / tester-1 `%3` / orchestrator `%0`，四进程均存活（opencode.exe） |
| outbox | 三份 `result.md` 均 0 字节（新 session 已清空） |
| Worker 就绪 | coder-1 ✅ ｜ coder-2 ✅ ｜ tester-1 ✅（含 worker-guide/build-test-guide 已读自述），三方 ACK 均在 120s 内到达，无需重启 |

### 二、handoffs.md 归档（触发「>200 行立即归档」规则）

- **触发条件**：session 启动检查 `wc -l handoffs.md` = **346 行**，超过 200 行阈值。
- **动作**：2026-08-15 及更早的全部条目（原第 17–346 行）追加至 `collab/handoffs-archive.md` 末尾，
  带分隔标记 `# 归档批次：2026-08-16 主控归档`；`handoffs.md` 只保留当天（2026-08-16）BUILD-016 一条。
- **结果**：`handoffs.md` 346 → **15 行**；`handoffs-archive.md` 4298 → **4628 行**（内容零删改，仅搬移）。
- **备份**：搬移前已 `cp` 原文件到本次 session scratchpad，防误操作。

### 三、🔴 遗留隐患（已识别，待 Gavin 拍板处置）

`collab/todo.md` 当前 **2262 行 / 约 105K tokens**，严重违反两条既有规则：
「todo.md 只保留待处理任务，已完成移入归档」与「每 session 必读文档合计不超过 ~20K tokens」。
文件内 `✅ 已结案` / `✅ 已闭环` 段落占绝大部分（可追溯到 2026-08-02 批次）。
`collab/todo-archive.md` **尚不存在**。归档属大范围文档重构，按「不可逆操作单独成轮」纪律，
本轮只记录不执行，待 Gavin 确认后单独成轮处理。

---

## 2026-08-16 18:14 — coder-2 · onboarding（零改动）

- 读取 worker-guide.md、todo.md、handoffs.md、decisions.md、troubleshooting.md

---

## 2026-08-16 — orchestrator · session 重启复位（文档核查，零代码改动）

| 项 | 实测结果 |
| --- | --- |
| HEAD | `9087d3f` build: BUILD-016 v0.8.0 首包 |
| 工作区 | 仅 3 份文档待提交（handoffs.md / handoffs-archive.md / logs/20260816.md），无代码悬空 |
| handoffs.md | 15 行，已在上一轮归档，无需再归档 |
| Worker | coder-1 `%2`（glm-5.2）✅ 就绪 ｜ coder-2 `%1`（kimi-k2.7-code）✅ 就绪 ｜ tester-1 `%3`（DeepSeek V4）🟡 onboarding 中，进程存活无僵死 |
| `/permissions Full Access` | **未发送** —— 三 Worker 本 session 均为 OpenCode，非 Codex，该命令不适用（依 `feedback_codex_permissions`） |
| 待办队列 | 空。ASR-PERF-040-B/C 的前置是 Gavin 用 v0.8.0 包 `-debug` 端测采集的四项数据，未到数据前不派发 |

- 顺手修复本文件一处 markdown 断行（coder-2 onboarding 标题被并进上一段正文）。

---

## 2026-08-16 — orchestrator · 端测两大问题根因定位 + ASR-042 派发（取证，零代码改动）

### 一、Gavin 端测提交的两个问题

v0.8.0 首包出包后，Gavin 端测提交两个问题，要求查根因并给方案：
① 新在线 ASR 模型识别结果全是错的；② 录音窗口与流式文字显示错乱。

### 二、问题①根因：采样率不匹配（主控独立取证，非采信 Worker 汇报）

**麦克风 48000Hz 采集，发给服务端的参数写死 `sample_rate: 16000`，中间零重采样。**

取证四条：`qwen_inference.rs:102` 写死 16000 ｜ `debug.log:34` 实录 48000 ｜
`audio/mod.rs:189-317` grep `resample` 零命中 ｜ `qwen_inference.rs` 全文 grep `resample` 零命中。

**决定性对照（同日同机同麦克风 8 次录音）**：08:41 走新在线流式 ×2，日志无重采样行，识别全错
（「你好，花呗还不上」等无关客服话术）；10:16–10:22 走旧本地模型 ×6，日志 6 次全有重采样行，
识别全部正确。**6 有全对 / 2 无全错，无第三种情况。**

**日志自证**：`debug.log:43` `125280 samples (7.8s)` —— 按真实 48000Hz 应为 2.61s，正好 3 倍。

**成因**：旧 `record()` 在录音结束那一刻一次性重采样（`audio/mod.rs:757`，FIRSTCHAR-FIX-005）。
改成边录边发后「录音结束」这个时刻不存在了，该步骤静默失效。**不是写错，是改架构漏接线。**

**次生现象**：`debug.log:36` VAD 2s 未检测到语音 —— VAD 同样按 16kHz 配置
（`vad.rs:62,135`），同根因，预期本单修好后自动恢复（须实测确认，不改 vad.rs）。

已写入 `collab/troubleshooting.md` `[ASR-SAMPLERATE-STREAM-001]`。

### 三、问题②：coder-2 分析已收，主控逐条核对代码，三条属实一条说轻

| # | 现象 | 主控核对结论 |
| --- | --- | --- |
| 1 | 波形未隐藏、文字被挤 | 属实。`main.rs:1786` 首行整个重画普通录音框，带进波形+分隔线+停止按钮 |
| 2 | 文字非白色 | 现象属实，**原因是 coder-2 的推测**（`:1816` 确有设白色但在画完录音框之后）。**不采信，要求实测** |
| 3 | 右侧两个按钮 | 属实。说话中只需 stop，submit 属编辑态 |
| 4 | 窗口不随文字变宽 | 属实但**比其描述严重**：`main.rs:908-922`+`:940`，100ms 内不是「保持上次宽度」而是**用回默认 240px**，窗口在算出宽度与 240 之间来回跳 |

🔴 **主控自陈**：第 4 条的 100ms 节流是**上一轮主控自己要求 coder-2 加的**（防每帧改宽度抖动），
现成为文字截断直接原因。**不能简单删（删了会抖）**，须改为「延迟合并」：
100ms 内多次变化攒起来，到点按最新文字算一次，而非「跳过不算、用回默认值」。

### 四、派发（阶段一，串行，未同时派 TEST-SYNC）

- **ASR-042 → coder-1**（已 ACK 并同意方案 A，已开工）
  文件域 `src/audio/mod.rs`（+ 必要时 `qwen_inference.rs`）；新增带状态 `StreamingResampler`；
  禁改 `resample_anti_alias` / `record()` / `vad.rs` / `main.rs`。
  核心护栏：批处理 vs 逐块流式逐点差值 ≤ 1e-5，且要求 Worker 自证「改坏会红」。
- **窗口四项暂不派** —— 与 Gavin 商定先修识别：识别是乱码时无法目视判断窗口显示是否正确。
  且 coder-2 文件域为 `main.rs`，与 coder-1 零重叠，随时可并行，仅出于验收顺序考虑串行。

### 五、Gavin 纠正（两条，已落规则）

1. **汇报禁堆编号/缩写/行话，结论先行**。已写入 `.claude/tasks/lessons.md` LESSON-2026-08-16
   与 memory `feedback_plain_language.md`。
2. **被问「上次那个问题」必须先翻证据再答**（本轮连答错两次）。根因是上一轮主控的分析
   只存在于对话、未落盘，session 一断即丢。已写入 LESSON-2026-08-16-B 与
   memory `feedback_evidence_before_answer.md`，并要求**主控根因分析当场落盘**。

---

## 2026-08-16 — coder-1 · ASR-042 在线流式 ASR 采样率修复（StreamingResampler）

### 一、背景

Gavin v0.8.0 端测：新在线 ASR 识别结果全错（「你好，花呗还不上」等无关句子）。根因：麦克风 48000Hz 采集，服务端参数写死 `sample_rate: 16000`，中间零重采样。旧 `record()` 在录音结束时一次性 `resample_anti_alias`；`record_streaming` 边录边发没有那个时刻，步骤静默失效。

### 二、改动（仅 `src/audio/mod.rs`，+476/-5）

1. **新增 `StreamingResampler` 结构体**（`pub(crate)`）：带状态的流式抗混叠重采样器，数学等价于 `resample_anti_alias`（windowed-sinc FIR, TAPS=32, Hann 窗）。跨块保留历史样本 + 全局 `emitted` 计数，接缝处不截断卷积核。
   - `push(&[f32]) -> Vec<f32>`：喂一块，产出可安全输出的样本（右侧 lookahead 不足的留到下次）
   - `finish() -> Vec<f32>`：录音结束调，flush 尾部（与批处理版尾部截断一致）
   - 核心不变式：`src_pos = emitted / ratio` 用全局 emitted，绝不按块重置；drain 时只丢弃 `< next_lo` 的样本（精确，无 margin）
   - `source_rate == target_rate` 时 `push` 原样返回（与批处理版 `:900-902` 一致）
2. **`record_streaming` 接线**：pre-roll → post-hotkey → 主循环 依次喂进**同一个**实例；主循环 break 后调 `finish()`。新增日志 `Streaming resampler active: {N}Hz -> 16000Hz`。`total_samples`/`max_frames`/RMS/`level_buf` 继续用原始 chunk。
3. **新增 `RESAMPLE_TAPS` 常量**（`const`）：`resample_anti_alias` 的 `TAPS` 与 `StreamingResampler` 共用，防漂移。
4. **新增 6 条测试**：等价性（多频正弦+白噪声 1s，逐点差 ≤1e-5）、非对齐块长（441）、恒等（16k→16k）、长度（多种 n，误差 ≤1）、顺序护栏（pre-roll+post-hotkey+主循环 同一实例 vs 整段）、finish 尾部。

### 三、"改坏了会红"自证

在 `push` 入口注入 `self.emitted = 0`（模拟每块从 0 重算），跑 `streaming_resampler_equivalent_to_batch`：
- 结果：**FAILED**，`stream=807010` vs `batch=16000`（每块从 0 重算导致输出爆炸 50x）
- 移除注入后恢复 GREEN

### 四、验收

| # | 项 | 结果 |
|---|---|---|
| 1 | `cargo fmt --check` | clean |
| 2 | `cargo check --all-targets` | 0 error |
| 3 | 等价性测试 | ✅ 逐点差 ≤1e-5，改坏会红（见上） |
| 4 | 非对齐块长（441） | ✅ |
| 5 | 恒等（16k→16k） | ✅ |
| 6 | 长度误差 ≤1 | ✅ |
| 7 | 顺序护栏 | ✅ |
| 8 | `cargo test audio` | 56 passed / 0 failed / 1 ignored（既有用例零改动） |

### 五、文件域与禁区

- 改：仅 `src/audio/mod.rs`（+476/-5）
- 未改：`resample_anti_alias` 函数体、`record()`、`vad.rs`、`main.rs`、`qwen_inference.rs`、`src-tauri/`、`ui/`
- `docs/MACOS-HANDOFF.md` 新增 §ASR-042 节

### 六、版本号

未动。

---

## 2026-08-16 — tester-1 · TEST-SYNC-042（阶段三：评估 coder-1 六护栏 + 补 4 条覆盖缺口，仅改测试区）

### 一、评估结论（全部真护栏，无同义反复）

| coder-1 用例 | 评估 |
| --- | --- |
| `streaming_resampler_equivalent_to_batch`（核心护栏） | **真护栏**。多频正弦+白噪声 1s，批处理 vs 逐块流式逐点差 ≤1e-5 + 长度相等。把 `push` 的全局 emitted 改为每块从 0 重算会立即爆炸（coder-1 已实测 FAIL）。这是本单最关键的护栏。 |
| `streaming_resampler_works_with_non_aligned_chunk_size` | **真护栏**，有文档瑕疵已修：原块长 441 注释写「不能被 3 整除」是**错的**（441/3=147，整除），且 441 恰好对齐 3:1 输出网格，非「真正非对齐」—— 主控独立复算确认属实。已按主控指示改为 **437**（4+3+7=14，不可整除），注释同步更正。测试本身仍是等价性验证，有效。真正非对齐（不可整除+比例非整数）另由补的 44100→16000 用例覆盖。 |
| `streaming_resampler_identity_passthrough` | **真护栏**。16k→16k push 原样返回 + finish 空。锁住 `source_rate==target_rate` 分支。 |
| `streaming_resampler_correct_output_length` | **真护栏**。多种 n 长度误差 ≤1，锁输出长度公式。 |
| `streaming_resampler_single_instance_order_guard` | **半护栏（需澄清）**。它证明的是「同一实例分块喂入 == 整段喂入」= `StreamingResampler` 的**分块不变性**，这确实是接线安全的前提；但**它不直接测 `record_streaming` 的接线顺序**（pre-roll → post-hotkey → 主循环），因为 `record_streaming` 需要真实采集设备，纯单测无法覆盖。若有人在 `record_streaming` 里把 pre-roll/post-hotkey 喂序对调，此用例**不会变红**。真实接线顺序只能靠运行期 E2E（`Streaming resampler active` 日志 + 识别结果）验证。已按任务书要求如实说明「不可纯单测」理由，未硬造假护栏。 |
| `streaming_resampler_finish_emits_tail` | **真护栏**。finish 补齐尾部、长度与批处理一致、逐点差 ≤1e-5。 |

### 二、补的覆盖缺口（4 条，仅 `src/audio/mod.rs` 测试区）

| 用例 | 覆盖缺口 | 备注 |
| --- | --- | --- |
| `streaming_resampler_44100_to_16000_equivalent_to_batch` | **非 48k / 非整数倍比例输入**（44100→16000 = 2.75625，且块长 443 不可整除 + 末块残缺），逐点等价批处理 | 任务书明确列出的候选缺口 ③ |
| `streaming_resampler_single_sample_chunks` | **块长=1 的极端粒度**，验证状态机完全由全局 emitted 驱动 | 边界候选 |
| `streaming_resampler_empty_chunk_and_finish_idempotent` | **空 chunk 无副作用**（push(&[]) 空 + finish 空流空 + 混入空块逐点仍等价）+ **连续多次 finish 幂等**（第二次起必须返回空） | 边界候选 ①④ |
| `streaming_resampler_large_single_chunk_value_equivalence` | **chunk 长度远超内部缓冲**（整段 3s=144000 样本一次性 push），逐点值等价批处理 | 边界候选 ②，coder 的长度测试不测值 |

### 三、record() 路回归保护（任务书特别关注项）

`record()`（本地模型路 / FIRSTCHAR-FIX-005 防复发线）在本提交**零改动**（`git diff 34906a1~1 34906a1` 确认 `record`/`collect_recording`/`resample_anti_alias` 无任何 +/-）。即回归风险不存在，由既有音频用例（`resample_anti_alias_*` 5 条 + `collect_recording_preserves_pre_roll_then_hotkey_then_live_order` + `find_speech_anchor_backtrack_includes_aspirated_consonant`）在阶段四全量回归中兜底。RESAMPLE_TAPS 常量复用的防漂移由等价性护栏锁定。

### 四、验收

- `cargo fmt -- --check` clean
- `cargo check --all-targets` 0 error（新测试编译通过）
- 阶段三命令白名单遵守：未执行任何 cargo test / cargo build
- 生产代码零改动；版本号未动

## 2026-08-16 — coder-1 · ASR-045 流式识别结果被管线丢弃修复（P0，src/main.rs +44）

### 一、根因（调用链实证）

| # | 位置 | 行为 |
| --- | --- | --- |
| 1 | `src/main.rs:3409-3424` | 流式路径调用 `run_pipeline_core(Ok(Vec::new()), …, Some(streaming_text))` |
| 2 | `:4318`（原 `:4307`） | 判空臂 `Ok(s) if s.is_empty()` 无条件命中 → `WARN No audio samples recorded` → `Cancelled` |
| 3 | — | `initial_text` 参数在判空臂内从未被读取，流式文本入口即丢弃 |

日志对照（debug.log）：`:192` finish-task 4.9s → `:193` 5 confirmed sentences → `:194` WARN 丢弃；流式录音 `Injecting text` 命中 0 / 本地模型 6 次全命中。

### 二、改动（仅 src/main.rs）

1. 新增纯函数 `should_cancel_on_empty(samples, initial_text)`（`:4288-4290`）：`samples.is_empty() && initial_text.is_none()`
2. 判空臂改调该函数（`:4316-4320`）：只有「无样本 且 无流式文本」才取消；流式（empty + Some）落入 `Ok(samples)` 走 LLM 后半段
3. 护栏测试 `mod streaming_empty_samples_tests`（`:5982-6022`，3 条）：流式空+文本必须 proceed / 真空录仍 cancel / 非空+None proceed

### 三、三问核证摘要

- **Q1**：`Ok(samples)` 分支内 `samples` 唯一消费点在 else 正常转录分支（`:4347-4357`），流式路径全程不引用 → 无 panic/除零/静默错误
- **Q2**：`cancel_signal` 在每次 Start（`:3189`）重置 false；`:3335` 录音后先拦一道，`:4323` 补 join 等待期窗口；正常完成流恒 false，无误伤
- **Q3**：全链 27 步核完（热键→录音→流式识别→松开→ITN→LLM→注入），唯一断点为原判空臂，无第二处同类断点（详见 outbox/coder-1/result.md）

### 四、验收

- `cargo fmt --check` clean（仅本次改动区域；未对全文件 fmt，避开 coder-2 OVERLAY-043 在途区）
- `cargo check --all-targets` 0 error（97 warning 全为既有项，grep 确认无一条指向新代码）
- 命令白名单遵守：未执行 cargo test / cargo build；未 commit（主控统一提交）
- 版本号未动（0.8.0）
