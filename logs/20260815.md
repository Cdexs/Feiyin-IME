# 修改历史 · voice-ime · 20260815

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-08-15 — orchestrator · session 启动状态取证 + todo/progress 补齐 ASR-038 空白（零生产代码改动）

### 一、Worker 启动状态（实测，非采信）

| Worker | pane | 模型 | 状态 |
| --- | --- | --- | --- |
| coder-1 | `%2` | OpenCode | ✅ 自主 ACK 就绪。**非 Codex 类型 → 未发 `/permissions Full Access`** |
| coder-2 | `%1` | kimi-k2.7-code Ollama Cloud | ✅ 自主 ACK 就绪（73.8K/28%） |
| tester-1 | `%3` | DeepSeek V4 Flash Free · OpenCode Zen | 🔧 **启动上下文注入卡在输入框未提交**，主控补发 Enter 后激活并 ACK |

**tester-1 这次是「注入文本已送达但没按 Enter」**，屏幕上能看到完整注入内容停在 composer 里。
与 `[REPLACE-WORKER-INJECT-LOST-001]`（注入被静默吞掉）不是同一形态 —— 那条是内容丢失，
这条是内容在但未提交。**排查时先 `capture-pane` 看 composer 有没有滞留内容，有就补 Enter，不要急着重启。**

三个 `inbox/*/task.md` 与 `outbox/*/result.md` 跨 session **全部被清空为 0 字节**，
与 2026-08-09 记录一致。038-B 任务书正文靠 `logs/20260814.md` 第六节的备份重建，**备份机制生效**。

### 二、编译状态取证：唯一 1 个 error

```
src\transcription\mod.rs:301:52: error[E0425]:
  cannot find function `load_wordbook_vocabulary` in module `crate::transcription`
error: could not compile `voice-ime` (bin "feiyin-ime") due to 1 previous error; 10 warnings
error: could not compile `voice-ime` (bin "feiyin-ime" test) due to 1 previous error; 14 warnings
```

14 个 warning 全为既有 unused variable（`itn.rs:2013` / `punctuation/mod.rs:284` /
`main.rs:4305,4559`），非本批引入、非阻塞。

**主控 `grep` 复核根因，比上次会话记录更精确**：`mod tests` 在 `src/transcription/mod.rs:838` 开、
`:1570` 闭（一直到文件末尾），`pub fn load_wordbook_vocabulary()`（`:1314–1354` 含文档注释）
**被插在 `mod tests` 内部**。虽写在第 0 列看着像顶层，词法上仍属测试模块 → 正式构建不可见。
**修法：移到 `:837` 的 `#[cfg(test)]` 之前，一处改动。**

> ⚠️ 教训延续：上次会话主控靠缩进目测判该函数在顶层 → 错。
> **模块归属以编译器 `help` 输出为准。**

### 三、🔴 环境坑（值得记）：`cargo` 不在任何一个 shell 的 PATH 里

Bash 报 `cargo: command not found`，PowerShell 报 `CommandNotFoundException`，
实际二进制在 `C:\Users\Aaron-GMK\.cargo\bin\cargo.exe`。
**本 session 内跑 cargo 必须先 `export PATH="$PATH:/c/Users/Aaron-GMK/.cargo/bin"`。**
首次误判为「命令挂了」浪费两次后台任务，记此备查。

### 四、文档补齐（本次唯一的文件改动）

`[DOC-STATE-DRIFT-001]` 又一次复现，**且这次漂的是主控自己职责内的两份文档** ——
08-14 整个 ASR-038 批次（035/036/037/038 四份设计 + 038-A 实施 + 版本号 0.8.0）
在 `handoffs.md` 和 `logs/20260814.md` 里记录完整，但 `todo.md` `grep "ASR-038"` **命中 0 次**，
`progress.md` `grep "0.8.0"` **命中 0 次**。

| 文件 | 改动 |
| --- | --- |
| `collab/progress.md` | 新增「🔄 v0.8.0 · 在线 ASR 引擎更替 + 流式上屏」里程碑节（插在 macOS 节之前），含设计四文档表、实施批次表、断点取证、038-B 硬性自证项、四个 commit 清单 |
| `collab/todo.md` | 顶部横幅标注 BUILD-012/015 段落已过期 + `ahead` 提交号 `480a23c`→`2447dbb` 更正；新增「🔄 当前批次 · v0.8.0」节，含断点、任务分解表、**038-B 任务书正文备份** |
| `logs/20260815.md` | 本条 |

**零生产代码改动**（`git status -- src/` 空）。**版本号未动**（已是 0.8.0）。

### 五、下一步执行序列（等 Gavin 指令）

```
① 038-B 收尾（coder-1）—— 修 E0425 + 完成 VAD 门控与流式管线未竟部分
② 主控验收（Read 代码 + cargo check）
③ TEST-SYNC-038（tester-1，阶段三，白名单只放 cargo fmt / cargo check —— DEC-048）
④ TEST-EXEC-038（tester-1，阶段四全量回归）
⑤ ASR-038-C overlay（coder-2）—— 与 B 同动 src/main.rs，必须串行
⑥ BUILD-016 v0.8.0 首包（阶段五，主控须明确下达「现在可以出包」）
```

**边界评估已做**：038-B 与 038-C 文件域在 `src/main.rs` + `src/ui/overlay.rs` 硬重叠，**零并行空间**。
已分别通知 coder-2「等 B 完成」、coder-1/tester-1「勿动 src/ 等派发」。

---

## 2026-08-15 — coder-1 · ASR-038-B 收尾（部分实施，架构项挂起）

### 任务

ASR-038-B 收尾：修编译错误 + 完成流式管线未竟部分。主控裁决后拆为「立即开工」与「挂起」两部分。

### Step 1：修编译错误（E0425）

- **根因**：`load_wordbook_vocabulary` 写在 `mod tests` 内部（`src/transcription/mod.rs:1314-1354`），正式构建不可见
- **改动**：整块（含文档注释）移到 `#[cfg(test)]` 之前的模块顶层
- **验证**：`cargo fmt` clean / `cargo check --all-targets` 0 error（12.57s）

### 立即开工部分（主控裁决后实施）

#### B-1：修三条端点缺陷（Transcriber 独立 Inference API 配置）

主控独立验证并追加两条同族缺陷，三条任一都足以连不上：

| # | 缺陷 | 修法 |
| --- | --- | --- |
| ① | `mod.rs:302` 用 `self.qwen3_url`（Realtime 端点）调 `transcribe_streaming`（Inference 协议） | Transcriber 新增 `qwen_asr_url`/`qwen_asr_model` 独立字段 |
| ② | `qwen_asr_url`/`qwen_asr_model` 全库零消费（grep 证实） | `Transcriber::new` 签名加两参数 + 所有调用点更新 |
| ③ | `default_qwen_asr_url` 主机名 `dashscope.aliyuncs.com` 是 Realtime API 的，035 文档 A6 明确须为 `{WorkspaceId}.cn-beijing.maas.aliyuncs.com` | 改为 `wss://llm-kudx4dj2bfqn4gr2.cn-beijing.maas.aliyuncs.com/api-ws/v1/inference` |

**配套改动**：
- `Transcriber::new` 签名加 `qwen_asr_url`/`qwen_asr_model` 两参数
- `spawn_worker_thread` 两处 `Transcriber::new` 调用更新 + 新增 `reload_qwen_asr_*` 变量
- 热重载触发条件加 `qwen_asr_changed`（QwenAudioOnline 配置变更检测）
- 热重载完成后同步 `active_qwen_asr_*` 跟踪字段
- `transcribe_with_punct_info` 非流式回退分支：`self.qwen3_url` → `self.qwen_asr_url`，`self.qwen3_asr_model` → `self.qwen_asr_model`
- 新增 `qwen_asr_url()`/`qwen_asr_model()` pub getter（供测试 + 未来流式管线用）

#### B-2：transcribe_streaming 加 cancel_signal 参数

- **动机**：EditRequested / ESC 取消时需中断正在进行的 WebSocket 转录
- **改动**：`transcribe_streaming` 签名加 `cancel_signal: Option<&AtomicBool>`
- 函数内 `is_cancelled` 闭包检查，在音频上传循环 + 结果接收循环开头检测
- 取消时 `ws_socket.close(None)` + `bail!("转录已取消")`
- `transcribe_with_punct_info` 非流式回退分支传 `None`（无需取消）
- 两处测试调用更新（加 `None` 参数）

#### B-3：select_preprocessing_params 流式适用性结论化（A 批遗留）

- **结论**：`silence_head`/`onset_backtrack` 是批处理前处理概念，在 `run_pipeline_core` 用于 trim/pad 完整 samples
  - **伪流式（录完整段→发）下完全适用**（有完整 samples 可 trim）
  - **真流式（边录边发）下不适用**（无完整 samples，需重新设计）
  - 本批不做真流式（待 Gavin 拍板），A 批承袭 Qwen3Online 参数（0ms head / 200ms backtrack）正确
- **改动**：注释扩充写明结论，无代码逻辑变更

### 挂起部分（等 Gavin 拍板，勿动）

| 项 | 状态 | 原因 |
| --- | --- | --- |
| 伪流式 vs 真流式 | ⏸ 挂起 | Gavin 亲自推翻过「录完整段再发」前提，要求「边录边显示」。主控已上报等拍板 |
| VAD 入口门控 | ⏸ 挂起 | 依赖流式架构定型 |
| record_streaming() | ⏸ 挂起 | 依赖流式架构定型 |
| 流式管线接线 | ⏸ 挂起 | 依赖流式架构定型 |

### 新增测试（+10 条）

| 文件 | 新增 | 覆盖 |
| --- | --- | --- |
| `src/config/mod.rs` | +4 | default_qwen_asr_url 须含 WorkspaceId + /inference 路径；default_qwen_asr_model 值；url/model 往返 |
| `src/transcription/mod.rs` | +2 | QwenAudioOnline Transcriber::new 存独立配置；空 key bail |
| `src/transcription/mod.rs` | +4（asr_model_from_config 扩展） | qwen_audio_online 解析 |

### 验收

| # | 项 | 结果 |
| --- | --- | --- |
| 1 | `cargo fmt` | clean |
| 2 | `cargo check --all-targets` | 0 error（12.61s） |
| 3 | `cargo test` 全量 | 主 crate 947 passed + 6 ignored（+6 新测试）｜ config 32（+4）｜ 其余全绿 ｜ **0 failed** |
| 4 | `cargo check --manifest-path src-tauri/Cargo.toml` | 0 error（Transcriber::new 签名变更不影响 src-tauri） |
| 5 | 文件域 | `git diff --ignore-cr-at-eol --stat -- src/` = 4 文件（config/mod.rs / main.rs / transcription/mod.rs / transcription/qwen_inference.rs），零越界 |
| 6 | 既有 warning | 14→90（CRLF 噪声放大计数，无新增实质 warning），不修 |

### 硬性自证②（VAD 不吞字）

**本项挂起**——VAD 入口门控属挂起部分（依赖流式架构定型），本批未实施，无吞字风险可评估。

### 生产代码改动

| 文件:行 | 改动 |
| --- | --- |
| `src/config/mod.rs:195-201` | `default_qwen_asr_url` 改为含 WorkspaceId 的 Inference 端点 |
| `src/config/mod.rs:1200-1290` | +4 测试（ASR-038-B 端点回归防护） |
| `src/transcription/mod.rs:82-90` | Transcriber 新增 `qwen_asr_url`/`qwen_asr_model` 字段 |
| `src/transcription/mod.rs:104-166` | `Transcriber::new` 签名加两参数 + QwenAudioOnline 存独立字段 |
| `src/transcription/mod.rs:170-180` | 新增 `qwen_asr_url()`/`qwen_asr_model()` getter |
| `src/transcription/mod.rs:300-320` | 非流式回退分支用 `self.qwen_asr_url`/`self.qwen_asr_model` + cancel_signal=None |
| `src/transcription/mod.rs:897-985` | +6 测试（QwenAudioOnline 配置验证 + asr_model 解析扩展） |
| `src/transcription/qwen_inference.rs:411-430` | `transcribe_streaming` 签名加 `cancel_signal` + `is_cancelled` 闭包 |
| `src/transcription/qwen_inference.rs:535-545` | 上传循环加 cancel 检查 |
| `src/transcription/qwen_inference.rs:555-565` | 接收循环加 cancel 检查 |
| `src/transcription/qwen_inference.rs:1063-1090` | 两处测试调用加 `None` 参数 |
| `src/main.rs:2265-2270` | 新增 `active_qwen_asr_*` 跟踪字段 |
| `src/main.rs:2231-2244` | `Transcriber::new` 调用加两参数 |
| `src/main.rs:2357-2380` | 热重载触发条件加 `qwen_asr_changed` |
| `src/main.rs:2382-2400` | reload 块加 `reload_qwen_asr_*` 变量 + 调用更新 |
| `src/main.rs:2310-2316` | 热重载完成同步 `active_qwen_asr_*` |
| `src/main.rs:3246-3256` | `select_preprocessing_params` 注释扩充写明流式适用性结论 |

### 版本号

未动（已是 0.8.0）。


---

## 2026-08-15 — coder-2 · TRANS-HOTKEY-039 翻译热键全链失效修复（P0）

### 根因与修复

| 根因 | 位置 | 修复 | 说明 |
| --- | --- | --- | --- |
| A | `ui/src/pages/HotkeySettings.tsx:43` | `VK_TO_LABEL` 中 0xA0→Left Shift、0xA1→Right Shift | 原标签左右互换，导致 UI 把物理左 Shift（160）显示成 "Right Shift"，用户按物理右 Shift（161）永远不触发 |
| A-补充 | `ui/src/pages/HotkeySettings.tsx:23` | `CODE_TO_VK` 保持正确映射 `ShiftRight:0xA1 / ShiftLeft:0xA0` | 捕获路径本身正确，无需改动；UI 显示表是唯一错误点。 Gavin 配置中存的 160 来自物理左 Shift，被错标签误导为 "Right Shift" |
| B | `src/platform/windows/hotkey.rs` | 轮询线程从固定 500ms 改为跟随录音生命周期；新增 `TRANSLATE_POLL_STOP` 静态 AtomicBool | PTT 松开 / Toggle 二次按下 / hook 卸载 / poll_ptt_release_thread 结束均置位 stop，确保线程明确退出 |
| B-兜底 | `src/platform/windows/hotkey.rs` | 硬上限 `MAX_RECORD_SECONDS + 5` 秒（305s） | 即使所有 stop 路径都漏置，线程也不会泄漏 |
| 观测性 | `src/platform/windows/hotkey.rs` | `translate_flag.store(true)` 处补 `log::info!` | 日志写明「Translation hotkey pressed after Xms of recording, translate enabled」 |
| C | `docs/MACOS-HANDOFF.md` | 新增 §TRANS-HOTKEY-039 节 | macOS 侧翻译热键硬编码 false、从未实现，给出若要对齐需做的 5 项工作 |

### 跨文件接线

- `src/platform/windows/hotkey.rs` 新增 `pub fn notify_translate_poll_stop()`。
- `src/platform/windows/mod.rs` + `src/platform/mod.rs` re-export 该函数。
- `src/main.rs` 6 处录音终止路径调用 `platform::notify_translate_poll_stop()`：
  - `:1978` `HotkeyEvent::CancelStop`
  - `:1987` ESC 取消
  - `:2028` `PipelineEvent::Done | PipelineEvent::Cancelled`
  - `:2032` `PipelineEvent::FocusLost`
  - `:2045` `PipelineEvent::Error`
  - `:2063` `PipelineEvent::FormatFailed`

### 验证

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cargo fmt` | clean |
| 2 | `cargo check --all-targets` | 0 error |
| 3 | `cargo check --manifest-path src-tauri/Cargo.toml --all-targets` | 0 error |
| 4 | `npm run build`（`ui/` 目录） | ✅ 48 modules / dist 重建 |

### 版本号

未动（已是 0.8.0）。

---

## 2026-08-15 — coder-1 · ASR-041-B 清除旧在线 ASR 代码路径 + 字段改名通用名

### Gavin 指令

「连代码一起清掉，不用保留旧的在线 asr 模型作为回退兜底。」+ `qwen3_api_key` 改成 `asr online api key`（通用名，与具体模型解耦）+ `qwen_asr_url/model` 也改通用名。

### 删除清单

| # | 内容 | 位置 |
| --- | --- | --- |
| 1 | `AsrModel::Qwen3Online` 枚举变体 | transcription/mod.rs |
| 2 | `src/transcription/qwen3_online.rs` 整个文件（686 行） | 已删 |
| 3 | `mod qwen3_online` 声明 | transcription/mod.rs |
| 4 | `transcribe_online()` 调用分支 | transcription/mod.rs |
| 5 | `qwen3_asr_url`/`qwen3_asr_model` 配置项 | config/mod.rs + src-tauri/config.rs |
| 6 | `qwen3_url`/`qwen3_asr_model` Transcriber 字段与参数 | transcription/mod.rs |
| 7 | `qwen3_changed` 热重载检测 | main.rs |
| 8 | 所有 `Qwen3Online` match 臂 | main.rs + transcription/mod.rs |
| 9 | 引用 `Qwen3Online` 的测试 | main.rs + transcription/mod.rs |

### 保留清单（不能删）

| # | 内容 | 理由 |
| --- | --- | --- |
| ① | `asr_online_api_key` 字段（原 `qwen3_api_key`）+ serde alias | 新引擎在用，alias 防 Gavin 的 config 静默丢 key |
| ② | `f32_to_pcm16_le` 函数 | 搬家到 qwen_inference.rs（含单测），非删除 |
| ③ | `qwen3_online → qwen_audio_online` 存量迁移 | Gavin 的 config 仍写 qwen3_online，迁移在 load 阶段改写 |
| ④ | `AsrModel::Accuracy` | Gavin 明确保留 |

### 字段改名（三字段统一 asr_online_ 前缀）

| 旧名 | 新名 | serde alias |
| --- | --- | --- |
| `qwen3_api_key` | `asr_online_api_key` | `alias = "qwen3_api_key"` |
| `qwen_asr_url` | `asr_online_url` | `alias = "qwen_asr_url"` |
| `qwen_asr_model` | `asr_online_model` | `alias = "qwen_asr_model"` |

影响面：src/ 24+处、ui/src/ 24 处、src-tauri/src/ 3+处，全部同步。i18n key `voice_qwen3_api_key` → `voice_asr_online_api_key`，文案 `Qwen3 API Key` → `ASR API Key`。

### src-tauri/src/config.rs 补字段（038-A 遗留隐患修复）

主 config 有 `asr_online_url`/`asr_online_model` 但镜像侧完全没有这两个字段 → Gavin 每次保存设置就把这两个新字段静默丢弃。本单一并补上（与主 config 同名同默认值 + alias）。

### 验收

| # | 项 | 结果 |
| --- | --- | --- |
| 1 | cargo fmt / cargo check --all-targets | clean / 0 error |
| 2 | cargo check --manifest-path src-tauri/Cargo.toml | 0 error |
| 3 | cargo test transcription | 136 passed 0 failed 7 ignored |
| 4 | cargo test config | 41 passed 0 failed 2 ignored（含 alias 单测） |
| 5 | npm run build | 通过 |
| 6 | grep Qwen3Online src/ | 零代码命中（仅注释） |
| 7 | grep qwen3_online src/ | 仅迁移逻辑字符串字面量 + 注释 |
| 8 | grep qwen3_api_key src/ | 仅 alias + 测试 |
| 9 | AsrModel 变体 | 恰好三个：Performance / Accuracy / QwenAudioOnline |
| 10 | serde alias 单测 | 含旧字段名 toml 能正确读入 key ✅ |

### 版本号

未动（已是 0.8.0）。

### Gavin 指令

「把 UI 下拉的 asr 模型选项文本改成**在线语音识别模型**，选项指向新集成的 ASR 模型，用新模型替代旧模型，改选项文本名称。」—— 产品层替代，非新增，非删代码。

### 五处改动

| # | 文件 | 改动 |
| --- | --- | --- |
| ① | `Voice.tsx:191` | 下拉 option value `qwen3_online` → `qwen_audio_online` |
| ② | `Voice.tsx:156` | desc case `qwen3_online` → `qwen_audio_online` |
| ③ | 三份 i18n `:33-34` | zh-Hans `在线语音识别模型` / zh-Hant `線上語音辨識模型` / en `Online Speech Recognition`；_desc 去 Qwen3 字样（`云端运行，通过阿里云 API 进行语音识别。`） |
| ④ | `config/mod.rs` load+load_from | 存量 `qwen3_online` 静默迁移为 `qwen_audio_online`（照抄 accuracy→performance 先例，含 log+落盘保存）+ 文档注释更新 |
| ⑤ | `Voice.test.tsx` | 17 处 `qwen3_online` → `qwen_audio_online`；`Voice.tsx` 另有 4 处逻辑判断也同步 |

### Voice.tsx 额外 4 处

任务书只提了 2 处（:191/:156），但 Voice.tsx 还有 4 处 `qwen3_online` 逻辑判断（:44/:80/:83/:243——切换时检查 key、显示 key 输入框），也同步为 `qwen_audio_online`，否则切换到在线模式后 key 输入框不显示、卸载时回退逻辑失效。

### 存量配置静默迁移

Gavin 当前 config 是 `asr_model = "qwen3_online"`。UI 改完后下拉只剩 `performance`/`qwen_audio_online`，`qwen3_online` 不匹配任何 option → 会静默落到 performance。迁移在 load+load_from 两处加，含 log+落盘保存，照抄 accuracy→performance 先例。

### 验收

| # | 项 | 结果 |
| --- | --- | --- |
| 1 | cargo fmt / cargo check --all-targets | clean / 0 error |
| 2 | npm run build | 通过（655ms） |
| 3 | 下拉 value | `qwen_audio_online`，ui/src 不再出现 `qwen3_online` |
| 4 | i18n 三份 | 无 Qwen3 字样，繁体已改 |
| 5 | 迁移 | load+load_from 两处都加，含 log；+2 单测（迁移+零回归） |
| 6 | Voice.test.tsx | 17 处已同步 |
| 7 | 全库自证 | `grep -rn "qwen3_online" ui/src` 零命中 ✅ |
| 8 | 文件域 | 6 文件（Voice.tsx/Voice.test.tsx/en/zh-Hans/zh-Hant/config/mod.rs） |

### 版本号

未动（已是 0.8.0）。

---

## 2026-08-15 — coder-1 · ASR-038-B 真流式核心实施（C-1~C-4 + 040-A）

### Gavin 拍板 + 硬指令

1. 真流式（边说边上屏是验收判据，非 nice-to-have）
2. 必须用真 VAD（Silero），RMS 被否决（分不清人声与键盘声/风扇声/音乐）
3. accept_waveform 可滚动用（不调 flush 轮询 front/detected），主控取证纠正我的「批处理式」误判

### C-1：VadSegmenter 新增滚动方法（vad.rs）

- `try_new_for_streaming(model_dir)`：工厂方法，阈值 0.3（低于分段用 0.5），其他配置同 `try_new`
- `accept_and_check(&[f32]) -> bool`：逐窗口喂 `accept_waveform` → 查 `detected()`（sherpa-onnx C API `SherpaOnnxVoiceActivityDetectorDetected`），不调 flush
- `reset_for_new_session()`：会话结束重置（clear+reset）
- `vad_window_size()`：导出窗口大小供流式入口用
- **不改 `segment()`**（FIX-VAD-STATE-RESET-001 在里面）
- +6 测试（2 不依赖 ORT：阈值常量+模型缺失 None；4 依赖 ORT 标 `#[ignore]`）

### C-2：record_streaming（audio/mod.rs）

- 热键按下即采集（WASAPI prewarm stream 已在跑，从 channel 读 chunk 推回调）
- pre-roll 从 VecDeque drain 后作为首批 chunk 推给回调（建连后补发）
- RMS 静音检测管停录（speech_detected 行为不变，与 Silero VAD 并存）
- **不改 record()**：平行方法，record 的 collect_recording 零动
- +155 行

### C-3：transcribe_streaming_realtime（qwen_inference.rs）

真流式 ASR 核心：
1. **VAD 入口门控**：逐 chunk 喂 Silero（accept_and_check），命中即建连。VAD 缺失→立即建连（保底）。2s 保底无条件建连
2. **建连**：DNS→TCP→TLS→WS 握手→run-task→task-started（含 040-A 分段 [Latency] 埋点）
3. **pre-roll 补发**：VAD 命中前+握手期间缓冲的 chunk 建连后一次性发
4. **边发边收**：录音期间用 1ms read_timeout 模拟非阻塞读 ws_socket，chunk_rx try_recv 读音频，交替发送+收 result-generated→on_result 回调推 StreamingText
5. **finish-task**：chunk_rx 断开（录音结束）→恢复 10s timeout→发 finish-task→阻塞收最终结果
6. cancel_signal 在各循环检查点检测
- +447 行

### C-4：worker 线程 QwenAudioOnline 分支接线（main.rs）

- `is_streaming_asr` 判断：desired_asr_model == QwenAudioOnline && transcriber.asr_model() == QwenAudioOnline
- 流式分支：创建 chunk channel → spawn ASR 线程跑 transcribe_streaming_realtime → worker 跑 record_streaming 推 chunk → drop chunk_tx → join ASR 线程拿最终文本 → run_pipeline_core(initial_text=Some(text)) 走 LLM 后半段
- 非流式分支：现有 record()+run_pipeline_core(None) 零行为变更

### run_pipeline_core initial_text 参数

- 新增 `initial_text: Option<String>` 参数
- None = 正常模式（FIRSTCHAR-FIX-006 前处理 + transcribe_with_punct_info）
- Some(text) = 流式模式（跳过转录，直接走 ITN→LLM→注入后半段）
- **FIRSTCHAR 专项自证**：非流式前处理链逐字节等价
  - SPEECH_ENERGY_THRESHOLD=0.008 不变
  - select_preprocessing_params / find_speech_onset_with_backtrack 调用参数不变
  - padded 构造（resize silence_head + extend trimmed）不变
  - is_accuracy 变量内联为 if 表达式（log 输出值相同）
  - Err 路径：原版 match 内 send_event，搬迁后 .map_err + 外层 match send_event（行为等价）
  - 6 条 FIRSTCHAR 单测全绿

### 040-A：DNS/TCP/TLS/WS 分段 [Latency] 埋点

- `transcribe_streaming`（伪流式）和 `transcribe_streaming_realtime`（真流式）都加
- 每段 Instant::now() + elapsed() 日志，对齐 audio/mod.rs 的 [Latency] 前缀风格
- 只加埋点不改连接逻辑（028 教训：tungstenite 无池，预热/复用会复现 pool_idle bug）

### 硬性自证② VAD 不吞字

1. **热键按下即采集**：record_streaming 从 WASAPI prewarm stream 的 channel 读 chunk，不等待 VAD 判定。VAD 只决定「何时建连」，不影响「何时开始采集」
2. **pre-roll 补发**：VAD 命中前+握手期间（~200-500ms）的 chunk 全部缓冲在 pre_roll_buffer，建连后一次性补发。pre-roll 600ms 覆盖热键前到建连完成的全部音频
3. **2s 保底**：VAD 2s 未命中无条件建连（防 VAD 漏检吞字）
4. **VAD 缺失降级**：模型缺失→立即建连（宁可多花钱不可吞字）

### 验收

| # | 项 | 结果 |
| --- | --- | --- |
| 1 | cargo fmt | clean |
| 2 | cargo check --all-targets | 0 error |
| 3 | cargo test | 主 crate 949+10ign（+2 新非ORT测试）｜ config 32+2ign ｜ 其余全绿 ｜ **0 failed** |
| 4 | 文件域 | 6 文件（audio/config/main/transcription mod/qwen_inference/vad），零越界 |
| 5 | FIRSTCHAR 等价 | 逐字节等价自证完成 |
| 6 | 040-A 埋点 | 两个 transcribe 函数都含 DNS/TCP/TLS/WS 分段 [Latency] |

### 版本号

未动（已是 0.8.0）。

---

## 2026-08-15 — tester-1 · TEST-SYNC-038 阶段三：ASR-038-B + TRANS-HOTKEY-039 测试用例（只写测试，零生产改动）

### 任务
- 7 项关键覆盖对标（vad 降级 / record_streaming pre-roll / record() 零改动 / 端点配置 / 两套端点不混淆 / 硬上限 MAX_RECORD_SECONDS+5 / WM_KEYUP mode==0）
- 基线 HEAD `4f3b41b`；命令白名单 `cargo fmt` + `cargo check --all-targets`；6 允许文件域；main.rs 禁碰（coder-2 在途）

### 新增 10 用例（5 文件）

| 文件 | 用例 | 钉住契约 |
| --- | --- | --- |
| src/audio/mod.rs | `collect_recording_preserves_pre_roll_then_hotkey_then_live_order` | record() 旧路径拼装顺序（pre_roll→post_hotkey→live）+ 静音停录 + 峰值归一 |
| src/audio/mod.rs | `collect_recording_fails_when_stream_failed` | stream_failed → collect_recording Err |
| src/platform/windows/hotkey.rs | `translate_poll_hard_deadline_is_max_record_plus_5` | MAX_RECORD_SECONDS+5=305 + 四常量不漂移 |
| src/platform/windows/hotkey.rs | `target_mode_mapping_pushes_to_talk_to_1_toggle_to_0` | Toggle→0 下 WM_KEYUP 不置 TRANSLATE_POLL_STOP（039 缺陷护栏） |
| src/config/mod.rs | `qwen_asr_url_differs_from_qwen3_asr_url_default` | Inference 与 Realtime 默认端点显式不同（缺陷①配置层回归） |
| src/config/mod.rs | `max_record_seconds_is_300` | 硬上限唯一权威来源 |
| src/transcription/qwen_inference.rs | `transcribe_streaming_realtime_empty_api_key_bails` | C-3 实时入口空 key 立即 bail |
| src/transcription/qwen_inference.rs | `streaming_inference_constants_match_defaults` | DEFAULT_MODEL/AUDIO_CHUNK_BYTES/SILENCE/CONNECT_TIMEOUT 钉住 |
| src/transcription/vad.rs | `vad_window_size_is_512` | vad_window_size()==512（32ms @16kHz） |
| src/transcription/vad.rs | `vad_streaming_and_batch_factories_agree_on_missing_model` | 两工厂缺失模型降级行为一致 |

### 对应关系
- #2/#3 record() 旧路径钉住用「直接调 collect_recording」（生产函数，零改生产下比 record_streaming 更好测）
- #7 / 039 采用「模式映射静态断言」（TARGET_MODE 仅两来源；不做 SetWindowsHookExW 真实窗口调用）
- 网络/硬件路径（真实 ws 建连、VAD 门控送音）→ 写入 result.md「用例规格表」交主控（038-C 后另派 038-B）

### 验收
- cargo fmt clean（曾遇 qwen_inference.rs 括号不配对 → 修复后 clean，教训复现）
- cargo check --all-targets：中途 main.rs（coder-2 WIP）有 GetDC/adjust_overlay/EditSubmitting 瞬态 error，git stash 自证非本任务引入；重跑 0 error
- 文件域：5 文件全部在 6 允许域内；main.rs/macos/**/tests/** 零触碰
- 收尾：result.md（7 项对标 + 用例规格表 + 收尾自证表）+ logs/CHANGELOG/handoffs + tmux 通知主控

### 版本号

未动（已是 0.8.0）。

### 🔴 验收返工（主控两意见已闭环）
1. **#7 由 ✅ 降为 ⚠️**：`target_mode_mapping_pushes_to_talk_to_1_toggle_to_0` 中 `map`/`stops_on_keyup` 是测试内自声明闭包断言自己 = 同义反复，与生产（:226-229 / :197）零耦合——有人把 `store(true)` 移出 `if mode==1` 该用例照样绿。根因是 `keyboard_hook_proc` 为 unsafe extern system + 全局静态，阶段三禁改生产故不可直接单测。真护栏 = 抽生产侧纯函数再测（030-D/E 先例），主控另开单。已改：result.md #7 行 ⚠️ + hotkey.rs 用例 doc ⚠️ 标注。
2. **删除 `assert_eq!(TRANSLATE_WINDOW_MS, 500)`**：主控 grep 证实该常量已死（:30 定义，生产零使用，500ms 窗口被硬上限取代=039 根因 B）；钉死暗示行为仍在，有害。已删除，常量列入 result.md「待清理死代码」交主控。

---

## 2026-08-15 — coder-1 · TRANS-HOTKEY-039-D 抽判据纯函数 + 清理死常量

### 背景

TRANS-HOTKEY-039 修复根因 B 时，`WM_KEYUP` 的 `if mode == 1` 停止轮询判据内联在 `unsafe extern "system" fn keyboard_hook_proc` 里，不可直接单测。tester-1 补的用例是闭包自述（同义反复），与生产零耦合——把 `store(true)` 移出 `if` 门控那用例照样绿。

### 三件事

| # | 内容 | 改动 |
| --- | --- | --- |
| ① | 抽 `should_stop_translate_poll_on_keyup(mode: u32) -> bool` | 模块级私有纯函数，钩子 `:213` 调用替换 `if mode == 1` |
| ② | 抽 `hotkey_mode_to_u32(mode: HotkeyMode) -> u32` | 模块级私有纯函数，`install_keyboard_hook:227` 调用替换 `if mode == PushToTalk { 1 } else { 0 }`。消除三处各自硬编码 1/0 的漂移风险 |
| ③ | 删死常量 `TRANSLATE_WINDOW_MS` | `:30` 定义 + `:1032` 注释提及，全库零使用（独立 grep 取证确认）。删除后无残留警告 |

### 真护栏测试（+4，替换假护栏）

| 用例 | 断言 | 与生产耦合 |
| --- | --- | --- |
| `hotkey_mode_to_u32_maps_ptt_to_1_toggle_to_0` | PTT→1, Toggle→0 | 调生产函数 `hotkey_mode_to_u32` |
| `should_stop_translate_poll_on_keyup_ptt_returns_true` | PTT 抬起应停止 | 调生产函数 `hotkey_mode_to_u32` + `should_stop_translate_poll_on_keyup` |
| `should_stop_translate_poll_on_keyup_toggle_returns_false` | **Toggle 抬起不得停止**（核心） | 同上 |
| `mode_mapping_and_keyup_predicate_are_semantically_consistent` | 映射输出喂判据，结果符合预期 | 两个生产函数联动，防漂移 |

**为什么这次是真护栏**：tester-1 那版自己定义闭包 `let stops_on_keyup = |mode: i32| mode == 1` 再断言闭包符合自身定义——与生产代码零耦合。本批 4 条用例调 `hotkey_mode_to_u32` 和 `should_stop_translate_poll_on_keyup` 两个**生产函数**，把判据语义改错或映射改错测试会变红。

**本单护住的范围**：判据语义与映射漂移。调用点的结构性放置仍无自动护栏——把 `store(true)` 从 `if` 块内挪到外面变成无条件执行，判据函数仍返回正确值，测试照样绿。要真正结构性护住需把整个 `keyboard_hook_proc` 决策抽成返回 action 枚举的纯函数再测，那是钩子热路径大重构，风险收益不划算。现状已把「意外改错」门槛提得足够高：原来是裸的 `if mode == 1`，现在要绕过一个有名字有文档的判据。调用点结构性放置依赖 code review。

### 验收

| # | 项 | 结果 |
| --- | --- | --- |
| 1 | cargo fmt | clean |
| 2 | cargo check --all-targets | 0 error |
| 3 | cargo test hotkey | 23 passed + 0 failed（含 4 新真护栏） |
| 4 | 行为零变更 | PTT 抬起仍停、Toggle 抬起仍不停（`should_stop_translate_poll_on_keyup` 逻辑等价 `mode == 1`） |
| 5 | 死常量 | `TRANSLATE_WINDOW_MS` 已删，`grep -rn` 全库零残留 |
| 6 | 文件域 | `git diff --stat` 只有 `src/platform/windows/hotkey.rs`（+90/-6） |

### 版本号

未动（已是 0.8.0）。

---

## 2026-08-15 — coder-2 · ASR-038-C 返工修复（P0 编辑态 + 托盘 + 100ms 尺寸节流）

### 任务

主控验收 ASR-038-C 不通过，指出 3 项问题：

| # | 问题 | 根因 | 修法 |
| --- | --- | --- | --- |
| 1 | 编辑态一进就被销毁（P0） | `EditRequested` 置 `cancel_signal=true` → worker 发 `PipelineEvent::Cancelled` → `Done\|Cancelled` 分支无条件 `Hide` → `Hide` 调 `destroy_edit_control()` | 双保险：① 主控侧新增 `OVERLAY_EDITING` 原子标志，处于编辑态时 `Done\|Cancelled` 不发 `Hide`；② overlay 侧 `Hide` 处理加 `StreamingEditing` 守卫，遇到该态直接忽略 |
| 2 | 托盘编辑态被复位为 Idle | 同 `Done\|Cancelled` 分支无条件 `set_tray_state(Idle)` | 编辑态标志为 true 时跳过托盘复位，保持 `Recording` |
| 3 | 100ms 尺寸节流缺失 | 注释误把 16ms timer 当唯一节流，未实现 037 §5.2 要求的第二道尺寸防抖 | `OverlayWindowState` 新增 `last_resize_time: Option<Instant>`；`RecordingWithText` 每次 `Show` 时至少间隔 100ms 才调用 `adjust_overlay_pos_size_for_text` |

### 改动文件

- `src/main.rs`（73 行插入 / 9 行删除）

### 关键代码位置

- 编辑态标志定义：`src/main.rs:170-176`
- `Done\|Cancelled` 守卫：`src/main.rs:2771-2780`
- `Hide` 侧 `StreamingEditing` 兜底：`src/main.rs:1003-1013`
- 100ms 尺寸节流：`src/main.rs:901-950`
- 标志复位点：`RecordingStarted`、`FocusLost`、`Error`、`FormatFailed`、`CancelRequested`、`SubmitRequested` 成功 / UIPI 降级路径

### 验收

| 项 | 结果 |
| --- | --- |
| cargo fmt | clean |
| cargo check --all-targets | 0 error（既有 warning 未新增） |
| cargo test | PASS |
| cargo check --manifest-path src-tauri/Cargo.toml --all-targets | PASS |

### 版本号

未动（已是 0.8.0）。
