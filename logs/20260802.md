# 修改历史 · voice-ime · 20260802

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-08-02 — coder-2 FORMAT-F3-SHORTITEM-014：多行分支补「短项内联 / 长句列表」分流 + 四语措辞补充

**改了什么**：`src/llm/mod.rs` 的 `build_output_format`（真/假两分支 + 新增共享常量 `INLINE_SEPARATOR_RULES`），返回类型 `&'static str` → `String`。

**改动 A · 短项内联/长项列表分流（重点）**：多行分支在 DECISION RULE 确认枚举（≥2 项）后新增 **F3-item form** 二维分流——

| 项目形态 | 输出 | 例 |
| --- | --- | --- |
| SHORT（无谓语、无内部标点、≤6 字/词） | **内联分隔符，不做列表** | `买了3斤土豆、一个西瓜、20斤大米、还有3斤香蕉` |
| LONG（含谓语或内部标点） | **列表**（有序 `1. `/无序 `- `） | `- 有些学生头发过长` |

边界：混合长短按**多数项**决定、仍不确定**用列表**（对长项更安全）；有序短项保留序号词（`第一个土豆，第二个西瓜` 内联）。

**改动 B · 四语措辞补充**：中文 `再者/最后一点/另外一点/第X条`；English `in addition/plus/and then/namely`；日本語（有序最薄仅 3 组）`①②③/最初に/続いて/それに/加えて/ほかにも`；한국어 `또/이어서/끝으로/아울러`。**单行分支标记词同步补齐**至与多行分支覆盖一致（Python 核对 18 词两分支 count=2 全 OK）。

**分隔符表抽共享常量**：五语表从假分支提取为 `INLINE_SEPARATOR_RULES` 常量，两分支 `format!(..., INLINE_SEPARATOR_RULES)` 共用——保证分隔符规则**字面一致**（避免两套说法漂移，DEC-038 精神）。为此 `build_output_format` 返回 `String`（真分支需运行时拼接常量）。`format!` 中 JSON `{` 需转义为 `{{`（:891/:924）。

**为什么**：Gavin 2026-08-02 端测 `今天出去买菜了，买了3斤土豆，一个西瓜，20斤大米，还有3斤香蕉`（msedge/Memos/multiline_safe=true）被拆成四行 `- ` 列表——短名词短语清单应顿号内联。根因：短项 `、`/长句 `；` 规则只存在于 `multiline_safe=false` 分支，`true` 分支没有「短 vs 长」区分。正确规则是两维（数量 ≥2 判枚举已有；项目长度判列表 vs 内联缺多行分支）。

**验证**：
- `cargo check` + `cargo check --tests` 双 0 errors
- `cargo test --bin feiyin-ime llm::` **114 passed / 0 failed** **零红条**（tester-1 已把断言锚到 build_output_format，改动保留全部关键措辞；新增分流不破坏任何断言）
- UTF-8 Python 验证无 mojibake（U+FFFD=0）；18 个补充词两分支覆盖一致
- 字符数（临时测试测量后删除）：`build_output_format(true)`=6371 / `(false)`=3345（对比 HEAD 版字面量 4297/2649，增长主要为分流规则 + 四语词，均紧凑列举未造句）
- `cargo fmt -- src/llm/mod.rs` 连带 3 处：既有测试块 :1659/:1872/:1934 三条 `assert!` 长行 rustfmt 重排（零逻辑变化，[FMT-COLLATERAL-001] 保留）

**影响范围**：仅 `src/llm/mod.rs` 的 `build_output_format`。`src/text_normalizer.rs`/`src/scene/mod.rs`/`scene-rules.toml`/`src/itn.rs`/`itn-rules.toml`/`src/main.rs`/`src-tauri/**`/`ui/**` 零触碰。未构建/出包/启动 exe；未改版本号（0.7.3）；未用 git 破坏命令；UTF-8 用 edit 工具。

---

## 2026-08-02 — coder-2 FORMAT-F3-SHORTITEM-015：F3b 与输出契约对称补 LONG 限定（收口 014 的 recency 冲突）

**改了什么**：`src/llm/mod.rs` 的 `build_output_format` 真分支四处文本（三处语义 + 一条示例），假分支零改动。

| # | 位置 | 改动 |
| --- | --- | --- |
| 1 | F3b 标题 :872 | 补 `AND items are LONG`，与 F3a :869 的 `items are LONG` 对称 |
| 2 | F3b 正文 :875-876 | `List items may be FULL SENTENCES ... do NOT need to be short noun phrases.` → 语义反转 `List items here are FULL SENTENCES ... SHORT noun phrases MUST NOT be bulleted — per F3-item form above they are joined INLINE with the enumeration separator.`（`Narrative exemplification` 句保留） |
| 3 | Output format :890-892 | `MUST span multiple lines when F3 applies` → `when F3 applies AND F3-item form routes the items to a LIST`；补 SHORT 内联例外 `or when F3-item form routes SHORT items INLINE, output a single continuous paragraph` |
| 4 | F3c :882 | 插入 `- Chinese SHORT items inline (enumeration confirmed but NO list): "今天出去买菜了，买了3斤土豆，一个西瓜，20斤大米，还有3斤香蕉" → "今天出去买菜了，买了3斤土豆、一个西瓜、20斤大米、还有3斤香蕉".` |

**为什么**：主控独立 Read 取证发现 014 不对称缺口——新增的 F3-item form（SHORT→内联）位于 F3a/F3b 之前，而下游两处仍无条件要求 bullet/多行且位置更靠后 recency 更高，正是 `src/llm/mod.rs` 注释 :813-817 记载的「后段软化前段」失败模式。Gavin 买菜用例（无序 + 短名词短语）恰好命中，端测大概率仍被拆成 4 行 bullet。三处改动消除下游与 F3-item form 的直接矛盾。

**验证**：
- `cargo check` + `cargo check --tests` 双 0 errors
- `cargo test --bin feiyin-ime llm::` **113 passed / 1 failed**——唯一红 `build_format_instruction_block_f3_exemplification_enumeration:1856` 断言 `safe.contains("may be FULL SENTENCES")`，**断言过时**（任务书改动2明确删除该措辞，断言仍锚旧措辞），归 tester-1 TEST-SYNC 换锚点，未改断言
- Python 解码后核对全 PASS：`AND items are LONG`/`MUST NOT be bulleted`/买菜内联示例/`Narrative exemplification` 保留/Output format 含 `routes SHORT items INLINE`；全文件不再含 `they do NOT need to be short noun phrases`
- UTF-8 无 mojibake（U+FFFD=0）
- 字符数（解码后）：`build_output_format(true)` HEAD=5183 → NEW=5511（+328）

**影响范围**：仅 `src/llm/mod.rs` 的 `build_output_format` 真分支。假分支/`src/text_normalizer.rs`/`src/scene/mod.rs`/`scene-rules.toml`/`src/itn.rs`/`itn-rules.toml`/`src/main.rs`/`src-tauri/**`/`ui/**` 零触碰。未构建/出包/启动 exe；未改版本号（0.7.3）；未用 git 破坏命令；UTF-8 用 edit 工具。

## 2026-08-02 — coder-1 ITN-FIX-GRADECLASS-016：年级班级简写被逐位串误合并

**改了什么**：`src/itn.rs` + `itn-rules.toml`。

- **方案协商**：主控原方案「跳过 serial early return 落进位组合路径」经分析会产出 `("3",2)`（末位 digit 覆盖）→「3班」撕裂。协商采纳正确落点：`parse_cn_number` 守卫命中时直接 `return None`，主循环按单字路径处理，班非单位/量词 → 保持汉字。
- **数据层**：itn-rules.toml 新增 `[protect.serial_suffixes]`（`words = ["班"]`），未来加「届/排」只改数据。
- **代码层**：`Protect` 结构体加 `serial_suffixes: ProtectList`；`CompiledRules` 加 `serial_suffix_set: HashSet<String>`；`from_rules` 填充；`parse_cn_number` 加守卫（`serial_len == 2 && 后继命中 serial_suffixes` → return None）；订正过时注释 `连续≥3` → `连续≥2`（不改实现，保 date_short_year）。
- **主控两个复核点**：① 链扫描三处 None 分支均 break 推进无死循环，且守卫 None 短路使 :1622 永不到达 ② 甲乙丙型 `?` 传播期望行为，既有断言零误伤。

**为什么**：Gavin 2026-08-02 端测 `我是一三班的学生`（=一年级三班）被转 `13班`。根因：`parse_cn_number` 逐位串路径 `serial_len >= 2 && !next_is_unit` 把「一三」当两位数，`decide_conversion` `consumed>=2` 无条件转。DEC-038：保护词表不得承载规则性语法族，词表化需 81+ 条且随前缀组合爆炸；「五一」在 proper_nouns:「一三」不在，同构表达行为相反，正是 DEC-038 病症。

**验证**：目标用例 4 条全过（一三班/五一班/初二三班/高一四班 全汉字）；反向护栏 10 条全过（九八年→98年、三零二房间→302、二零二六→2026、幺三八零零→13800、三年二班保持、十三班→13班现行为实测、一班/三班保持、五一/五一广场保护）；`cargo test --bin feiyin-ime itn::` **128 passed / 0 failed**；全量 **723/1/6**（唯一红 `llm::tests::build_format_instruction_block_f3_exemplification_enumeration` = coder-2 并行域断言过时，src/llm/mod.rs 工作区零改动，归 tester-1 TEST-SYNC）。

**影响范围**：仅 `src/itn.rs` + `itn-rules.toml`（45 insertions/1 deletion）。`src/llm/mod.rs`/`scene*`/`main.rs`/`src-tauri/**`/`ui/**` 零触碰。未构建/出包/启动 exe；未改版本号；未用 git 破坏命令；UTF-8 用 edit 工具，U+FFFD=0。

---

## 2026-08-02 — tester-1 TEST-SYNC-016：015 + 016 测试同步（阶段三，零命令执行）

**改了什么**：仅两文件测试区。`src/llm/mod.rs`（A 项改 1 断言 + B 项新增 6 测试）+ `src/itn.rs`（C 项新增 6 测试 T1-T6）。生产代码零改动（`git diff -w` 逐行核查，真实 diff `+265/−3`）。

**A · 过时断言修正**：`build_format_instruction_block_f3_exemplification_enumeration` d 项——旧 `contains("may be FULL SENTENCES")` 锚 015 反转前措辞。改为三断言：长句侧 `List items here are FULL SENTENCES or longer clauses` + 禁止侧 `SHORT noun phrases MUST NOT be bulleted` + 负向护栏 `!may be FULL SENTENCES`。意图=F3b 须同时说明「长句 AND 短项禁止 bullet」，两侧缺一即退化（参照 c 项保守默认双向写法）。

**B · 015 行为覆盖 6 条**：`item_form_short_long_split`（SHORT 内联 + LONG 列表整串锚点防假绿）/`f3a_f3b_long_symmetry`（F3a/F3b items are LONG 对称）/`output_contract_short_inline_exception`（条件式 MUST + SHORT 内联例外 + 负向旧无条件契约）/`f3c_short_inline_example`（买菜顿号示例 + 负向列表形态）/⭐`item_form_structural_guard`（F3-item form SHORT 规则 与 F3b/Output format LONG 限定同时存在，注释写 015 recency 软化教训）/`false_unchanged_drift_guard`（单行分支零改动防漂移）。

**C · 016 行为覆盖 6 条**：T1 正向 4 条全汉字（Gavin 原话）｜T2 句子形态整句无阿拉伯数字｜T3 反向护栏 8 条（九八年→98年 / ≥3 位逐位串 / 三年二班全或无 / **十三班→13班**（code 走查 + coder-1 实测双重确认）/ 一班三班保持汉字）｜T4 proper_nouns 保护不受影响｜T5 班在非数字后不受影响｜⭐T6 降级测试（缺 serial_suffixes 段旧 toml → 一三班→13班 回修前，锁 [TOML-STALE-001]；对照五一广场/三零二房间不受缺段影响）。

**验证（阶段三禁执行命令，静态验证）**：锚点逐一与生产文本字节比对（Python）；负向锚点生产代码确认缺席；括号平衡 6+6 闭合；UTF-8 U+FFFD=0。

**为什么**：三阶段规则——等 015/016 代码收口后才派 TEST-SYNC。A 项是 015 的唯一红条（断言过时非回归），B/C 补新功能覆盖，T6 防外置旧 toml 静默失效。

**影响范围**：仅 `src/llm/mod.rs` + `src/itn.rs` 的 `#[cfg(test)]` 测试块。未跑任何命令；未构建/出包/启动 exe；未改版本号（0.7.3）；未用 git 破坏命令；UTF-8 用 edit 工具。

---

## 2026-08-02 — 主控 BUILD-RELEASE-20260802-009 补记（session 中断致漏记，事后独立取证）

**背景**：08-02 01:35 的出包实际已完成，但会话在收尾前被终止，五文档全部漏记。Gavin 2026-08-02 22:5x 告知「之前已经出包了，只是在刚刚出包之后会话被终止」。主控当晚重新独立取证补记，**无任何 Worker 汇报可采信，全部结论来自实测**。

**产物**（含 014/015/016 三项）：

| 产物 | sha256(前16) | 大小 | 两副本 |
| --- | --- | --- | --- |
| feiyin-ime.exe | `b2c7b8091de9fc56` | 11,908,608 B（较 008 的 11,901,440 +7,168） | ✅ target/release == Publish |
| crash-reporter.exe | `6d589e9c62a4f590` | 24,858,624 B | ✅ |
| feiyin-ime-ui.exe | `16acff200bb37fd3` | 10,026,496 B | ✅ 未重建（07-30 22:59 产物，ui/src-tauri 零改动） |

**主控 6 项独立验收全过**：

1. **mtime 链自洽**：src/itn.rs 01:14:45 / src/llm/mod.rs 01:15:22 → cargo test 二进制 `feiyin_ime-2005e566889dc8d7.exe` 01:32:23 → 主程序 01:35:32 → toml 同步 01:35:41 → Publish 01:35:49（`[BUILD-002]` 时间戳纪律满足）
2. **双向探针（判别力已验证）**：新增串 `serial_suffixes`=2、`MUST NOT be bulleted`=1、`AND items are LONG`=1、`routes SHORT items INLINE`=1、`F3-item form`=4 两副本全命中；**015 删除的旧措辞 `they do NOT need to be short noun phrases`=0** —— 正反双向成立，非单向恒真探针
3. **`itn-rules.toml` 三副本 sha256 `c5511562daf46364` 一致**（本轮最关键，016 的 `[protect.serial_suffixes]` 段确认进入外置 toml，`[TOML-STALE-001]` 闭环）；`scene-rules.toml` 三副本 `910b2c1ff8829e26` 一致
4. **ProductVersion / FileVersion 0.7.3.0**，版本号红线遵守（未擅改）
5. **运行时实证**：Gavin 实例 PID 9412 自 16:27:45 运行至今 Responding=True；debug.log 16:28:19 `ITN rules loaded from "...target\release\itn-rules.toml"` + `Scene rules loaded from ...` —— **ITN 懒加载证据链本次完整出现**（BUILD-001 时曾缺此项）
6. **cargo test 已执行的物证**：debug 测试二进制 01:32:23 重建，晚于 TEST-SYNC-016 编辑（01:14/01:15）→ 12 条新测试确实被编译并运行。**但具体通过数无记录（会话中断丢失），本次未补跑**

**如实标注的两处缺口**：① 全量回归的具体数字（passed/failed）永久丢失，只能证明「跑过」不能证明「全绿」 ② handoffs 记录的提交哈希 `a03b89c`/`2d7703d` 与实际 `ae452fb`/`81cf51a` 不符，已由 tester-1 订正

