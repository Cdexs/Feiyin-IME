# 2026-09-04 代码改动日志

## TEST-FIX-080 · tester-1 · 阶段三：修 4 条错测试 + 补 5 条 AltGr 键序护栏

**基线**：HEAD `175d661`（HOTKEY-078/079 均已验收提交），v0.9.0
**性质**：纯测试代码改动（阶段三，不跑测试），生产代码零改动
**命令白名单合规**：cargo fmt --check ✅ clean ／ cargo check --all-targets ✅ 0 error ／ npx tsc --noEmit ✅ 0 error

### 裁定事实独立复核（动手前完成）

主控对 TEST-EXEC-076 五条 FAIL 的裁定，四处生产代码逐行复核全部对上：

1. `src/itn.rs:1711-1729` `format_remainder_suffix`：`is_time = real_unit.is_none() && unit_word == "点"` → `Half => 30` → `format!("{}:{}", h, mm)` → `1:30`（甲型时间族，DEC-037）。既有护栏 `:2630 八点半→8:30`、`:3456 一点半→1:30`、`:3714-3726` 七条时段词全是 `H:MM` 格式 → 期望值写「1点半」确属护栏作者笔误。
2. `src/itn.rs:1383-1400` `format_time_chain`：`unit=="点"→hours`、`unit=="分"→minutes` → `format!("{}:{}", 1, 15)` → `1:15`。
3. `src/audio/mod.rs:313-331` 生产 drain：`Err(Empty) => break`（非自旋）、`drain_deadline = now + 500ms`、第二段 while 排空计 abandoned；`:305-308` 注释确认 `on_chunk` 落到 `chunk_tx.send()` 是 **bounded 阻塞 send** → deadline 的真实触发路径是 on_chunk 阻塞，原用例「活生产者灌包 + Err(Empty) 自旋 continue」双重建模错误（后者是我 TEST-EXEC-076 侧写已报的缺陷 1，前者是主控补的缺陷 2）。
4. `src/main.rs:3996-4013` 生产消费序：gen 闸门 continue（不落镜像）→ **写 last_streaming_text 镜像** → `should_ignore_streaming_text`（043 门闩，仅渲染）。原用例末段断言 `mirror == Some("本session文本")` 与闭包先写镜像的事实自相矛盾（其注释甚至写着「镜像更新在门闩之前」）——主控裁定「用例与自己的模拟代码矛盾」成立，无需 053-B/075 语义重裁。

### 改动明细（四个测试文件，`git diff -w --stat`：+190/-42，全部在 mod tests / .test.tsx）

1. **`src/itn.rs`（mod tests，+10/-6）**
   - `itn_071b_legal_time_yidianban_still_converts`（:3004）：期望 `下午1点半` → `下午1:30`；注释改写为「一点半不在保护表（红线：绝不许加）→ 走甲型时间族 → 渲染为 1:30（DEC-037）」并锚定 :3456/:2630 同族既有护栏。消融不变：误加保护表 → check_protection Some → 输出原文 → 红。
   - `itn_071b_legal_time_yidianshiwufen_still_converts`（:3012）：期望 `1点15分` → `1:15`；注释注明走 format_time_chain unit chain 时间族路径。

2. **`src/audio/mod.rs`（mod tests，重写 1 条用例，+59/-42）**
   - 旧 `asr_074_stop_drain_gives_up_at_deadline_with_abandoned_count` 删除（活生产者 + Err(Empty) 自旋 continue，与生产 :322 break 不同构 → abandoned 结构性不可达；且「坚持到 deadline」是伪契约）。
   - 新 `asr_074_stop_drain_slow_consumer_abandons_backlog_at_deadline`：按生产语义重建 —— 预灌 200 chunk 后停止生产（对齐 stop 信号一到 capture 侧即停灌）+ 消费端每包 `sleep(10ms)` 模拟 on_chunk 阻塞慢消费 + `Err(Empty)=>break` 与生产同构。200×10ms=2s ≫ 500ms → deadline 必先于队列见底触发。断言四条：drained>0 / abandoned>0 / elapsed>=500ms（deadline 真被触发）/ elapsed<1500ms（松手最坏 0.5s 返回契约，第二段排空不计 sleep）。消融：删生产 :309 时间上限 → 200 个全消费完 → abandoned==0 且 elapsed≈2s → 两条断言同红。

3. **`src/main.rs`（mod tests，改名 + 期望修正，+29/-13）**
   - `stale_generation_must_not_touch_mirror_and_stopped_gate_is_orthogonal` → `generation_gate_blocks_mirror_but_043_gate_is_render_only`（旧名与修正后语义相反，留着误导）。
   - 前两段断言零触碰（陈旧包不落镜像 ✅ / 本 session 包落镜像 ✅）。末段期望 `Some("本session文本")` → `Some("松手后迟来包")`：闭包在 043 判断之前写镜像（忠实复刻生产 :3996-4013），迟来包是同一句话的更完整版本，必须进 053-B 镜像。主控裁定的「代际闸门=数据+渲染双拦 / 043 门闩=仅渲染」语义已原样落进 doc-comment。消融两条：代际闸门移到镜像后 → 陈旧包段红；043 改成数据抑制 → 末段红。

4. **`ui/src/pages/HotkeySettings.test.tsx`（纯追加 +128/-0，新 describe 块「HOTKEY-078/079 AltGr 键序矩阵」5 条用例）**
   - `HOTKEY-079-T1` 翻译侧 AltGr（Ctrl↓→AltRight↓→任意 keyUp）→ `translation.vk_code=0xA5 'Right Alt'`，且不弹冲突弹窗（mock available=true）。
   - `HOTKEY-079-T2` 翻译侧单按 Left Ctrl：keyDown 后 updateConfig **未**被调（pending 延后裁决）→ keyUp 后录 `0xA2 'Left Ctrl'`。
   - `HOTKEY-079-T3a`（本单有判别力的泄漏探针，coder-2 原规格拆分而来，主控已批准）：真实泄漏路径 = 按住 Left Ctrl 不放 → Escape → 重进录制 → 才松手（孤儿 keyUp 落在无 keyDown 的新会话）。断言钉住用户可见症状：updateConfig 零调用（正确代码下 pending 已被 :160 清行归零；删行消融 → 孤儿 keyUp 命中 :408 分支 → finalize 0xA2，用户第二次会话一个键没按热键被静默改成 Left Ctrl → 红）。
   - `HOTKEY-079-T3b`（coder-2 原规格正向回归，保留）：Escape → 重进录制 → 单按 Right Ctrl 正常录 `0xA3 'Right Ctrl'`。
   - `HOTKEY-078-T4b` 语音侧 AltGr ControlLeft 先抬（AltRight keyUp 带 ctrlKey=true）→ `hotkey {vk_code:0xA5, modifiers:0, 'Right Alt'}` 且 toHaveBeenCalledTimes(1)（防双 finalize）。
   - **T4a 不重复造**：AltRight 先抬键序与 HOTKEY-047-S1 完全相同（S1 = 可用路径、S12 = 冲突弹窗变体），矩阵成员保留。
   - **T5 不写**：coder-2 已逐路径证否「旗残留误抑制」不存在可执行键序，主控采纳。

### 🔴 重要结论（TEST-FIX-080 任务书要求明确复述）

**ITN-071-B 的生产修复本身是好的**：`itn_071b_yidianbandian_protected`（差的不是一点半点）与 `itn_071b_yidiandian_protected`（我只是有一点点不信）两条保护用例都是绿的，Gavin 报的两个 bug 确实修好了。本单修红的只是顺手写的两条回归护栏，作者凭直觉把期望写成了「1点半/1点15分」，与生产时间族渲染（DEC-037）不符。**不存在「071-B 可能没修彻底」的问题**，生产代码零改动。

### coder-2 T3 规格偏差（主控已独立复核并批准）

coder-2 原 T3 字面序列（Escape→重进→单按 Right Ctrl）在「删 resetTranslationRecordingState 清 pending 行」的消融下**不会红**：pending 泄漏对 ControlRight（走 :387 通用路径查表 0xA3）与 AltRight（新分支硬编码 0xA5，通用路径 CODE_TO_VK['AltRight'] 也是 0xA5，:22 可证）两条路径的产出均零影响。原规格消融无判别力 —— 判断本身是本单有价值产出。处理：拆成 T3a（孤儿 keyUp 泄漏探针，消融会红）+ T3b（原序列正向回归），既有断言粒度不降反升。

### 覆盖缺口（如实声明，不写假护栏）

1. 真实 AltGr 硬件键序（Windows 键盘驱动的合成事件真实时序）—— jsdom fireEvent 无法复刻驱动层行为，只能按 Web 事件模型建模；建议阶段五实机按 AltGr 目视验证翻译侧录制。
2. Win32 RegisterHotKey 层对 0xA5/0xA2/0xA3 的实际响应 —— 前端用例只能钉 UI 录制层契约。
3. T3a 的失焦（onBlur）变体：onBlur 与 Escape 同走 resetTranslationRecordingState（:529/:353），语义等价，未重复造用例。
4. audio 新用例的 10ms sleep 依赖线程调度：200×10ms=2s 对 500ms 的余量是 4 倍，sleep 抖动（Windows 定时器精度 ~15ms）不影响判定方向（drained 至少几十、abandoned 至少百余）；阶段四执行时若 CI 机器极端卡顿再议参数。

### 红线合规

生产代码零改动（diff 只含四个测试文件）／既有用例除指定四条外零触碰（root 1050 批、HOTKEY-047-S12 断言原文未动）／cargo test / cargo build / npm run test 全程未跑（阶段三红线）／未出包 / 未 commit / 版本号 v0.9.0 未动 / git 只用 status/diff/log / UTF-8（Edit 工具直编，未碰 PowerShell）/ 密钥凭证零落盘。
## SECRET-082 · coder-1 · pre-push 密钥/隐私闸门 + 修 pre-commit 换行符隐患

**基线**：HEAD `ace4a5c`，v0.9.0（未动）
**性质**：工程基建，零生产代码改动（不碰 src/、ui/、src-tauri/、Cargo.toml、package.json）

### 交付物（4 个文件）

| 文件 | 动作 | 说明 |
| --- | --- | --- |
| `scripts/git-hooks/secret-patterns.sh` | 新增 | 共享模式表 + `scan_diff` 函数，被 pre-commit / pre-push 同时 `source`，杜绝正则漂移 |
| `scripts/git-hooks/pre-push` | 新增 | push 时扫待推提交范围，覆盖三种 ref 情况（删除/推新/全新分支） |
| `scripts/git-hooks/pre-commit` | 改 | 改为 `source` 共享文件，行为不变 |
| `.gitattributes` | 新增 | `scripts/git-hooks/** text eol=lf`，修 CRLF 静默失效隐患 |

### 模式表

- 密钥前缀：`sk-`/`sk-ant-`/`gh[pousr]_`/`github_pat_`/`AKIA`/`AIza`/`xox[baprs]-`/`glpat-`
- 通用赋值型：`(api_key|apikey|access_token|auth_token|secret_key|client_secret|password)[:=]` + ≥20 位串，排占位符
- **新增隐私组**：邮箱（配白名单）、中国手机号 `1[3-9][0-9]{9}`、身份证 18 位（带日期段校验）
- 邮箱白名单：`cdexs@hotmail.com`、`gavinshare6@outlook.com`、`noreply@*`、`*.example.com`、`@users.noreply.github.com`

### pre-push 三种 ref 情况

| 情况 | 判据 | 处理 |
| --- | --- | --- |
| 删除分支 | `local_sha` 全 0 | 跳过 |
| 推新提交 | `remote_sha` 非全 0 | `git diff remote_sha..local_sha` |
| 推全新分支 | `remote_sha` 全 0 | `git rev-list local_sha --not --remotes` 求本地独有提交，从最老父提交 diff 到 local_sha |

### 实测（临时仓库 `/c/msys64/tmp/opencode/secret082/tmprepo/`，测完 `rm -rf` 已删）

| 用例 | 期望 | 实测 |
| --- | --- | --- |
| A1 `sk-`+20位 | 拦 | ✅ rc=1 |
| A2 `ghp_`+20位 | 拦 | ✅ rc=1 |
| A3 手机号 | 拦 | ✅ rc=1 |
| A4 身份证 | 拦 | ✅ rc=1 |
| B1 占位符 | 放行 | ✅ rc=0 |
| B2 白名单邮箱 | 放行 | ✅ rc=0 |
| B3 中文 UTF-8 文档无真值 | 放行 | ✅ rc=0 |
| C1 推全新分支干净 | 放行 | ✅ rc=0 |
| C2 删除分支 | 跳过 | ✅ rc=0 |
| D .gitattributes 生效 | 新 clone 钩子仍 LF 可执行 | ✅ |
| E SECRET_SCAN_SKIP=1 | 放行+警告 | ✅ rc=0 |
| pre-push 拦绕过 commit 的密钥 | 拦 | ✅ rc=1 |
| F 本仓 `git push --dry-run` | 放行 | ✅ rc=0，26 待推提交 0 命中 |

### 修复的正则 typo

`secret-patterns.sh` 首次写入时 `AIza[0-9-Za-z_-]` 缺 `A`（应为 `[0-9A-Za-z_-]`），导致 `grep -E` 报
「无效的结束地址」，整个 `scan_diff` 失效，A1/A2 漏拦。已修复，重跑全绿。

### 红线合规

- 生产代码零改动；cargo/npm 未跑（本单无 Rust/TS 改动）
- 未 commit、未 push（仅 `git push --dry-run`）
- 临时仓库测完 `rm -rf` 已删
- 钩子脚本全 LF（`file` 实测 Bourne-Again shell script，无 CRLF）
- 版本号 v0.9.0 未动

## SECRET-082-FIX · coder-1 · pre-push 根提交静默放行 + fail-open 设计缺陷修复

**基线**：SECRET-082 工作区改动（未提交），v0.9.0（未动）
**性质**：返工，修主控验收复现的两个缺陷，零生产代码

### 缺陷一：根提交无父时静默放行

**根因**：`pre-push` 情况 3 用 `${FIRST}^` 当 diff base，根提交无父 → `git diff` fatal → `2>/dev/null` 吞错 → DIFF 空 → `continue` 放行。
**修法**：`git rev-parse --verify -q "${FIRST}^"` 安静失败时 fallback 到 git 空树对象（`4b825dc6...`），扫根提交引入的全部内容。
🔴 **必须用 `--verify -q`**：裸 `git rev-parse` 解析失败时仍把字面量 `<sha>^` 打到 stdout，`$(cmd || echo $EMPTY)` 会把字面量和空树 hash 都吃进变量，BASE 变垃圾，diff 继续失败，看似修好实则没有。

### 缺陷二：fail-open → fail-closed

**根因**：`git diff ... 2>/dev/null` + `[ -z "$DIFF" ] && continue`，把「命令失败」和「diff 确实为空」当同一件事，任何故障都静默放行。
**修法**：`if ! DIFF_RAW=$(git diff ... 2>&1); then exit 1; fi` 分开判断，命令失败时拒绝放行并打印 git 报错。pre-commit 一并收敛（理由：失效形态同源，修成本极低，保持一致性）。

### 实测（临时仓库 `/c/msys64/tmp/opencode/secret082fix/tmprepo/`，测完 rm -rf 已删）

| 用例 | 期望 | 实测 |
| --- | --- | --- |
| A1 sk-+30字符 | 拦 | ✅ rc=1 |
| A2 ghp_+30字符 | 拦 | ✅ rc=1 |
| A3 手机号 | 拦 | ✅ rc=1 |
| A4 身份证 | 拦 | ✅ rc=1 |
| B1 占位符 | 放行 | ✅ rc=0 |
| B2 白名单邮箱 | 放行 | ✅ rc=0 |
| B3 中文 UTF-8 文档无真值 | 放行 | ✅ rc=0 |
| C1 推全新分支（有 remote）干净 | 放行 | ✅ rc=0 |
| C2 删除分支 | 跳过 | ✅ rc=0 |
| C3 推全新分支首提交=根提交含密钥 | 拦 | ✅ rc=1，`[已知密钥前缀]` 命中（空树 base 生效） |
| C4 推全新分支首提交=根提交干净 | 放行 | ✅ rc=0 |
| C5 喂无效 sha | 拦+打印报错 | ✅ rc=1，fail-closed 生效 |
| D .gitattributes 生效 | 新 clone 钩子 LF 可执行 | ✅ |
| E SECRET_SCAN_SKIP=1 | 放行+警告 | ✅ rc=0 |
| F 本仓 git push --dry-run | 放行 | ✅ rc=0，26 待推提交 0 命中 |

### 红线合规

- 生产代码零改动；未 commit、未 push（仅 --dry-run）；临时仓库测完 rm -rf 已删
- 钩子脚本全 LF；版本号 v0.9.0 未动；假密钥只存在于临时仓库
