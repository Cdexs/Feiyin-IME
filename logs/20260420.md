## 2026-04-20 — tester-1 协作初始化

- 改动：无代码改动
- 原因：完成协作启动流程
- 影响：已阅读 worker-guide.md、todo.md、handoffs.md、decisions.md、troubleshooting.md，回传 ACK 并进入待命状态

## 2026-04-20 — tester-1 新增录音输入全流程 E2E 测试

- 改动：新增 `tests/test_cases/test_full_pipeline_e2e.py`（4 个用例）
  - `TestFullPipelineToggle.test_full_recording_pipeline_toggle`：Toggle 模式完整录音输入流程
  - `TestFullPipelinePTT.test_full_recording_pipeline_ptt`：PTT 模式完整录音输入流程
  - `TestFullPipelineCancel.test_recording_cancel_flow`：ESC 取消录音流程
  - `TestFullPipelineFocusLost.test_focus_lost_preview_flow`：焦点丢失预览窗口流程
- 修改：`tests/conftest.py` 新增 `e2e` 标记注册
- 原因：主控要求补充"按热键 → 录音 → 识别 → 文字注入"全流程端到端测试
- 影响：
  - 验证完整链路：热键触发 → overlay Recording → 停止 → overlay Processing → overlay Hidden → 文字注入
  - 需要真实麦克风或虚拟音频输入（标记 `@pytest.mark.hardware`）
  - 使用 SendInput 模拟热键，记事本作为注入验证目标
  - 覆盖 4 个场景：Toggle/PTT/ESC取消/焦点丢失

## 2026-04-20 — tester-1 E2E 测试加入动态热键读取

- 改动：修改 `tests/test_cases/test_full_pipeline_e2e.py`
  - 新增 `HotkeyConfig` 类：封装 vk_code、modifiers、mode、display_name
  - 新增 `_read_config_toml()` / `_get_current_hotkey_config()`：从 `%APPDATA%\voice-ime\config.toml` 读取当前热键
  - 新增 `_trigger_hotkey_start()` / `_trigger_hotkey_ptt_hold()` / `_trigger_hotkey_ptt_release()`：根据实际配置发送对应按键
  - Fixtures `e2e_toggle_config` / `e2e_ptt_config` 改为返回 `HotkeyConfig` 对象，不再强制 F9
- 原因：主控指出测试应使用系统当前配置的热键，而非硬编码 F9
- 影响：
  - 测试自动适配用户当前热键设置（如 Ctrl+Space、Alt+` 等）
  - 支持修饰键组合（Alt/Ctrl/Shift/Win）
  - 测试开始时打印 `🔑 使用热键配置: HotkeyConfig(...)` 便于日志追溯

## 2026-04-20 — tester-1 TEST-SYNC-MAC-011 同步 macOS 热键测试用例

- 新增：`tests/test_cases/test_hotkey_macos.py`（7 个 macOS 热键测试用例）
  - `TestHotkeyToggleMode`（2 用例）：Toggle 模式启动/停止
  - `TestHotkeyPTTMode`（1 用例）：PTT 模式按住录音
  - `TestHotkeyCancel`（1 用例）：ESC 取消录音
  - `TestHotkeyPresets`（2 用例）：Ctrl+Space / Command+` 热键预设
- 新增：`conftest.py` 添加 `@pytest.mark.macos` 标记
- 特点：
  - 在 Windows 上自动跳过（`pytest.skip`）
  - 使用 pyautogui 模拟键盘事件（DEC-016 推荐方案）
  - 包含辅助功能权限检测（macOS 安全要求）
  - 对标 Windows 端 `test_hotkey.py` 的测试覆盖
- 遗留：macOS overlay 状态检测待完善（当前使用 AppleScript 窗口查找）

## 2026-04-20 — tester-1 TEST-SYNC-MAC-012+013 同步 macOS 注入+Overlay 测试用例

- 新增：`tests/test_cases/test_injection_macos.py`（3 个 macOS 文字注入测试用例）
  - `TestTextEditInjection`（1 用例）：TextEdit 文字注入
  - `TestClipboardPreserve`（1 用例）：剪贴板内容保留验证
  - `TestFocusLostPreview`（1 用例）：焦点丢失场景
- 新增：`tests/test_cases/test_overlay_macos.py`（7 个 macOS Overlay 测试用例）
  - `TestOverlayVisibility`（3 用例）：Overlay 窗口显示/隐藏
  - `TestOverlayStates`（2 用例）：状态切换（录音→处理→隐藏）
  - `TestFocusLostPreview`（2 用例）：焦点丢失预览窗口
  - `TestOverlayWindowProperties`（2 用例）：窗口属性（位置、透明度）
- 对标 Windows 端：`test_injection.py` + overlay 状态检测
- 特点：
  - 使用 TextEdit 替代记事本作为注入目标
  - `pbcopy`/`pbpaste` 处理 macOS 剪贴板
  - AppleScript 激活应用（`tell application "TextEdit" to activate`）
  - Windows 环境自动跳过
- 遗留：macOS Overlay 窗口精确属性检测需 pyobjc/Accessibility API 支持

## 2026-04-20 — coder-2 协作初始化

- 改动：无代码改动
- 原因：完成 coder-2 协作启动流程
- 影响：已阅读 worker-guide.md、todo.md、handoffs.md、decisions.md、troubleshooting.md，写入 ACK 并进入待命状态

## 2026-04-20 — coder-2 完成 CLEANUP-CONFIRM-002 冗余文件确认

- 改动：删除 11 个冗余/临时文件
  - 路径错误垃圾文件（5个）：`DWorkspace...` 系列
  - 调试日志（4个）：`run_*.log`、`settings_*.log`
  - 过期文件（2个）：`nul`、`result.md`
- 原因：项目根目录存在多处路径转义错误产生的垃圾文件和过期日志
- 影响：仅清理项目根目录临时文件，不影响源代码、构建产物和有效日志

## 2026-04-20 — coder-2 完成 MAC-013 macOS 透明 Overlay 实现

- 改动：`src-tauri/src/main.rs` 添加 `.setup()` hook
  - macOS 平台调用 `overlay.set_shadow(false)` 移除默认窗口阴影
  - 使用 `#[cfg(target_os = "macos")]` 条件编译，Windows/Linux 不受影响
- 原因：macOS 上 WebView 窗口默认有阴影，即使设置 `decorations: false` 也无法完全透明
- 验证：cargo check 通过，0 errors
- 影响：仅 macOS 平台生效，overlay 视觉效果改善
## 2026-04-20 / coder-1 / MAC-011

- Change:
  - add `core-foundation = "0.10"` and `core-graphics = "0.25"` under `target_os = "macos"` in `Cargo.toml`
  - add `src/platform/macos/hotkey.rs`
  - update `src/platform/macos/mod.rs` to export the real macOS `HotkeyListener` and `HotkeyEvent`
- Design:
  - use `CGEventTap(Session)` plus a dedicated `CFRunLoop` thread
  - poll config every 250ms so Tauri/settings writes are observed without restart
  - keep event contract aligned with Windows:
    - `Toggle` sends `Start` on each activation and lets controller decide start-vs-stop
    - `PushToTalk` sends `Start` on press, `Stop` on normal release, `CancelStop` when held < 300ms
  - re-enable the tap after `TapDisabledByTimeout` / `TapDisabledByUserInput`
- Why:
  - the placeholder in `src/platform/macos/mod.rs` still referenced `kCGHIDEventTap`
  - after checking crate/docs behavior, `Session` scope is a better fit for a normal desktop app than leaning on a HID tap path
- Validation:
  - `cargo fmt --all` passed
  - root `cargo check` passed on Windows host
  - local Darwin cross-check is still blocked: `rustup target add x86_64-apple-darwin` failed during component install on this machine
- Impact:
  - `MAC-011` is no longer a stub; the synced macOS hotkey tests now have a concrete backend target
  - remaining risk is compile/runtime validation on an actual macOS toolchain and Accessibility-enabled host
## 2026-04-20 / coder-1 / CLEANUP-CONFIRM-001

- Checked `D:\Workspace\CodeLab\collab\inbox\coder-1\task.md` and confirmed current cleanup task dispatch
- Verified `D:\Workspace\CodeLab\collab\outbox\coder-1` contains only `result.md`
- Confirmed no task-local screenshots, helper scripts, or temporary logs remain from this round
- Scope is cleanup confirmation only; no product code changed

## 2026-04-20 - coder-1 - MAC-012 macOS text injection

- Change: add `src/platform/macos/injection.rs`, wire exports in `src/platform/macos/mod.rs`, and add macOS-only `enigo = "0.2.1"` in `Cargo.toml`
- Reason: replace the macOS text injection placeholder with a real backend aligned with the platform abstraction already used by `src/main.rs`
- Impact:
  - clipboard injection path now uses `pbcopy` / `pbpaste` and `Cmd+V`
  - direct injection fallback now uses `enigo.text()`
  - clipboard restore now runs even if the paste key event fails
  - focused text snapshot/readback remains stubbed and explicitly documented as residual work
