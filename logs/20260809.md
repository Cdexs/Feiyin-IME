# 修改历史 · voice-ime · 20260809

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-08-09 — 新 session 启动 + TEST-EXEC-030 重新派发（无生产代码改动）

**背景**：上一 session（08-09 00:30）因 tester-1 额度用尽中断，TEST-EXEC-030 已派发但零产出。

**主控启动取证（非采信文档，逐项实跑）**：

| 项 | 实测 |
| --- | --- |
| HEAD | `d2ee6b3`，本批四 commit 全部落地，`main...origin/main [ahead 4]` **未 push** |
| `git status -- src/` | ✅ 空，无悬空改动（仅 collab 文档 + research 临时脚本未提交） |
| `outbox/tester-1/result.md` | 0 字节 → 确认 TEST-EXEC-030 未开跑 |
| 三 Worker pane | %2 coder-1 / %1 coder-2 / %3 tester-1 全部存活，19:3x 陆续 ACK 就绪 |
| `handoffs.md` | 36 行，未超 200 行阈值，无需归档 |

**发现并更正一处交接错误**：handoffs 第 9 行称任务书「已写好可原样复用」，实测 `inbox/tester-1/task.md`
在 19:33 已被清空为 0 字节。主控按原设计重写任务书（7030B）后派发。

**本批合并 diff（仅 src/，供回归对照）**：`itn.rs +87` ｜ `llm/mod.rs +509` ｜ `main.rs +51`
｜ `punctuation/mod.rs +528` ｜ `transcription/mod.rs +109`，合计 +1180/−104。

**边界评估**：TEST-EXEC-030 为 tester-1 独占的只跑不改任务，coder-1/coder-2 已 tmux 通知空闲待命
且明令勿动 `src/` → 零文件级重叠。

**Codex 权限**：coder-1 pane 实测为 OpenCode 类型（非 Codex），按既定规则未发 `/permissions Full Access`。

**本条无任何生产代码改动。**

---

## 2026-08-09 19:4x — TEST-EXEC-030 A0 起点自证异议裁决：纯行尾噪声，放行（无代码改动）

**tester-1 上报**：`git status -- src/` 有 14 文件 M，但 `git diff -w` / `--ignore-space-at-eol` 均空，
判定为 CRLF/LF 行尾差异（`scene/mod.rs` = `i/lf,w/crlf`），建议不处理行尾继续执行。

**主控独立取证（未采信 Worker 结论）**：

| 证据 | 结果 |
| --- | --- |
| `git status --short -- src/` | **完全为空**（不复现 14 M） |
| `git diff --stat -- src/`（**无任何 flag**） | **完全为空** → 零差异，比 `-w` 更硬 |
| `core.autocrlf` | `true`；**无 `.gitattributes`** |
| `git ls-files --eol src/` | `i/lf w/crlf` **恰为 14 个**，与 tester-1 报的 14 完全吻合 |
| `i/crlf w/crlf` | 0 个 |
| 本批五文件 | `itn.rs` / `llm/mod.rs` / `main.rs` / `punctuation/mod.rs` / `transcription/mod.rs` **均为 `i/lf w/lf`，不在 14 之列** |

**结论**：属 `[CRLF-CROSSPLAT-001]`（macOS 侧合并 `7e76465` 带入的行尾差异）。tester-1 看到的 14 M
是 git 索引 stat 缓存陈旧的**瞬时态**，`git status` 本身会刷新该缓存，故主控复查时不复现。
**裁决：放行 A1–A7，明令不得处理行尾。**

**顺带纠正 tester-1 一个方法论问题（值得记）**：不可把 `git diff -w` 为空当作放行判据 ——
`-w` 会连**真实的纯空白改动**一起吞掉；而本批 `llm/mod.rs` 大量内容是提示词字符串常量，
**字面量内部的空格是语义的一部分**（改一个空格就改了送给 LLM 的提示词），`-w` 在此场景判别力不足。
已明令今后一律以**无 flag 的 `git diff`** 为准。

**红线重申给 tester-1**：禁止对这 14 个文件执行任何行尾归一化 / `git add` / `git checkout` / `git stash`
—— 依据 `[CRLF-CROSSPLAT-001]`，碰它们会制造 4800 行幽灵 diff。

---

## 2026-08-09 20:0x — TEST-EXEC-030 全量回归执行完成（零生产代码改动）

**tester-1 执行 A0–A7 + 四族零回归专项，全程只读/只跑，未改任何生产代码。**

### A0 起点自证

| 项 | 结果 |
| --- | --- |
| HEAD | `d2ee6b3` ✅ |
| 工作区 `src/` | 14 文件纯行尾 M（主控已裁决放行，未处理） |
| `cargo fmt --check` | clean ✅ |
| `cargo check --all-targets` | 0 error（51.79s，仅 86 warnings 78 重复）✅ |

### A1–A7 回归结果（全绿，零 FAIL）

| Step | 模块 | 实测 | 基线 | 判定 |
| --- | --- | --- | --- | --- |
| A1 | itn | **225** passed | 221 | ✅ +4 |
| A2 | punctuation | 43 passed | 38+10 | ✅ +5 |
| A3 | transcription | 105 passed + 4 ignored | 105 | ✅ |
| A4 | llm | 140 passed | 134+9 | ✅ +6 |
| A5 | 主 crate 全量 | **958** passed + 8 ignored | — | ✅ |
| A6 | src-tauri | 53 passed | 53 | ✅ |
| A7 | --list 自洽 | 966 == 966 | — | ✅ 完全自洽 |

**零 FAIL，无红条。** 任务书"高风险红区"三项属 ② 类行为变更，均已在 TEST-SYNC-030/030-B 同步断言，全绿无残留。

### 四族零回归

① 017 重量链（1斤2两/2.5两/2斤）✅ ② 026 货币链（5块一斤/8角/5.1元/3.48元一斤）✅
③ 027 大额（10468741/123456789/3.5亿）✅ ④ 031 万一守卫 13 词保汉字 + 放行组 12 条 + 跨模块 5 条 ✅

### itn:: 真实用例数裁定

tester-1 双口径（`cargo test itn::` + `--list`）均 **225**。handoffs 旧记录 212 失效，以 225 为准；
基线 221→225 +4 来自 TEST-SYNC-030/030-B 新增。coder-1 报的 219→221 为中间态。

### 结论

✅ 无红条、无回归、无阻塞项 → **可进入 BUILD-015（待主控下达出包指令）**。
Vitest/pytest 本批 SKIP（零前端零 UI 改动）。结果全文：`outbox/tester-1/result.md`。

**本条无任何生产代码改动，仅本日志 + result.md 产出。**

---

## 2026-08-09 21:1x—21:5x — BUILD-015 出包执行完成（零生产代码改动）

**tester-1 执行 BUILD-015 完整出包**（HEAD `faa672d`，基线产物 08-04 19:04~19:06）。

### Step1–4 执行

| Step | 动作 | 结果 |
| --- | --- | --- |
| 1 | 清理 feiyin 进程 | 无残留进程 ✅ |
| 2 | `npm run build`（ui/，2.04s）+ Tauri release UI（custom-protocol，2m01s） | ✅ |
| 2c | `cp` src-tauri 产物到 target/release | ✅ 21:09 |
| 3 | 主程序 `cargo build --release`（2m24s） | ✅ |
| 4 | 同步 Publish/ + toml | ✅ 全部 21:13 |

### 七项核验全记录（详见 outbox/tester-1/result.md）

1. **时间戳**：六产物全变今天（target 21:09/21:11/21:12，Publish 全 21:13）✅
2. **sha256 两副本**：三 exe 每对全相同 ✅
3. **toml 三副本**：itn-rules `b208271b…` 三处相同；**scene-rules 发现 Publish/ 与 target 残留 08-03 旧版（sha `7c1f2408`，41714B）不一致，已按 Step4 规范同步为根目录最新版，三处 sha 收敛 `0a3a0b9…`（真实 diff 45591B → 关键更新）** ⚠️ 已修复+通告
4. **ProductVersion**：0.7.3.0 / 0.7.3 / 0.7.3.0 ✅
5. **UI exe 晚于前端**：21:09 salvage js ✓ ✅
6. **冒烟启动**：PID 24024，Responding=True，Path=Publish\feiyin-ime.exe，存活不退出，测后清理 ✅
7. **产物大小**：主 11.96MB / UI 10.03MB / crash 24.86MB（与 08-04 基线一致量级，主程序 +17KB 为 030/031 代码增量；任务书预期 ~31MB/~22MB 疑为旧描述，如实标注供主控核对）✅

**本批无任何生产代码改动；仅出包产物 + 文档 + result.md。**


---

## 2026-08-09 21:5x — Gavin 两项拍板落地：删探测文件 + 阶段三开命令白名单（零生产代码改动）

### 拍板一：`examples/probe_031.rs` 删除

- **文件**：79 行临时探测程序，coder-1 08-08 查「万一→0.1万」时写，`#[path = "../src/itn.rs"]`
  挂载生产模块跑 41 个词，**只 println 不断言**。第 2 行作者自注：「跑完删除，不留在仓库」
- **删除理由**：位于 `examples/`，cargo **自动发现**，每次 `cargo check --all-targets` /
  `cargo test` 都连带编译；且引用 `itn.rs` 内部符号，itn.rs 一改就可能编译不过
- **不可逆操作纪律**：删前已存底到 scratchpad
  `probe_031.rs.deleted-20260809`（2095B）；确认 `git ls-files examples/` 为空（未跟踪，删即永久）
- **执行**：`rm examples/probe_031.rs` + 移除空目录 `examples/`

### 🔴 删除前的顺带发现 → 已开单 ITN-IDIOM-COVER-032

核查该探测文件覆盖词的测试情况时发现**六个成语在 `src/itn.rs` 零出现**：
`十全十美` / `十有八九` / `百般` / `千方百计` / `百里挑一` / `成千上万`（各 0 次）。
对照 `十分` 8 次、`千千万万` 1 次。

**与 031 同类** —— 量级单位字开头的固定表达。031 只给万/亿补了守卫，
十/百/千三分支至今无守卫（按既有记录靠 `result==0` 被拒「歪打正着」），
`:682` 十分支默认 1 长期挂「只报不改」。已开 032-A/B/C 三段（先实测现状再定改法，
禁走保护词表 —— DEC-038）。文件域 `src/itn.rs`，与任何 ITN 任务互斥。

### 拍板二：阶段三（TEST-SYNC）开命令白名单 → DEC-048

- **白名单**：只允许 `cargo fmt` + `cargo check`（含 `--all-targets`）**两条**
- **明令禁止**：`cargo test`（含任何过滤参数）/ `cargo build` / `cargo build --release` / 其余一切执行类命令
- **写成白名单而非「允许只读命令」**：否则「我只是 check 一下」会滑向「顺手跑个测试」，口子守不住
- **理由**：规则要防的是「测到半成品拿假结果」与「提前出包」；`fmt` 只格式化、`check` 只做类型检查，
  **都不执行测试、不产出二进制**，与该风险无关。禁止它们反而使 tester-1 交付前无法自查 ——
  2026-08-08 一批之内引爆三次，第三次 `src/llm/mod.rs:4776` 多一个 `}` 致整个 crate 编译失败

### 🔴 连带改正一处长期规则冲突（本次一并修）

项目级 `.claude/CLAUDE.md` §3 此前写作「**三阶段**」且**阶段一为并行**
（代码任务与 TEST-SYNC 同时派发），与 `worker-guide.md` 自 2026-05-05 起执行的
「**五阶段禁止并行**」**长期冲突**。并行会让 coder 与 tester 同时写同一个 `mod tests`，
制造文件级冲突。已改正为五阶段全串行，并注明以该节为准。

### 落地位置

| 文件 | 改动 |
| --- | --- |
| `.claude/CLAUDE.md` §3 | 三阶段→五阶段全串行 + 阶段三命令白名单表 |
| `collab/docs/worker-guide.md` §二-3 | 阶段三补白名单说明 + 原因 |
| `collab/docs/task-book-template.md` | 常备红线表对应行改写 |
| `collab/decisions.md` | 新增 **DEC-048** |
| `collab/todo.md` | 新增待派发单 **ITN-IDIOM-COVER-032** |

**本条无任何生产代码改动。**
