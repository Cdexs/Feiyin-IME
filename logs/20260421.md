# 2026-04-21 tester-1 改动日志

---

## 协作启动同步

- 完成：阅读 worker-guide.md、todo.md、handoffs.md、decisions.md、troubleshooting.md、CLAUDE.md、build-test-guide.md
- 完成：回传 ACK，待命等待任务派发

---

## 2026-04-21 22:49 / coder-1 完成 UI-FIX-005

- 完成：恢复 Sidebar 顶部 12px 留白；清理 `main-content` 滚动与隐藏滚动条样式的职责边界
- 涉及文件：`ui/src/styles.css`
- 决策：不改 `App.tsx`，因为实际滚动容器一直是 `.main-content`，问题是 CSS 责任分散而不是 JSX 结构错误
- 具体改动：
  - 保留 `.sidebar` 的 `padding-top: 12px` 作为唯一顶部留白来源
  - 移除 `.sidebar-title` 的额外顶部偏移，避免叠加出不可控空白
  - 将 `.main-content` 的滚动与滚动条隐藏规则集中维护，`::-webkit-scrollbar` 只保留隐藏表现
- 验证：`cd ui && npm run build` 通过（完成时间 `2026-04-21 22:49:34`）

---

## TEST-SYNC-UI-FIX-005 测试同步

- 完成：分析 UI-FIX-005 CSS 修改内容（Sidebar padding-top + 滚动条隐藏）
- 完成：检查现有 Playwright/Vitest 测试覆盖情况，发现缺口
- 完成：补充 3 个 Playwright 测试用例（`TestSidebarLayout` 类）
  - `test_sidebar_padding_top`: 验证 Sidebar padding-top = 12px
  - `test_sidebar_nav_items_count`: 验证导航项 ≥ 5 个
  - `test_main_content_scrollbar_hidden`: 验证滚动条完全隐藏
- 涉及文件：`tests/test_cases/test_webview_ui.py`（+56 行）
- 评估：Vitest 无需补充，现有覆盖已足够

---

## UI-FIX-005-BUILD + UI-FIX-005-TEST 构建与测试执行

- 完成：发现 dist/ CSS 产物为 4月18日旧版，阻塞测试
- 完成：完整构建 Step 1/2/3
  - Step 1: 退出旧进程 ✅
  - Step 2a: npm build ~582ms ✅
  - Step 2b: Tauri UI release ~48.6s（5 warnings）✅
  - Step 3: 主程序 release ~0.85s（47 warnings，dead code）✅
- 完成：产物时间戳验证 voice-ime-ui.exe 22:56（新构建）
- 完成：Vitest 8/8 PASS
- 完成：Playwright 9 PASS / 4 SKIP / 0 FAIL
  - 新增 3 用例全部 PASS
- 额外修复：`msOverflowStyle` 断言兼容 Chromium（null）返回

---

## TEST-SYNC-UI-FIX-006 测试同步

- 完成：分析现有 `test_main_content_scrollbar_hidden` 覆盖情况（CSS 属性断言 3 条）
- 完成：补充 `offsetWidth - clientWidth === 0` 真实视觉断言
  - 原理：offsetWidth 含 scrollbar，clientWidth 不含，差值 = 滚动条实际像素
  - 优势：比 CSS 属性断言更贴近实际渲染效果
- 涉及文件：`tests/test_cases/test_webview_ui.py`（+5 行）
- 评估：Vitest 无需补充（happy-dom 无法计算真实 offsetWidth）

---

## UI-FIX-006-BUILD+TEST 构建与测试执行

- 完成：完整构建 Step 1/2/3
  - Step 1: 退出旧进程 ✅
  - Step 2a: npm build ~532ms ✅
  - Step 2b: Tauri UI release ~42.8s（5 warnings）✅
  - Step 3: 产物复制 ✅
- 完成：产物时间戳验证 voice-ime-ui.exe 23:26（新构建）
- 完成：Vitest 8/8 PASS
- 完成：Playwright 9 PASS / 4 SKIP / 0 FAIL
  - TestSidebarLayout 3/3 PASS（含 offsetWidth-clientWidth 真实视觉断言）

---

## VITEST-WEBVIEW-CAPABILITY-001 调研分析

- 完成：Vitest + happy-dom 能力边界分析（能做什么/不能做什么）
- 完成：happy-dom vs jsdom 对比
- 完成：Playwright WebView2 CDP 真实环境能力评估
- 完成：场景匹配矩阵（12 个场景 × 2 框架）
- 完成：测试分层策略建议（L1 Vitest 70% + L2 Playwright 30%）
- 结论：Vitest 无法测试真实 WebView2，两者互补不可替代
- 产出：result.md（4000+ 字详细报告）

---

## VITEST-BROWSER-MODE-001 调研分析

- 完成：Vitest Browser Mode 官方文档研究（browser mode + playwright provider）
- 完成：关键发现——Browser Mode 通过 `browser.launch()` 启动独立 Chromium，非 WebView2
- 完成：连接方式分析——不支持直接 CDP 连接现有 WebView2 实例
- 完成：13 维度对比表（Vitest Browser Mode vs Playwright + CDP）
- 完成：10 场景匹配矩阵
- 结论：不能完全替代，核心差异是 Browser Mode 不运行 WebView2 也不测试 voice-ime-ui.exe
- 建议：保持当前三层框架不变，不引入 Browser Mode

---

## UI-FIX-006-VERIFY 滚动条实际验证

- 完成：三种验证方式交叉验证
  - 方法 1：Playwright + CDP 自动化测试 `test_main_content_scrollbar_hidden`
    - `scrollbarWidth === 'none'` ✅
    - `offsetWidth - clientWidth === 0` ✅
  - 方法 2：pywinauto 原生 UI 元素扫描（60 个元素，0 个 scrollbar）
  - 方法 3：截图视觉验证（2 张截图：默认 + 最大化）
- 结论：PASS ✅ 真实 WebView2 环境下滚动条完全隐藏
- 截图：`scrollbar_verify.png` + `scrollbar_verify_maximized.png`

⚠️ **误判纠正**：用户截图证实滚动条实际可见。自动化测试 PASS 为假阳性（页面内容不足未触发滚动条渲染，导致 offsetWidth-clientWidth=0）。需退回 coder-2 修复。

---

## UI-FIX-007-BUILD+TEST 构建与验证

- 完成：完整构建 Step 1/2/3
  - Step 2a: npm build ~606ms ✅
  - Step 2b: Tauri UI release ~42.9s（5 warnings）✅
  - Step 3: 产物复制 ✅
- 完成：产物时间戳验证 voice-ime-ui.exe 00:20（新构建）
- 完成：截图验证（3 张：默认/关于/滚动后）
- 完成：最大化按钮验证
  - Win32 API: `WS_MAXIMIZEBOX = False` ✅（原生无）
  - UI 树：发现 `EdgeWindowsCaptionButton` "最大化"按钮 ❌（Tauri 自定义标题栏渲染）
- 结论：最大化按钮仍存在（Tauri 自定义标题栏问题），退回 coder-2 修复

---

## UI-FIX-008+009-BUILD+TEST 构建与验证

- 完成：完整构建 Step 1/2/3
  - Step 2a: npm build ~636ms ✅
  - Step 2b: Tauri UI release ~39.2s（5 warnings）✅
  - Step 3: 产物复制 ✅
- 完成：产物时间戳验证 voice-ime-ui.exe 00:55（新构建）
- 完成：滚动条验证
  - 改进测试方法：临时设置 `overflow-y: scroll` 强制渲染滚动条轨道
  - Playwright 单项测试：PASS（gutter = 0）✅
  - Win32 API 验证：正确 ✅
- 完成：最大化按钮三重验证
  - Tauri 配置：`maximizable: false` ✅
  - Win32 API：`WS_MAXIMIZEBOX = False` ✅
  - App.tsx：`setMaximizable(false)` 运行时禁用 ✅
- 结论：两项验证均通过
- 注：环境限制无法截图，已通过 Win32 API/Playwright/配置文件三重交叉验证

---

## coder-2 工作记录

### UI-ICON-001 关于页图标恢复
- 时间：2026-04-21
- 改动：`ui/src/pages/About.tsx` 第 23 行
  - 图标路径从 `/icons/128x128.png` 改为 `/icons/icon-source.png`
  - 注释更新："使用橘黑主题图标" → "使用高清原版图标"
- 原因：恢复 About 页面使用高清原版麦克风图标（不锈钢+橘色）
- 验证：`npm run build` 0 errors（596ms）
- 影响范围：仅 About 页面图标显示，无其他功能影响

### UI-FIX-007 两项紧急修复
- 时间：2026-04-21
- 改动 1：`ui/src/styles.css` 第 224-240 行
  - 简化滚动条隐藏逻辑，移除错误的 `@supports` 条件组合
  - 直接设置 `scrollbar-width: none` + `::-webkit-scrollbar { display: none }`
  - 根因：之前使用 `@supports not (scrollbar-width: none) and selector(::-webkit-scrollbar)` 导致部分浏览器滚动条未被隐藏
- 改动 2：`src-tauri/tauri.conf.json` 第 22 行
  - main 窗口新增 `"maximizable": false` 配置
  - 根因：Tauri v2 窗口默认允许最大化，需显式禁用
- 验证：`npm run build` 0 errors（553ms）
- 影响范围：
  - 滚动条：全局 main-content 区域滚动条隐藏
  - 最大化按钮：配置窗口不再显示最大化按钮

### UI-FIX-008 最大化按钮彻底修复
- 时间：2026-04-21
- 根因分析：
  - 根据 Tauri v2 schema 文档，`maximizable` 在 `resizable: false` 时会被忽略
  - 文档原文："If resizable is set to false, this setting is ignored."
  - 因此 UI-FIX-007 添加的 `maximizable: false` 配置未生效
- 解决方案：
  - 在前端使用 Tauri API 运行时禁用最大化按钮
  - `ui/src/App.tsx` 添加 `getCurrentWindow().setMaximizable(false)` 调用
  - 窗口加载后立即执行，确保按钮被禁用
- 改动文件：`ui/src/App.tsx`（+13 行）
  - 导入 `getCurrentWindow` from `@tauri-apps/api/window`
  - 添加 `useEffect` hook 在组件挂载时调用 `setMaximizable(false)`
- 验证：`npm run build` 0 errors（1.04s）
- 影响范围：配置窗口最大化按钮在运行时被禁用

---

## 2026-04-21 23:11 / coder-1 完成 UI-DEBUG-001

- 完成：仅做滚动条根因诊断，未修改任何代码
- 诊断范围：
  - `ui/src/styles.css`
  - `ui/dist/assets/index-Culu6L-0.css`
  - `ui/src/App.tsx`
  - `ui/src/pages/*.tsx`
  - `tests/test_cases/test_webview_ui.py`
- 结论：
  - `.main-content` 规则未被覆盖，普通页面无额外常驻滚动容器
  - 现有测试只验证声明值，不验证真实视觉滚动条或 gutter
  - 当前 `::-webkit-scrollbar` fallback 同时设置 `width: 0` / `height: 0` / `display: none`，与 Chromium 官方关于 scrollbar 渲染模式的说明存在冲突风险
  - 本机 WebView2 Runtime 为 `147.0.3912.72`，已属于支持 `scrollbar-width` 的 Chromium 代际；继续无条件混用 `scrollbar-width: none` 与带尺寸的 `::-webkit-scrollbar` fallback 没有必要，且可能导致行为不一致
- 验证与限制：
  - 已核对源码和构建产物一致
  - 尝试本地复跑 `test_main_content_scrollbar_hidden`，因 Python 环境缺少 `playwright` 失败，无法在当前终端直接做真实渲染复现

---

## 2026-04-21 23:21 / coder-1 完成 UI-FIX-006

- 完成：落实滚动条根治方案 A，修复 `ui/src/styles.css` 中 `.main-content` 的 Chromium/WebView2 fallback 策略
- 涉及文件：`ui/src/styles.css`
- 具体改动：
  - 从 `.main-content` 基础规则中移出 `scrollbar-width: none`
  - 新增 `@supports (scrollbar-width: none)`，让现代 Chromium/WebView2 走标准路径
  - 新增 `@supports not (scrollbar-width: none) and selector(::-webkit-scrollbar)`，让 `::-webkit-scrollbar { display: none; }` 只作为 fallback 生效
  - 删除旧 fallback 里的 `width: 0` 和 `height: 0`
- 决策：比“只删两行”更稳的做法是显式分流，避免未来再次把标准路径和私有 fallback 叠回一起
- 验证：`cd ui && npm run build` 通过（完成时间 `2026-04-21 23:21:26`）

---

## 2026-04-22 00:41 / coder-1 完成 UI-DEBUG-002

- 完成：仅做滚动条深入排查，未修改任何代码
- 关键取证：
  - 核对 `ui/src/styles.css` 与 `ui/dist/assets/index-B0TxTrJi.css`，确认 `.main-content { scrollbar-width: none; }` 与 `.main-content::-webkit-scrollbar { display: none; }` 已正确打包
  - 通过 WebView2 CDP 连接 `https://tauri.localhost/`，对 `.main-content` 执行 `Runtime.evaluate` 与 `CSS.getMatchedStylesForNode`
  - 运行时结果：`.main-content` 的 `scrollbarWidth = none`、`pseudoDisplay = none`、`offsetWidth - clientWidth = 0`
  - 同时发现 `body/html/app-container/sidebar` 的 `scrollHeight = 740`，比 `clientHeight = 720` 多 20px
  - 进一步读取 `.sidebar::after` 伪元素样式，确认其 `bottom = -20px`
  - DevTools 临时覆写 `.sidebar { overflow: hidden !important; }` 后，根层 `scrollHeight` 立即回落到 `720`
- 结论：用户目视看到的并不是 `.main-content` 的滚动条，而是被 `.sidebar::after` 光晕装饰撑出来的页面根滚动条
- 根治建议：优先在 `.sidebar` 上裁剪该伪元素溢出；避免对 `body/html` 全局隐藏滚动，因为那会掩盖未来真实的页面溢出问题

---

## 2026-04-22 00:51 / coder-1 完成 UI-FIX-009

- 完成：在 `ui/src/styles.css` 的 `.sidebar` 上添加 `overflow: hidden`，裁剪 `::after` 光晕伪元素的溢出
- 变更范围：仅 `ui/src/styles.css`
- 原因：`UI-DEBUG-002` 已通过 DevTools 实测确认，用户看到的是页面根滚动条，而非 `.main-content`；根因是 `.sidebar::after` 的 `bottom: -20px`
- 验证：`cd ui && npm run build` 通过（完成时间 `2026-04-22 00:51:35`）
