# 修改历史 · voice-ime · 20260808

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-08-08 — PUNCT-GOVERNANCE-030-E（L2 后处理抽纯函数）

> 触发：030-A 的 L2 后处理块内联在 `run_pipeline_core` 主链路上（被 ASR/LLM/注入副作用包住），tester-1 走查确认**零回归护栏**——「最关键的判定」和「零测试覆盖」并存不可接受。

- **目的**：纯重构，零行为变更。把 L2 判定抽成可单测纯函数。
- **改动 `src/punctuation/mod.rs`**：新增 `L2Action` 枚举（StripAll/StripTrailing/NoOp）+ `pub fn apply_l2_postprocess(text, punctuation_enabled) -> (String, L2Action)`——(a) 关 → strip_punctuation；(b) 开且 ≤5 单位 → strip_trailing；(c) 其余 → 原样。日志方案选 C：函数返回 action 枚举，日志留在 main.rs 调用侧按 action 打，测试可断言判定分支。
- **改动 `src/main.rs` `run_pipeline_core`**：L2 块替换为单行调用 + `match action` 打原文案日志（沿用「输出变化才打」的 `!=` 判定）。上游 ITN/标点引擎、下游 cancel/focus_lost/inject 零改动。**工作区 main.rs 曾混合 CRLF/LF 行尾（git diff 全文件 4972 行噪音），已统一 LF 与 HEAD 一致，`git diff -w` 自证仅 L2 块真实改动，不带行尾污染**。
- **验证**：判定矩阵 8 条（tester-1 规格）临时探针逐条实测 + 与内联等价逻辑逐字符对照零偏差（探针已删，不交付）；`cargo check --all-targets` 0 error；`punctuation::` 38/0；`transcription::` 105/0/4；既有测试零改动、断言零改动。
- **边界**：NoOp 分支返回 `text.to_string()`（clone）而非借用——输出逐字符相同，仅多余的 allocations，非行为变更；未碰 llm/mod.rs（coder-1 并行）；未 build--release/未出包/未 commit。

---

## 2026-08-08 — PUNCT-GOVERNANCE-030-A-2（A-2-① trailing 字符集拆 + A-2-② native_punctuated 实测）

> 触发：主控 Read 代码发现 ① `strip_trailing_punctuation` 从末尾无脑剥 `PUNCT_CHARS`（含成对符号右半）致 `他说"好"`→`他说"好` 左引用孤儿；② `native_punctuated` 硬编码 `true`，Qwen3 短句/单词无尾标点时开关启着却跳过标点引擎、拿到无标点文字。前者端测必撞，后者主控核官方文档确认（官方 API 无标点参数，源头关不掉）。

- **A-2-①**（`src/punctuation/mod.rs`）：新增 `TRAILING_PUNCT_CHARS`（`。！？；，、…—：` + `.!?;:,`，不含成对符号右半），`strip_trailing_punctuation` trailing 路径改用它。`PUNCT_CHARS` 原样保留（`strip_punctuation` 全文剥离仍需）。反例实测：`"好"。`→`"好"`（终结标点在成对符号外侧仍剥）。🔴 **验收返工**：主控查出漏全角冒号 `：`（`重点：` 2 单位不剥 vs `重点，` 剥，同族挑掉一个）→ 已补 `：` + 断言 `重点：`→`重点`；新增 `test_trailing_chars_partition_pair_symbols_and_separator` 逐字符对照护栏（PUNCT 每个终结类字符必须 ∈ TRAILING，TRAILING 外仅成对符号/`·` 间隔号，显式枚举），doc 注释同步写明排除项。
- **A-2-②**（`src/transcription/mod.rs` + 新增 `has_effective_punctuation`）：qwen3 分支 `native_punctuated = punctuation::has_effective_punctuation(&normalized)`（改前 `normalized.chars().any(is_punctuation)` / 再前硬编码 true）。新函数对 `PUNCT_CHARS` 全集合统一「词内嵌豁免」——字符两侧为 ASCII 字母数字（`3.14` 小数点 / `don't` 撇号 / `3:30` 时间冒号 / `example.com` 域名点）不计为有效标点。与 `strip_punctuation` 共用同一「什么算标点」口径，避免子系统分裂。c 方案（a 只改后半点 / b 复用 strip_punctuation 保护被主控判定边界），非逐字打补丁。
- **验证**：`cargo check --all-targets` 0 error（中途一次 `src/llm/mod.rs` `NO_PUNCT test-scope 未找到`——coder-1 030-B 在途中间态，重跑复位，非本任务回归）；`cargo test --bin feiyin-ime punctuation::` 23→**28**/0（含返工补冒号 + 护栏）；`-bin feiyin-ime transcription::` 104/0/4；`rustfmt --check`（两文件）clean。
- **边界钉测**：`他说"好"` 引号无句号 → true（主控裁定接受，附句子级方案差异记录）；全角数字 `３.１４` → true 与半角行为不一致（只报不修，主控②）；全角冒号时间 `3：30` → false（两侧半角数字夹持，非缺陷）。
- **文档**：`collab/handoffs.md` + `CHANGELOG.md` + `collab/outbox/coder-2/result.md` + `docs/MACOS-HANDOFF.md` 均已记录（含上轮 030 补收口）。
- **边界**：未改 `strip_punctuation` 本体（030-A 实现零改动）；未碰 llm/mod.rs、main.rs；未 build --release / 未出包 / 未改版本号 / 未 commit。

---

## 2026-08-08 — LLM-CONN-POOL-028 连接池僵尸连接修复

> 触发：Gavin 端测发现 LLM 优化间歇性 0ms 失败（请求未上网络即挂）。见 `docs/MACOS-HANDOFF.md` §2.9.5 跨端记录。

- **根因**：`reqwest::Client::builder()` 只设 `connect_timeout`，吃全局默认 `pool_idle_timeout=90s`；DeepSeek 服务端 keep-alive 约 60s 关连接 → 60-90s 窗口内池中残留「服务端已关、客户端以为活着」的死连接，复用即失败（失败点 62.5/67.9/72.3/72.8s，<60s 与 >98s 成功，与窗口假说吻合）。
- **改动 `src/llm/mod.rs`**：
  1. 新增 `POOL_IDLE_TIMEOUT=30s`（必须 < 服务端 keep-alive 余量）+ builder `.pool_idle_timeout(POOL_IDLE_TIMEOUT)`
  2. 重试判据 `e.is_connect() || e.is_timeout()` → `+ e.is_request()`（reqwest `Kind::Request` 桶，覆盖连接复用失败；body/decode/builder/status 各自独立桶不会误吞，已核 reqwest-0.12.28 error.rs）
  3. 新增 `fmt_error_chain`（逐层 `Error::source()` 展开），三处日志改用完整链路，解决 Display 吞底层错误的排障盲区
- **镜像 `src-tauri/src/llm.rs`**：同一 `pool_idle_timeout(30s)` + `is_retryable_error` 加 `is_request()`。
- **验证**：cargo fmt clean；cargo check 0 errors（dev 13.5s，pre-existing warnings）；`cargo check --manifest-path src-tauri/Cargo.toml` 0 errors（33.56s）；cargo test `llm::` 131/0；src-tauri 测试 53/0。
- **边界**：未改 CONNECT_TIMEOUT / ATTEMPT_TIMEOUTS / MAX_ATTEMPTS 现有值；未碰 itn.rs / prompt；未出包。

## 2026-08-08 — PUNCT-GOVERNANCE-030-D（翻译路径 system_content 抽纯函数）

> 触发：tester-1 走查 + 主控核实——主路径有 `punctuation_disabled_no_punct_instruction` / `_no_punct_marker` 两条契约断言，翻译路径零覆盖。正是 PROMPT-ARCH-020 复发形态（当年 `UNIT_SYMBOL_PROTECTION_TRANSLATE` 的假前提靠翻译路径零断言逃过 749 条测试）。

- **性质**：纯重构，一票否决项 = 零行为变更。
- **改动 `src/llm/mod.rs`**（唯一文件）：`pub async fn optimize_and_translate` 内联的翻译路径 `system_content` 装配（原 :692-737）抽为模块级**私有**自由函数：
  ```rust
  fn build_translate_system_content(
      target: TranslationLanguage,
      punctuation_enabled: bool,
      wordbook_block: Option<String>,
      extra_instruction: Option<&str>,
  ) -> String
  ```
  函数内做 `target→target_desc` match（原 :692-695）、wordbook/extra 的 `\n\n` 前缀拼接（原 :697-705）、`punct_instruction` 双形态（原 :709-715）、`step1_correct` 双分支（原 :717-723）、六参 `format!`（占位顺序 `step1_correct, target_desc, punct_instruction, wordbook_block, extra, UNIT_SYMBOL_PROTECTION_TRANSLATE`，原 :726-737）。调用侧只剩一行：
  ```rust
  let system_content = build_translate_system_content(target, punctuation_enabled, build_wordbook_prompt_block(), extra_instruction);
  ```
  `build_wordbook_prompt_block()`（内含 `WordbookCache::load_from_db` SQLite I/O）留在调用侧，纯函数不碰 I/O；`text` 参数弃用（实测不参与 system_content，仅进 `<speech>` 用户消息）。
- **签名定案**：tmux 发主控确认后批准——私有 fn（与 `f3_rules_text` / `output_contract_text` / `build_output_format` 同性质，不加 pub 无谓扩大 API 面）；`target` 传 Copy 枚举（target_desc match 移入函数可单测）；`wordbook_block` 传 `Option<String>`（省调用侧临时 let）。
- **逐字节验证**：临时断言按旧内联实现逐字重建参考函数，穷举 `2 目标 × 2 标点开关 × 2 wordbook × 3 extra = 24 组合`，`old == build_translate_system_content(...)` 全 PASS，验证后按验收要求删除临时测试。
- **缺口 3**：`step1_correct` 双形态（030-B B5）、target_desc 映射、punct_instruction 双形态全部可在返回值断言。不抽 `correct_step1` 薄层包装（任务书明确不需要）。本任务不新增测试（TEST-SYNC-030-B 归 tester-1）。
- **验证**：`cargo check --all-targets` 0 error（13s）/ `cargo test llm::` 134/0（temp 测试删后重跑，既有 134 条全绿，签名未破坏任何断言期望值）。
- **边界**：未碰 `translate()`（:616）自身内联的另段 system prompt（不在任务范围）；未碰 main.rs / punctuation/mod.rs / src-tauri / UI；未跑全量测试；未 build --release；未出包；未改版本号；未 commit。


---

## 2026-08-08 — PUNCT-GOVERNANCE-030 标点子系统治理全批 + ITN-FIX-WANYI-031

> 四个 commit：`94bfb0b`（功能主体）→ `5fc390d`（TEST-SYNC-030）→ `201bb4f`（030-D/E 抽纯函数）→ `d2ee6b3`（TEST-SYNC-030-B）。
> **本地 ahead 未 push**（push 需 Gavin 明确指示）。

### 一、做了什么

标点开关从「6 个产出源只完全控制 1 个」收敛为全源受控。架构：**L1 源头控制为主，L2 后处理补位**。

| 产出源 | 治理方式 |
| --- | --- |
| ① ASR native / Qwen3 在线 | L2 兜底（官方 API 无标点参数，源头物理不可控——已核实阿里云文档） |
| ② 本地标点引擎 | L1 不调用（既有） |
| ③ LLM optimize | L1 `NO_PUNCT` 明确禁止句（原为「什么都不说」） |
| ④ LLM translate | L1 同上 + `step1_correct` 硬编码条件化 |
| ⑤ LLM 列表分隔符 | L1 `INLINE_SEPARATOR_RULES_NO_PUNCT`（空格连接，禁 `、；,;`） |
| ⑥ 本地 NLLB 翻译 | L2 兜底（seq2seq 无参数） |

新增能力：`count_units` / `strip_trailing_punctuation` / `TRAILING_PUNCT_CHARS` /
`has_effective_punctuation`（DEC-047）/ `apply_l2_postprocess` + `L2Action` /
`build_translate_system_content`；`qwen3` 的 `native_punctuated` 由硬编码 `true` 改为实测；
`itn.rs` 万/亿分支加「大单位前必须有数字」守卫（修「万一」→`0.1万`）。

### 二、值得记的四件事

**1. 同一个病发作四次：改了「说什么」，没改「示范什么」/「校验什么」。**

| # | 改了 | 漏了 |
| --- | --- | --- |
| 030-A | `PUNCT_CHARS` 集合 | `strip_trailing_punctuation` 的成对符号 → `（笑）`→`（笑` |
| 030-B | `ADD_PUNCT`/`NO_PUNCT` 常量 | `whitelist_new` 契约名单 → 两条断言必红 |
| 030-C | 分隔符规则文本 | F3c/F3-item 示例 → 同一 L3 块内规则与示例互相矛盾（违反 DEC-040） |
| 030-A-2 | `TRAILING_PUNCT_CHARS` | 同族字符全角 `：` |

→ **派发提示词/字符集类任务时，任务书必须显式列出「规则文本 + 示例 + 契约断言」三处同步检查项。**
根子是靠人记；唯一治本产出是 coder-2 那条双向 partition 护栏（见下）。

**2. 三个 Worker 各驳回主控一次，三次都成立。**

- coder-2：`has_effective_punctuation`（全集合词内嵌豁免）优于主控的 `has_sentence_punctuation`（另立「句子级标点」分类）——**理由是让检测器与 `strip_punctuation` 共用同一判据，不分裂**。已立 DEC-047。
- tester-1：主控给的护栏「punct=false 时输出不得含 `、`/`；`」**物理上不可满足**（`NO_PUNCT` 常量的禁令句自身即含这两字符）；改用**差异—重建护栏**（off 三处切换点回播后须与 on 逐字相等），比主控修正版更强。
- tester-1：`#[ignore]` 骨架方案不可行（`#[ignore]` 仍要编译，引用未存在的函数会让 crate 编译失败）——主控据此改为「规格表 → 抽函数 → 落测」三段串行。

**3. 两条机制级护栏（比修好 bug 本身更值钱）**

- `test_trailing_chars_partition_pair_symbols_and_separator`：`PUNCT_CHARS` 中每个非成对、非间隔号字符必须 ∈ `TRAILING`，反向亦然。以后谁加终结类字符忘了同步就直接红。
- `f3_rules_punct_off_switch_reconstructs_to_on`：把 off 分支所有 switch 点回播成 on 形态后须与 on 逐字相等。任何未申报的分支差异立刻红，封死 030-C 的根因。

**4. 阶段三「禁止执行任何命令」规则的缺口（待 Gavin 拍板）**

tester-1 两次交付非 rustfmt-clean、一次交付**编译不过**（`llm/mod.rs:4776` 多一个 `}`），
均因规则禁止它跑 `cargo check`/`fmt`，交付前无从自查。主控建议开白名单例外。详见 todo.md 交接节。

### 三、遗留

- 🔜 **TEST-EXEC-030 未跑**（tester-1 额度用尽中断）。这批到中断为止**一次 `cargo test` 都没跑过**，只有 `cargo check` 与各 Worker 局部自验。
- 🔜 BUILD-015 出包未做。
- 只报不改：`零万`/`零亿` → `0万`/`0亿`（`零` 使 `has_digit=true` 绕过守卫，非真实中文表达）；`３.１４` 全角数字 → 判有标点（与半角不一致）。
- 已知接受边界（DEC-047）：`他说“好”` 有引号无句号 → 判 `true` 跳过标点引擎。
