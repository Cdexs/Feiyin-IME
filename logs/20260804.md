# 修改历史 · voice-ime · 20260804

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-08-04 — TEST-SYNC-026 ITN-FIX-CHAIN-TEAR-026 测试同步

**来源**：基线 `9edc839`（coder-1 026+026-B 代码落地但 TEST-SYNC 缺失）。改动 B 允许单段 currency 链，打开误转风险面。

**改动**：`src/itn.rs` `mod tests` 块内新增 114 行，生产代码零改动。

| 组 | 内容 | 行数 |
|----|------|------|
| T10 补齐 | 补齐至 12 个 DEC-038 货币族保护词条（+二块钱/六块钱/八块钱/一毛钱/一角钱/五角钱） | 6 |
| B1-B5 | 五组歧义反向护栏（块/角/分/元/毛），每条标注「现状锁定」 | 23 |
| C1-C4 | 交叉回归护栏（017 六条端测/尾零边界/weight 族/016 班级简写） | 21 |

**疑似误转**（B5）：`三毛`→`3毛`（人名）、`九牛一毛`→`九牛1毛`（成语尾段），已标注 `TODO-026-REGRESSION`。

**边界**：生产代码零改动；版本号未改（0.7.3）；未触碰 `src/llm/mod.rs` / `itn-rules.toml` / `src/main.rs` / `src-tauri/**` / `ui/**`；UTF-8 U+FFFD=0；未执行任何命令（阶段三）。

## 2026-08-04 — TEST-EXEC-026 + BUILD-013 全量回归 + 出包

**来源**：基线 `9edc839` + TEST-SYNC-026（`src/itn.rs` mod tests +114）。Gavin 已授权出包。

### Step A · 全量回归

| 步骤 | 结果 |
|------|------|
| A1 `cargo test --bin feiyin-ime` | **766 passed / 0 failed / 6 ignored**（基线 762→766，+4 来自新增测试） |
| A2 `itn::` | **153 passed / 0 failed**（coder-1 自报 143 + 10 新增 = 153） |
| A3 `llm::` | **131 passed / 0 failed**（026 未波及 coder-2 域） |
| A4 `src-tauri` | **53 passed / 0 failed** |
| A5 `--list` | 772 = 766+6 自洽 |

### Step B · 红条分类

4 个红条全部 **① 断言写错**（走查推断值与实测不符），无 ③ 真回归。修正 6 处断言值：
- `一元二次`→`1元二次`（单段 currency 链捕获）
- `三元钱`→`3元钱`（同上）
- `五块零`→`5元`（隐式尾数「零」=0毛 → total=5.0）
- `三块钱`→`3块钱`（不在保护词表）
- `五块钱`→`5块钱`（不在保护词表）
- `九牛一毛`→`九牛一毛`（走查误判，「九牛」非数字开头不触发丙型链）

### Step C · BUILD-013 出包

三步构建 + Publish/ 同步。产物：`feiyin-ime.exe` `626bc2e2bc4f` / `feiyin-ime-ui.exe` `7f5b0ce6a6f4` / `crash-reporter.exe` `afd7f48d4f1b`。ProductVersion 0.7.3.0/0.7.3。

### Step D · 验收 7 项全过

mtime 链通过（src/itn.rs 00:43 < exe 00:46~00:48）；两 toml 三副本一致（`ed77a912`/`7c1f0620`）；二进制变化确认（≠ BUILD-012 `db07cefd8d51`）；反向探针 3/3=0；正向探针 4/4≥1；冒烟 PID 21604 Responding=True 零 panic。

**边界**：仅改 `mod tests` 断言（6 处修正）；版本号未改；未用 git 破坏命令；UTF-8 U+FFFD=0。

**触发**：Gavin 问「上次会话是否有测试或出包任务未完成」。

**核查方法**（`[SESSION-CRASH-RECOVERY-001]` 三件套）：

- sha256 两副本一致 `db07cefd8d51` → 构建发生过，但那是 BUILD-012
- **mtime 链不通过**：产物 `17:42:42` ＜ `src/itn.rs` `23:47:25`，差 6 小时 → **026 不在 exe 内**
- `git status` 查出 `M src/itn.rs`（+115/−9）+ CHANGELOG/decisions/troubleshooting/logs 共 349 行改动全部悬空未提交

**状态漂移事实**：coder-1 已写 CHANGELOG（026 + 026-B 两条）、DEC-040、`[F3-UNORDERED-LIST-001]`、`logs/20260803.md`，**唯独 todo.md 与 handoffs.md 无任何 026 条目**，且 todo 顶部仍写「全部 Worker 空闲待命」。若不主动取证，会被误判为无遗留。

**处置**：

1. 补提交 `9edc839`（5 files, +349/−9），降低 `[GIT-RESET-INCIDENT-001]` 悬空爆炸半径。ahead 41→43
2. 派发 **TEST-SYNC-026** 给 tester-1（阶段三，禁执行命令）。方案重点：改动 B 放开单段 currency 链 → **块/角/分/元/毛 五组非货币义误转风险**（一块石头/八角茴香/三分之一/三元一次方程/一毛不拔），要求锁现状而非写期望值，疑似误转单列清单报主控
3. todo.md 补 P0 章节 + 顶部标注产物不含 026

**影响范围**：本次主控零生产代码改动，仅提交既有改动 + 文档。026 的代码影响面在 `src/itn.rs` 的 `try_parse_unit_chain` / `format_currency_chain` 两函数，currency 族专属，weight/time 族不受影响。


---

## 2026-08-04 — coder-1 — ITN-FIX-BIGNUM-027-A + 027-B 大额数字进位缺陷修复

**来源**：Gavin 2026-08-04 端测「总共金额是一千零四十六万八千七百四十一元」ITN 输出 `10469740`（应为 `10468741`），第四种失败模式（数值静默改错，用户看不见）。Gavin 新指令「数字要支持从亿到个位的量级跨度」倒逼主控复查出 027-B（万级公式错误，差 4 个数量级）。基线 `ad8bbd0`（ahead 41）。

**两项缺陷**（同在 `parse_cn_number`，合并修复合并回归）：

| 项 | 位置 | 缺陷 | 修复 |
|----|------|------|------|
| 027-A | `:800-808` 末尾挂起分支 | 隐式千位补全守卫只检查 `big_unit_seen && !zero_since_big`，未检查万/亿后是否已消费进位单位（千/百/十），完整进位链末尾个位被套 +1000 | 新增 `unit_since_big` 状态位；十/百/千分支在 `big_unit_seen` 时置 true；万/亿结算重置 false；末尾判据加 `!unit_since_big` |
| 027-B | `:707` 万分支 | `result = (result + section) * 10000` 把已结算的亿也卷进乘法，亿+万嵌套差 4 个数量级 | 改为 `result += section * 10000`；亿分支 `:724` 保持 `(result+section)*1e8` 不变（亿是最大单位，其前一切是它的系数） |

**方案协商过程**：coder-1 实测发现公告 8 用例中 2 条（一亿两千.../一亿两千三百四十五万）在生产规则下因「两」∈`units.weight` 被 `large_amount_keep_wan_yi` 的 `is_unit` starts_with 命中而在亿分支 break，027-B 公式修复不可触及 → 独立第三缺陷 **027-C**。主控拍板：本轮仅修 A+B，027-C 另开单（is_unit 是 ITN 最核心公共函数，动它风险最高，Gavin 明令不能改坏现有逻辑）。

**改动**：仅 `src/itn.rs`，生产代码 +191/-5（含注释），测试 +15 条。

**验证**：
- `cargo test --bin feiyin-ime itn::` **168/0/0**（基线 153 + 新增 15，零绿转红）
- `cargo check` + `cargo check --tests` 双 0 errors
- UTF-8 U+FFFD = 0
- 4.2/4.3/4.4 共 11 条用例逐条通过（见 result.md）
- 027-B 8 条用例：4 条生产规则可达全过，2 条 027-C 阻断标注，2 条 rules=None 隔离验证全过
- 4.6 亿级注释-实现不符确认：「三亿五」=300005000 ≠ 注释 350000000，只报不改

**边界**：`src/llm/mod.rs`/`src/main.rs`/`src-tauri/**`/`ui/**`/`scene-rules.toml`/`itn-rules.toml` 零触碰；未构建/出包/启动 exe；未改版本号（0.7.3）；未用 git 破坏命令；UTF-8 用 edit 工具。



---

## 2026-08-04 — coder-1 — ITN-FIX-BIGNUM-027-C + 027-C-2 ✅ 「两」误判致亿级金额蒸发修复

**来源**：接续 027-A/B（已提交 `136f70f`）。Gavin 需求「数字要支持从亿到个位的量级跨度」。027-A/B 修完后 `一亿两千...` 仍返回错误，027-C 是最后一块。基线 `136f70f`（ahead 42）。

**方案协商**：coder-1 实测发现 027-C 有**两个阻断点**（主控原分析只定位第一处 large_amount_keep_wan_yi）。修第一处后亿能结算（result=1e8）但 :677 two_is_unit 在「两」处 break（match_unit_word 查「千」不在 all_units → two_is_unit=true）。主控拍板扩范围 027-C-2 一并修（同一根因，分单失去判别力）。

**两处改动**：
1. **027-C** large_amount_keep_wan_yi 万/亿分支：加 `big_starts_new_number` 消歧——after_big 首字是中文数字 + 第二字是进位单位（is_cn_unit_char）→ 不 break
2. **027-C-2** two_is_unit :567：加「两后紧跟进位单位（is_cn_unit_char）→ 返回false（两是数字2）」，补齐注释「进位单位由进位组合路径自行继续此处不误判」原意。is_unit 函数本体零改动。

**注释-实现不符汇总**：本轮第三处——two_is_unit 注释说不误判进位单位但实现只查 all_units（进位单位不在内）。连同 4.6 亿级隐式单位、geometric_order_hazard，共 3 处，主控要求单列判断是否系统性问题。

**验证**：
- 基线 `itn::` 168/0/0 → 改后 **181/0/0**（+13 新增，零绿转红）
- cargo check + --tests 双 0 errors；UTF-8 U+FFFD=0；is_unit git diff 空自证
- 017 全套 14 条逐条实测零回归（一斤二两/二两/二十五两/两斤/三两/二两半/一两/半斤八两/六两五/三块两毛五/两百/两块/两吨/一两半）
- 4.2 六条 large_amount_keep_wan_yi 原有行为保住（五万吨/一亿吨/三万元/两斤/二两/一斤二两）
- 027-C 必修 4 条全过（一亿两千...→123456789 / 一亿两千万→120000000 等）
- 027-A/B 成果 6 条不回归
- 第六节#4 实测确认**不是真缺陷**（丙型链 parse_cn_number 内部已折叠万/亿到 result，不存在丢弃）

**边界**：仅 `src/itn.rs`（两处消歧 + 13 条测试）；is_unit 本体零改动；未构建/出包/启动 exe；未改版本号（0.7.3）；未用 git 破坏命令；UTF-8 用 edit 工具。

**详情**：`/d/Workspace/CodeLab/collab/outbox/coder-1/result.md`（12757 B 非空）


---

## 2026-08-04 — coder-1 — ITN-FIX-BIGNUM-027-D (DEC-042) 隐式补全保留锚定单位

**来源**：Gavin DEC-042 拍板。基线 967cd8d（027-C 已提交）。

**方案评估**：coder-1 动手前盘点 14 个 parse_cn_number 调用点，发现 UnitChain 的 format_currency_chain/format_unit_chain 对 num_str 执行 .parse()，若返回 3.5亿 会崩。解决：加「孤立判定」——后继无单位才保留锚定单位，有单位维持 +1000 展开。

**改动**：新增 big_unit_anchor 记录万/亿单位+系数；隐式分支加 after_digit_is_unit 孤立判定；4 条旧断言更新 + 5 条新测试；is_unit 本体零改动。

**验证**：基线 itn:: 181/0/0 → 186/0/0（+5 新增，4 条预期内绿转红=DEC-042 变更，177 条预期外全绿）；cargo check + --tests 双 0；UTF-8 U+FFFD=0；调用点安全 6 条全过；4.4 四条边界实测报主控。

**详情**：result.md（11499 B 非空）


---

## 2026-08-04 — coder-1 — ITN-FIX-BIGNUM-027-E (DEC-042 补完) 数量级锚定最小单位全面落地

**来源**：Gavin 否定 027-D 的二分（隐式补全才保留锚定），DEC-042 补完规则：最小单位落万/亿→带后缀，落千/百/十/个→普通数字，升亿阈值（≥1亿且≤1位小数）。基线 f489e5b（代码 e79ed05 027-D）。

**改动**：新增 format_dec042_magnitude 函数（亿层/万层格式化+升亿判定）；隐式分支乘数修正（亿级×1e7千万位，万级×1e3千位，修复4.6亿级隐式补全）；DEC-042 孤立判定统一（循环结束后 big_unit_seen&&!unit_since_big&&孤立→format_dec042）；12条旧断言更新（DEC-042变更）；10条新测试。is_unit/format_currency_chain/format_weight_chain本体零改动。

**验证**：基线 itn:: 186/0/0 → 196/0/0（+10新增，12条预期内绿转红=DEC-042变更，174条预期外全绿）；cargo check + --tests 双0；UTF-8 OK；调用点安全6条全过；第八节4组零回归8条全绿。

**详情**：result.md（10353 B 非空）


---

## 2026-08-04 — coder-1 — ITN-FIX-BIGNUM-027-F 修027-E引入的静默归零（P0阻塞出包）

**来源**：tester-1 TEST-SYNC-027走查申报，主控复核判定真风险。027-E后parse_cn_number可能返回带单位串，隐式尾数吸收(:1130)未做纯数字校验，带单位串进format_currency_chain的.parse()静默归零。基线91c84ac。

**改动**：src/itn.rs隐式尾数吸收处+1行纯数字校验(num_str.bytes().all(|b| b.is_ascii_digit()))，对齐capture_price_per_unit:1241先例。非纯数字跳过吸收由主循环独立解析。is_unit/format_currency_chain/format_weight_chain本体零改动(DEC-043)。mod tests块零改动(tester-1补测待落地)。

**调用点重盘**：try_parse_unit_chain内3个消费点——隐式尾数吸收(已修)、主流parts.push(DEC-042孤立判定间接保护)、capture_price_per_unit(先例已有校验)。

**验证**：基线itn:: 196/0/0 → 196/0/0零绿转红；cargo check + --tests双0；UTF-8 OK；四条bug实测(五块三亿→5块3亿等)恢复；026正主6条+DEC-042四条全过。

**MACOS-HANDOFF**：2.9.4节新增027-D/E/F三行+跨端提示。

**详情**：result.md（6318 B 非空）

---

## 2026-08-04 — tester-1 — TEST-EXEC-027 + BUILD-014 S1-S5补测落地→全量回归→027全批首次出包

**来源**：TEST-SYNC-027 补测清单落地 + 阶段四执行 + 阶段五出包。基线 HEAD `d115965`（027-F 已提交）。三段串行。

**Step A（禁命令）**：`src/itn.rs` mod tests 内新增 **16 条**补测（S1 升亿 2 位边界成对 `一亿五千万`→`1.5亿`/`一亿两千五百万`→`12500万`/`三亿五百万`→`30500万`；S2 C-only 隔离 `一亿三千万`→`1.3亿`；S3 B1-B5 契约护栏 6 条；S4 big_unit_seen 反向 `五百三`→`503`/`三百二`→`302`；S5 静默归零钉现状 4 条按 027-F 修复后值；D 组回归 6 条 八角/三斤六两五/五一班/初二三班/高一四班/一块钱）。S5 三条（五块一万/三毛五亿/一块三亿。）Step A 禁命令故按 027-F 修复后代码路径走查推导，Step B 实测**全部一次命中**，无断言修正。生产代码零改动。

**Step B（B1-B5 全绿，无红条）**：B1 `cargo test --bin feiyin-ime` **825/0/6**；B2 itn:: **212/0**（基线 196 +16 新增）；B3 llm:: **131/0**；B4 src-tauri **53/0/0**（=基线）；B5 --list 831 自洽（itn::=212）。红条三分类：无。重点三类零回归全维持（货币链 五块一=5.1元/五块一斤=5块一斤/一块两毛二=1.22元/三块四毛八一斤=3.48元一斤；重量链 一斤二两=1斤2两/二两半=2.5两/三斤六两五=3斤6两5；016 班级 一三班/五一班/初二三班/高一四班 全汉字）。Vitest/pytest SKIP（零前端零 UI 改动）。

**Step C（BUILD-014）**：清理进程 0 残留 → npm build 1.94s → Tauri UI（--features custom-protocol）1m57s → 主程序 2m27s → UI exe 同步 target/release（mtime 一致）→ Publish 同步（三 exe 18:08:07）→ **toml 三副本核验**：itn-rules.toml `ed77a912…` / scene-rules.toml `7c1f0620…` 根/Publish/target-release 三处逐一一致 → 版本号未改（0.7.3 / ProductVersion 0.7.3.0）。

**Step D（七判据全过）**：① mtime 链：src/itn.rs 18:00:39 < 三 exe（18:08:02/18:05:30/18:06:59）② sha256 两副本逐一一致 ③ 二进制变化：新 feiyin-ime.exe `0e13cff5…` ≠ BUILD-013 `626bc2e2bc4f` ④ 反向探针 023 旧措辞 3/3=0 ⑤ 正向探针 023 新措辞 4/4≥1（markers DIFFER=8/TWO OR MORE=5/はじめに=2/가령=2 真次数）⑥ ProductVersion 0.7.3.0/0.7.3/0.7.3.0 ⑦ 冒烟 PID 22336 Responding=True 零 panic 无残留。

**三 exe sha256 前 12 位 + mtime**：feiyin-ime.exe `0e13cff59320…` 18:08:02（Publish 18:08:07）；feiyin-ime-ui.exe `cf7bddae7c87…` 18:05:30；crash-reporter.exe `eb9c04e288de…` 18:06:59。

**⚠️ 过程事故（已恢复）**：曾用 `py open(path,'w')` 规范化 src/itn.rs 行尾，未指定 encoding → cp936 编码错误且 open('w') 先截断文件至 0 字节。已用只读 `git show HEAD:src/itn.rs` 完整恢复 + 重 apply 测试块 + 重跑 B1/B2 确认无差异。生产代码零受损（release exe 在生产内容被截断前已编译）。已记入 lessons.md（GBK locale 陷阱）。

**边界**：仅 src/itn.rs（mod tests）+ 文档；零生产代码改动；未提交；未改版本号；UTF-8 用 edit 工具。

**详情**：result.md（26668 B 非空）+ handoffs.md + CHANGELOG.md + docs/MACOS-HANDOFF.md


---

## 2026-08-04 — coder-1 — ITN-FIX-BIGNUM-027-G 万亿层(DEC-042补充二)+专名白名单(DEC-044)

**来源**：Gavin端测反馈，一万亿→10000亿不符合中文习惯→DEC-042补充二加万亿层；十万个为什么→10万个为什么书名被改→DEC-044加专名。基线ade02e1。

**G-1改动**：format_dec042_magnitude亿分支加升万亿判定(≥1e12且≤1位小数)，与万层升亿同构。一万亿→1万亿/三万亿→3万亿/一万五千亿→1.5万亿。十亿/一千二百三十四亿五千万等不足万亿不得变。

**G-2改动**：itn-rules.toml proper_nouns+1条(十万个为什么)。前缀遮蔽自查无误伤(十万个人→10万个人/十万块钱→十万块钱)。

**验证**：基线itn:: 212/0/0 → 207/5(5条一万亿预期内绿转红=10000亿→1万亿，mod tests归tester-1)；cargo check + --tests双0；UTF-8两文件U+FFFD=0；DEC-043三套逻辑9条全绿；027-F护栏五块三亿→5块3亿。

**MACOS-HANDOFF**：2.9.4节新增027-G-1/G-2+跨端提示。

**详情**：result.md（7111 B 非空）

## 2026-08-04 — TEST-SYNC-027-G + TEST-EXEC + BUILD-015 万亿层换锚补测 → 全量回归 → 出包

**来源**：基线 `f0e6f4d`（027-G 已提交：G-1 万亿层 + G-2 十万个为什么白名单）。coder-1 预判 5 条 `一万亿` 断言待换锚归 tester-1。三段串行。

**改动**：`src/itn.rs` `mod tests` 块内换锚 5 条 + 新增 7 测试函数（A2 万亿层 4 / A3 专名白名单 3），生产代码零改动。

**A1 换锚（5 条，grep 独立核实确为 5 条）**：`10000亿`→`1万亿`（:4136/:4172/:4298/:4355/:4432），依据 DEC-042 补充二——1e12≥1万亿 且 rem=0 升万亿；亿分支公式不变断言保留。

**A2 万亿层**：`一万亿`/`三万亿`→1万亿/3万亿；`一万五千亿`→1.5万亿（trim 1 位升）；`一万两千三百亿`→12300亿（trim 2 位不升，边界另一侧成对）；`十亿`/`一千二百三十四亿五千万`→10亿/1234.5亿（<1e12 不变）。走查确认万/亿不在任何 [units.*] → `三万亿` 万处不 break。

**A3 专名白名单**：`十万个为什么`/`我买了一本十万个为什么`→保持汉字（proper_nouns 最长 starts_with + 含非数字字 → 右邻否决不触发）；`十万个人`→10万个人（🔴 反向护栏）；`十万块钱`→十万块钱（万后块∈units → break 保汉字，同 十万次 路径）；`十万`→10万。

**Step B 全绿无红条**：B1 832/0/6（基线 825+7）｜B2 itn:: 219/0（基线 212+7）｜B3 llm:: 131/0｜B4 src-tauri 53/0/0｜B5 --list 838 自洽，itn::=219。红条三分类：无，5 条换锚均 ② 设计变更。DEC-043 三类零回归维持。Vitest/pytest SKIP。

**Step C BUILD-015**：清理 0 残留 → npm 669ms → Tauri UI 1m40s → 主程序 2m00s → UI 同步 target/release → 🔴 itn-rules.toml 三副本同步 → Publish 三 exe 同步。版本号未改。

**Step D 7/7 过**：①itn-rules 三副本 `b208271b…` 一致且 ≠`ed77a912`（新词条进包）②mtime 链（itn.rs 18:58:58/itn-rules 18:45:20 < 三 exe）③sha256 两副本一致 ④新 exe `d29b8325…`≠`0e13cff59320` ⑤scene-rules 仍 `7c1f0620` 未漂 ⑥ProductVersion 0.7.3.0/0.7.3/0.7.3.0 ⑦冒烟 PID 4248 Responding=True 零 panic。

**产物**：feiyin-ime.exe `d29b8325b2c6…` 19:06:21 / feiyin-ime-ui.exe `067b7895bfd5…` 19:04:08 / crash-reporter.exe `c5f9c094c6e4…` 19:05:21。

**详情**：result.md（TEST-EXEC-027-G 报告节）
## 2026-08-04 — macOS 侧 Orchestrator session 启动：交接文档通读 + 环境取证（零代码改动）

**动作**：新 session 主控接管，按 worker-guide §二完成必读文档通读，并对仓库状态做独立取证（不以 todo/handoffs 记载为准，遵守 [DOC-STATE-DRIFT-001]）。

**取证结果（文件系统与 git 为准）**：

| 项 | 实测值 |
| --- | --- |
| HEAD | `0adb819` `feat(itn): Type A 单位碰撞保护词表落地 + 五角大楼白名单` |
| 与远端 | `main...origin/main` 已同步，无 ahead/behind |
| 工作区 | `M collab/todo.md`（07-31 主控自己的改动）、`?? logs/20260731.md`、`?? logs/20260804.md` —— **源文件零改动** |
| 版本号三处 | `Cargo.toml` / `src-tauri/Cargo.toml` / `tauri.conf.json` 均 **0.7.3**，一致 |
| Worker 状态 | coder-1(%2, glm-5.2) / coder-2(%1, kimi-k2.7-code) / tester-1(%3, DeepSeek V4 Flash) 三个 pane 均存活、idle，无僵死 |
| dispatch.sh | `bash -c` 下 `list_workers` 正常返回三个 Worker |

**⚠️ 与 07-31 记载的一处差异（需注意）**：07-31 todo.md 顶部写「9 个真实改动文件悬空未 commit」，本次实测工作区已 clean（源文件零改动），HEAD 即 `0adb819`。即那批 ITN 改动**已由 Windows 侧提交并被本侧 pull 到位**，不再存在悬空改动。todo.md 顶部该行已过时，待下次维护时更正。

**[SHELL-BASHSOURCE-ZSH-001] 仍未修复（复验证实）**：`zsh -c 'source collab/dispatch.sh'` 后 `COLLAB=/Users/gavinsun/Workspace`（应为 `/Users/gavinsun/Workspace/CodeLab/collab`），根因仍是 `collab/lib/env.sh:17` 的 `${BASH_SOURCE[0]}`。该文件属 `collab/` 框架、在 voice-ime 仓库之外，本主控**未改动**，一律用 `bash -c` 绕过。修法与 `scripts/env-macos.sh:11` 同（`${BASH_SOURCE[0]:-$0}`），待 Gavin 授权后可一并收口。

**当前状态**：三 Worker 空闲待派发，**无进行中的代码任务**。等 Gavin 指令。

---

## 2026-08-04 — Gavin 指令「拉一下最新代码」：实测已是最新，无需拉取（零改动）

**执行**：`git fetch origin`（只读取远端 ref，未 merge、未 rebase、未使用任何破坏性命令）。

**结果：本地已是最新，Windows 侧自 `0adb819` 以来无新提交。**

| 取证项 | 值 |
| --- | --- |
| `git ls-remote origin main`（绕过本地缓存直问远端） | `0adb819e495d8e6d43372bc86d0056c7bbc5f851` |
| 本地 `HEAD` | `0adb819e…` |
| 本地 `origin/main` | `0adb819e…` |
| `git log HEAD..origin/main` | **空** |
| 远端分支 | 仅 `refs/heads/main` 一个 |

**未执行 `git pull`**：`HEAD..origin/main` 为空，pull 是 no-op；且工作区有 `M collab/todo.md`，无谓的 merge 动作只会徒增风险。**工作区三项状态与拉取前完全一致**（`M collab/todo.md` / `?? logs/20260731.md` / `?? logs/20260804.md`），未触发 [CRLF-CROSSPLAT-001] 的幽灵 diff。

**含义**：Windows 侧团队自 07-30 23:00 出包 v0.7.3 后**至今（08-04）无新提交**，双侧代码完全同步。本侧 07-31 的 `MACOS-TESTEXEC-V073-001` 结论（macOS 上零编译错误、701/1/8）对当前 HEAD **仍然有效，无需重新取证**。

---

## 2026-08-04 — Gavin 指令「把 macOS 侧管线 Phase 4 的任务规划出来」：产出规划稿（零代码改动）

**产出**：`collab/research/macos-phase4-plan-001.md`（MACOS-P4-PLAN-001），todo.md 新增对应任务表 + 指针。**未派发任何任务**，等 Gavin 就 5 项决策拍板。

**主控本次逐文件实测取证（未采信既有文档结论，遵守 [DOC-STATE-DRIFT-001]）**：

- `run_pipeline` = `main.rs:2812-3214`（402 行）｜ `spawn_worker_thread` = `:2161-2459`（299 行）｜ `run_controller` = `:2460-2641`（182 行）
- 13 个共享模块 `grep -c target_os` 实测 **全部为 0**（transcription/llm/itn/config/translation/punctuation/scene/wordbook/version_check/ui-overlay/ui-tray/injection/hotkey）
- `mod macos_stubs`（`:3431-3486`）**全仓零引用**，当前是死代码脚手架

**🔑 本次规划的核心发现（改变任务分解方式）**：`run_pipeline` 的平台接触点**只有 4 个**（`:2823` HWND 签名 / `:2939` capture_scene_signals / `:3178` GetForegroundWindow / `:3192` inject），`spawn_worker_thread` **只有 1 个**（`:2450` HWND 转换）。**约 700 行核心管线可一次性转为双侧共享**，macOS 无需重写业务逻辑。

**📌 连带修正一处长期表述偏差**：`docs/MACOS-HANDOFF.md` §2.8 与 DEC-035 均称「`run_pipeline` 在 cfg 内，代码层无法共享，顺序约束是纯文档契约」。前半句是事实，但**「无法共享」是当前状态而非固有属性**。阶段 B 完成后，DEC-035 的 ITN 顺序契约即可从文档契约升级为代码层强制。

**❌ 主控在 Gavin 追问后修正了自己的一处建议**：初稿写「四个浮层整体不进第一版」。追问「录音/处理中/失焦返显/错误信息窗口是否还未实现」后逐个复核用途，发现该建议**对其中两个不成立** —— 录音/处理中可降级为托盘状态，但**失焦返显窗口缺失会导致 `PipelineEvent::FocusLost` 携带的整段转写文本被静默丢弃**（数据丢失，非观感问题），错误窗口缺失会让所有失败静默。故新增 `MACOS-P4-FEEDBACK-001`（托盘状态 + 系统通知 + FocusLost 自动入剪贴板）作为必需替代层，而非零成本省略。

## 2026-08-04 — DEC-036：Gavin 推翻主控降级建议，macOS 浮层必须是独立窗口且与 Windows 一致

**Gavin 原话**：「录音需要有单独的窗口，必须和 windows 侧的实现一致，不能用托盘图标代替录音窗口，不符合用户体验」。

**处置**：已写入 `decisions.md` **DEC-045**；`MACOS-P4-OVERLAY-001` 从阶段 E 提到 **阶段 C**（录音窗口 P0）；原 `MACOS-P4-FEEDBACK-001` 的「托盘替代浮层」定位**作废**。

**主控据此提取了 Windows 侧规格基线**（macOS 对齐依据，全部实测自 `src/main.rs`）：窗口 240×36（失焦返显 320×140）｜水平居中、底部上移 64px（`overlay_geometry:1731-1744`）｜`WS_POPUP` + `WS_EX_TOOLWINDOW｜TOPMOST｜LAYERED｜NOACTIVATE`｜DWM 圆角回落 `SetWindowRgn`、半径 10｜配色 背景 `#0D0F11` / 边框 `#070606` / 品牌橙 `#FF6B00` / 故障红 `#FF0000` / 静音灰 `#808080`（COLORREF 为 `0x00BBGGRR`，已转 RGB）｜18px 指示灯、4× 超采样｜三态优先级 红（缓冲空＝设备故障）> 橙（level>0.01）> 灰（静音）。

**⚠️ 连带风险已前置标注**：本决策要求启用 `Cargo.toml:115-116` 预留的 `objc2`/`cocoa`，加依赖时必须遵守 `[TOML-SECTION-DRIFT-001]`——Windows 侧 07-30 正是因为条件依赖段头插在 `[dependencies]` 表中间，静默把 3 个依赖划成单平台专属且在本平台完全不可见。

**Windows 侧影响：零**（只新增 macOS 实现，不触碰任何 Win32 绘制代码）。

---

## 2026-08-04 — 阶段 A 探针已派发 + 一轮方案协商裁定

**派发**：`MACOS-P4-PROBE-001` → tester-1（8,862 字节任务书，零源文件改动）。派发前主控代为确认素材齐备：sherpa dylib 4 个、SenseVoice 模型 + tokens.txt、`test_wavs/zh.wav`、系统默认麦克风存在。

**tester-1 提出有效技术异议（记录为正面案例）**：本仓是 binary-only crate（无 `src/lib.rs`、`Cargo.toml` 无 `[lib]` 段，主控已复核属实），`src/bin/probe_mic.rs` 作为独立 bin target 无法 `use` 主 bin 的 `audio` / `platform` 模块。它给出两个选项并倾向选项②。

**主控裁定：两个选项都不采纳，给出选项③**：
- 选项①（建 `lib.rs` + 改 `main.rs` 的 `mod`→`use`）→ **否决**，要动 `main.rs` 波及 Windows 编译路径，探针任务不值得冒这个险
- 选项②（probe 直接调 cpal/core_graphics）→ **降为兜底**。它验的是底层库而非本仓代码，A1 会退化成"cpal 能录音"这种近乎必然成立的结论，A4 更会整个绕开 `hotkey.rs` 448 行的配置解析与事件映射
- **选项③（采纳）：用 `#[path]` 属性挂载既有模块源文件** —— 测真实代码且零改动既有文件。主控实测依赖闭包封闭且极小：`ui/overlay.rs` 无 crate 依赖 / `i18n`↔`config` 互引 / `audio` 只依赖 `ui::overlay`（`:1` `:647`，`:1142-1143` 在 `#[cfg(test)]` 内不展开）/ `platform` 只依赖 `config`（`injection.rs`、`accessibility.rs` 零 crate 依赖）

裁定与完整写法已追加至任务书 §八。允许③编译不通时降级②，但必须标注为「底层库验证」而非「本仓代码验证」并贴出③的原始 rustc 报错。

---

**四浮层实测规模**（全在 `#[cfg(target_os="windows")]` 下）：录音 288 行 / 处理中 126 行 / 失焦返显 190 行 / 错误 74 行 = 绘制小计 678；加浮层线程 261 + 窗口过程 157 + 位图合成 74 + 文本助手 14 = **≈1,184 行 Win32 GDI**。macOS 侧 `grep -rn "NSWindow\|NSPanel\|overlay" src/platform/macos/` 命中 **0**。**但状态层 `src/ui/overlay.rs`（214 行，cfg 计数 0）已平台中立**，macOS 缺的是渲染+窗口而非状态机。

## 2026-08-04 — DEC-046（事件宿主定案）+ 阶段 B 派发 + FIXHOTKEY 验收 + 第 5 接触点裁定

**DEC-046（Gavin 拍板「事件宿主选型按你建议方案来」）**：macOS 事件宿主改用 **objc2 + NSApplication + CFRunLoop 直驱**，DEC-015 的「以 Tauri 为宿主」作废。理由：① 主程序至今不依赖 tauri，照原文执行等于为借 run loop 拖进整个 WebView ② DEC-045 已要求浮层是独立窗口，AppKit 成硬需求，objc2 无论如何都要引 ③ 与 Windows 结构一一对应（NSApplication↔隐藏 controller 窗口 / CFRunLoopTimer↔15ms WM_TIMER / CFRunLoopSource↔WM_APP_* 唤醒）④ 仓库已有 CFRunLoop 先例（`hotkey.rs`，DEC-017）。**遗留风险**：`tray-icon 0.19` 官方示例基于 winit/tao，自建 NSApplication 能否工作需实测，列为 HOST-001 第一关卡，不通则退回 winit。

**主控澄清（Gavin 追问「winit 是 macOS 一侧的事件循环机制吗」）**：不是。winit 是跨平台库，在 macOS 上通过 objc2 驱动 NSApplication；macOS 原生机制是 NSApplication + NSRunLoop/CFRunLoop + NSEvent。另更正主控前一版口径：`cargo tree -i winit` 实测 winit 0.30.13 来源是 `eframe 0.29.1 → voice-ime`，而 eframe/egui **全仓唯一使用者是 `src/crash/reporter.rs`**，与 tray-icon 及管线代码无关。

**派发（文件级零重叠，并行）**：coder-1 ← `MACOS-P4-NEUTRAL-001`（12,273 B）｜ coder-2 ← `MACOS-P4-FIXHOTKEY-001`（7,502 B）。

**✅ MACOS-P4-FIXHOTKEY-001 已验收（主控独立取证，未采信汇报）**：`git diff` 仅一个 hunk（`[CGEventType; 5]`→`[; 3]`，删两个带外事件，附 6 行依据注释）；源文件只动 `src/platform/macos/hotkey.rs`；**要求"不许动"的带外回调完好**（`:106-107` `TapDisabled*` 分支、`:276-278` `needs_reenable` 重启逻辑均在）；**主控独立复跑 `cargo check --all-targets` → 0 errors**（6.09s）。

**🔑 coder-1 查出第 5 接触点（记一功，主控裁定 B）**：`WorkerCommand:120` / `SendHwnd:149` / `StartCmd:154` 三类型定义均带 `#[cfg(windows)]`，故无 cfg 的 `spawn_worker_thread_core` 在 macOS 必然编译不过。

**主控补出一条 coder-1 未提及、但决定性的事实**：`SendHwnd` **不只服务 worker 路径，同时是 overlay 线程的通道载荷**（`:565` `bounded::<SendHwnd>(1)` / `:589` `Sender<SendHwnd>` / `:641` `send(SendHwnd(...))`，三处均在 Win32 GDI overlay 代码内、本轮不动）。**故即便走 A 去掉两类型 cfg，`SendHwnd` 仍须为 overlay 保留 `cfg(windows)`，A 换不来干净收口，反而要修改 Windows 既有调用点 `:1929-1930` 与 `:2293` —— 属"修改"而非"新增"，违反验收标准第 2 条与 Gavin 三次重申的红线。** A 否决、C 否决。

**裁定 B**：`spawn_worker_thread` 本轮完全不动（连薄封装都不做）；`run_pipeline` 照原方案；`run_pipeline_core` 加 `#[cfg_attr(not(target_os="windows"), allow(dead_code))]`（**禁止用无差别 `#[allow(dead_code)]`**，那会连 Windows 侧也放行、掩盖真实死代码）。**成功判据不变：`run_pipeline_core` 能在 macOS 编译通过本身即交付物**，它证明 402 行业务逻辑平台中立。

**衍生任务 `MACOS-P4-NEUTRAL-002`（待排期）**：`spawn_worker_thread` 去平台化，届时需一并处理 `StartCmd.target_hwnd: SendHwnd→WindowId`、`WorkerCommand` 去 cfg、`macos_stubs` 三个同名定义清理、两处 Windows 调用点修改。**因必然要改 Windows 既有代码行，拆为独立批次单独取证，不与本轮混做。**

---

## ⚠️ 2026-08-04 — 主控取证：macOS 出包的两个陷阱（写入未来出包任务书红线）

**背景**：Gavin 指令「开发完就派发测试任务，测试完成就派发出包任务」。主控据此前置取证出包环境，发现两个必须写死进任务书的陷阱。

**陷阱一 · `target/release/` 内全是 Windows 残留产物**（仓库自 Windows 迁移时连 `target/` 一并带过来）：

| 文件 | 大小 | mtime | `file` 实测 |
| --- | --- | --- | --- |
| `feiyin-ime.exe` | 11,603,456 | 07-28 18:42 | **PE32+ executable x86-64, for MS Windows** |
| `feiyin-ime-ui.exe` | 10,027,008 | 07-27 20:31 | 同为 Windows PE |
| `crash-reporter.exe` | 24,858,624 | 07-28 18:41 | 同上 |
| + `ctranslate2.dll` / `onnxruntime.dll` / `cudnn64_9.dll` 等 | | | Windows DLL |

**风险**：macOS 构建产物名为 `feiyin-ime`（**无扩展名**，实测当前不存在）。任何沿用 Windows 出包流程、按 `.exe` 名做校验的步骤，都会命中 **07-28 那个旧 Windows 二进制**并误报「产物已就绪」。属 `[BUILD-002]` 陷阱的跨平台变种。

**陷阱二 · `Publish/` 是 Windows 侧交付目录**（实测三个文件全为 Windows exe）。**macOS 产物绝对不得同步进去**，否则直接违反 Gavin 的 Windows 红线。

**✅ 一条好消息**：release 依赖树是热的——探针 11:41 用 `--release` 跑通 `poc_funasr_nano`（产物 `Mach-O 64-bit arm64`，419,776 B），`target/release/CTranslate2-4.6.0` 与两个 `ctranslate2-sys-*` build 目录均在。**macOS release 构建不会触发 20+ 分钟的 CTranslate2 全量重编。**

**⚠️ 且需先向 Gavin 说明**：`fn main()` 非 Windows 分支仍是 `log::warn!` 后直接返回（`main.rs:2738-2741`），`run_controller` 整个在 `#[cfg(windows)]` 内。**在 `MACOS-P4-HOST-001` 落地前，macOS 出包只能是「release 编译验证」，产出的二进制跑起来即退出，不是可分发的 `.app`。**

---

## 2026-08-04 — tester-1 执行 MACOS-P4-PROBE-001：A1/A2/A5 PASS，A4 FAIL，A3 部分 PASS

**动作**：按主控派发任务书执行五项探针（A1~A5），零源文件改动，临时探针 bin 跑完删除。

**结果摘要**：

| 探针 | 结果 | 关键数据 |
|------|------|----------|
| A1 cpal 录音 | PASS | 设备「外置麦克风」、48,128 样本/3s、max=0.791、RMS=0.108048 |
| A2 sherpa-onnx 转录 | PASS | 加载 0.594s、转写 0.256s、RTF=0.0458、文本「开饭时间早上九点至下午五点」 |
| A3 TCC 终端 vs .app | 部分 PASS | Terminal 与 .app bundle 主体不同，.app 未阻塞 |
| A4 CGEventTap 热键 | **FAIL** | `KEYBOARD_EVENTS` 含 `TapDisabledByTimeout(0xFFFFFFFE)` 导致 `1_u64 << 0xFFFFFFFE` panic |
| A5 辅助功能弹窗 | PASS | `ax_is_process_trusted_with_prompt()` 确为 stub |

**关键发现**：

- A4 根因：`src/platform/macos/hotkey.rs:28-34` 的 `KEYBOARD_EVENTS` 数组包含 `CGEventType::TapDisabledByTimeout` (0xFFFFFFFE) 和 `CGEventType::TapDisabledByUserInput` (0xFFFFFFFF)。`core-graphics-0.25.0` 的 `CGEventTap::new_unchecked` 用 `CGEventMaskBit!` 宏计算 mask：`1 << etype as u64`。当 etype = 0xFFFFFFFE 时，左移 >=64 位触发 Rust panic `attempt to shift left with overflow`。**这是 DEC-017 实现首次在实机上暴露的真实 bug**。
- A5 根因：`src/platform/macos/accessibility.rs:43-48` 的 `ax_is_process_trusted_with_prompt()` 函数体只有一行 `log::info!`，`AXIsProcessTrustedWithOptions` 从未被调用。

**下游影响**：

- A1/A2 成立 → 后段（ASR/LLM/ITN/翻译/标点/词库）管线可以复用既有代码，Phase 4 排期不受录音/模型假设影响。
- A4 FAIL → 必须修 `hotkey.rs:28-34` KEYBOARD_EVENTS（移除 `TapDisabledByTimeout/UserInput`）后才能进行热键实机验证。
- A5 stub → 需补真实 `AXIsProcessTrustedWithOptions` FFI 调用（CoreFoundation CFDictionary）。

**红线遵守**：零源文件改动（src/ src-tauri/ ui/ Cargo.toml 全部 clean），临时 `src/bin/probe_mic.rs` + `src/bin/ui/overlay.rs` 已删除，未使用 git 破坏性命令。

**关联文件**：`collab/outbox/tester-1/result.md`（10,353 字节）

---

## 2026-08-04 — MACOS-P4-FIXHOTKEY-001 · 修 CGEventTap 事件掩码越界（P0）

- **改动**：`src/platform/macos/hotkey.rs:28-38` 的 `KEYBOARD_EVENTS` 常量由 5 项改为 3 项，移除 `TapDisabledByTimeout` 与 `TapDisabledByUserInput`。
- **原因**：这两个枚举是 core-graphics 标注的 "out of band" 事件（`event.rs:226-229`），判别值 `0xFFFFFFFE` / `0xFFFFFFFF` 远超 `u64` 位宽；进入 `CGEventMaskBit!(1 << etype)` 计算后，debug 下会 panic（`attempt to shift left with overflow`），release 下虽被 wrapping_shl 掩码为 62/63 位但掩码语义错误。它们本就不应作为可订阅事件放入 event mask。
- **影响范围**：仅 macOS 专属文件 `src/platform/macos/hotkey.rs`，零 Windows 风险；`:102-103` 的回调分支与 `:272-275` 的 re-enable 逻辑保持完整，未受影响。
- **验证**：`source scripts/env-macos.sh && cargo check` → 0 errors；`cargo check --all-targets` → 0 errors。
- **文件数**：仅 1 个文件 1 个 hunk，diff 面最小。


## 2026-08-04 — coder-1 — MACOS-P4-NEUTRAL-001: run_pipeline 管线去平台化（402 行业务逻辑变双侧可复用）

**任务**：把 `run_pipeline`（src/main.rs，原 :2812-3214，402 行）从 Windows 专属变为平台中立，让 macOS 复用后段业务逻辑（ASR 前处理 → 转录 → 场景采集 → 翻译/优化三分支 → ITN → 标点 → 注入 → 词库学习），Windows 侧行为一字不变。

**方案协商**（两轮，主控裁定）：
- **第 5 接触点**（我提出）：`spawn_worker_thread` 参数 `Receiver<WorkerCommand>`，而 `WorkerCommand`/`StartCmd`/`SendHwnd` 三类型带 `#[cfg(windows)]`，无 cfg 的 `spawn_worker_thread_core` 在 macOS 无法编译。主控补充：`SendHwnd` 还服务 overlay 线程（:565/:589/:641），A 方案换不来干净收口。**裁定 B**：`spawn_worker_thread` 本轮完全不动（连薄封装都不做），其去平台化另立 MACOS-P4-NEUTRAL-002。
- **第 6 接触点**（我提出）：`run_pipeline` 函数体调用 3 个 `#[cfg(windows)]` 辅助函数 `select_preprocessing_params`/`should_try_llm_translate`/`try_nllb_translate`，无 cfg 的 `run_pipeline_core` 在 macOS 编译报 E0425。主控穷举核查全部 29 个 cfg 门控函数，确认仅此 3 个被 run_pipeline 调用，且均为平台中立纯 Rust。**裁定 A**：删除这 3 行 `#[cfg]` 属性（对 Windows 构建为可证明 no-op），函数体一字不动，每处补说明注释。`translation_needs_reload` 不动（不在 run_pipeline 调用集）。

**改动**：
1. `src/main.rs`：
   - `run_pipeline` 改为薄封装：保留原签名（含 `target_hwnd: HWND`）+ `#[cfg(windows)]`，函数体缩为一行委托 `run_pipeline_core(..., target_hwnd.0 as usize, ...)`。
   - 新增 `run_pipeline_core`：无 cfg，签名 `target_hwnd: platform::WindowId`，函数体为原 run_pipeline body 逐字搬入 + 2 处接触点改动：
     - 接触点②：`platform::capture_scene_signals(target_hwnd)` → `platform::capture_scene_signals_by_id(target_hwnd)`
     - 接触点③：`GetForegroundWindow()` + `is_null()`/`.0` 比较块 → `platform::foreground_window_id()` + `!= 0` 比较（日志改打 usize）
   - `run_pipeline_core` 加 `#[cfg_attr(not(target_os = "windows"), allow(dead_code))]`（macOS 暂无调用者，Windows 侧不放行真实死代码）。
   - 删除 3 行 `#[cfg(target_os = "windows")]` 属性（select_preprocessing_params / should_try_llm_translate / try_nllb_translate），各补一行说明注释。
2. `src/platform/mod.rs`：新增 `pub type WindowId = usize;` + 两份导出清单各加 2 符号（`capture_scene_signals_by_id`、`foreground_window_id`）+ 契约注释表登记。
3. `src/platform/windows/scene.rs`：纯新增 `capture_scene_signals_by_id`（委托既有 `capture_scene_signals`）。
4. `src/platform/windows/event_loop.rs`：纯新增 `foreground_window_id`（新增单独 `use GetForegroundWindow` 行不扰动既有 use 块）。
5. `src/platform/windows/mod.rs`：纯新增 2 行 re-export（不动既有 `pub use` 行）。
6. `src/platform/macos/mod.rs`：纯新增 `capture_scene_signals_by_id`（委托既有 stub）+ `foreground_window_id`（返回 0 降级）。

**验证**：
- 验收#1（函数体逐字节）：原 body 去空白 md5 `869e8e8a...`；core body 经 2 接触点改动后与预期 body 逐行比对 0 mismatch，去空白 md5 `87bc7c292d02d3d41a721d9970047c29` 与预期完全相等。
- 验收#2（Windows 零删除）：`git diff --numstat -- src/platform/windows/**` 三文件删除列全为 0。
- 验收#3/#4：`source scripts/env-macos.sh && cargo check` → 0 errors；`cargo check --all-targets` → 0 errors。
- 验收#5：两份导出清单各 17 符号，逐一对应（含新增 2 个）。
- 验收#6：`git diff --stat` 仅 6 个任务文件。
- 验收#9：6 文件首字节均无 BOM。
- 验收#10：`git diff src/main.rs | grep '^-[^-]'` 输出 9 行（3 行 cfg 属性删除 + 6 行原接触点代码移动替换），全部为批准项。

**衍生收益（本轮不做，写入 result.md 供 TEST-SYNC）**：`select_preprocessing_params` 去 cfg 后，`6f0b51e` 当初为它加 cfg 的 5 个 `#[test]` 具备在 macOS 跑的条件，TEST-SYNC 一并解除 `#[cfg(all(test, target_os = "windows"))]` 可把 07-31 量化的 22 条 macOS 测试盲区减少 5 条。

**红线遵守**：未破坏 Windows 既有功能；未碰 `src/platform/macos/hotkey.rs`（coder-2 并行占用）；未动 `spawn_worker_thread`/`macos_stubs`/`WorkerCommand`/`StartCmd`/`SendHwnd`/`translation_needs_reload`；未 `cargo build --release`/出包/碰 Publish；未改版本号/Cargo.toml；未 `npm install`/`cargo clean`；未用 git 破坏命令；UTF-8 无 BOM 用 edit/Python 脚本。

## 2026-08-04 — tester-1 执行 TEST-SYNC-P4-NEUTRAL-001：13 条新增/解封测试，cargo check --tests 0 errors

**动作**：按主控 TEST-SYNC 阶段三纪律（只写测试，禁止执行 cargo test/build/pytest），覆盖 FIXHOTKEY + NEUTRAL 两批改动的测试同步。

**P0-1 解除 cfg 门控**：`src/main.rs` 的 `select_preprocessing_params` 5 条测试，解除 6 处 `#[cfg(target_os = "windows")]`（1 use + 5 test）。函数已平台中立（只做 AsrModel match 返回 (usize, usize)），确认安全。盲区从 22 条降至 17 条。

**P0-2 KEYBOARD_EVENTS 护栏**：`src/platform/macos/hotkey.rs` 新增 2 条测试：① 断言所有 KEYBOARD_EVENTS 判别值 < 64（CGEventMaskBit 安全范围）；② 显式黑名单断言不包含 TapDisabledByTimeout/UserInput。

**P0-3 foreground_window_id 契约**：`src/platform/macos/mod.rs` 新增 1 条测试，断言第一版返回 0（将来真实实现时会变红，强制更新契约）。

**P0-4 focus_lost 真值表**：`src/main.rs` 新增 4 条测试，镜像 `run_pipeline_core:3217` 的表达式 `target_hwnd != 0 && current_id != target_hwnd`，覆盖 (0,0)/(0,123)/(123,123)/(123,456) 四行真值表。

**P1 capture_scene_signals_by_id 降级**：`src/platform/macos/mod.rs` 新增 1 条测试，断言 stub 恒返 None。

**新增测试函数总数**：8 条全新 + 5 条解封 = 13 条 macOS 首次可见。

**编译验证**：`cargo check --tests` → 0 errors，6.72s，100 warnings 全部既有 dead_code。

**红线**：零生产代码改动，仅改 `#[cfg(test)]` 块与 6 行 cfg 属性。未执行 cargo test / cargo build --release / pytest。未触碰 src/platform/windows/、Cargo.toml、版本号。未使用 git 破坏性命令。

**关联文件**：`collab/outbox/tester-1/result.md`（9,110 字节）

---

## ⏸ 2026-08-04 — Gavin 指令暂停（额度用尽）· 断点状态固化

**已闭环并验收（4 项）**：`MACOS-P4-PROBE-001`(tester-1) / `MACOS-P4-FIXHOTKEY-001`(coder-2) / `MACOS-P4-NEUTRAL-001`(coder-1) / `TEST-SYNC-P4-NEUTRAL-001`(tester-1)。

**TEST-SYNC 验收记录**：`cargo check --tests` 0 errors（主控复跑 6.96s）；P0-1 **6 处缩进 cfg 确已解封**（1 `use` + 5 `#[test]`）→ **macOS 测试盲区 22 → 17**；新增 8 条 `#[test]` + 解封 5 条 = 13，数字自洽；护栏断言 `v < 64` 且不含 `TapDisabled*` 已就位。

### 🔄 `MACOS-P4-HOST-001`（coder-2）代码已写完但**未汇报、result.md 仍 0 字节** —— 恢复后从此处接续

`src/platform/macos/event_loop.rs` 已创建（7,964 B，12:40）。coder-2 屏幕自述「event host is ready」「staged 7 files, 581 insertions」。

**主控已完成的部分验收（未采信汇报，恢复后接着查剩余项）**：

| 项 | 结论 |
| --- | --- |
| ✅ `src/platform/windows/**` 未被 coder-2 触碰 | `numstat` 仍为 `12/0`、`3/0`、`7/0`（coder-1 的纯新增），硬门槛守住 |
| ✅ macOS 依赖段落位置正确 | `objc2 0.5` / `objc2-app-kit 0.2` / `objc2-foundation 0.2` / `core-foundation-sys 0.8` / `libc 0.2` 全部加在 `:115-119` 段内，其后即空行 + `[target.'cfg(target_os="windows")'.build-dependencies]`，**未触发 `[TOML-SECTION-DRIFT-001]`** |
| ✅ `main.rs:389` 的 cfg 放宽属正确改动 | `#[cfg(target_os="windows")]` → `#[cfg(any(target_os="windows", target_os="macos"))]`（`platform::notify_config_changed()` 调用点）。**对 Windows 是 no-op**（`any(...)` 在 Windows 上仍为真，item 照常包含），与 NEUTRAL-001 的 cfg 删除同理 |

### ❌ 恢复后必须处理的两个问题（主控已查出，尚未通知 coder-2）

**问题 A · `ctrlc = "3.4"` 被加进了 `[dependencies]` 主表**（`Cargo.toml:72` 附近，注释写 "used by macOS controller loop"）。

- **直接违反任务书 §四 明确指令「不要动 `[dependencies]` 主表」**
- 后果：这是**为 macOS 目的引入的跨平台依赖**，Windows 侧也会编译它、进二进制、改 `Cargo.lock` —— **不是「Windows 零影响」**
- 处置方向：移入 `[target.'cfg(target_os = "macos")'.dependencies]` 段；或直接去掉——Windows 侧本就没有 ctrlc 处理器，按「参考 Windows 尽量一致」的口径，tray-first 应用靠托盘菜单退出，SIGINT 处理并非必需
- **⚠️ 移动时同样必须遵守 `[TOML-SECTION-DRIFT-001]`**

**问题 B · `tray-icon` 可行性验证被跳过**。coder-2 屏幕自述 "Tray-icon feasibility was not tested in this pass; tray integration is next"。

- 该项是**任务书验收标准第 4 条**，也是 **DEC-046 唯一遗留风险的出清关卡**（自建 NSApplication 下 tray-icon 能否工作，官方示例均基于 winit/tao）
- 不能顺延给 `MACOS-P4-TRAY-001` —— 若不通，DEC-046 需整体退回 winit，**越晚发现返工面越大**
- 处置：要求 coder-2 补做最小验证（临时代码，验完删）

### 📌 其他记录

- coder-2 执行了 `git add`（7 文件进入暂存区），使 index 混入了 FIXHOTKEY + NEUTRAL + TEST-SYNC 三批已验收改动。**非破坏性，恢复后按需处理；严禁用 `git reset` / `git checkout --` 等破坏性命令收拾**（`[GIT-RESET-INCIDENT-001]`）
- **coder-2 报告的一条有价值发现（供出包任务书采纳）**：release 产物需把 vendored `.dylib` 拷到可执行文件同级并设置 `@executable_path` rpath，其本轮只对 debug 做了该处理

### 恢复后的既定顺序

1. 通知 coder-2 处理问题 A + B → 重新验收 `MACOS-P4-HOST-001`
2. 验收通过后**并行派发**：`MACOS-P4-OVERLAY-001`(coder-1，四浮层，DEC-045 规格已备) ∥ `TEST-EXEC`(tester-1，全量回归 + **A4 热键实机复测**——此时宿主已就位，探针那轮卡在 panic 未走到的验证才有意义)
3. 出包（范围待 Gavin 定：`HOST-001` 落地前 macOS 只能做 release 编译验证，非可分发 `.app`）

## 2026-08-04 — coder-1 — MACOS-P4-PERM-001: 辅助功能授权弹窗真实现（macOS 专属，零 Windows 风险）

**任务**：`src/platform/macos/accessibility.rs` 末尾 `ax_is_process_trusted_with_prompt` 是假实现（仅 log，从未调用 `AXIsProcessTrustedWithOptions`），导致用户未授权时看不到系统弹窗、热键静默失效。补全 FFI 绑定与调用。

**改动**（仅 1 文件 `src/platform/macos/accessibility.rs`）：
- 新增 FFI 声明 `AXIsProcessTrustedWithOptions(options: CFDictionaryRef) -> bool` + `static kAXTrustedCheckOptionPrompt: CFStringRef`（ApplicationServices framework）。
- `ax_is_process_trusted_with_prompt` 真实现：用 `core_foundation` 构造 `CFDictionary::from_CFType_pairs([(kAXTrustedCheckOptionPrompt→CFString, CFBoolean::true_value())])`，调 `AXIsProcessTrustedWithOptions(options.as_concrete_TypeRef() as CFDictionaryRef)` 触发系统弹窗。
- `ensure_accessibility_at_startup`：未授权时清晰 log::warn（说明热键在授权前不工作 + 引导系统设置路径）+ 触发弹窗 + 返回 Ok(())（不阻断启动，与 Windows 侧 RegisterHotKey 失败不阻断对齐）。
- 3 个公开函数（`is_accessibility_granted`/`request_accessibility_permission`/`ensure_accessibility_at_startup`）名称与签名均未改，两个既有调用点（mod.rs/hotkey.rs）不受影响。
- 依赖零新增：`core-foundation = "0.10"` / `core-foundation-sys = "0.8"` 已在 Cargo.toml macOS 段，未改 Cargo.toml。

**验证**：
- 验收#1：`git diff --numstat -- src/platform/macos/accessibility.rs` = `42 11`（仅本文件，其余工作区 diff 为 coder-2 的 HOST-001 改动非本任务）。
- 验收#2：`source scripts/env-macos.sh && cargo check --all-targets` → 0 errors。
- 验收#3：3 个公开函数签名未改（`is_accessibility_granted() -> bool` / `request_accessibility_permission()` / `ensure_accessibility_at_startup() -> Result<()>`）。
- 验收#4：`AXIsProcessTrustedWithOptions` 确实被调用（见 diff 与源码）。
- 验收#5：**未实机验证，仅代码层完成**——本机大概率已授权辅助功能，无法安全复现未授权状态（`tccutil reset` 需谨慎，且授权后需重启应用才生效）。如实降级。
- 验收#6：未跑 cargo test / cargo build --release / 出包 / 改 Cargo.toml。
- 验收#7：UTF-8 无 BOM（首字节 `2f`）；`cargo fmt -- src/platform/macos/accessibility.rs` 已执行。

**跨端影响（§2.10）**：纯 macOS 平台代码（`src/platform/macos/accessibility.rs`），仅调用 macOS 专属 framework（ApplicationServices），对 Windows 零影响——Windows 侧无 accessibility 概念，该文件在 Windows 编译时被 `#[cfg(target_os="macos")]` 门控不参与编译。

**红线遵守**：未破坏 Windows 既有功能；未碰 coder-2 占用的 main.rs/mod.rs/event_loop.rs/Cargo.toml；未碰 hotkey.rs（只读）；未用 git 破坏命令；未 npm install/cargo clean；未出包/碰 Publish/改版本号；UTF-8 无 BOM。

## 2026-08-04 — tester-1 — TEST-EXEC-MERGE-001 完成（合并后全量回归 + A4 热键实机首测成功）

**任务**：Windows 侧 68 提交合并（`cc12381`）后的首次 macOS 全量回归 + A4 热键实机复测

### Step 1 主程序 `cargo test --no-fail-fast`
- 880 passed / **0 failed** / 8 ignored，与 `--list` 888 自洽
- 较 07-31 基线（701/1/8）大幅提升：新增 79 条 + `time_half` 已知失败修复

### Step 1b src-tauri
- 53 passed / 0 failed / 0 ignored ✅ 与基线一致

### Step 2 前端
- 54 passed (54 total) ✅ 与基线一致

### Step 3/4 pytest
- 147/156 收集（9 deselected）✅ 与基线一致

### Step 5 A4 热键实机复测（首次成功）
- **辅助功能权限已授予**（未触发 "not granted" 警告）
- **CGEventTap(Session) 成功收到全局按键事件**：Start → Stop（500ms hold）+ Start → CancelStop（short tap）
- **无 panic**：FIXHOTKEY-001 的 `KEYBOARD_EVENTS [CGEventType;3]` 修复已生效
- 临时 probe 文件已删除

### 盲区复算
- `platform/windows/hotkey.rs` 15 + `platform/windows/scene.rs` 2 = **17 条**
- `pipeline_logic_tests` 7 条已由 TEST-SYNC-P4-NEUTRAL-001 解封，不再属盲区

### 跨端影响评估（回报 Windows 侧）
- 68 提交（itn.rs +4411 / llm/mod.rs +1683 / scene/mod.rs +464）全部通过，**零 (a)/(b) 类失败**
- `time_half` 由 ITN v2 修复（`"8:30"`），不再是失败
- 我方三批改动（FIXHOTKEY-001 / NEUTRAL-001 / TEST-SYNC-P4-NEUTRAL-001）全部验证通过

### 红线遵守
- 零源文件改动（probe 已删）；未 cargo build --release / npm install / cargo clean / git 破坏性命令
- 遇 coder-2 在途编译错误（3 errors）上报主控，未自行修复（红线 #6）

- 详情：`collab/outbox/tester-1/result.md`（7,483 字节）

## 2026-08-04 — coder-1 — MACOS-P4-NEUTRAL-002: 打通语音输入闭环（worker 去平台化 + macOS 接线）

**任务**：让 macOS 上按热键 → 录音 → 转录 → LLM → ITN → 注入光标处完整闭环跑通。spawn_worker_thread（main.rs:2161）去 cfg，macOS 侧 run_controller_macos 接线。

**方案协商**：
- 主控已替我穷举扫描全部 29 个 cfg(windows) 门控函数对 spawn_worker_thread body 的调用，确认接触点仅 run_pipeline（删薄封装）+ load_hotwords_for_accuracy（2次）+ compute_hotwords_version（1次）+ spawn_worker_thread 自身。
- 实施中我撞到【第7、第8接触点】：load_hotwords_for_accuracy(:2121) 与 compute_hotwords_version(:2153) 带 #[cfg(windows)]，spawn_worker_thread 去 cfg 后 macOS 报 3 个 E0425。主控裁定同 NEUTRAL-001：删 2 行 cfg 属性（对 Windows 为 no-op），函数体不动。
- macOS 接线方案（我提出）：run_controller_macos 内 spawn 逻辑线程轮询 hotkey_rx + pipeline_event_rx，调 platform::run_message_loop()（非 _with_hotkey_listener）做 CFRunLoop 宿主，CONTROLLER_CTX 为 null 时 timer 空转，严格只动 main.rs。主控认可。

**改动**（仅 src/main.rs）：
1. §2.1 WorkerCommand/StartCmd 去 cfg，StartCmd.target_hwnd: SendHwnd → platform::WindowId。SendHwnd 保留 #[cfg(windows)]（overlay 仍用）。
2. §2.2 Windows 唯一实质修改：main.rs:1929 `target_hwnd: SendHwnd(hwnd.0 as isize)` → `target_hwnd: hwnd.0 as usize`。
3. §2.3 spawn_worker_thread 去 cfg + :2439 调用 run_pipeline → run_pipeline_core（target_hwnd 不再转 HWND）。
4. §2.4 自证 run_pipeline 零调用者（grep 仅自身定义），删除薄封装（:2897-2929）。
5. §2.5 macos_stubs 删重复定义：SendHwnd/StartCmd/WorkerCommand/spawn_worker_thread（保留 Overlay*/encode_wide/spawn_overlay_thread）。
6. §2.6 run_controller_macos 重写：建 worker/pipeline channel + spawn_worker_thread + 逻辑线程（handle_hotkey_event 处理 Start/Stop/CancelStop + handle_pipeline_event 消费 PipelineEvent）+ run_message_loop 宿主。FocusLost 复制剪贴板防丢词。新增 handle_hotkey_event / handle_pipeline_event 两辅助函数。
7. 第7/8 接触点：删 load_hotwords_for_accuracy / compute_hotwords_version 的 #[cfg(windows)]，补注释。

**预存在删除行（非本任务产生）**：main.rs 的 ctrlc::set_handler 代码块（8行）+ Cargo.toml 的 `ctrlc = "3.4"`（3行）是 coder-2 的 MACOS-P4-HOST-001 问题A修复（主控裁定 A1 后执行），本任务开工前已存在于工作区。我保持一致未恢复，因恢复会与无 ctrlc 依赖的 Cargo.toml 冲突致编译失败（Cargo.toml 我禁碰）。

**验证**：
- 验收#1：删除行清单 = 5 个 cfg 属性 + SendHwnd(hwnd...)行 + run_pipeline 调用 2 行 + run_pipeline 薄封装整体 + macos_stubs 重复定义 + 2 个 cfg 属性（第7/8接触点）+ 预存在 ctrlc 块（8行，非本任务）。
- 验收#2：`git diff --numstat -- src/platform/windows/` 空。
- 验收#3：run_pipeline( 零调用者自证（grep 仅自身定义行）。
- 验收#4：spawn_worker_thread body 除 :2439-2440 两行外逐字未改，去空白 md5 `fd38b981a109aa4aab2337bbdf946aa5` 与预期完全相等，0 mismatch。
- 验收#5：cargo check --all-targets → 0 errors。
- 验收#6（闭环）：**部分验证**。cargo run --bin feiyin-ime 启动成功，模型加载完整（SenseVoice + Punctuation + hotkey listener CGEventTap + CFRunLoop 宿主均日志确认）。但热键闭环（Recording → Transcribed → Injecting text）**未实机验证**——本环境 osascript System Events 模拟 F6 按键不经过 CGEventTap(Session) 捕获层（事件源不匹配），无法触发；需物理 F6 键实测。如实降级。
- 验收#7（退出路径）：**未验证干净退出**。SIGINT 后进程不退出——因 ctrlc 处理器已被预存在删除（见上），无信号触发 platform::request_stop()。这是 ctrlc 移除的副作用，非本任务引入。pkill -9 强制退出无 panic。建议主控或 Gavin 评估是否补 ctrlc 或其他退出机制（属 HOST-001/OVERLAY-001 范畴）。
- 验收#8：foreground_window_id() 返回 0 → focus_lost 恒 false（降级），已记录在 handle_pipeline_event 的 FocusLost 分支注释。
- 验收#9：不跑 cargo test / build --release / 出包。
- 验收#10：UTF-8 无 BOM（首字节 23）；cargo fmt -- src/main.rs 已执行。

**跨端影响（§2.10）**：本任务改了 Windows 既有调用点 main.rs:1929（SendHwnd→usize）、删了一个 Windows 可见函数 run_pipeline 薄封装、改了共享类型 StartCmd/WorkerCommand 定义（去 cfg + target_hwnd 类型）。但语义等价、Windows 行为零改动（run_pipeline 薄封装零调用者、删除是行为中性；cfg 删除对 Windows 为 no-op）。详见 §2.10-C。

**红线遵守**：未碰 src/platform/windows/**、macos/overlay.rs、macos/mod.rs、accessibility.rs、Cargo.toml、版本号、Publish/、ui/、src-tauri/；未用 git 破坏命令；未 npm install/cargo clean；未出包；UTF-8 无 BOM。

## 2026-08-04 — coder-1 — MACOS-P4-EXIT-001: 修复 macOS 无干净退出路径（P0，主控裁定失误补救）

**任务**：HOST-001 问题A 主控裁定移除 ctrlc，但 macOS 托盘未实现 → SIGINT 不触发 request_stop → 只能 kill -9。补回信号处理，让 Ctrl-C/SIGTERM 触发与 Windows 等价的干净收尾。

**方案**：B1（不引 crate，用 libc::signal）。libc=0.2 已在 macOS 段，零新增依赖。handler 只置 AtomicBool（async-signal-safe），轮询线程在普通上下文调 platform::request_stop()（CFRunLoop API 非 async-signal-safe，不能在 handler 内调）。

**改动**（仅 src/main.rs，全部在 #[cfg(target_os="macos")] 内）：
- 新增 `static MACOS_SIGINT_RECEIVED: AtomicBool`（cfg macos）
- 新增 `extern "C" fn macos_signal_handler`（cfg macos，只置 bool）
- 新增 `fn install_macos_signal_handler`（cfg macos）：注册 SIGINT/SIGTERM handler + spawn 轮询线程检测 flag 后调 platform::request_stop()
- `run_controller_macos` 开头调 `install_macos_signal_handler()`
- handler cast 用 `as *const () as libc::sighandler_t`（避免 function_casts 警告）

**验证**：
- 验收#1（实测退出）：cargo run 启动 → kill -INT → 完整日志链：`SIGINT/SIGTERM received, requesting controller stop` → `macOS logic thread exiting` → `macOS controller loop exited cleanly` → 进程 EXITED，无残留。✅
- 验收#2（二次启动）：退出后立即再启动，正常运行（无 "Application already running"），flock 已释放。✅
- 验收#3：`git diff --numstat -- src/platform/windows/` 空。
- 验收#4：新增代码全部在 `#[cfg(target_os="macos")]` 内。
- 验收#6：cargo check --all-targets 0 errors。
- 验收#7：不跑 test/build --release/出包。
- 验收#8：UTF-8 无 BOM；cargo fmt -- src/main.rs（未全量 fmt）。

**跨端影响（§2.10-D）**：纯 macOS 平台代码（cfg 门控），对 Windows 零影响。

**红线遵守**：未碰 windows/**、macos/overlay.rs、macos/mod.rs、Cargo.toml、版本号、Publish；未用 git 破坏命令；未出包。

## 2026-08-05 — coder-1 — MACOS-P4-OVERLAY-001: macOS 录音浮层 1:1 复刻 Windows（P0）

**任务**：创建 `src/platform/macos/overlay.rs`，实现 macOS 录音浮层窗口，1:1 复刻 Windows `draw_recording_overlay`/`overlay_geometry` 的视觉与动画。只做 Recording 状态（P0）；Processing/FocusLost/Error 留给 OVERLAY-002。不接线 main.rs（macos_stubs 的 OverlayCommand/OverlayRequest 保留）、不改 mod.rs/Cargo.toml/windows/**。

**方案**：
- `NSPanel`（`NSBorderlessWindowMask | NSNonactivatingPanelMask`，≈ Windows `WS_POPUP|WS_EX_TOOLWINDOW|WS_EX_NOACTIVATE`）+ `setLevel(NSStatusWindowLevel)`（≈ TOPMOST）+ `setOpaque(false)` + `setBackgroundColor(clearColor)`（≈ LAYERED）
- 自定义 `OverlayView`（`declare_class!` 子类化 NSView，重写 `isFlipped`→true 使坐标与 Windows GDI 同为左上原点 + `drawRect:`）
- 绘制走 `NSGraphicsContext::currentContext()` → `graphicsPort()` → `CGContext`（core-graphics 0.25），原生抗锯齿等价 Windows HALFTONE 4x 超采样
- 圆角矩形用 `move_to_point`/`add_line_to_point`/`add_quad_curve_to_point`/`close_path` 手工构建（core-graphics 0.25 无 add_arc；曾请求 NSBezierPath feature 后撤回——CGContext 方案足够）
- 60fps 用 16ms `CFRunLoopTimer`（default mode），可见时 `setNeedsDisplay(true)`
- 模块级单例（`ACTIVE_OVERLAY: AtomicPtr` + `PENDING_LEVELS: Mutex<Option>`），与 Windows overlay 单窗口语义一致；`OverlayIvars` 非 ObjC 类型，经 `initWithFrame:` 重写时从静态注入
- 公共 API：`RecordingOverlay::new(levels)` / `show()` / `hide()` / `is_visible()` / `destroy()`（Drop 自动收尾：invalidate+release timer、orderOut+close）

**视觉规格（1:1，全部来自 Windows 常量）**：
- 240×36，圆角 10，`x=(screen_w-240)/2`，`y=(screen_h-36-64).max(0)`（底部上方 64px）
- 背景 `#0D0F11`，边框 1px `#060607`
- 三态指示灯：直径 18px，`left+6`，垂直居中；红 `#FF0000`（缓冲空=设备故障）> 橙 `#FF6B00`（level>0.01）> 灰 `#808080`（静音）
- 波形：32 柱（3px 宽/2px 间隔）中心对称展开；`maxh=48`/`static_h=12`/`minh=8`；`gain=2.5`；峰值衰减 `DECAY_RATE=0.02`/帧；权重 `0.4+0.6*cos(π/2·i/15)²`（中心=1.0 边缘=0.4）
- 左右分隔条（x=30 与 right-36，2px 宽 20px 高）+ 停止按钮（16px 外框 1px 橙 + 8px 实心内芯）

**验证**：
- 验收#1：cargo check --all-targets → 0 errors（probe bin 及 probe test target 均通过）
- 验收#2：`cargo test` 全绿（主 crate 816 + probe 13，0 failed），含 overlay 新增 5 条单测（权重映射/高度映射/三态判定/衰减率/中心对称）
- 验收#3（实测）：`cargo run --bin probe_overlay` → 浮层正常显示（NSPanel+NSView+drawRect 全链路无 panic），20s 合成电平驱动，两段 5s 报告 **59.1 / 59.0 fps**，接近 60fps 目标
- 验收#4：`git diff --numstat -- src/platform/windows/` 空；新增文件仅 overlay.rs + probe_overlay.rs
- 验收#5：未跑 build --release/出包；未改版本号
- 验收#6：UTF-8 无 BOM（cargo fmt -- 已执行）

**跨端影响（§2.10-E）**：纯 macOS 新文件，Windows 零影响。Cargo.toml 的 overlay feature（NSPanel/NSWindow/NSView/NSResponder/NSScreen/NSColor/NSGraphics）由主控加，本任务未改。

**红线遵守**：未碰 windows/**、main.rs、macos/mod.rs、macos/tray.rs、Cargo.toml、版本号、Publish、ui/、src-tauri/；未用 git 破坏命令；未出包。
