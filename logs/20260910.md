# 修改历史 · voice-ime · 20260910

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-09-10 — Session 启动：三个 Worker 全部因 OpenCode Go 月度额度耗尽而不可用

**主控开工检查**（13:19-13:23）结论：**本次会话零可用 Worker**，任务无法派发。

| Worker | pane | 模型档 | 状态 |
| --- | --- | --- | --- |
| coder-1 | %2 | `GLM-5.3-Flash (2x usage) · OpenCode Go` | ❌ `monthly usage limit reached, reset in 24 days 10 hours`，`[retrying in ~3 weeks attempt #1]` |
| coder-2 | %1 | `GLM-5.3-Flash (2x usage) · OpenCode Go` | ❌ 同上 |
| tester-1 | %3 | `deepseek-v4-flash · OpenCode Go` | ❌ 启动上下文停在输入框未提交；主控补发 Enter 后，**同样**命中 monthly limit |

**判据**：`capture-pane` 末行有 `monthly usage limit reached` / `retrying in ~3 weeks`
⇒ 与 `[WORKER-RESTART-MODEL-RESET-001]` 同一失效机制，但**这次是账户级 OpenCode Go 月度额度**，
不是单个模型档耗尽 —— 换 GLM↔deepseek 无效（tester-1 已实证：不同模型名、同一 provider、同一条限额）。

**主控未做的事及原因**：
- 未重启任何 Worker —— 重启只会把模型重置为默认档，对额度耗尽零帮助（该条目明载）。
- 未盲发 `/models` 切档 —— 该条目「主控踩坑」节明确：带弹层的交互切档交给 Gavin 手工做，
  主控盲发 send-keys 判断不了焦点归属，上次越弄越乱。

**待 Gavin 决策**：① 在 OpenCode 里开启 "continue using usage from your available balance"（余额付费）；
或 ② 手工把三个 Worker 切到仍有额度的 provider（注意同名模型有 Zen / Go / Ollama Cloud 多档，
Zen 是历史上余额耗尽的那个）；或 ③ 本轮不派 Worker，主控单干轻量任务。

**本次会话零代码改动、零 commit、零出包。**

