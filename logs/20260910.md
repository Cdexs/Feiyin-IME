# 修改历史 · voice-ime · 20260910

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---

## 2026-09-10 — Session 启动：三个 Worker 全部因 OpenCode Go 月度额度耗尽而不可用

**主控开工检查**（13:19-13:23）结论：**本次会话零可用 Worker**，任务无法派发。

| Worker | pane | 模型档 | 状态 |
| --- | --- | --- | --- |
| coder-1 | %2 | `GLM-5.3-Flash (2x usage) · OpenCode Go` | ❌ `monthly usage limit reached, reset in 24 days 10 hours`，`[retrying in ~3 weeks attempt #1]` |
| coder-2 | %1 | `GLM-5.3-Flash (2x usage) · OpenCode Go` | ❌ 同上 |
| tester-1 | %3 | `deepseek-v4-flash · OpenCode Go` | ❌ 启动上下文停在输入框未提交；主控补发 Enter 后，**同样**命中 monthly limit |

**判据**：`capture-pane` 末行有 `monthly usage limit reached` / `retrying in ~3 weeks`
⇒ 与 `[WORKER-RESTART-MODEL-RESET-001]` 同一失效机制，但**这次是账户级 OpenCode Go 月度额度**，
不是单个模型档耗尽 —— 换 GLM↔deepseek 无效（tester-1 已实证：不同模型名、同一 provider、同一条限额）。

**主控未做的事及原因**：
- 未重启任何 Worker —— 重启只会把模型重置为默认档，对额度耗尽零帮助（该条目明载）。
- 未盲发 `/models` 切档 —— 该条目「主控踩坑」节明确：带弹层的交互切档交给 Gavin 手工做，
  主控盲发 send-keys 判断不了焦点归属，上次越弄越乱。

**待 Gavin 决策**：① 在 OpenCode 里开启 "continue using usage from your available balance"（余额付费）；
或 ② 手工把三个 Worker 切到仍有额度的 provider（注意同名模型有 Zen / Go / Ollama Cloud 多档，
Zen 是历史上余额耗尽的那个）；或 ③ 本轮不派 Worker，主控单干轻量任务。

**本次会话零代码改动、零 commit、零出包。**

## 2026-09-10 — VERBOSE-195 正式写作场景冗余压缩 + ZCode 补录（纯词表，免构建）

**Gavin 指示**：「AI agent 补录 zcode、pi desktop，ide_terminal 补录 pi」
「除了 chat 类，其他都算正式写作场景，在去除结巴字词、重复字句、纠正字词后都要进行冗余内容的优化简化」。

### 根因（为什么之前不生效）

`src/llm/mod.rs:283-286` 的 L0-1(B) **早就授权**删冗余
（`YOU MAY REMOVE OR CONDENSE … verbatim repetition and redundant restatement`），
没生效是三条叠加锁死：

| # | 位置 | 锁死机制 |
| --- | --- | --- |
| 1 | `scene-rules.toml:95`（agent）`Keep the intent and wording intact` | 与 L0-1(B) **形式上不冲突**（「可以删」vs「保持措辞」）⇒ `META_RULE_PRECEDENCE:266` 的层级优先级判不了，模型听更具体的 L3 = 不删 |
| 2 | `scene-rules.toml:200/265`（terminal）一个字没提冗余 | L0-1(B) 是 **MAY** 不是 MUST，无 L3 驱动 ⇒ 默认不行使 |
| 3 | `src/llm/mod.rs:299-301` TIE-BREAKER `if unsure … KEEP IT` | 口语冗余天然是灰色地带，一犹豫就保留 |

**落点选 L3 不选 L0 的硬理由**：① L0 全场景吃，升 MUST 会连微信 QQ 一起压缩，
正是 Gavin 排除的；② `src/llm/mod.rs:294-296` 红字自陈
`🔴 WHETHER to polish at all is decided ONLY by the scene style rule in layer L3, never here`。
L0-1(A) 与 TIE-BREAKER **一字未动**（DEC-060 保真底线）。

### 改动（全在 scene-rules.toml，零 Rust 改动）

7 组 style 追加统一 CONDENSE 条款，社交 chat 两组**零改动**：

| 组 | 改动 |
| --- | --- |
| chat 社交 ①(44 exe) / ②(12 title) | ❌ **不动**（Gavin 明确排除） |
| chat **agent**（`:95`） | 重写：`Keep the intent and wording intact` → `Keep the INTENT intact` + 覆盖 kind label + CONDENSE；exe **+`ZCode.exe`**（19→20） |
| email ×2 | 追加 CONDENSE（额外护栏：不得删说话人真说了的称呼/落款） |
| ide_terminal ×2（`:200`/`:265`） | 追加 CONDENSE（额外护栏：flag/option/command/path/identifier） |
| doc | 追加 CONDENSE |
| browser | 追加 CONDENSE（额外护栏：URL） |

**CONDENSE 条款结构**（对应 Gavin 原话四步）：
`After correcting the transcription, CONDENSE it: state each point ONCE`
—— 去结巴 filler → 删重复陈述 → 自我更正只留最终版 → `Tighten wordy spoken phrasing into its plain written equivalent`（第四步「冗余内容的优化简化」）。

🔴 **每条自带刹车**：`HARD LIMIT — brevity NEVER outranks completeness`，
钉死数字/单位/日期/人名/路径/标识符/否定/情态/限定词不得削弱，
且 `never harden a hedge into a flat assertion (我觉得 stays hedged)`
—— 精简最容易误伤 hedge，这是 L0-1(A) 保护项。
末句 `If unsure … KEEP it` 保留 tie-breaker 精神做安全网。

### 三条录入的分别结论（不是三条都要加）

| Gavin 要录的 | 本机取证 | 结论 |
| --- | --- | --- |
| **zcode** | `D:\ZCode\ZCode.exe`（17 进程，Electron） | ✅ 已加入 agent 组 |
| **pi**（terminal） | `@earendil-works/pi-coding-agent`，npm CLI 跑在宿主终端 | ✅ **已被覆盖，无需也无法补录** —— `src/scene/mod.rs:157-180` 优先级 1 是 exe 精确匹配，命中即返回**不看 title**；且 title 是子串匹配，`"pi"` 会误命中 API/Pipeline/Copilot |
| **pi desktop** | 本机未安装，全盘扫描零命中 | 🔴 **exe 名待 Gavin 提供**，猜=静默失效 |

### 验证

- TOML 解析 ✅（`py -c tomllib`，9 条目全解析；解析失败会静默回落 include_str 内置 = TOML-ALL-NUL 同款形态，故必验）
- 边界精确 ✅ 社交 chat 两组 `CONDENSE` 零命中，其余 7 组全命中，`HARD LIMIT` 计数=7
- 三副本同步 ✅ md5 全等 `fb5a54ca0a85186a14ae6f98ed59df65`（根 / Publish/ / target/release/），NUL 字节三副本均=0

### 🔴 已知限制（本次未解，需 Rust 改动 = 需 Worker）

1. **开翻译时本次改动全部失效** —— `optimize_and_translate`（`main.rs:8783`）只传 4 参，
   `scene_context`/`multiline_safe` 就在 `:8717` 同作用域却没传；
   `build_translate_system_content`（`llm/mod.rs:997`）签名无 scene 位。
   既有裁定（`llm/mod.rs:53`）只管 **L0 不注入**，从未涵盖场景块，属裁定适用范围被无意扩大。
2. **翻译路径无 `multiline_safe` 保护** —— `llm/mod.rs:898` 自陈
   「translate 路径不通过 try_once（用 try_once_raw 直接返回），不受影响」⇒ 绕过 `flatten_multiline`（`:903`）。
   终端/vim（`multiline_safe=false`）开翻译上屏可能吃到换行。**推自代码未运行时实测。**
3. **agent 组 kind 仍是 `chat`** —— `build_scene_prompt_block`（`scene/mod.rs:350-353`）首句拼
   `typing into a {kind} application`，agent 场景实际写的是「typing into a **chat** application」，
   与紧随其后的 style 自相矛盾且反向拉口语。本次用 style 内 `NOTE: despite the 'chat' label above` **治标**；
   根治需 `SceneKind` 加 `Agent` 变体（Rust 改动 + 构建 + 全量回归）。

### 红线

未 commit 时状态：未构建 / 未出包 / 版本号未动 / 零凭证 / 无临时文件。
运行时优先读 exe 同级 toml（`scene/mod.rs:284-305`）⇒ **重启程序即生效，无需构建**。
DEC-059 要求的真实 API A/B 实证**未做**（Worker 全额度耗尽），待 Gavin 端测充当 A/B。

