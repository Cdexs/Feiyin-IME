# 修改历史 · voice-ime · 20260905

> 记录规则（Rule 7）：每次代码修改、优化或里程碑必须追加到本文件。
> 格式：`## YYYY-MM-DD — <一句话标题>`，正文说明改了什么、为什么、影响范围。

---


## 11:07 · 主控 session 启动维护 · handoffs.md 归档

- **触发**：worker-guide 第九节「handoffs.md >200 行归档」，session 启动检查得 299 行
- **动作**：`handoffs.md` 中 2026-09-03 全部条目（原 29-174 行，7 条：HOTKEY-060 / OVERLAY-075 /
  REPRO-061-COLD / ASR-074-FIX / ASR-074-GUARD / TEST-SYNC-074-075-D2D / TEST-EXEC-076 /
  HOTKEY-078 / HOTKEY-079）整段移入 `handoffs-archive.md`，追加归档批次标题
- **结果**：handoffs.md 299 → 153 行；handoffs-archive.md 5304 → 5455 行；保留 09-04 与 09-05 条目
- **校验**：条目数 13，编码 UTF-8 未损（中文正常），备份留在 scratchpad `handoffs.md.bak-20260905`
- **零代码改动**，版本号未动

## 11:04-11:08 · Worker 就绪状态

| Worker | 模型 | 状态 |
| --- | --- | --- |
| coder-1 | GLM-5.3-Flash（OpenCode Go） | ✅ 11:06 就绪，已 ACK |
| coder-2 | GLM-5.3-Flash（OpenCode Go） | ✅ 11:06 就绪，已 ACK |
| tester-1 | DeepSeek V4 Flash（OpenCode Zen） | ⚠️ 启动注入疑似被吞（pane 空闲、ack 0 字节），11:08 主控补注入上下文 |

- **`/permissions Full Access` 未发送**：三个 Worker 均为 OpenCode，非 Codex 类型，该命令不适用

## 11:15 · tester-1 模型切换（阻塞解除）

- **现象**：tester-1 pane 报 `Insufficient balance`（OpenCode Zen / DeepSeek V4 Flash），
  两次启动上下文注入均未被处理，ack 文件 0 字节
- **处置**：主控经 `/models` 切至 **GLM-5.3-Flash (2x usage) / OpenCode Go**（与 coder-1/2 同源），
  重新注入上下文后 11:1x 回报就绪
- **对照**：`[WORKER-RESTART-MODEL-RESET-001]`（重启后模型回落到额度耗尽档）。
  本次不是重启导致，是 Zen 侧余额本身耗尽 —— **再遇 Worker 静默先看 pane 有无 balance 报错，别直接判僵死**

## 11:2x · REPRO-094 派发（tester-1）· D2D-HANG-001 根因定位

**Gavin 指示「派吧」。** 阶段：定位（非代码、非测试执行）。

### 主控派发前实读源码取证（非凭记忆）

- `create_resources()` `src/main.rs:2973-3045`，调用序：`D2D1CreateFactory` →
  `DWriteCreateFactory(SHARED)` → `CreateDCRenderTarget` → `CreateTextFormat`×2 + Set* →
  `SetTextAntialiasMode` → `CreateSolidColorBrush`
  ⇒ 「CreateDCRenderTarget 之后」**= 阻塞落在 `CreateTextFormat` 或其后**
- `thread_local! { static D2D }` `:3047-3049` → **线程退出时在线程尾部做 COM Release**
- `shutdown_and_join()` `:937-947` → 主线程 `join.join()` **阻塞等 overlay 线程结束**
  ⇒ 与端侧「点托盘退出无响应只能 kill」是**直连路径**，同源怀疑有了具体机制
- 全仓 `src/` 下 `CoInitialize` / `CoUninitialize` / `OleInitialize` **命中数 0**

### 任务设计

- 四探针：A 主线程复刻 ／ B 子线程 + `join()`（验 COM Release 阻塞）／
  C `cargo test` harness 对照（验环境差异）／ D 端侧托盘退出 ×5
- 四假设 H1-H4 逐条判真伪，**明令不许命中一条就停手**（`[PLAUSIBLE-FIX-NOT-ACTUAL-CAUSE-001]`）
- 探针工程置于 `collab/outbox/tester-1/repro094/`（`.gitignore:70` 内，不入库）
- 红线：`src/**` `ui/**` 零改动、不出包、不 commit、不动版本号
- 任务书同时写入工作区级与项目级两处 inbox（`feedback_collab_path`）

### 边界评估

tester-1 只碰 `collab/outbox/tester-1/repro094/`（独立 cargo 工程，空 `[workspace]` 脱离父 workspace）；
coder-1 / coder-2 本阶段待命零文件占用。**文件级零重叠。**

## 11:4x · REPRO-094 中期同步（tester-1）· 根因已实证锁死

**tester-1 六条探针结论**：

| 探针 | 结果 |
| --- | --- |
| A 独立 exe 主线程复刻全序列 | **正常**，11ms 跑完并 drop |
| B 子线程 + `join()` | **挂死**，卡在 `[Drop 6/6] brush Release`；`join()` 永不返回，`process::exit(9)` 也杀不掉 |
| b2 / b5 线程体内逐对象 Release | **全部正常**（同样的对象，位置不同结果就不同） |
| b6 反向字段序 | **照样挂** → 与释放顺序无关 |
| b3 `CoInitializeEx(STA)` | **不救** → H4 不成立 |
| BS4 挂死线程栈 + PEB | **铁证**：Rip 在 ntdll 等待原语；PEB→LoaderLock `OwningThread` = 挂死线程自身 tid，`RecursionCount=1` → **自持加载器锁死锁** |

**根因定性**：Rust `thread_local!` 析构器在 Windows 上运行于 `DLL_THREAD_DETACH`，
加载器锁已被本线程持有；此时做 D2D/DWrite 最后一次 COM Release → 自持锁死锁。

**与端侧「托盘退出无响应」的连接**：`shutdown_and_join()`（`:937-947`）主线程
`join.join()` 死等 overlay 线程结束，而它结束前必跑那批析构 → 主线程永不返回。
探针 D（隔离 APPDATA + `PostThreadMessageW(WM_QUIT)` 直驱同一 shutdown 链，5 次复测）进行中。

## 11:5x · D2D-HANG-095 派发（coder-2）· 根因修复

**两处改动**：① `mod d2d` 新增 `release_resources()`（`take()` 就地 drop，仍在线程体内）；
② `spawn_overlay_thread` 线程闭包尾部调用它 —— 放闭包而非 `run_overlay_thread` 尾部，
因为后者有 `?` 早返回路径（`:1088` / `:1543`）会漏。

**明令不许**：动字段顺序（b6 已证无关）／加 CoInitialize（b3 已证不救）／
改 `with_d2d`·`create_resources`·绘制原语／碰两条 `#[ignore]` 测试（阶段三 tester-1）／
给 `join` 加超时（掩盖非修复）／碰 `troubleshooting.md`（本批归 tester-1）。

**硬判据**：产出源盘点 —— 全仓还有哪些线程会往 `D2D` 这个 thread_local 里写，
逐个列出并说明是否需要同样处理（`feedback_no_patch_design`）。答不出不许交付。

**边界评估**：coder-2 占 `src/main.rs` + `docs/MACOS-HANDOFF.md`；
tester-1 占 `collab/outbox/tester-1/repro094/`（独立 cargo 工程）。**文件级零重叠，可并行。**
`troubleshooting.md` 归 tester-1，coder-2 禁写 —— 防 `[WORKER-DOC-OVERWRITE-001]`。

## 15:0x · D2D-HANG-095 实施完成（coder-2）· 根因修复落地

**改动（src/main.rs 仅两 hunk，+23/-0）**：

1. `mod d2d`（thread_local! `D2D` 块之后）新增 `pub(crate) fn release_resources()`：
   `D2D.with` 内 `borrow_mut().take()` 就地 drop —— 值仍在线程体栈上析构，
   COM Release 发生在 DLL_THREAD_DETACH 之前（探针 b2/b5 证明安全的位置）。
   doc-comment 写明根因与「不得返回出去/延后 drop」约束。
2. `spawn_overlay_thread`（windows-only，:1062 闭包）尾部调用 `d2d::release_resources()`。
   放闭包而非 `run_overlay_thread` 尾部：后者有 `?` 早返回路径
   （`:1096 GetModuleHandleW(None)?` / `:1552 DestroyWindow(hwnd)?`，行号已按 +15 偏移核实），
   闭包是唯一覆盖全部返回路径的位置。

**产出源盘点（feedback_no_patch_design 硬判据，逐线程）**：

| 线程 | 是否写 D2D thread_local | 处理 |
| --- | --- | --- |
| overlay 线程（`spawn_overlay_thread` :1062 → `run_overlay_thread` → WM_PAINT → `d2d::draw_processing_overlay` :2131 / `draw_streaming_idle_overlay` :2493 / `draw_streaming_text_overlay` :2550 → mod 内 `with_d2d` :3119/:3618/:3638） | **是**（create + RECREATE 置 None） | ✅ 本单处理：闭包尾部 `release_resources()` |
| cargo test 工作线程（`#[ignore]` 两条 :8842/:8883/:8889 调 `d2d::draw_*` → `with_d2d` → thread_local 在**测试线程**上创建） | 是（仅当解除 ignore 后才会实际创建） | ⚠️ **tester-1 阶段三注意**：解除 `#[ignore]` 时每条用例末尾必须同样调用 `d2d::release_resources()`，否则测试线程退出时同机制死锁（这正是 D2D-HANG-001 的挂死机制）。本单明令不许碰测试模块，未改 |
| 其余全部 `thread::spawn`（main.rs :415/:4801/:4817/:4995/:5184/:5243/:5554/:5718/:5805/:5821/:5885/:6885；hotkey/mod.rs :81/:279） | **否** —— `with_d2d` 全仓唯一，且只被 mod d2d 三个 draw 入口调用，后者只被 overlay 线程 WM_PAINT 路径与上述两条测试触达 | 无需处理 |

**验证**：`cargo fmt --check` exit 0；`cargo check --all-targets` **0 error**；
全量 warning 141 条（plain check 口径）经 grep 核实**无一条**指向本次新增
（无 `release_resources` / d2d 命中），全部为既有类别（ctranslate2-sys 18 + unused import 等）。
`git diff src/main.rs` 逐 hunk 核查：**仅两 hunk**（+1066-1073 / +3059-3073），测试模块零触碰。

**⚠️ 上报主控（非本单引入，未动）**：工作区存在 45 个文件的**纯 CRLF 换行符改动**
（`git diff --ignore-cr-at-eol` 后仅剩 handoffs.md / handoffs-archive.md / todo.md / src/main.rs
四个文件有实质改动），基线 `4015c96` 之上即存在，疑似此前工具链所为。
按 git 破坏性命令禁令未做任何清理，待主控定性。

**版本号 v0.9.0 未动 / 未 commit / 未跑 test / 未 build --release。**

## D2D-HANG-095 主控验收 ✅ 通过（2026-09-05）

### 主控独立复验（不只看 result.md 报告）

| 项 | 结果 |
| --- | --- |
| `cargo fmt --check` | **exit 0** |
| `cargo check --all-targets` | **0 error**（102 warnings，其中与 d2d/release_resources 相关 **0 条**，`--message-format short` 过滤实测） |
| `git diff --numstat` | 7 文件：`src/main.rs +23/-0`、`docs/MACOS-HANDOFF.md +10`、CHANGELOG/progress 各 +1、todo +56、handoffs ±（主控归档）；**测试模块零触碰**（逐 hunk 核对：两处 hunk 分别落 `:1063` 闭包与 `:3056` mod d2d，均非测试区） |
| 五文档 grep 核对 | logs ✅ / CHANGELOG ✅ / handoffs ✅ / progress ✅ 全部命中；todo 填 N/A 属实（主控自行写入，非虚报） |

### 产出源盘点独立核实（不采信 Worker 自述）

- `draw_overlay_to_dc` **全仓唯一调用点 `:1976`**，位于 `overlay_wnd_proc` 的 `WM_PAINT` 分支
  → 窗口由 `run_overlay_thread` 创建，窗口过程只在 overlay 线程派发
  → **overlay 线程是 `D2D` thread_local 的唯一生产写入者**，coder-2 的盘点成立
- `with_d2d` 三个调用点 `:3142` / `:3626` / `:3646` 全在 `mod d2d` 内，均由上述路径进入
- 另一类写入者 = 两条 `#[ignore]` 测试（各自测试线程），**移交阶段三 tester-1**：
  解除 ignore 时必须在用例尾部显式调 `release_resources()`，否则测试线程退出即复现死锁
  —— 这恰是 D2D-HANG-001 机制本体，可当护栏用

### CRLF「45 文件即脏」定性：**虚惊，无污染**

coder-2 上报「工作区 45 文件纯 CRLF 改动、基线即脏」。主控核实：

- 那 45 行是 `core.safecrlf` 的 **警告输出**（`LF will be replaced by CRLF the next time Git touches it`），**不是 diff 条目**
- `git -c core.safecrlf=false diff --numstat` 实测 **7 个文件，全部为真实内容改动**，零 CRLF-only 改动
- `git status` 当前 0 条该警告

**但暴露一个真隐患（记录，不在本单处理）**：`core.autocrlf=true` 且 `.gitattributes`
只覆盖 `scripts/git-hooks/**`（SECRET-082 只解了钩子那一个洞）。仓库其余 321 个跟踪文件
工作区是 LF，一旦新 clone 会被检出成 CRLF → 此后任意一次编辑都会产生整文件级 diff。
**待 Gavin 定夺是否补全 `.gitattributes`（如 `* text=auto eol=lf`），本次不动。**

### 验收结论

代码 **PASS**。运行时证明（托盘退出是否真的好了）由 tester-1 探针 D + 后续出包端测承担，
不在本单验收范围。

## REPRO-094 D2D-HANG-001 根因定位（tester-1，2026-09-05）

- 探针工程：`collab/outbox/tester-1/repro094/`（独立 cargo 工程，不入库）
- 结论：**Q1 `ID2D1SolidColorBrush::Release` 在 thread_local 批量析构（线程退出 FLS 回调，loader lock 自持）中自锁**；Q2 非 harness 环境差异（独立 exe 子线程同样挂）；Q3 是（探针 D 真实 exe 5 次 4 挂）。
- H1 成立（LoaderLock OwningThread=挂死线程自身，bs4 铁证）；H2/H3/H4 不成立（逐条独立验证）。
- 释放顺序无关（b2/b5/b6）、COM 初始化无效（b3 STA 仍挂）。
- 修法建议：overlay 线程收尾时在**线程体内**显式清空 D2D thread_local 槽（首选）；生产修复落地后移除两条 `#[ignore]` 测试。
- 产出：`collab/outbox/tester-1/result.md`。

## REPRO-094 主控验收 ✅ 通过（2026-09-05）

### 三问结论（tester-1 交付，主控核对证据链成立）

| | 结论 |
| --- | --- |
| **Q1 确切阻塞 API** | `ID2D1SolidColorBrush::Release()`（thread_local 批量析构第 6/6 步），在线程退出的 ntdll FLS/TLS 回调内执行，**loader lock 已被该线程自持** → 自锁死锁 |
| **Q2 是否 harness 环境差异** | **不是**。独立 exe 子线程照样挂（探针 B/b3/b6）；主线程显式 drop 不挂（探针 A，11ms） |
| **Q3 线程退出 COM Release 会否阻塞** | **会**，且与端侧托盘退出同源 |

**决定性取证**：`PEB+0x110 LoaderLock: LockCount=-2 RecursionCount=1 OwningThread=0x3764`，
挂死线程 tid=14180（=0x3764）**逐位对上**；`Rip=ntdll.dll+0x161914` 落在等待原语区段。

### 探针 D —— 端侧同源正式坐实

- BUILD-093 真实 exe，隔离 APPDATA，`PostThreadMessageW(WM_QUIT)` 直驱与托盘退出同一 shutdown 链
- **5 次运行 4 次挂死**；挂死态：进程 10s 不消失、线程数定格 33、`Responding: True`
  （消息循环已退出但托盘无反应）—— 与 Gavin「点托盘退出无响应只能 kill」完全一致
- 挂死日志尾部：`Message loop exiting` → `Low-level keyboard hook uninstalled`
  → 此后再无 shutdown 后续 ⇒ 卡在 `shutdown_and_join()` 的 `join()`
- **run4 未挂 = 当次 overlay 线程未画过 D2D 迁移状态、资源槽为空** ——
  这条把 Gavin 报的「间歇性」解释干净了。🔴 **别被「有时能正常退出」误判成已修好。**

### 假设判定

H1 **成立**（唯一）；H2（CreateTextFormat/字体缓存）、H3（test harness 并行 + DWrite SHARED 单例锁）、
H4（缺 CoInitialize）**逐条判否**，各有独立探针，未因 H1 先命中而停手。

### 主控发现的漏网路径 → 追加 D2D-HANG-095-B

095 的写法是**闭包尾部直调** `d2d::release_resources()`。`if let Err(...)` 接得住
`Result::Err`，**接不住 panic** —— 展开会跳过尾部语句 → 死锁原样复发。

**真实 panic 点（主控 grep 取证）**：`src/main.rs:1462` `state.request.as_ref().unwrap()`、
`:3102` `expect("just initialized")`；`Cargo.toml` **未设 `panic = "abort"`**，默认 unwind。

改法：闭包内声明 `D2dReleaseGuard`（Drop 守卫），展开路径照样执行。
coder-2 已独立复核「守卫触发时不会双重借用 RefCell」成立 ——
`with_d2d` 的 `RefMut slot` 是同帧局部，panic 展开时同帧局部按逆序先 drop，
借用在到达线程闭包层之前必已归还，故用 `borrow_mut` 而非 `try_borrow_mut`
（后者「借不到就跳过释放」= 拿 abort 换 hang，是退回死锁）。

### 补账两处（REPRO-094-DOC 派 tester-1）

- `handoffs.md` 漏写（`grep REPRO-094` 命中 0）—— 根因是 tester-1 用了**自定义收尾自证表**，
  没有逐文档打钩那几行，自己发现不了（`[DOC-STATE-DRIFT-001]` 同一失效机制）
- `troubleshooting.md` 缺 `[D2D-HANG-001]` 根因条目 —— **这条是主控任务书疏漏，不算 Worker 失职**，
  已补派并给出七项内容清单

### 边界评估

coder-2 占 `src/main.rs` + `docs/MACOS-HANDOFF.md`；tester-1 只写
`collab/handoffs.md` + `collab/troubleshooting.md`。**文件级零重叠，可并行。**

## 17:3x · D2D-HANG-095-B 实施完成（coder-2）· Drop 守卫堵住 panic 展开路径

**改动**：`spawn_overlay_thread` 闭包（windows-only）改写——删尾部直调 8 行，
改为守卫 21 行（相对 095 交付后基线仅此一个 hunk；vs HEAD 显两 hunk 因 095 未 commit）：

- `struct D2dReleaseGuard` + `impl Drop` 调 `d2d::release_resources()`
- 守卫声明在 `run_overlay_thread` **之前**，覆盖调用期间任意 panic 的展开路径
- `release_resources()` 函数本体零改动（红线遵守）

**双重借用推理复核（硬判据，结论：成立）**：

- `with_d2d` 的 `cell.borrow_mut()` 产物 `slot`（RefMut）是 `D2D.with` 闭包帧的局部变量。
  panic 展开时**同帧局部按声明逆序析构**：`res`（借用，无析构）→ `slot`（RefMut 析构 = 归还借用），
  然后闭包帧退出。栈退到线程闭包层时，守卫 drop 才调 `release_resources()` → `borrow_mut()` 必然成功
- **含 :3115 `expect("just initialized")` 本体**：该 panic 就发生在 borrow 持有帧内，
  展开同样先析构 `slot` 归还借用，再逐层上行到守卫——结论不变
- 帧内逆序析构的另一推论：守卫在本帧声明最早 → 析构最晚，**不存在**
  守卫先于任何内层 RefMut 释放而运行的场景
- 故用 `borrow_mut`（借不到就跳过释放=退回死锁，是拿 abort 换 hang，取舍归主控；
  本单推理成立，无需该取舍）

**注释内 panic 点行号已按改后实测校正**：`:1475`（`state.request.as_ref().unwrap()`）、
`:3115`（`expect("just initialized")`）。首写 ：1477/:3117 有偏，随即 grep 校正。

**消融自证**：若改回尾部直调，overlay 线程在 :1475/:3115 或 Mutex 中毒处 panic →
展开跳过尾部语句 → `D2dResources` 六对象留在 thread_local → TLS 回调在
**DLL_THREAD_DETACH**（loader lock 被本线程持有）析构 → 最后一次 COM Release
（探针 B 卡点 `[Drop 6/6] brush Release`）撞自持加载器锁 → `NtWaitForAlertByThreadId`
死锁 → `shutdown_and_join()` 的 `join.join()` 永不返回。守卫在展开路径上照常析构，
该路径闭合。

**验证**：fmt exit 0 / check --all-targets 0 error / 新增代码零 warning；
守卫仍在 `#[cfg(target_os = "windows")]` 区内，macOS 零影响（HANDOFF 已追加一句）。
红线：release_resources 本体未动 / 无 catch_unwind / with_d2d 与原语未动 /
测试模块与 troubleshooting.md 未碰 / 未 commit / v0.9.0 未动 / 零凭证。

## REPRO-094-DOC 补账（tester-1，2026-09-05）

- handoffs.md 末尾追加 REPRO-094 条目（Q1/Q2/Q3 + H1-H4 + 探针 D 4/5）。
- troubleshooting.md 末尾追加 [D2D-HANG-001] 根因条目（七项齐全：症状两副面孔/机制/取证手法/四条证伪岔路/线程体内释放安全/修法/间歇性来源）。
- 追加保真：handoffs 增量 +18/0 删、troubleshooting +39/0 删（git diff --numstat）。
- 自证表 progress.md 按规则 7 填 N/A（定位/测试类不记里程碑）。

## TEST-SYNC-096 阶段三（tester-1，2026-09-05）

- 基线 38eb289；白名单内（cargo fmt / cargo check --all-targets）0 error。
- 1.1 解除两条 #[ignore]（:8842/:8881）+ 用例尾部 d2d::release_resources()；测试内全部 3 个 with_d2d 调用点已覆盖释放。
- 1.2 新增护栏 d2d_thread_with_populated_slot_exits_after_in_thread_release（is_finished 10s 超时断言线程真实退出）。
- 1.3 新增护栏 d2d_release_resources_is_idempotent_on_empty_slot（空槽幂等）。
- diff：src/main.rs +59/-6，全部 hunk 在 #[cfg(all(test, windows))] 模块内；生产零改动。
- 阶段四 TEST-EXEC-097 首跑这三条用例。

## TEST-EXEC-097 阶段四（tester-1，2026-09-05）

- root 主 bin 1065P/0F/9I（逐位吻合期望）+ src-tauri 76P + vitest 89P，三层全绿。
- 受控消融：注释 src/main.rs:9269 线程体内 release_resources() → 单跑护栏用例 → 整个 test 进程挂死（D2D-HANG-001 本体复现，判别力最强形态），timeout 清理无残留。
- 还原后 diff=59/6 逐字节一致；重跑该用例 1P 绿。
- Publish/config.toml sha=3186ec8c 未变；E2E/smoke 按主控指定 SKIP（旧包不含本批修复）。

## BUILD-098 阶段五出包（tester-1，2026-09-05）

- 四步构建完成：feiyin-ime 12,229,632B / feiyin-ime-ui 10,053,632B / crash-reporter 24,887,808B；toml 三副本一致。
- 七项核验全过：sha target↔Publish 三对相等且 main 异于 093(e6f55e0a→39cca97c)；ProductVersion 0.9.0.0；D2D-HANG-095 探针=1（新代码入包硬证据）、D2D-P1=2；冒烟无 panic。
- 托盘退出 E2E（第8项）：新包 5/5 干净退出（每次先触发录音画 D2D），对比修复前 5次4挂 —— D2D-HANG-095 端到端成立。debug.log 出现 4 次 "D2D resources released in-thread before thread exit"。
- E2E 门禁（第9项）：补装缺失 toml 后全量 61P/5F/33S/6deselected。5 条失败=4 条 full_pipeline（processing state not observed，可复现）+1 条 focus_lost_preview（_no_hardware 预存在测试代码 bug）。toggle_stop：本批全量转绿一次但 standalone 仍 PASS/FAIL/PASS 间歇，交主控归因。
- 运行时数据零覆盖：config.toml sha 3186ec8c 保持；debug.log 恢复 41 行原样；执行日志全部 mv 至 build098/。

## BUILD-098 主控验收 ✅ 通过（2026-09-05）

### 主控独立复验产物

| 项 | 实测 |
| --- | --- |
| 三 exe 时间戳 | 09-05 13:36 |
| `Publish` ↔ `target/release` sha256 | 相等 `39cca97c925abdfd…`，**异于 BUILD-093 `e6f55e0a…`** |
| **判别探针** | `grep -c 'D2D-HANG-095' Publish/feiyin-ime.exe` = **1**（旧包实测 **0**）= 新代码进包硬证据 |
| 对照探针 | `D2D-P1` = 2（与 BUILD-093 一致） |
| toml 三副本 | `scene 0a3a0b9a…` 三处相同 |
| 运行时数据 | `Publish/config.toml` sha `3186ec8c…` 未变 |
| 大小 | main 12,229,632B（较 093 +1,024B，同量级） |

### 第 8 项托盘退出端到端 —— 本批目标达成

`probeD_results.json` 五次全部 `recording_started=true` + `hung_after_10s=false`
⇒ **5/5 干净退出**；4/5 的 tail 日志可见 `released in-thread` 佐证。
**修复前同手法 5 次 4 挂。D2D-HANG-095 端到端成立。**

### E2E 61P/5F —— 主控取证：**非本批引入，且暴露门禁三周虚高**

不是回归。取证链：

1. 本批 `pytest_e2e.log`（**补装 toml 模块之前**的第一次跑）里，那 4 条 full_pipeline
   全是 `ERROR ... ModuleNotFoundError: No module named 'toml'` —— **根本没执行**
2. `logs/20260817.md:706` 明载「test_full_pipeline_e2e ModuleNotFoundError toml | 4 |
   环境缺 toml pip 包」—— **自 8-17 起一直如此**
3. `logs/20260817.md:705` 明载「test_injection `_no_hardware` AttributeError | 1 |
   预存 harness 缺陷」—— 第 5 条 F 同样是 8-17 已在案的老问题
4. 两次数字**不可直接比**：BUILD-093 用 `-m "not hardware"`（7 deselected），
   本次 6 deselected，选集不同

🔴 **真正的问题不是那 5 个 F，是 E2E 出包门禁三周来一直是虚的**：
BUILD-085/093 报的 65P/0F、64P/1F 看着漂亮，但 **full_pipeline 四条全链路用例根本没跑**
（ERROR 不计入 FAIL，数字上看不出来）。门禁实际覆盖比数字少 5 条，
**少的恰恰是最有价值的那几条。**

### 4 条 full_pipeline 失败的性质：未知，不作定论

失败集中在 `Expected processing state after stop, got hidden`。主控**倾向** harness/环境，
但**明确不结案**，理由与待排项：

- **强先例**：`logs/20260817.md:703` 记过 6 条 test_hotkey 同样报「overlay hidden」，
  经决定性实验证明是 harness 缺陷（`[E2E-CONFIG-PATH-STALE-001]`）、**产品正常**
- 自动化环境无真实麦克风 → 录到静音 → 无转写内容 → overlay 直接隐藏不进 Processing；
  佐证：同文件其他类有 `_no_hardware()` 跳过守卫，full_pipeline 这几个类没有
- 待排除：DeepSeek key 已吊销，新 key 未配则 LLM 401

⇒ 列入 **E2E-GATE-099**，Gavin 端测后派。**必须做决定性实验，不许推断结案**
（`[PLAUSIBLE-FIX-NOT-ACTUAL-CAUSE-001]`）。同批要求加一条
**「ERROR 数 > 0 即门禁不通过」**的判据，堵住同类静默失效。

### toggle_stop 观察

本批全量跑中**转绿过一次**，standalone 仍 PASS/FAIL/PASS 间歇。
主控此前列的三个归因方向之一是「D2D 绘制阻塞（D2D-HANG-001 关联）」——
**本批修了 D2D-HANG 后仍未稳定转绿，该方向证据不足，不宜再当主要嫌疑**。归 E2E-GATE-099 一并查。

## BUILD-098 Gavin 端测结果（2026-09-05）+ 下一轮派发

### 7 项过 6，结案 5 条历史待办

- ✅ **托盘退出 OK** —— D2D-HANG-095 端到端确认，**P0 结案**
- ✅ 开嗓空窗口（086 Bug 2）／流式文字细腻度（D2D-P1）／超长滚动行为 —— 全 OK 结案
- ✅ **语音侧 AltGr OK** —— HOTKEY-078 结案，最后一个「未知」项清掉
- ✅ **toggle 连按两次实机 OK**
- 🔴 宽度扩展：两个新缺陷 → OVERLAY-101
- 🔧 超长滚动上限：65% → 50% → OVERLAY-102

🔴 **toggle 那条的情报价值**：`test_hotkey_toggle_stop` E2E 长期间歇 FAIL，
**Gavin 实机确认产品能正常停** ⇒ 天平明显偏向 harness/环境缺陷。
且本批修完 D2D-HANG 后它仍未稳定转绿 ⇒ **「D2D 绘制阻塞」这个归因方向证据不足，
不再作主要嫌疑**（主控此前的三方向列表据此修正）。并入 E2E-GATE-103。

### OVERLAY-101 派发前主控静态取证（已定位一处确凿源头不一致）

`src/main.rs:1396` 的 `SetWindowPos`：**尺寸取在飞的 `state.current_size`，
而位置 `computed_pos` 来自 `adjust_overlay_pos_size_for_text`（`:4241`）
用 clamp 后的 target 宽算出的居中 x** ——

```
adjust_overlay_pos_size_for_text: x = work.left + (work_w - w_target) / 2
插值循环 :1765            : pos[0] = work.left + (work_w - current_width) / 2
```

增长期 `w_target > w_current` ⇒ Show 摆偏左，插值循环推回右 ⇒ **每帧互相拉扯 = 水平窜动**。

🔴 **该契约测试里早已标为缺口**：`src/main.rs:9123` 注释原文
「真实契约 = 『用于居中的宽度 == SetWindowPos 应用的宽度』」，TEST-SYNC-087 列为判别力缺口未覆盖。
本次要求收敛成共用纯函数 `centered_x()` 并补成真护栏。

**Bug B 第二条线索**（给方向不给结论）：插值循环整块被
`if state.current_size != state.target_size`（`:1739`）门控，
**到上限后 `current == target` ⇒ 整块含 x 重算全不执行**，此后只剩 Show 设位置 ——
时间点与 Gavin「上限后才瞬跳右窜」吻合。**但无实测证据，已明令 coder-2 自证、
不许用第一条修法顺手宣布 Bug B 也好了**（`[PLAUSIBLE-FIX-NOT-ACTUAL-CAUSE-001]`）。

**Bug A 是宽度异常不是位置异常**，上述修法解释不了。已要求逐环取证：
① `measure_text_width` 吃的是**全量文本**而渲染画的是 timeline 揭示部分，是否同一段？
② `[ASR-SERVER-REWRITES-TIMELINE-001]` 服务端整段重写时间轴时 target 会不会瞬时算大？
③ 暴涨后「很快缩回」符合 **target 先跳大又跳回**的形态，**重点查 target 怎么算的，别改插值**。

### 主控无法取证的原因（如实记录）

`Publish/debug.log` 仍是 tester-1 恢复后的 41 行（2026-08-17），
`%APPDATA%\voice-ime\` 下无日志目录 —— **Gavin 端测未带 `-debug`，无运行时日志可查**。
故 Bug A/B 只有静态取证，动态证据须由 coder-2 自行获取。
**下次端测若再遇疑难现象，考虑请 Gavin 带 `-debug` 跑一次。**

### 本轮派发（文件级零重叠，可并行）

| Worker | 任务 | 占用文件 |
| --- | --- | --- |
| coder-2 | OVERLAY-101 + OVERLAY-102 | `src/main.rs` + `docs/MACOS-HANDOFF.md` |
| tester-1 | E2E-GATE-103 | `tests/**` + `collab/build-test-guide.md` |

E2E-GATE-103 三件事：① 4 条 full_pipeline 逐条判性质（**必须决定性实验**）
② 修 `_no_hardware` 预存 bug ③ **把「ERROR>0 即门禁不通过」落成机器闸门**
（`feedback_security_rules_need_gates`：只加文字规则等于没解决），
并要求做一次「故意制造 ERROR」的验证证明闸门真会拦。

## 20:4x · OVERLAY-101（Bug B 修复）+ OVERLAY-102（宽度上限 50%）实施完成（coder-2）

**Bug A 取证结论（先于实施）**：我最初提出的「服务端新句累计式携带前缀→display 双计」假说被主控驳回——我第二次把 log 的 `display` 输出字段误当 raw 输入反推。正确事实（主控独立统计）：52 包 display 严格单调、最大 61、回落次数 0；id=2 首包 display=14 = confirmed(14)+current(0) = **空包**而非累计包；「8 个词是 s1 的词」= display_words() 本就含 confirmed 词，非包输入。Bug A（Gavin 09-05 端测所见宽度暴涨缩回）**转入待证据**——该端测为 Publish 构建、无 -debug 日志，无数据可循因。qwen_inference.rs 一字未动。

**Bug B 机制（主控确认我方分析正确）**：Show 流式分支的 **!do_it（100ms 节流命中）分支不重算 x**，final_pos 沿用 resolved_pos = `show_overlay` 传入的 overlay_geometry 默认位（基准宽 240 居中），而同调用 SetWindowPos 应用 in-flight current（到上限后 ≫240）→ 中心右偏 (current−240)/2 px；到上限后 current==target、插值循环休眠，无帧纠正，持续到下一包 do_it（~700ms）→「瞬跳向右窜」。触发=句界双 Show 同毫秒（09-04 日志 25.835 id=2 end+id=3 首包同 ms，第二包节流命中）。注：主控原判断「Show 用 target 宽居中」已过时（OVERLAY-068-A R1 早已修 do_it 分支）。

**改动（src/main.rs +43/-23，7 hunk）**：
1. 新增纯函数 `centered_x(work_left, work_w, applied_w)`（:4341，cfg(windows)，doc-comment 含缺陷史+消融参考）
2. Show 流式分支（:1236-1262）：x **不分 do_it 与否**一律由 `state.current_size[0]` 经 centered_x 现算；!do_it 分支 y 取 adj_pos[1]（与旧 resolved_pos[1] 同值——两处同公式同 h）
3. 插值循环（:1761）：`pos[0] = centered_x(work.left, work_w, current_width)`
4. `overlay_geometry`（:4228）：内联式→centered_x，**传 size[0] 不变**（主控边界：只换调用不换宽度）
5. `adjust_overlay_pos_size_for_text`（:4281）：内联式→centered_x，**传 clamp 后的 w 不变**
6. OVERLAY-102：`STREAMING_OVERLAY_MAX_SCREEN_RATIO` 0.65→0.50（:1052，101 验证后单独改，比例注释写明分步原因）

**同值证明（主控要求的逐处清单）**：
| 调用点 | 传参宽度 | 改前 | 改后 | 同值 |
| --- | --- | --- | --- | --- |
| Show 流式分支（do_it 帧） | current_size[0] | `work.left+(work_w-current)/2` | centered_x(同参) | ✅ 逐字同式 |
| Show 流式分支（!do_it 帧） | **无→current_size[0]**（行为变更本体） | resolved_pos[0]（基准宽居中） | centered_x(current) | ❌ **有意变更=Bug B 修复** |
| 插值循环 | current_width | 同式内联 | centered_x(同参) | ✅ |
| overlay_geometry | size[0] | 同式内联 | centered_x(同参) | ✅ 纯重构 |
| adjust…for_text | clamp 后 w | 同式内联 | centered_x(同参) | ✅ 纯重构 |
| Show y（!do_it 帧） | — | resolved_pos[1] | adj_pos[1] | ✅ 同值（同公式同 h=RECORDING_OVERLAY_SIZE[1]） |
| EnterEditMode SetWindowPos | adjust 返回 new_pos/new_size | 未动 | 未动 | —（adjust 内部 x 已同源，current==target 先行赋值，内部自洽） |
| :520/:538 remove/restore_noactivate | N/A（SWP_NOMOVE|NOSIZE） | 未动 | 未动 | — |

**产出源盘点（硬判据）**：全仓唯一改窗几何 API = SetWindowPos，共 5 调用点（:520/:538/:1396/:1483/:1768→现 :1401/:1488/:1775 附近），逐点宽度来源上表列尽；无 MoveWindow/DeferWindowPos/SetWindowPlacement。测试模块零触碰（最深 hunk :4343，测试区 :8900 起）。

**消融推演**：① 回退 Show !do_it 为 resolved_pos x → 右窜复发（Gavin 原话「宽度扩展到上限后瞬跳向右窜」）；② 任一调用点换宽度源（target/基准宽）→ 居中与应用宽度分叉回归；③ centered_x 改回各处内联 → 零行为变化但丢失单一源（REFACTOR-088 同理）；④ OVERLAY-102 回退 0.65 → 上限触发点外移，Bug B 触发面变窄但优化撤销。

**验证**：fmt exit 0 / check --all-targets 0 error / 163 条 warning 与改动前持平（无新增指向）。**红线**：interpolate_step 未动 / 无 grow-snap / streaming_scroll_offset 未动 / d2d release_resources·D2dReleaseGuard 未动 / 测试模块未碰 / troubleshooting.md 未碰（主控本批所有）/ 未 commit / v0.9.0 未动 / 未跑 test / 零凭证。

## TEST-SYNC-105 阶段三（tester-1，2026-09-05）· OVERLAY-101 + OVERLAY-102 测试同步

- 基线 HEAD `4805039`（coder-2 交付 OVERLAY-101/102，主控已验收提交，工作区 `src/main.rs` 本批前干净）。
- **改动**：`src/main.rs` +164/-0，单 hunk 在 `:9360` 后新增
  `#[cfg(all(test, target_os = "windows"))] mod overlay_101_centering_guard_tests`，生产零改动。
- **G1** `centered_x_formula_exact_values`：work_left=0 / ≠0（负坐标屏）、奇偶取整（向零截断）、
  applied_w>work_w 给负 x 不 panic 不 clamp。消融=改 :4342 公式。
- **G2** `centered_x_baseline_to_max_width_offset_is_half_the_delta`：Bug B 量化 ——
  同 work_w=1920 下 `centered_x(240)`−`centered_x(960)` == (960−240)/2 == 360，
  负坐标屏下偏移量不随 work_left 变。消融=不再按 applied_w 居中。
- **G3** `centering_keeps_window_center_fixed_as_width_grows`：中心不变量 x+w/2 == work_left+work_w/2，
  偶数宽逐位恒等 + 宽+2→x−1 增量 + 奇数宽 ≤1 截断容差。消融=按 target/基准宽居中。
- **G4** `streaming_max_width_ratio_is_half_with_960_on_1920`：`STREAMING_OVERLAY_MAX_SCREEN_RATIO==0.50`
  + 1920 复算 max_w==960（可测性边界：overlay_max_width 需真 HWND，本用例钉常量+复算同式）。
  消融=改回 0.65 红。
- **G5** `inline_centering_formula_appears_only_in_centered_x_body`：`include_str!("main.rs")` 读自身源码，
  逐行去空白后含「(work_w−」形态必须恰 1 行且该行含 applied_w（=centered_x 定义体）+ `fn centered_x` 存在。
  needle 用 `format!` 拼装避免字面量自我命中；注释全部用全角减号 `−` 规避 ASCII 自触发。
  实测命中恰 1 行（:4342）。消融=任一处内联回去 / centered_x 改名删除 → 红。
  🔴 判别力边界（如实）：只匹配 work_w 变量名形态，若用别的变量名内联（如 `(ww - w)/2`）会漏。
- **验证**：`cargo fmt --check` exit 0 ／ `cargo check --all-targets` **0 error**（102 warnings 与基线持平）。
- **红线**：生产代码零改动（`git diff --numstat src/main.rs` = 164/0，全部 hunk 在测试模块）／
  既有用例零触碰 ／ 未跑 cargo test（阶段三白名单）／ 未 commit ／ v0.9.0 未动 ／ 零凭证。

## SECRET-104 · 修手机号闸门对 Cargo.lock 校验和的误报（coder-1）

- **改动**：`scripts/git-hooks/secret-patterns.sh:26` `PRIVACY_PHONE_PATTERN` 边界
  `(^|[^0-9])…([^0-9]|$)` → `(^|[^0-9A-Za-z])…([^0-9A-Za-z]|$)`，并加注释说明原因与已知代价
- **为什么**：Cargo.lock 十六进制校验和里 11 位连号必被 hex 字母包夹（实测
  `checksum = "…ea19749016521d6dff…" 命中其中的 11 位连号 `1974<REDACTED>521`），旧「非数字」边界判定为中国手机号
  → 任何含 Cargo.lock 改动的 push 都可能被误拦，高噪音训练人用 SECRET_SCAN_SKIP=1 绕闸
- **实测**（一次性临时仓库，测完即删）：七用例全过（checksum 放行／裸手机号·中文标点·
  `phone=`·JSON 形态全拦／真实 Cargo.lock 全量 diff 放行／sk-·邮箱·身份证仍拦），
  另验 push 侧干净放行 + `--no-verify` 后 pre-push 拦截正常
- **已知代价**：紧贴字母/数字的手机号（如 `id13812345678`）会漏，可接受
- **文档**：README.md「## 构建」下新增「### Git 提交钩子（每个新 clone 必须执行一次）」
  （第 6 问核实：worker-guide.md:156 已写明，README 此前缺失）
- **影响范围**：仅 pre-commit/pre-push 的手机号判据；PREFIX/GENERIC/EMAIL/IDCARD 零改动

## 21:xx · 协作会话重启后状态复盘与重排（orchestrator）

### 上一会话中断点

- **已完成并提交**：`4805039`（OVERLAY-101 Bug B + OVERLAY-102 0.50）、`b072383`（SECRET-104）。工作区干净。
- **零产出被打断**：E2E-GATE-103（tester-1）。`git log -- tests/` 无相关提交、`git status` 干净 ⇒ 一行未写。
- **流程缺口**：OVERLAY-101/102 是代码改动，阶段三/四/五全未走，Gavin 手上仍是 BUILD-098 旧包，Bug B 修复未经端测。

### 🔴 中断真因：不是终端卡死，是 tester-1 模型档余额耗尽

`capture-pane -t %3` 末行：`Insufficient balance. Manage your billing here: opencode.ai/...`，
模型档 `DeepSeek V4 Flash · OpenCode Zen`。请求挂在失败重试里，Worker 看着活着实则永不响应
= `[WORKER-RESTART-MODEL-RESET-001]` 第二次复发（重启把模型重置回默认档）。

**处置**（按 troubleshooting 记载的顺序，③ 必须在 ⑤ 之前）：
Esc×2 打断 → `C-u` 清空输入框 → `/models` → 搜索 `GLM` → 选中
**`GLM-5.3-Flash (2x usage) · OpenCode Go`**（与 coder-1/coder-2 同档）→ Enter →
状态栏确认已切换 → 才重注入 tester-1 角色上下文 → 收到「已就绪」。

**踩坑记录**：`/models` 打开后立刻 `send-keys -l "GLM"` 会把选择器关掉、文本落进输入框；
逐字符发送（每字 0.4s 间隔）才进得了 Search 框。高亮行判定用 `capture-pane -p -e`
看背景色（`48;2;250;178;131`），不能靠缩进猜。

### 文档卫生

`collab/handoffs.md` 236 行 > 200，按 worker-guide §九归档：09-04 共 11 条移入
`handoffs-archive.md`，主文件保留当天 10 条，**236 → 113 行**。UTF-8 完好（`file` 复验）。

### 本轮重排（文件级零重叠）

| Worker | 任务 | 占用文件 |
| --- | --- | --- |
| tester-1 | **TEST-SYNC-105** 阶段三 | `src/main.rs` **测试模块** |
| coder-2 | **D2D-P2P3-PLAN-108** 方案设计（零代码改动） | `collab/drafts/d2d-p2p3-plan.md` |

**调度理由**：P2+P3 实施与 TEST-SYNC-105 都动 `src/main.rs`，五阶段禁止并行。
先把 101/102 送到 Gavin 手上端测（Bug A 还卡在缺 `-debug` 日志，出包能一次解两个阻塞），
coder-2 同期做不冲突的方案备料。E2E-GATE-103 排在 BUILD-107 出包之后。

### TEST-SYNC-105 护栏设计要点

G1 `centered_x` 公式（含负 work_left / 奇偶取整 / applied_w > work_w）；
G2 Bug B 量化偏移 `(W_max-240)/2`；G3 两边等量扩展（中心不变）；
G4 `STREAMING_OVERLAY_MAX_SCREEN_RATIO == 0.50` + 1920 屏 max_w == 960；
**G5 源码级单一居中源结构护栏**（`include_str!` 断言全文无内联居中公式）——
这是 TEST-SYNC-087 列为「判别力缺口」的那条契约（*用于居中的宽度 == SetWindowPos 应用的宽度*）
第一次有可测形态，但正则易脆，**已明确授权 tester-1 评估后提异议**。

### coder-2 已回 ✅ 同意（21:xx）

主控 ACK 时追加两条提醒：`src/main.rs` 只读不写；`StreamingEditing` 的 Win32 EDIT
子控件共存结论必须在方案里定死，不许留「实施时再看」。
